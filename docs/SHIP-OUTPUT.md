<!-- author: Code | date: 2026-06-11 -->
# v1.10.0 — GATE 20.5: the last build gate. Smoke fixes + FRAME THEMES + the catch-up surface + all four pre-cutover wants. DONE · STAGED · NO deploy

Mode **ULTRAMAX**. The **5-lens panel confirmed 2 HIGH + 8 MED (unique; 0 refuted) + 12 LOW — every HIGH/MED fixed (one by deliberate REVERT), 8 LOW fixed, 4 accepted + documented.** All tracks green at new highs. **Nothing remains before gate 21 — THE CUTOVER.**

## Design vision (1 para)
This gate makes the profile feel like a possession: pick an edge and your whole card wears it — rainforest vines, mountain stone, cosmic sky, a witch's violet miasma, the five-leaf grimoire, Frieren's silver-flower dusk, shadow-garden smoke, sakura, flame, tide, lightning, moonlight — **twelve frames anyone can wear, and one nobody else can: The Den Keeper, your gold filigree-and-lantern frame, locked to your account in the security rules themselves.** Around it, the door finally keeps its promise (the catch-up buttons open a real destination — a veil sheet that walks you through what you missed), the accent system tells the truth (real two-stop gradients around the card edge), every season has a room, and the launcher just works.

## PART A — your 12, item by item
1. **Surprise me** — gone from the door (spec pins it can't return). The nav's Random untouched.
2. **The door rule, verified + in plain language:** a fresh visitor — account or not — ALWAYS gets the door on their first load in a new tab/window (verified live in a clean browser). It will NOT re-show while browsing in that same tab (that's the once-per-session design), and it returns after closing the tab, after a new session, **and now after any sign-out**. The one honest edge: a browser that *restores* a previous session (e.g. "reopen closed tab") restores the "already greeted" flag with it — that restored-tab case is most likely the "sometimes it didn't" you saw. No redesign needed; say the word if you want restored tabs to re-greet too.
3. **WHILE YOU WERE AWAY — bigger + a real destination:** the strip is now a framed door feature (bigger type, primary-grade buttons), and the buttons open **THE CATCH-UP SHEET** — a full veil surface: **✍ NEW FROM BLAKE** as a cover-card rail (tap → the review), **🏮 YOUR LANTERN** as letters (your gold edge on Blake-origin ones; viewing them marks them seen, like the real lantern), **📺 AIRING FOR YOU** chips (tap → the deep-dive). Quick sub-links stay on the door. Walked live: stale-snapshot → "Blake added 43 new reviews" → sheet → card → landed on the review modal.
4. **"Blake's reviews"** — every "Blake's 44"/"the 44" audited out of visitor-facing copy (account chips ×2, the shelf-adder group, the Tavern's "Not yet in Blake's reviews — suggest it", season fallback labels).
5. **Status alignment** — structural: every editor row's header is now ONE line (pill + hint share it), so side-by-side inputs always start level. STATUS lines up with DISPLAY NAME.
6. **My Activity round 2** — explicit feed geometry ([thumb | text | date] grid; the old inherited grid shoved text right), snippets breathe to three lines, everything left-aligned.
7. **DM Blake** — renamed across the card, button (✉ DM Blake), and thread title bar.
8. **Hover scoping** — the avatar pill shows only when the circle itself is hovered; the background pills only on the rest of the hero (and never while you're on the avatar). Keyboard focus still raises either.
9. **Gradients fixed for real:** they never rendered — round 4 painted two *solid* rings ("it's just one solid color" was exactly right). Now a **true two-stop gradient ring wraps the entire card edge** (the avatar keeps a slim first-stop ring), the glow aura rides the card, and it all composes with the frames.
9b. **🌟 FRAME THEMES** — the headline: a **Frame** row in the identity studio with **13 tile previews**. Your three seeds (Rainforest Vines 蔦 · Mountain Stone 岩 · Cosmic Sky 宇宙) + the anime-modeled set (**Witch's Pact** 魔女 — Re:Zero · **Five-Leaf Grimoire** 五つ葉 — Black Clover, one corner wears the devil-dark five-leaf · **Long Journey** 旅路 — Frieren's silver-flower dusk · **Shadow Garden** 陰 — Eminence, one crimson slash) + Sakura Drift · Ember Breath · Deep Tide · Storm Sign · Moonlit Night — **and THE DEN KEEPER: your exclusive gold frame (filigree bands, corner studs, a hanging lantern with a glowing ember). The rules deny the 'blake' key to any other account** (tested); Mika's picker doesn't even show the tile, yours does. Frames render on the account hero, the Public-view sheet, AND for viewers of live profiles. All CSS/SVG, compositor-cheap, reduced-motion honored, no gold in any user theme (spec-pinned).
10. **Rooms everywhere:** every season's deep-dive carries a room now. Un-reviewed titles get their own lead-in in your voice — *"I haven't reviewed this one (yet) — the room's open anyway. Keep spoilers behind the tag."* — purple, no gold, full moderation reach (same comments machinery: consent, rate limits, report, lock). Honest risk, flagged: rooms now exist on any real AniList title, which widens the spam surface — the consent gate + rate limits + reports bound it, and error-state rooms stay watched-only (a successful fetch is what proves an arbitrary id is real).
11. **The demand chip — CONFIDENT, with receipts.** I ran the whole chain live, twice: Yuki requests via the suggest page → **"👁 1 request"** appears on the title's deep-dive for another member → you Mark-reviewed in the admin queue → the migration picker offers → **Yuki's lantern receives the GOLD "Blake reviewed it — you asked for this one" letter** → the chip retires itself. The first run also caught WHY you couldn't smoke it: **no client ever wrote `submitterUid`** (rules-ready since gate 18, never attached) — the gold letter could never have fired. Fixed in suggest.js; signed-in requests carry it now.
12. **The launcher:** root-caused — my .bat shipped with Unix (LF) line endings and cmd.exe choked, hence the half-second flash. Rebuilt with CRLF + an npm presence check + every exit path ends at a visible message-and-pause. **Tested the way you use it (real double-click): Mode 1 came up on :8888 and the window stayed.**

## PART B — all four wants + housekeeping
- **The gold-flip migration half:** `migrateRequestThread` callable (admin-locked, retags `anime:al:<id>` Tavern threads onto the catalog slug → they GAIN your gold verdict rail) + a branded picker in the admin suggestions flow after Mark-reviewed (skippable; **click-twice-to-confirm** since a wrong pick is a bulk retag; a capped 50-thread run says "more may remain"). +3 cf tests.
- **Season-room ↔ AniList decouple:** the room mounts even when the detail fetch fails (it never needed the metadata) — load-bearing now that rooms are everywhere. The `alId` starvation in the error path fixed with it.
- **The hardening trio:** Tab containment on the secondary (and the catch-up sheet), the loadSecondary **generation token** (slow fetch A can't paint over newer fetch B), and the close-window guard (+ the reopen-inside-the-window timer cancel the trio originally missed).
- **users/{uid} email tightening — built, then deliberately REVERTED:** the panel proved it would tombstone ACTIVE members ("a former member") whose identity predates the profile Studio (profiles docs are Studio-save-only). Slow-and-safe wins: the flip is re-banked behind a profiles backfill (designed in NEXT). The email-field exposure stays the pre-existing accepted trade it always was.
- `Start Mode 1.ico` honored in place; `*.ico` + `tmp*` in BOTH ignore mirrors.

## Adversarial (5 lenses, refute-verified: 17 confirmed → 2 HIGH + 8 unique MED + 12 LOW; 0 refuted)
- **HIGH×2 (fixed):** the accent ring collapsed to an in-flow blob on framed/background sheets (the content-lift rules overrode its positioning — excluded now, spec-pinned `position:absolute`) · the catch-up sheet's close timer wiped the scroll lock of the modal it had just deep-linked into (lock now releases only when no other layer is open).
- **MED (fixed):** sheet pings never marked read (now they do — viewing IS reading) · the users-tightening tombstones (REVERTED, above) · error-state rooms on unverifiable ids (watched-only there) · no sheet Tab containment (added, disabled/invisible filtered — the secondary's trap got the same filter) · the enlarged strip could bury Enter on short viewports (capped + compact breakpoint) · the glow was invisible (a mask clips its own shadow — the aura moved to the host) · silent capped migration + one-click bulk retag (the "more may remain" copy + the two-click confirm).
- **LOW (8 fixed):** close-timer reopen guards both sheets · sheet + frame-picker focus restore · a frames.js load failure can't silently wipe a saved frame · the `:has()` selector split (keyboard path survives engines without it) · disabled/hidden focusables filtered. **Accepted + documented:** the suggest-page auth-race can drop submitterUid on a tab-instant submit (anonymous-by-design fallback) · `anime:al:` threads minted AFTER a migration need a re-run (the admin can re-offer) · the 300ms close scrim cosmetic.
- **Also logged (pre-existing, not this gate):** the *anonymous* `#secondary=` boot-route has never opened the modal (all real consumers are signed in; banked for cutover polish) · a cf-cascade wait margin was raised 20s→45s after the suite's trigger volume grew (the heaviest pass sat at 20.3s — right on the old line).

## Tests — ALL GREEN (new highs)
`npm test` **200** (g24: 8 frame/ring/room/sheet contracts) · `test:rules` **152** (+6 frames/users, the users matrix re-pinned to the RESTORED public contract) · `test:functions` **77** · `test:cf` **67** (+3 migration) · e2e **10**.

## Checkpoint / deploy
One checkpoint, pushed. APP_VERSION stays **1.9.1**. **NO deploy.** **Gate 21 — THE CUTOVER — is the only thing left** (on your explicit "ship it": indexes → hosting → firestore → storage → functions, the full-floor pass, the close-out checklist).

## YOUR NUMBERED SMOKE (practice is up + seeded: http://127.0.0.1:8765/?emu=1)
**Sign-ins:** `prac-mika`…`prac-sora` / `practice123`; admin `blake@practice.test` / `practice123`.

1. **YOUR FRAME:** sign in as **blake@practice.test** → Account → Profile → the Frame row → **The Den Keeper** (the gold tile only YOUR account shows) → watch the hero put on the gold filigree + the hanging lantern. Check Public view wears it too.
2. **Member frames:** as Mika — pick **Witch's Pact** or **Five-Leaf Grimoire**, Save, then open her live page (Open your live public page ↗) — the frame travels. Confirm the Den Keeper tile does NOT exist in her picker.
3. **Real gradients:** Accent → **Nebula** + Glow on — a violet→rose gradient ring around the WHOLE card now (not one solid color), aura behind it.
4. **The catch-up sheet:** sign out, reopen the tab (fresh session) → the door (no 🎲) → the WHILE-YOU-WERE-AWAY strip is big now → tap a button → **the sheet**: cover cards / your lantern letters / airing chips — tap any one and land exactly there.
5. **Rooms on everything:** open a deep-dive for something you never reviewed → scroll to the bottom band → **THE ROOM** with your "haven't reviewed this one (yet)" lead-in. Post in it.
6. **The chip chain (your item 11):** as Yuki, Request an anime (📝 on its deep-dive) → reopen that deep-dive: **👁 1 request** → as admin, Suggestions → Mark reviewed → the migration picker offers (Skip is fine) → back as Yuki: the **gold letter** in your lantern → the chip is gone from the title.
7. **The renames + alignment:** Account — "Blake's reviews" chips, DM Blake everywhere, STATUS level with DISPLAY NAME, Activity reads like a feed.
8. **The launcher:** double-click **Start Mode 1.bat** — it opens, greets, stays.

## One-liner reply
Gate 20.5 closes the build: all twelve of your smoke items landed at their roots (the launcher was my own bare-LF line endings — rebuilt and double-click-tested; the catch-up buttons now open a real veil-sheet destination with cover cards, your gold-edged letters, and airing chips; gradients finally render as true two-stop rings around the whole card; rooms open on every season with your voice leading the un-reviewed ones; and the demand-chip chain is verified end-to-end with receipts — including finding and fixing the reason it never worked: no client ever wrote submitterUid), the **13 frame themes** shipped with your seeds + the anime-modeled set and **The Den Keeper locked to your account in the rules themselves**, all four pre-cutover wants landed (the migration callable + admin picker, the room decouple, the hardening trio, and the email tightening — which the panel proved would tombstone real members, so it was deliberately reverted and re-banked behind a backfill), the 5-lens panel confirmed 2 HIGH + 8 MED with zero refuted and everything fixed, every track is green at **200 / 152 / 77 / 67 / 10**, the checkpoint is pushed, practice is up — **and the only gate left is the cutover.**
