<!-- author: Code | date: 2026-06-11 -->
# v1.10.0 — GATE 20.6: the final polish round. All 9 of Blake's items landed at their roots. DONE · STAGED · NO deploy

Mode **ULTRAMAX**. The **5-lens panel confirmed 2 MED + 4 LOW (0 HIGH; 3 findings refuted)** — both MEDs fixed and pixel-proven, 3 LOW fixed, 1 LOW accepted + documented. All tracks green at new highs. **Gate 21 — THE CUTOVER — is again the only thing left.**

## Design vision (1 para)
This round makes the profile background honest: the picture you choose is the picture everyone sees — full brightness, framed in the same 4:3 window the card actually paints, with the FRAME-IT preview showing the exact shading the card will wear. Text never fights the art because the art was never dimmed for it: the name rides a soft top cap, the bio rides the floor, and every mid-card chip and row carries its own little glass plate. Around that: gradients that finally read as gradients from across the room, rooms that open in your plain voice, rules that come to the Save button instead of sending people away, and a request flow that actually routes.

## PART A — your 9, item by item (your numbering)
1. **Background cropper — preview accuracy + brightness parity.** Root causes found, both halves: **(a)** the cropper captured at 16:9 while every card paints a ~4:3 window with cover-fit, so the render systematically chopped the sides of what the preview promised — the capture is now **4:3 (480×360 stage, 1280×960 out)**, and a pixel walk with a colour-ramp test image proved the framed edges now survive onto the card (left edge rgb(226,168,45) vs source (230,170,40); right edge rgb(34,168,228) vs (30,170,230)). **(b)** the card was painting your picture at **38% opacity under a full-card scrim** — that dim is dead. The picture renders at FULL brightness (hero midband sampled rgb(130,170,135) — the source's exact value; the old code painted (49,64,51)); legibility comes from two localized caps (top, under the name; bottom, under the bio) plus text shadows — and the FRAME-IT preview now paints those SAME caps, with a hint line saying so. Preview = render.
2. **Gradients round 3 — root cause: analogous color pairs.** The machinery was fine; five of the six pairs were near-identical hues (orange→soft-red etc.) that read solid on a thin ring. Every pair is now nebula-grade (a dark anchored stop vs a bright far stop: ember #c62828→#ffab91, ocean #283593→#4dd0e1, meadow #2e7d32→#aed581, dusk #303f9f→#ff8a65, sakura #ad1457→#f8bbd0; nebula untouched), the ring's stops are corner-anchored (each color OWNS a corner before the blend) and the gradient edge is 4px. **Verified each of the 6 with sampled pixels**, corner vs corner: Δ140 / 290 / 268 / 292 / 336 / 360 — all read as two colors. The picker swatches mirror the new pairs. No gold anywhere (spec-pinned).
3. **Room copy** — your sentence, verbatim, nothing more: *"I haven't reviewed this one (yet). Feel free to discuss anyway."* (The spoiler reminder lives on naturally in the composer's spoiler button.) Spec-pinned so it can't drift.
4. **The catch-up strip — diagnosed, verified, the rule in one sentence:** **The strip appears on the welcome door only when something is actually waiting for you — new reviews since your last visit, unread lantern pings, or watchlist titles airing — and the last two only exist signed in, so a signed-out visit with nothing new gets a quiet door on purpose.** That's what you hit: signed out + nothing new = no strip, by design, not a regression. The signed-in path is PROVEN live: a returning user with one unread letter, fresh session, warm auth (the exact race shape) → the door showed "🏮 1 new lantern ping" reliably, screenshot-verified. The 20.5 warm-auth fix holds.
5. 🔴 **Request-an-anime 404 — fixed at the root, both environments.** The practice server had no clean-URL handling: prod runs `cleanUrls: true` (firebase.json, read verbatim — `/suggest` → `suggest.html` is automatic there), but practice served the literal extensionless path → 404. **You found a practice-only bug — prod was never broken, but it would have hidden any real routing bug behind that 404 wall, so practice now routes exactly like prod** (extensionless → `.html`). Then I re-ran the chip chain end-to-end **through the real request flow**: Yuki → deep-dive → the actual Request pill click → landed on /suggest → title + card prefilled → submitted (submitterUid attached, doc-verified) → the 👁 1 request chip appeared → admin marked reviewed → **Yuki's gold letter landed** → the chip retired. Every link held.
6. **Consent comes to the Save — verified, hardened, and the real story:** the modal-on-save wiring already existed and WORKS (driven live: fresh user → Save → the rules appear in place → I agree → the same save completes, no re-click — now a permanent e2e spec). What you hit was the degraded path: when the consent check can't read your gate (exactly what a half-dead sandbox does — I found the emulators in that state on boot), it skipped the modal and the failure copy pointed at a banner the save had just hidden. Fixed: **a failed public write now re-offers the rules modal IN PLACE and retries the save once** — and the adversarial panel caught that a staged background used to bypass that re-offer with a false "Saved —" message; the ordering is fixed so the re-offer always runs first and a fresh consent retries the background upload too. Audited every other gated surface: comments, replies, likes, reviews, threads, posts, votes, collections-publish all offer the modal inline already — no other dead-ends.
7. **Update log** — "Big Update" → **"Major Update"** everywhere it renders (label only; the purple rail is untouched). Touch-up: the dormant gold semver-MAJOR tier was also labelled "Major Update" — it's renamed **"Milestone Update"** in the authoring skill so the two can never collide. No other widget copy needed work (it already reads visitor-first).
8. **The backgrounds fine print** → a branded spec row: a small **BACKGROUNDS 背景** kicker + three scannable pills (**JPEG · PNG · WebP · GIF** / **up to 5 MB** / **verified email**) + one quiet line: "They follow the community rules and can be reported."
9. **Frame personality (your optional latitude)** — the four flattest frames got the Shadow-Garden treatment with their own assets (asymmetry, depth, zero new animations): **Ember** banks a bonfire heart in one corner of its flame line; **Deep Tide** rolls a second, far swell behind the crisp crest; **Stormcall**'s answering strike is bigger now with a third distant fork; **Moonlit Veil**'s moon gained a halo and a second drifting mist band. Compositor-cheap, reduced-motion untouched, no gold (the gold-grep spec still passes).

## Adversarial (5 lenses, refute-verified: 2 MED + 4 LOW confirmed, 0 HIGH, 3 refuted)
- **MED (fixed):** the pre-existing bg-upload early-return ran BEFORE the new consent re-offer — with a background staged, the blip scenario showed "Saved —" while nothing public had saved, and the re-offer never fired. Reordered: the profile-write outcome (incl. re-offer + retry + a bg-upload retry once consent lands) resolves first; the bg message only stands once it's honest.
- **MED (fixed + pixel-proven):** the full-brightness midband left mid-card surfaces (activity tabs, rows, tag chips, the pinned review, shelves) shadow-less over arbitrary art — measured 1.5–2.8:1 on bright images. Every mid-card surface now carries its own dark plate on a has-bg sheet (the picture stays full-brightness around them; bg-less sheets unchanged; the active tab keeps a distinct purple plate). Proven against the worst case — a pure-white background: plates hold at luminance 28–97 while the open midband reads 226. Spec-pinned.
- **LOW (fixed):** the profile kicker lacked a dark edge in the top-cap zone (now shadowed) · cropper.js's header still documented 16:9 (corrected) · the preview caps rebuilt two gradients every drag frame (hoisted — built once per dialog).
- **LOW (accepted + documented):** light frame motifs (the moon, fork tips) can wash out where they cross the top cap's fade over a BRIGHT background — aesthetic only, the frame's ring/glow/washes always read; banked as cutover-polish if Blake ever notices.
- **Refuted (3):** a "mixed-vintage write" claim (pre-existing, not this gate); a duplicate of the ordering bug with an unreachable scenario; a "the 4:3 capture made the hero chop worse" framing that collapsed under its own numbers (the hero shows MORE of the image than before).

## Tests — ALL GREEN (new highs)
`npm test` **209** (g25: 9 gate-20.6 contracts — gradient pairs Δ≥120, corner-anchored ring, full-brightness + two-cap structure on both surfaces, 4:3 capture, the verbatim room copy, the label rename, the spec-chip form, the midband plates, the frame layers) · `test:rules` **152** · `test:functions` **77** · `test:cf` **67** · e2e **11** (+1: the real consent-on-save flow, self-resetting). Two account-page Playwright specs flaked once under full parallelism and passed isolated + on the clean re-run (the documented flake class; final full run was a clean 209).

## Notes for Cowork
- `docs/SKILLS/widget-update-skill.md` (a Cowork-excluded file) carries the tier-label rename in the working tree — "Major Update" for MINOR-overhauls, "Milestone Update" for the gold semver-MAJOR. Keep it when you next touch that file.
- Practice-serve now emulates prod's `cleanUrls` resolution (extensionless → .html). The other half (prod 301s `/suggest.html` → `/suggest`) is deliberately NOT emulated — e2e specs navigate by .html paths.

## Checkpoint / deploy
One checkpoint, pushed (hash in CODE-HANDOFF). APP_VERSION stays **1.9.1**. **NO deploy.** Practice restarted with a clean seed for your smoke. **Gate 21 — THE CUTOVER — remains the only gate left** (on your explicit "ship it").

## YOUR NUMBERED SMOKE (practice is up + re-seeded: http://127.0.0.1:8765/?emu=1)
**Sign-ins:** `prac-mika`…`prac-sora` / `practice123`; admin `blake@practice.test` / `practice123`; fresh-rules user `prac-newbie@practice.test`.

1. **The background, honest:** as Mika → Account → hover the hero → Change background → pick a bright picture from your PC → **the FRAME-IT window is the card's real shape now, full brightness, with the soft text-shading shown in the preview** → Apply → Save → the hero (and Open your live public page ↗) shows the SAME picture at the SAME brightness, framed the way you framed it.
2. **Gradients, all six:** Accent → try every gradient pill — each ring now shows one color at one corner of the card and the second at the other. Nebula is no longer the only honest one.
3. **The room, your words:** open a deep-dive for something you never reviewed → the room's lead-in reads exactly *"I haven't reviewed this one (yet). Feel free to discuss anyway."*
4. **Request an anime:** on that same deep-dive hit 📝 **Request this anime** → the suggestion page OPENS (no more "not found"), title pre-filled → send it → reopen the deep-dive → **👁 1 request**.
5. **Consent at the Save:** sign in as **prac-newbie** → Account → change the status line → **Save** → the rules appear right there → I agree → the save completes by itself.
6. **The strip rule:** sign in as Yuki (she has an unread letter), close the tab, reopen → the door shows **🏮** on the strip. Then sign out and reopen → quiet door (that's the by-design case you hit).
7. **The update log:** the door's changelog reads **Major Update** now.
8. **The fine print:** Account → the backgrounds line under the hero is the new chip row.
9. **Frames:** as Mika try **Ember Breath / Deep Tide / Stormcall / Moonlit Veil** — each has more going on than before (a bonfire heart, a far swell, a third strike, a moon halo). The Den Keeper stays yours alone.

## One-liner reply
Gate 20.6 lands all nine of your items at their roots — the background picture now renders at its TRUE brightness in a cropper window that matches the real card (preview = render, pixel-proven against a test ramp), all six gradients read as two colors from across the room (sampled corner-vs-corner, Δ140–360), the room speaks your exact sentence, the catch-up strip's rule is one honest sentence (quiet door signed-out with nothing new IS the design — and the signed-in path is proven live), the request-anime 404 was a practice-server-only routing gap now fixed to route exactly like prod with the whole chip chain re-verified through the real page, the rules modal now always comes to the Save (verified live, hardened against the degraded path the panel caught, and pinned as a permanent e2e spec), the update log says Major Update, the fine print became branded spec chips, and the four flattest frames got their personality — the 5-lens panel confirmed 2 MED + 4 LOW (zero refuted-that-stood, both MEDs fixed and pixel-proven against a pure-white worst case), every track is green at **209 / 152 / 77 / 67 / 11**, the checkpoint is pushed, practice is re-seeded for your smoke — **and the only gate left is the cutover.**
