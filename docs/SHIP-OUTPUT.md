<!-- author: Code | date: 2026-07-02 -->
# v1.10.2R — a fully functional site + admin, the Creator profile per Blake's clarified spec. **STAGED, all tracks green — deploys on your go (hosting + firestore rules).**

Mode **ULTRAMAX**. Every end-state item met, self-verified like your eyes: live walks on the seeded sandbox, SAMPLED PIXELS, real double-clicks, and a 5-lens adversarial panel (13 confirmed findings — every one fixed or formally accepted; the panel caught a mid-flight spec update I'd have missed). **Nothing deployed.**

## Item 1 — THE CREATOR PROFILE, your clarified spec (evidence: pixel walk + e2e + rules tests)
- **Your sheet is a member sheet now.** The staged gold showcase never deployed and is DELETED: the forced Den Keeper default, the gold Visit-the-Den door, the suppressed Reviews tab, and the imposed gold avatar-ring/name-glow are all gone. Your sheet renders from YOUR profiles doc — accent, frame, background, bio, tags, status, pinned review — wherever a member's would (name clicks on threads/comments/rooms, your Public view). **The ONE difference: CREATOR 創り手 in gold where members wear MEMBER 旅人.**
- **Pixel-proven, not class-proven:** the walk signed in as Mika, opened your sheet, screenshotted it, and sampled raw pixels — **300 gold pixels painted in the kicker glyphs; ZERO gold pixels in the avatar/ring region.** The Appreciate row renders live with its count; no den door; Threads + Reviews tabs both present; no forced frame attribute.
- **Appreciate ON — your explicit call ("I want my account to be appreicated.") is BUILT:** the rules' like-on-Blake denial is REMOVED (this is the firestore-rules change in the deploy); members can Appreciate you exactly like anyone; the CF counts it and sends your ping like any member's; self-like stays denied for everyone including you (no inflation). Rules-tested both ways: `a member CAN appreciate BLAKE` passes, `BLAKE self-liking stays DENIED` passes.
- **Kept from the staged build:** the reserved-name carve-out (you save as Blake; "bLaKe" impersonators still bounce — rules-tested), the no-report-flag on the owner, all entry-point wiring, the never-a-tombstone fallback. **The Den Keeper frame stays yours alone (rules-locked) but is now optional — pick it in the Studio or don't; nothing wears it for you.**
- e2e re-pinned: a member meets your member-parity sheet (kicker + Appreciate row + Reviews tab + no door + no imposed frame) and your Public view renders without refusal.

## Item 2 — the account nav (kept)
View All · Random · Filter on the account header, the `?open=` boot mechanic untouched, all g29 nav specs green (incl. the painted-pixels filter-panel spec).

## Item 3 — the member-reported prod bugs, dead at the root
- **3a picker crush:** real root cause — a flexbox min-height collapse (`overflow:hidden` on `.acct-dd-opt` zeroes the flex minimum, so 60 review rows in the 300px menu crushed to ~3px and painted into each other). One rule (`flex: 0 0 auto`) fixes all four brand-select menus. Pinned by a RENDERED 60-option probe: row height > 20px, zero overlap, the menu scrolls (g30).
- **3b admin 404s:** root cause — `admin-fab.js` used RELATIVE hrefs; on `/admin/new-anime.html` (the only admin page mounting the FAB) every item resolved to `/admin/admin/*` → 404. All 7 destinations are root-absolute now. **Live-walked on the sandbox: the FAB from the Add-Anime page lands on `/admin/quotes.html`.** Where a feature genuinely needs Mode 1, the page says so plainly now: quotes + season-reviews banners rewritten ("double-click MODE 1, then reload"), the runtime save-path messages de-jargoned (no more "start `npm run mode1`"), the Add-Anime page announces its mode ("Generate mode — the Mode 1 desktop server is not connected…"), and the ✨ASK drawer explains it needs Mode 1 instead of erroring raw — **its old copy leaked the AI provider's name into the UI; scrubbed and spec-pinned.**
- **3c suggestion queue:** rows expand to a detail view — requester name resolved from `submitterUid` (profiles → users fallback, "Anonymous" for pre-v1.10.0 docs), the FULL request text, submitted date, AniList fields; keyboard-accessible (Enter/Space, aria-expanded), escape-first on all member text, `[hidden]` twin shipped. The NEW-badge clip is root-fixed (`flex-wrap` on the meta strip — the pill can no longer slide under "Add this anime") plus the mobile "ADD THIS ANIM" button truncation. Verified with real-pixel bounding-box checks at 4 widths; the sandbox seed has no suggestion rows, so your prod queue (the PLUTO row) is the natural final look.

## Item 4 — the Mode-1 desktop launcher (third ask — root causes found; the CRLF fix HAD held)
Byte-check proved the .bat was already CRLF — the real killers: **the server never opened a browser** (a perfect start looked like "nothing happened") and **a second double-click died in a raw EADDRINUSE stack**. Now: double-click → greeting banner ("keep this window open…") → server starts → **your browser opens to Mode 1 by itself**; double-click #2 identity-checks the port (curl to `/api/health` — a stray app on 8888 no longer counts as Mode 1) and just opens your browser to the running one; missing node_modules and missing npm each get a plain message; every exit path pauses — the window never vanishes. **Tested the way you run it (Start-Process double-click semantics): cold start → HTTP 200; second click → same server PID, no crash, clean exit.** The .bat re-verified CRLF byte-by-byte after every edit.

## Item 5 — nothing regressed
All tracks green **after** every change: `npm test` **229** (was 225; +4 g30 root-cause pins) · `test:rules` **157** (was 156; the like-on-Blake reversal tested both ways) · `test:functions` **77** · `test:cf` **67** (re-run after the rules change) · e2e **16**. friendly-errors + the v1.10.1 upload behavior untouched (g28 pins green).

## Adversarial panel (5 lenses → 13 confirmed / 6 refuted, 3-skeptic refute-verify per finding — ALL 13 fixed or formally accepted)
- **HIGH (fixed):** the SHIP-PROMPT was re-staged mid-flight with your Appreciate reversal — my build had kept the old no-Appreciate guard and the CHANGELOG claimed the opposite spec as your word. The panel caught the contradiction; Appreciate-ON is now built end-to-end (rules + client + preview + tests + docs).
- **HIGH (fixed):** the new browser auto-open fired during every `npm test` run (`tests/mode1-server.spec.js` boots the real server) — real tabs on your machine. Fixed twice over: the spec sets `MODE1_NO_OPEN`, and the server only auto-opens when attached to a real console (isTTY). *Honesty note: 1–2 stray tabs may have opened on your desktop during this session's test runs before the fix — close them if so; harmless.*
- **MED (fixed):** the .bat's already-running check trusted ANY process on port 8888 — now identity-checked via `/api/health`. · The Add-Anime notice claimed "you're on the live site" on localhost sandboxes — copy made location-neutral. · Stale docs the panel caught (NEXT.md's dead gold-sheet baseline, the old SHIP-OUTPUT smoke pointing at a deleted button, CODE-HANDOFF's old staged description) — all rewritten this gate.
- **LOW (fixed):** `admin/inbox.html` + `admin/reports.html` sat at `APP_VERSION="1.9.1"` for two ships — bumped to 1.10.2 AND the bump script's TARGETS now covers them so the class is closed.
- **Formally accepted:** a boot-window race where two near-simultaneous double-clicks each open one tab (identical to the designed one-tab-per-click outcome; skeptic-refuted as a defect, accepted as-is). The pre-existing DEP0190 deprecation warning printing at Mode-1 start (cosmetic, pre-existing — banked).
- **⚠️ For Cowork (your working-tree docs, not committed by me):** `docs/AI-PRIMER.md` says "Live at v1.7.1" and `docs/HANDOFF.md` says prod serves 1.10.0 — both stale; worth refreshing before the mega-run prompt.

## Found beyond the list (fixed)
The staged v1.10.2 had never run the version bump — every surface still said 1.10.1. Bumped to **1.10.2** (67 strings via `npm run bump`), CHANGELOG entry + the runtime widget section written, ROADMAP current-state updated (it was two ships stale), CLAUDE.md's bump checklist line numbers corrected.

## YOUR SMOKE (practice is up: http://127.0.0.1:8765/?emu=1 — 3 steps)
1. Sign in as **prac-mika@practice.test** / practice123 → open any thread or comment by Blake → **click your name** → your sheet: purple like a member's, **CREATOR in gold**, the **♥ Appreciate** button with a count. Tap Appreciate — the count moves.
2. Sign in as **blake@practice.test** → Account → **👁 Public view** → your sheet as members see it. Then Account → the pinned-review picker → open it — rows stand full-height and the list scrolls.
3. **Double-click MODE 1 on your desktop** → the window greets you and your browser opens to the Add-Anime page by itself. Double-click the icon AGAIN → it says Mode 1 is already running and opens the page instead of crashing.

## DEPLOY PLAN (awaiting your explicit go — nothing runs until then)
`firebase deploy --only hosting` then `firebase deploy --only firestore:rules` (the rules changed this gate — the Appreciate reversal). **No storage rules touched → no Google-permission dance.** Post-deploy verify: prod serves APP_VERSION 1.10.2, a member's Appreciate on your profile lands, `/admin/quotes.html` reachable from the FAB on prod.

## One-liner reply
Your profile is yours now — a member sheet from your own Studio with gold CREATOR as the only mark, and by your word members can finally Appreciate you (the rules block came out, tested both ways); the picker crush, the /admin/admin 404s, and the unreadable suggestion queue are dead at their real roots; Mode 1 greets you and opens your browser itself on a plain double-click (twice-clicked, test-proven); the adversarial panel caught your mid-flight Appreciate call AND a test-run browser-tab leak and both are fixed; every track is green at **229 / 157 / 77 / 67 / 16** — **staged, practice is up for your 3-step smoke, and it ships (hosting + firestore rules) on your word.**
