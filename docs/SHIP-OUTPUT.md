<!-- author: Code | date: 2026-06-10 -->
# v1.10.0 — GATE 20: round-4 smoke fixes + the catch-up door + THE CHERRIES + the sweepers. DONE · STAGED · NO deploy

Mode **ULTRAMAX**. The **5-lens adversarial panel confirmed 2 HIGH + 7 MED (unique; 0 refuted) + 8 LOW — all HIGH/MED fixed, 6 LOW fixed, 3 accepted + documented.** All tracks green at new highs. Production untouched. **Only gate 21 — THE CUTOVER — remains** (plus an optional 20.5, listed in NEXT).

## Design vision (1 para)
Gate 20 closes every item from your round-4 smoke at the root — the dropdowns that never collapsed were one CSS class of bug (`display:flex` beating `[hidden]`, the same trap that once hid the consent banner; now spec-pinned dead), the "missing" signed-out welcome was the once-per-session door not re-greeting after sign-out (it does now — a sign-out is a fresh arrival) — and then gives the door a reason to exist for returning users: **WHILE YOU WERE AWAY** buttons that catch you up (new reviews from Blake since your last visit · your unread lantern pings · watchlist titles airing right now), each expanding into deep-links, plus a **🎲 Surprise me** that rolls you straight into one of the 44. The shelves grew real search and descriptions, Activity reads like a feed instead of source code, Message Blake finally looks the part (pixel-proven), the season room sits exactly where you circled, and the one pre-prod Storage gap — images orphaned by EDITS — is closed by real sweepers.

## PART A — your 10, item by item
1. **Welcome signed-out** — diagnosed live: the door was NEVER auth-gated (a fresh signed-out visit shows it). Your repro was the in-tab sign-out: the door is once-per-SESSION and a sign-out didn't reset that. Now **sign-out clears the session flag** — the door greets every signed-out arrival.
2. **Redundant pickers** — the Avatar and Background form rows are GONE; the hero's hover pills are the one way (a "✕ Remove background" pill joins them when a background exists; the size/rules note moved under the hero).
3-5. 🔴 **brandSelect collapse** — ONE root cause for all three: the menu's `display:flex` beat the `[hidden]` attribute (the consent-banner trap class), so every dropdown rendered permanently open. Fixed with the `[hidden]` twin rule, **verified live (rest→closed, click→open, Esc→closed) + spec-pinned** so it can't regrow.
4. **Subtext copy** — every panel rewritten plain: "Anime you plan to watch — saved and sorted your way." / "Your favorite anime, all in one place." / "Make your own lists…" / "Your comments, reviews, threads, and replies — all in one feed." / "Manage your email, password, and sign-in." / "Your messages — send me one anytime, I read them all."
6. **Collections picker** — the add-picker now leads with **your watchlist + favorites**, then the 44, and typing 3+ letters **live-searches the wider world** (the Discover search infra, debounced + abortable, results under "From the wider world 検索" — no provider names).
7. **Collection descriptions** — optional, ≤200 chars, rules-validated (+5 rules tests), an inline editor on the card (Add/Edit description), shown under the shelf title on the account AND the public profile (escape-first both ends).
8. **My Activity** — snippets are plain sentences now: `[img:N]` tokens become 🖼, `**bold**`/`*italic*`/`~~strike~~`/spoiler pipes are stripped (render stays textContent — no new HTML sink), rows got the official pass (framed, roomier, 2-line clamp, type hierarchy).
9. 🔴 **Message Blake (the 4th ask) — WITH PIXEL PROOF:** the card is a hero now (swaying lantern beside a bright MESSAGE BLAKE 便り stack, a real gradient "✉ Write to Blake" pill, hover glow) and the thread is a room (a "🏮 Writing to Blake" title bar, a framed letter surface, a branded centered empty state, the chip back-button, a finished composer + Send ➤). Screenshot-verified this round — not claimed, shown.
10. **Season room** — out of the left rail, into a **full-width band at the modal's bottom: after LINKS, before MORE LIKE THIS** (your circled spot). Verified live in the DOM order + spec-pinned; H5 intact (your review leads the body far above it).

## PART B — gate 20 proper
- **The catch-up door:** signed-in returning users get up to three WHILE-YOU-WERE-AWAY buttons — **✍ Blake added N new reviews** (a local snapshot diff of the 44; expands into per-title deep-links), **🏮 N new lantern pings** (opens the Lantern), **📺 N on your watchlist are airing now** (per-title deep-links into the deep-dive; the airing pool rides a 6h cache). Buttons render progressively in a bottom strip (they never shove the Enter button); quiet visits show nothing. **Verified live:** seeded a stale snapshot → "Blake added 43 new reviews" + 4 sub-buttons → deep-link landed on the review modal.
- **🎲 Surprise me** — on the door beside Enter; rolls the existing Random machinery into one of the 44.
- **👁 N requested** — non-watched titles on the secondary modal show the demand chip beside Request (purple, 99+ capped). `suggestionCounts` single-doc GET went public for it; **LIST stays admin-only** so the queue can never be enumerated or ranked (the original anti-brigading intent, kept).
- **The gold-flip (ping half):** when you Mark-reviewed a suggestion in the admin queue, the requester gets a **gold Blake-origin lantern letter** ("reviewed it — you asked for this one") that lands on the title's deep-dive, and the chip retires itself (status mirrors to the rollup). Farm-proof: only the admin can flip a suggestion's status (rules-verified). Mutable via the Lantern's mute chips like every type. *(The thread-migration half — an `anime:al:` Tavern thread gaining your gold verdict rail — is BANKED with a full design in NEXT: the slug truth lives client-side, so it needs the admin page to pass it; ~half a gate.)*
- **Footer premium pass:** About/Contact wear the kicker vocabulary (ABOUT ME 自己紹介 / CONTACT ME 連絡), the panels got the gradient+frame+hover treatment, both boxes scroll-reveal. **Your copy untouched.**
- **Mode-1 launcher:** `Start Mode 1.bat` in the project root — double-click, no terminal (right-click → Send to → Desktop for a shortcut). `*.bat` is hosting-ignored.
- **THE SWEEPERS (the pre-prod gap):** `imageRefs` **edit-strip** triggers on all 5 surfaces (forum thread/post, comment, reply, review) — an edit that drops an image now deletes the orphaned object (mirroring the profile-background sweeper; thumbnail pointers stay protected) — plus a **daily orphan reaper** for uploads whose doc never landed (24h grace, keep-on-uncertainty, 500-delete cap, 540s/512MiB). +7 cf-tests, +21 unit tests.
- **Also:** admin TEXT reports now show the reported doc's attached images as thumbnails (you could only see reported images before) · the firebase-ignore mirror gained `tests-e2e/**`, the emu config, `*.log`, `*.bat`, and `tmp*` (a sweep found the e2e specs and practice.log were deployable — the 3×-bitten class, now structurally closed in BOTH .gitignore and firebase.json).

## Adversarial (5 lenses, refute-verified: 20 confirmed → 2 HIGH + 7 unique MED + 8 LOW; 0 refuted)
- **HIGH (both fixed):** catch-up signal 3 could NEVER render (it ran before the door opened and its snapshot advanced on every load, eating the diff unseen — 4 lenses caught it; the wiring moved inside the door-open and is now live-verified) · my own walk screenshots sat unignored in the deploy root (the leak class, twice in one session — now `tmp*` is dead in both mirrors).
- **MED (all fixed):** the warm-auth race dropping signals 1-2 (solved by the same re-wiring) · Esc tearing down the door AND a stacked deep-linked modal (key contained) · Enter on a focused door button dismissing the door instead of clicking (passthrough) · the collection description/rename editors never closing after save (the open input deadlocked all repaints + blur double-wrote; they self-remove on commit now) · the door's airing check hitting the network every session (6h cache) · late-arriving buttons re-centering the card under your cursor (the strip is position-anchored now) · the chip count's integrity (anonymous requests can inflate it — capped at 99+, accepted as a demand signal, never a trust surface).
- **LOW:** 6 fixed (malformed-imageRefs keeps everything · reaper got real limits · request_done joined the mute chips · no ghost rollup docs · search stale-row/case-fold · long-token clamp). **Accepted + documented:** duplicate gold pings on an admin status round-trip (admin-driven) · the sweeper's fence is owner-level, not doc-pinned (deliberate — doc tokens ≠ doc ids on every surface; a forged ref can only ever delete the SAME owner's objects, which they can do anyway).

## Tests — ALL GREEN (new highs)
`npm test` **192** (g23-gate20: 11 new contracts) · `test:rules` **146** (+5 description, +2 chip; one OLD test updated to the deliberately-flipped chip contract) · `test:functions` **77** (+21 sweep) · `test:cf` **64** (+7 sweepers) · e2e **10**. (The review-halo e2e reds on the first post-boot run and greens warm — the documented race, twice today.)

## Checkpoint / deploy
One checkpoint, pushed. APP_VERSION stays **1.9.1**. **NO deploy.** Remaining: optional **20.5** (the gold-flip migration half, the season-room decouple, the secondary hardening trio, users-email tightening — all designed in NEXT) → **gate 21 = THE CUTOVER** on your explicit "ship it."

## YOUR NUMBERED SMOKE (practice is up + seeded: http://127.0.0.1:8765/?emu=1)
**Sign-ins:** `prac-mika`…`prac-sora` / `practice123`; admin `blake@practice.test` / `practice123`.

1. **The door, both ways:** fresh tab → the door (Enter + 🎲 Surprise me). Sign in, then sign OUT — the door greets you again (your item 1). Tap **Surprise me** — it rolls one of your 44.
2. **Dropdowns collapse (your 3/4/5):** Account → Watchlist sort, the Pinned-review picker, and the Private/Public picker when creating a collection — all closed at rest, open on click, close on pick/Esc/outside.
3. **The hero is the only picker (your 2):** Profile — no Avatar/Background rows; hover the hero for ✎/🌌 (and ✕ Remove once a background exists).
4. **Shelves grown up (your 6/7):** Collections → ＋ Add anime → your saved lists lead; type 3+ letters of anything ("frieren") → "From the wider world" results appear. Add a description — it shows under the title here AND on your public page.
5. **Activity + Message Blake (your 8/9):** My Activity — sentences, not `**markdown**`; 🖼 where images were. Inbox — the lantern hero card, "✉ Write to Blake", and the framed letter room.
6. **The room, your spot (your 10):** any watched season's deep-dive → scroll past LINKS → **THIS SEASON'S ROOM**, full-width, above MORE LIKE THIS.
7. **The demand chip + the gold-flip:** open a NON-watched anime someone requested (or file a request via 📝 first) → the **👁 requested** chip sits beside Request. As **blake@practice.test** → admin Suggestions → Mark one reviewed → the requester's Lantern gets your **gold** "you asked for this one" letter, and it lands on that title's deep-dive.
8. **The launcher:** double-click **Start Mode 1.bat** in the project folder — Mode 1 boots with zero terminal typing.

## One-liner reply
Gate 20 lands all ten of your round-4 smoke items at their roots (the never-collapsing dropdowns were one `[hidden]`-vs-`display:flex` trap now spec-pinned dead; the "missing" signed-out welcome was the once-per-session door not re-greeting after sign-out — it does now; Message Blake finally got its premium pass with the pixel proof to show for it; and the season room sits full-width exactly where you circled — after LINKS, before MORE LIKE THIS) plus the gate-20 build proper: WHILE-YOU-WERE-AWAY catch-up buttons on the door (new-reviews/lantern-pings/watchlist-airing, all deep-linking, live-verified), 🎲 Surprise me, the 👁 requested chip (public single-doc read, list still admin-only so it can't become a leaderboard), the gold-flip's ping half (Mark-reviewed → a gold Blake letter to the requester, farm-proof), collection live-search + descriptions, the footer's veil-era pass, the double-click Mode-1 launcher, and the last pre-prod Storage gap closed (edit-strip sweepers on all five imageRefs surfaces + a daily orphan reaper) — the 5-lens panel confirmed 2 HIGH + 7 MED with zero refuted (notably: my catch-up signal that could never render, caught by four lenses independently, and my own screenshots recreating the leak class — both now structurally dead), everything green at **192 / 146 / 77 / 64 / 10**, the checkpoint is pushed, practice is up + seeded, and nothing deploys — **the cutover is all that's left.**
