<!-- author: Code | date: 2026-06-11 -->
# v1.10.0 — GATE 20.7: Blake's final smoke fixes + the full confirmation pass. DONE · STAGED · NO deploy — **READY FOR THE CUTOVER**

Mode **ULTRAMAX**. The **5-lens panel confirmed 3 MED + 4 LOW (deduped; 0 HIGH, 6 refuted)** — every one fixed and re-verified live. All tracks green at new highs. **The cutover (gate 21) waits for your explicit "ship it."**

## Design vision (1 para)
This round closes the trust gap between what you do and what you see: the save chain now provably works in the sandbox (the failure you hit was the sandbox rejecting its own emulator's avatar URLs — fixed at the root, with prod untouched), the catch-up demo is restart-proof and comes to you even when you sign in late, and every "did it work?" surface — the chip, the note, the tabs — sits where your eyes actually go.

## PART A — your 5, item by item (your numbering)
1. **The preview note (your call, your words honored):** no geometry changed. A small branded line now appears under the hero whenever a background is set or staged: *"👁 This hero is the editor's window — **Public view** (or your live page) shows the background the way everyone actually sees it."* It shows right after Apply and stays while a background is in play; it leaves when the background does. Spec-pinned with its `[hidden]` twin.
2. 🔴 **The flaky save + the stuck pfp — ONE root, found and fixed, and your instinct was right that they were connected.** The sandbox's own Storage emulator hands out `http://127.0.0.1:9199/...` avatar URLs, but the security rules (and the three client-side avatar gates) only accepted the real production origins — so the moment your save carried a new avatar, the WHOLE public-profile write was denied ("the public profile changes didn't go through") and the new pfp could never reach profiles, which is what every comment/thread/card reads. **Sandbox-only, proven:** prod URLs always passed (this could not have bitten on the live site). Fixed properly: the rules and the three client gates now accept exactly that one practice origin, scoped so prod stays https-only (2 new rules tests pin it, including near-miss hostile origins staying denied). **Walked live with receipts:** Mika changed her avatar to a test image → Save succeeded clean → the comment avatar repainted with the actual pixels of the new image (sampled `rgb(235,120,20)` — the exact test color), and the profile card matched. The save itself stays live-propagating (every author surface is an onSnapshot subscription — no refresh dance).
3. **Public profile, shallower:** Comments + Replies tabs are gone from the public card — Threads + Reviews stay, their loaders and helpers left with them. Your OWN My-Activity view on the account page keeps all four (that's your private view of yourself, not their public display). No deep-link orphaned: comment/reply letters never routed through those tabs (they land on the content itself). E2e re-pinned: the public card carries exactly 2 tabs.
4. **The 👁 chip moved:** it now docks neatly centered UNDER the cover thumbnail on the deep-dive (the cover rides a small column; the chip keeps its purple pill look and its unreviewed-only rule). The panel also caught that it used to pop in late and nudge the layout — the count now loads in parallel with the page so the chip lands in the same paint. Geometry asserted in the walk: below the cover, centered.
5. 🔴 **The lantern demo — my fault, twice, and now it can't happen again.** (a) *Why you saw nothing:* the seed only ever gave MIKA notifications; my 20.6 recipe pointed you at Yuki, whose letter existed only inside my own test run — the re-seed before your smoke erased it. You followed the steps correctly both times. (b) *Fixed at the seed:* every practice boot now mints Yuki **2 guaranteed-unread letters** (a gold Blake message + a Mika like), so the demo survives any restart — and the panel caught that my new letter deep-linked to the WRONG user's comment; it now lands on Yuki's own bubble (walked: the halo lit her comment). (c) *Your latitude, built:* signing in MID-SESSION (after the door is gone) now shows a compact dismissible **"🏮 While you were away — N new lantern pings"** strip under the header; tapping it opens the same catch-up sheet. Door behavior untouched; the strip tears itself down on sign-out/account switch, sizes correctly on phones, and never overstates the count. (d) **The foolproof recipe + pixel proof are below — and the screenshot is in this report's evidence set (tmp-207-door-strip / signin-strip shots were taken live).**

## PART B — THE CONFIRMATION PASS (I walked it; you don't have to)
| Area | Verdict | Evidence |
|---|---|---|
| Welcome door + catch-up | **PASS** | Door strip 🏮 walked (screenshot) · signed-out quiet door = design · sign-in strip → sheet → letter lands on Yuki's own comment (halo verified) |
| The Tavern | **PASS** | 5 seeded threads render · thread opens · a real post landed through the live composer |
| Rooms (unwatched + watched) | **PASS** | Unwatched: your verbatim lead-in + a real post landed · watched rooms ride the same machinery (spec-pinned, e2e-covered) |
| Images | **PASS** | e2e: seeded Tavern image decodes + inline `[img:1]` + card thumbnail + report affordance |
| Profiles / frames / accents / cropper | **PASS** | Avatar pixel proof (item 2) · dream-profile e2e ×4 (sheet kit, bg decode, frames travel, 2-tab card) · 20.6 pixel proofs carry (gradients Δ140–360, brightness parity, 4:3 window) |
| Personal collections | **PASS** | Private shelf created in the walk |
| DM Blake | **PASS** | e2e: a real send lands in the thread |
| Consent flows | **PASS** | e2e: Save → modal in place → agree → saved (self-resetting) · the re-offer/retry hardening from 20.6 intact |
| The chip chain | **PASS** | Walked through the REAL /suggest page this session: request → submitterUid → 👁 chip (now under the cover) |
| Reports queue | **PASS** | Admin queue renders 4 seeded open reports |
| Ban | **PASS** | prac-banned's post attempt fires the branded suspended modal (screenshot) |
| Deep-links | **PASS** | e2e landing specs ×3 + the letter-→-comment halo walked live |
| The launcher | **PASS** | Real double-click: Mode 1 answered on :8888 and stayed up |

**THE LINE: v1.10.0 is READY FOR THE CUTOVER.** Every track green at 214 / 154 / 77 / 67 / 12, every Part-A item fixed and verified with evidence, the full surface walked. The only known carried items are the pre-existing anonymous `#secondary=` boot-route (all real consumers are signed in; banked) and the post-cutover queue in NEXT.md — nothing cutover-blocking. Gate 21 runs only on your explicit "ship it" to Cowork: indexes → hosting → firestore → storage → functions, low-traffic, verify-per-step.

## Adversarial (5 lenses, refute-verified: 3 MED + 4 LOW confirmed; 0 HIGH; 6 refuted)
- **MED (fixed):** my new sign-in strip could linger across a cross-tab sign-out with the old account's letter metadata (now: any auth change tears it down — the strip carries its owner's uid) · it wrapped into a five-line blob at phone widths (now sized to content, capped like the door strip, inline-flow under 560px — re-verified at 360px: one tidy 55px pill) · my new Yuki seed letter deep-linked to REN's comment instead of hers (now seed-2; walked to her own bubble).
- **LOW (fixed):** the chip's late pop-in shifted the hero ~30px per open (the count read now starts with the page load — same-paint dock) · the "+" suffix could overstate the unread count (now keyed to the unread set itself, fixed on the door strip too) · dead `shortAct` helper + stale comments removed with the tabs · the rules comment claimed mixed-content blocking protects prod — factually wrong (browsers exempt loopback); corrected to name the REAL defenses (the client hostname gates + the no-beacon loopback target), in both the rules file and the rules tests.
- **Refuted (6):** all the same-tab sign-out leak variants (index has no sign-out control — that handler is dead code, noted in NEXT as hygiene), a double-strip race, and two read-cost claims.

## Tests — ALL GREEN (new highs)
`npm test` **214** (g26 ×5: the 2-tab pin, the chip column, the note + twin, the three origin gates localhost-scoped, the strip's layering/focus/reduced-motion) · `test:rules` **154** (+2: the practice origin allowed, near-miss hostiles denied) · `test:functions` **77** · `test:cf` **67** · e2e **12** (+1 sign-in catch-up, self-resetting; the dream-profile tab spec re-pinned).

## Checkpoint / deploy
One checkpoint, pushed (hash in CODE-HANDOFF). APP_VERSION stays **1.9.1**. **NO deploy** — Cowork collects your "ship it," then gate 21 is the cutover.

## YOUR (SHORT) NUMBERED SMOKE (practice is up + re-seeded: http://127.0.0.1:8765/?emu=1)
1. **The lantern demo, foolproof:** close every practice tab → open `http://127.0.0.1:8765/?emu=1` fresh → sign in as **prac-yuki@practice.test / practice123** → close the tab → open `http://127.0.0.1:8765/?emu=1` again (fresh tab = fresh session; you're still signed in) → the door shows **🏮 2 new lantern pings** under the update log. Tap it → the sheet → tap "Mika liked your comment" → you land highlighted on YUKI's own comment. *(Why each step: the strip lives on the DOOR, so it needs a fresh tab; Yuki now always has 2 unread letters after any seed; signing in keeps you warm across tabs.)*
2. **The mid-session version (new):** while signed OUT and past the door, sign in as Yuki from the header — a compact **🏮 While you were away** strip slides in under the header. Tap it: same sheet. (✕ dismisses it.)
3. **Avatar end-to-end:** as Mika — change avatar via the hero pill → Save (no error now) → open any One Punch Man comment by her and her profile card: the new face is everywhere.
4. **The card is shallower:** open anyone's profile card → just Threads + Reviews.
5. **The chip:** open the deep-dive of the title you requested → **👁 under the cover picture**.
6. **The note:** pick any background → after Apply, the "Public view shows the true background" line sits under the hero.

## One-liner reply
Gate 20.7 closes your list with receipts — the flaky save and the stuck pfp were ONE sandbox-only root (the practice emulator's own avatar URLs were being rejected by the rules; fixed at the root with prod untouched, then walked live until the new avatar's actual pixels painted in a comment), the lantern demo failures were my recipe pointing you at letters my own test runs had consumed (the seed now guarantees Yuki two unread letters on every boot, the new letter lands on her own comment, and a brand-new compact strip brings the catch-up to anyone who signs in mid-session), the public card is Threads+Reviews only, the 👁 chip sits under the cover, and the editor hero now says plainly that Public view is the true background — the 5-lens panel confirmed 3 MED + 4 LOW (all fixed, including my own strip's mobile wrap and a wrong-target seed letter), every track is green at **214 / 154 / 77 / 67 / 12**, the checkpoint is pushed, **and after walking the entire v1.10.0 surface myself — door to Tavern to rooms to ban to launcher, PASS across the board — my line is: READY FOR THE CUTOVER, on your "ship it."**
