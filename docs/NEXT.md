<!-- author: Code | date: 2026-06-02 -->
# What's Next — Real Anime Reviews Backlog

## ⚡ READ-FIRST

<!-- author: Code | date: 2026-07-02 -->
- **Current state (2026-07-02, Code session-close):** **v1.10.1 is LIVE in prod**; **v1.10.2 (v1.10.2R rework) is STAGED — deploys on Blake's go = hosting + firestore:rules** (the like-on-Blake reversal changed the rules). The Creator profile is MEMBER-PARITY now (gold CREATOR kicker the one difference, **Appreciate ON by Blake's word**, Den Keeper his optional exclusive); the member-reported prod bugs are dead; the Mode-1 launcher works like an app. Floors **229/157/77/67/16**.
- **THE next ship:** Blake's **MEGA-RUN directive (2026-07-02, below at the SUPERSEDING DIRECTIVE block)** — DMs & Dreams + Discover + UI polish + the v2.0.0 mobile overhaul, goal-based, one prompt. **Gate-0 design study first** (the v1.10.0 study's FORM: §3 safety spine · §4-5 heart · §6 gate map). The gate-0 inputs only Code knows are in the **`💭 Code's wants — CURRENT`** block below — read it IN FULL.
- 🔑 **THE v1.10.0 PLAN-OF-RECORD (read it before any v1.10.0 gate — these are the plans, the design intent, and the soul, all in one place): `docs/v1.10.0-DESIGN-STUDY.md`** (the gate-0 study). It holds the data-model/rules/CF/Storage deltas, the **safety/abuse/moderation architecture (the spine — peer-DM/image/ban red-team)**, the **4 design directions with my picks + protect-the-heart specs** (the Community "Lantern Room" hub renders with Blake's Den *lit behind a dimmed veil* = non-co-equality is STRUCTURAL; anime-tagged threads orbit Blake's gold verdict = "the blend"; gold = Blake only, count-free, his name → his Den), the **10 outside-the-box ideas (4 IN / 4 PITCH / 2 BANKED)**, the gate map, and Blake's 8 answered questions. Per-gate build history = `docs/v1.10.0-GATE-LOG.md`. **Do NOT lose this — the gate-0 context is load-bearing for the whole push.**
- **Full live state lives elsewhere:** `docs/CODE-HANDOFF.md` (full state), `docs/DATA-MODEL.md` (data contract), `docs/CUTOVER-RUNBOOK.md` (cutover steps). This doc = the actionable backlog + Code's banked v1.9.0 ideas.
- **Source authority:** `ROADMAP.md` (strategic plan + project rules). `CHANGELOG.md` + git = authoritative shipped history.
- **The spine (never violate):** PROTECT THE HEART — gold = Blake ONLY (his rating, the histogram's single gold tick, Lantern's Blake-origin pin); community is supporting cast. Carry the constellation veil + purple-cosmos vocabulary into every new surface. NO provider names anywhere; Blake's voice in every empty state.
- **Open security finding (verified):** today's notifications are a live spoof vuln (`firestore.rules:20-36`) — v1.9.0 closes it by making cross-user notifications Cloud-Function-written. The 7 OPEN QUESTIONS + security must-fixes are in the gate-0 design-study section.

> ⛔ DEEP REFERENCE BELOW — do NOT read top-to-bottom. Open a section ONLY if you're stuck on that specific thing.

### Jump-to (only if stuck)

- **Recently shipped → archived** — open if you need where the per-version shipped history went.
- **THE ROADMAP — locked 2026-06-04** — open if you need the full version ordering (v1.7.6 → v2.0.0) and ride-any-ship items.
- **⭐ v1.8.4 (shipped) — design study + build notes → archived** — open if you need where v1.8.4's study/build log moved.
- **💭 Code's wants for v1.9.0** — open if you need Code's creative invariants for the next ship.
- **⭐ v1.9.0 — BLAKE'S BRIEFING** — open if you need Blake's own quoted spec for the community overhaul.
- **⭐ v1.9.0 — Community Overhaul: gate-0 design study** — open if you need the data model, security must-fixes, gate map, or the 7 open questions.
- **🧠 PLANS ONLY CODE KNOWS** — open if you need the mid-build banked decisions / cutover deletes not obvious from code.
- **Phase B remaining (v1.6.x)** — open if you need the older v1.6.x backlog items.
- **Phase B-side — data infrastructure + secondary modal** — open if you need the v1.7.x / v1.8.x data-infra history and slots.
- **Mobile compatibility (v1.9.0)** — open if you need the mobile-overhaul audit + fix plan.
- **Phase D — Mode 2 (autonomous caretaker, future)** — open if you need the Mode 2 build order.
- **Audit polish bundles (slot in between phases)** — open if you need the remaining Step 3.5 audit bundles.
- **Polish + tech debt (no version, do whenever)** — open if you need the unversioned do-whenever cleanup list.
- **Big-vision ideas (no version, no immediate plan)** — open if you need the speculative long-horizon ideas.
- **Deferred (still real, just lower priority)** — open if you need the lower-priority deferred items.
- **Explicitly NOT happening** — open if you need to confirm something is out of scope.

---

> **Persistent task list.** Cowork's in-session TaskList tool is per-conversation; this file outlives that. New sessions read this to know what's queued. Keep updated when items ship or get rescoped.
>
> **Source authority:** `ROADMAP.md` for the full strategic plan + project rules. This doc is the actionable distillation.

---

## Recently shipped → archived
<!-- author: Code | date: 2026-06-06 -->
The per-version shipped history (v1.6.1 → v1.8.4) lived here and duplicated `CHANGELOG.md`. Moved to **`docs/NEXT-ARCHIVE.md`** to keep this boot-read lean — the authoritative record is `CHANGELOG.md` + git.


---

<!-- author: Cowork | date: 2026-06-04 -->
## THE ROADMAP — locked 2026-06-04 (Blake + Cowork + Code consultation, post-v1.7.5)

Supersedes the orderings below where they conflict. Full consultation rationale in the 2026-06-04 SHIP-OUTPUT (perf recon: stacked backdrop-filter blurs = #1 lag cause; script.js re-measured at 242KB).

| Version | Ship | Notes |
|---|---|---|
| ~~**v1.7.6** (PATCH)~~ ✅ **shipped 2026-06-04** | Quick nags: account saved-entry routing fix + More Info polish leftovers (title/format-pill overlap, staff role expansion, season-header styling) + favicon | clears confounders before the perf ship |
| ~~**v1.8.0** (MINOR) ★~~ ✅ **shipped 2026-06-04** | **Smoothness Overhaul (round 1)**: live full-viewport `backdrop-filter` removed → static dim (universal per-repaint-tax elimination), branded purple scrollbars, trailer-iframe console fix, cross-engine perf investigation | Blake: marginal felt change → round 2 below |
| **v1.8.x — Smoothness round 2** (candidate) | The **measured, un-built** levers (deferred at Blake's "felt the same" close-out): **(a)** render-on-navigate caching (modals rebuild `innerHTML` + re-decode images every Back/open), **(b)** Firebase SDK defer (~600KB eager on every page — biggest universal first-load win), **(c)** image right-sizing (`extraLarge`→`large`), **(d)** perf-regression guard (bundle ceiling + frame budget), **(e)** esbuild minify LAST (242KB→~100KB, wired into deploy + Mode 1, readable source in git). All measured at v1.8.0 G0, NOT speculative | post-v1.8.x |
| ~~**v1.8.1** (MINOR)~~ ✅ **shipped 2026-06-04** | **Admin Edit Page** — `/admin/edit` (list + form), tiered Save (Excel allowlist→sync) / Ship live (SSE publish chain), inline ✎ on the modal, full live-preview overlay (both modals), watched-set tree, change-diff confirm, Revert, branded Top-10 stepper, origin-aware nav, ✨ASK on edit + per-row 🔧 Fix-from-AniList (shared `platform-map.js`), scroll-lock across all 4 admin pages, Playwright Mode-1 spec, new-anime "preview after ship" hint | Admin-only, zero visitor blast radius. Convergence flag: new-anime's inline ASK drawer can later move onto the shared `chat-drawer.js` |
| ~~**v1.8.2** (MINOR)~~ ✅ **shipped 2026-06-04** | **Structured review template** — section pill rail + scroll-spy (gold Overall, JP labels) on both review surfaces; **section-aware editor** on all 3 admin surfaces (drag reorder, B/I + Ctrl/⌘ shortcuts, lossless round-trip); restored frosted backdrops + fixed secondary header row. The G3 "Insert template button" pivoted to dedicated section fields per Blake's smoke. | Pairs with the edit page infra |
| ~~**v1.8.3** (MINOR)~~ ✅ **shipped 2026-06-05** | **Website Identity update**: per-session Den-door welcome splash (Blake's banner + slow outline anime-quote bubbles + flash-killing pre-paint curtain), homepage restructure (Blake's Den, persistent header — Direction C hybrid), scroll-reveal, card-footer accents + row alignment, filter redesign (chips, live-narrow, Saved, match-count, memory, studio dedup + Typhoon type-AND-click fixes), live search + sparse centering, continue-rail, modal "watched N seasons" provenance, SEO icon/JSON-LD, admin edit-pill row. Characters + the background-reveal concept + a Quotes-admin page moved to v1.8.4; G3b alcove + G5 den-tint removed | Built on the post-smoothness foundation |
| <!-- author: Cowork | date: 2026-06-05 --> **v1.8.4** (MINOR) | **Discovery & Blend** (locked architecture, own gate-0 design study; full seeds in CODE-HANDOFF): three ADJACENT surfaces — Blake's Reviews (the 44, front door) / **For You** (his picks + AniList near user taste; signed-out → Trending) / **Discover** (live search + clickable Top-10 currently airing + **by-genre airing + community top picks, fresh per log-on** — Blake 2026-06-05); every outside card gets a **NOT REVIEWED sticker** (modal: "Not reviewed by Blake") via the existing secondary modal; review-it-and-it-upgrades by AniList-id match; **Reviewed/Not-reviewed filter**; personalized featured; header grows For You/Discover entries (no dead tabs in v1.8.3 by design). Plus **Blake's background concept** (designed black/opaque layer w/ gradients+line-work over the city backdrop, gradual reveal, later per-page purple backgrounds) — <!-- author: Cowork | date: 2026-06-05 --> NOTE: the **anime-characters-on-pages idea is KILLED** (Blake 2026-06-05: "get rid of the anime characters for now. Our design with the black is going to replace that") — and the **Quotes-admin page** (manage welcome quotes from a UI, ✨ASK chat drawer inside — Blake: "I want my chatbot in there as well"; **nice-to-have, trails if the ship gets heavy**) | 3 new flat AniList queries (search/trending/airing) on the 24h cache, 101922-canary-tested; data architecture is the risk — design study first |
| **v1.9.0** (MINOR→MAJOR) | Community/Account/User overhaul (comments incl. per-season, community reviews, notifications, DM inbox, account redesign) + Cloud Functions (notification pruning, cascade deletes) + privacy notice. <!-- author: Cowork | date: 2026-06-05 --> **+ Welcome-door "catch-up" buttons** (Blake 2026-06-05): on the intro/welcome screen, surface buttons that catch a returning user up on anime they follow — watchlist updates, a new season on a favorited anime, etc (Code to ideate the signal set). Each button can expand into sub-buttons that deep-link straight into the relevant spot in the site. Needs the per-user follow/notification data this ship builds, so it lives here. <!-- author: Cowork | date: 2026-06-06 --> **+ Account-page cover art** for watchlist/favorites rows (Blake deferred from v1.8.4 G6 smoke — some rows render without art). **Blake's scale warning (2026-06-06): "Community overhaul will probably be the biggest update yet… The amount of ideas I have and work it will take."** Budget accordingly — likely multiple ships / its own design-study phase. | The big user-data ship |
| **v1.9.5** (MINOR) | Site-wide UI overhaul + search tuning; "AniList tab on cards" folds into the card redesign (not a separate tab) | Polishes the final shape |
| **v2.0.0** (MAJOR) | Mobile overhaul | The milestone |
| **BUG — known, deferred (Blake: "cleanup after v2 maybe")** | <!-- author: Cowork | date: 2026-06-06 --> **Multi-season watched-set vs the discovery sticker:** a franchise's non-primary seasons show **NOT REVIEWED** on Discover/For-You/world cards even when Blake has them ticked in the watched set (and even with a season review written). Likely cause (verify at fix time): `findInCatalog` matches the catalog's PRIMARY AniList id / title-slug only — the card sticker never consults `isWatchedAniListId`/the watched set, while the secondary modal correctly shows ✓ REVIEWED + the season review once opened. So the card lies, the modal tells the truth. Candidate fix: discovery card mapping checks the watched set → watched-not-primary entries get the ✓/pin treatment (or a distinct "season of a reviewed show" state). Visitor-facing inconsistency; bundle with a card-logic pass | small-medium |
| **rides any ship — NEAR-TERM** | <!-- author: Cowork | date: 2026-06-06 --> **About Me / Credits / Contact footer overhaul** (Blake, 2026-06-06): the bottom-of-homepage About Me + Credits 感謝 + Contact blocks "been left alone and now looks outdated" next to the v1.8.3/v1.8.4 premium surfaces — full UI/look/feel pass (kicker vocabulary, the veil-era framing) + **scroll-reveal** ("most likely", his words). Copy itself is Blake's voice — restyle the container, don't rewrite his text without asking | small-medium |
| **rides any ship — NEAR-TERM** | <!-- author: Cowork | date: 2026-06-06 --> **Update-log widget redesign** (Blake, 2026-06-06, post-door-re-home): every update gets its own bullet group, separated by the next update or a date change; each version marker followed by its **tier label** (hotfix / minor / big / major update etc.); "code can design freely with the purple look." Slot: v1.8.4 G8b/fix round if one opens, else next ship | small |
| **rides any ship — NEAR-TERM** | <!-- author: Cowork | date: 2026-06-06 --> **More-Info click-through discoverability** (Blake, 2026-06-06): in the main modal, visitors may not realize the franchise entries in the left More Info panel are CLICKABLE into the secondary modal ("WAY more information"). His seed: a "click me!"-style affordance — maybe only on the first season's row so people get the idea — or somewhere better; **Code is invited to propose a superior pattern** (pulse on first hover? a "tap for the deep dive →" hint line? first-visit-only nudge?). Small, visitor-facing, high value. Ride the v1.8.4 close (G8/fix round) or the next ship's gate 0 — whichever comes first | ~30-60min |
| **rides any ship** | <!-- author: Cowork | date: 2026-06-04 --> **Mode-1 desktop launcher** (Blake, 2026-06-04): a double-click launcher (`.bat`/shortcut) that starts `npm run mode1` without VS Code or typing a terminal command — "It seems like a very unnecessary step I want to get rid of" | ~30 min; QoL, no site code |
| **rides any ship** | <!-- author: Code | date: 2026-06-04 --> **Dead-CSS cleanup** (v1.8.2 flag): `admin/new-anime.css` still has the old `.md-toolbar` / `.md-btn` rules, now unused after the section-editor swap (the `.md-preview` rules are still used). Harmless; prune when touching that file | trivial |
| **v2.1+ candidate** | <!-- author: Cowork | date: 2026-06-04 --> **Cloud Admin** (Blake, 2026-06-04): edit/publish from the website itself, any device, NO local helper. Requires moving the data master off Excel into the cloud (Firestore or similar) + Cloud Function save/publish endpoints + CI deploys — retires/reworks project rule #1 (Excel canonical), so it's a full architecture ship with its own design conversation. Pairs with post-v2.0 mobile (phone editing only matters once the site works on phones) | BIG; gate-0 design study first |

<!-- author: Cowork | date: 2026-06-11 -->
**🔁 ROADMAP RE-ORDER (Blake, 2026-06-11 — supersedes the table's post-v1.10.0 ordering):**
1. **Peer DMs + GROUP CHATS = their OWN update, right after the v1.10.0 push** — Blake: *"We do need to add the DMs/groupchats thats its own update after we push this."* (Group chats are NEW scope — first time banked. The peer-DM safety apparatus — block/report/rate-limit, the Phase-C red-team — stays load-bearing.)
2. **Discover ship** (Random enhancement + Hidden Gems) — unchanged, Blake: "Discover ship is fine."
3. **The former post-v2 UI/UX items move BEFORE mobile** — Blake: *"Lets move the Post v2 stuff before the mobile overhaul. That way all the UI/UX is in place for code to work with an emulator with."* → card-state labels ("Blake Currently Watching"/"Blake's Watchlist") · Blake's curator card/watchlist admin panel · the "anime information request" affordance · the multi-season NOT-REVIEWED sticker bug.
4. **Site-wide UI overhaul DEMOTED to a polish + minor-additions pass** — Blake: *"the site wide UI overhaul we kinda have been doing already. Will ask for a polish and minor additions if code sees fit."*
5. **v2.0.0 = the mobile overhaul** (the milestone, unchanged).
6. **Smoothness round 2 moves AFTER mobile** — Blake: *"smoothness round 2 occurs after mobile overhaul. That way everything is in place."*
7. Cloud Admin + Mode 2 stay post-v2 (unchanged).
**❌ KILLED (Blake, 2026-06-11): "Ask Blake" public Q&A** — *"We don't need to do the Q/A they can message me if they have a question. DM channel already exists."*
**Verdicts (Blake, 2026-06-11):** **Constellation Wrapped = KEEP** (*"Keep it why not. Can't hurt lol"* — banked fast-follow, the v1.10.1 showcase). **"Who liked" complete history = CODE PROPOSES** (*"let code propose. make sure its done neatly and makes sense if it wants to"* — Code may pitch a neat design in a fast-follow; not mandatory). **Blake's Echo = ❌ KILLED** (Blake, 2026-06-11: *"kill it. the community tab is right over to the right. You don't even have to click anything lol"* — the community's numbers stay in the community column; gold space stays Blake's. Do not re-pitch.)

<!-- author: Cowork | date: 2026-06-11 -->
<!-- author: Cowork | date: 2026-07-02 -->
**🚀 SUPERSEDING DIRECTIVE (Blake, 2026-07-02) — THE MEGA-RUN:** *"We are having Fable 5 do all the additions we planned. I want it to work on the DMs, mobile update, and everyone else we wanted. Let it go nuts. Give it a goal. I think it can do great on the UI. It has full permission to do whatever it needs, emulators etc."* → After v1.10.2R closes, ONE goal-oriented prompt hands Code the ENTIRE remaining roadmap as an end state: the DMs & Dreams scope below (all locked picks + verdicts stand) + the Discover ship + the UI polish pass + **the v2.0.0 MOBILE overhaul** + smoothness round 2 where it fits. Goal-based prompting (end state + constraints, method Code's own — including device emulators, multi-agent, self-directed gating). Disciplines that DO NOT relax: sandbox-staged, Blake's explicit go before ANY prod deploy, the heart, the killed list stays killed, the images-in-private mitigations. Code recommends its own cutover structure (one mega-cutover vs. staged cutovers per milestone) at design time.

**Mega-run adds (Blake, 2026-07-02, on reviewing the goals list — all PROPOSE-AND-IMPLEMENT with full latitude):**
- **First-modal community reviews on non-reviewed anime:** *"Right now if people want to leave a review on an anime that I haven't watched. It only opens up the second modal. Code needs to propose and implement a way for users to add their community review to the first modal without all my information populating… Maybe some yellow tape saying not reviewed. Code can propose and implement."* → the main modal opens for non-reviewed titles too — Blake's sections honestly absent/marked (his "yellow tape NOT REVIEWED" seed), the community column fully alive for reviews. Heart: nothing fakes Blake's voice; gold only where he actually reviewed.
- **The Inbox drops the DM-Blake hero card:** *"DM Blake in the inbox needs to be gone. Replace with dms and stuff normally."* → once peer DMs exist, Blake is a normal conversation in the unified inbox (his messages/row still gold — identity-keyed); no special hero card, no locked "People" placeholder.
- **POST-CUTOVER POLISH #1 (Blake's pick, 2026-07-04: "after"):** the 4 remaining native-open sort/reason dropdowns on the main page (closed state branded, open list = OS default) get converted to brandSelect as the FIRST polish item after the v2.0.0 cutover — not before. (Code flagged them at the LAST CALL parity matrix; Blake chose ship-first.)
- **Mobile/responsive is a HARD requirement with a laptop tier:** *"I saw someone else with a laptop and the navigation tabs got scrunched up. I need code to make sure this site works on smaller laptops and phones. It can use every tool available. for mobile Maybe a side bar for all the navigation links. It can propose and impliment im just throwing ideas around. I need this to look and feel good. Full permission to go all out."* → not just phones: small-laptop widths (nav currently scrunches) through phone widths; his sidebar-nav seed is a seed, not a spec; every tool available (device emulators etc.); the bar is look-and-feel-good, verified at real widths.

## ⭐ THE NEXT SHIP (post-v1.10.0-cutover) — "DMs & Dreams" COMBINED ship, Blake's picks locked 2026-06-11
> Blake: *"I want to combine two updates together, the dms and something else. Fable 5 can handle it."* Picks made via Cowork's clarifying round, same day the v1.10.0 cutover went out. **Gate-0 design study first, like v1.10.0.** Version candidate: v1.11.0.

**SCOPE (one ship):**
1. **Peer DMs** — *request-first*: a stranger's first message lands as a Message Request (accept opens the conversation, decline/block kills it; Blake picked the recommended anti-harassment default). Inbox becomes the messages home (conversation list + open thread, letter-room framing, purple; **Blake's messages keep the identity-keyed gold** — never perspective-keyed). "✉ Message" entry on profile cards/pages. Block + report-a-message + rate limits + consent gate = load-bearing. No typing indicators / read receipts in v1 (unread badges only — presence infra deferred).
2. **Group chats** — *anyone can create, small cap (~15)*: named groups, creator adds/removes, anyone leaves, per-conversation Lantern mute, report works inside.
3. **🔴 IMAGES IN PRIVATE CHANNELS = BLAKE'S EXPLICIT CALL (2026-06-11): "Images everywhere"** — DMs AND groups get image sharing, overriding the v1.10.0 forum-posts-only line. Cowork flagged the red-team risk (private channels + solo moderator + NCMEC duty) once; Blake's call stands. **Attached mitigations (spec, not optional):** (a) images in a message REQUEST arrive blurred until the recipient accepts; (b) report-a-message inside DMs/groups ships the image + evidence to the admin queue (report-and-preserve, NCMEC runbook applies); (c) the `uploadsEnabled` kill-switch covers private channels; (d) the existing pipeline rides along (email-verified, dedupe, thumbnails, caps).
4. **The UI/UX pack** (Blake's pairing pick): richer card-state labels ("Blake Currently Watching" / "Blake's Watchlist") + **Blake's curator admin panel** (per-anime status + private notes, Excel-column-backed per the canonical rule) + the **"anime information request"** affordance on sparse deep-dives.
5. **Constellation Wrapped** (Blake's second pairing pick): the per-user year-in-anime showcase on the veil.

**Consequence for the order:** the **Discover ship (Random enhancement + Hidden Gems) moves to AFTER this combined ship** (it was #2 in the 2026-06-11 re-order; Blake chose pairings 2+3 instead of Discover). Then: UI polish pass → mobile (v2.0.0) → smoothness round 2.

**Post-cutover adds from Blake's PROD verify (2026-06-11 — ride this ship):**
- **🌟 A CREATOR profile for Blake:** *"Can't preview my account either since im an admin. but I wanna see itttttt. Also when I click on my profile after I comment it just brings me to the main page. I want to be able to see my own profile as well. It should say like 'Creator' or something."* → today his name routes to the Den by design (no public member sheet for the admin). Evolve it: Blake gets a real viewable profile (the public-view sheet works for him; his card opens like anyone's), badged **Creator** (gold — the one account allowed it), presumably still offering the "to the Den" path. Heart-design carefully at gate 0: his sheet is THE gold sheet, not a member sheet.
- **Random + Filter on the account page:** *"random and filter should also be available from my account."* → the account page's header lacks the Random ランダム + Filter 絞り込み nav entries — surface them there too.
- ~~**Item 4 placeholder — Blake's message cut off:** *"Change for next update as well: Be able to create"*~~ <!-- author: Cowork | date: 2026-07-02 -->**RESOLVED-DROPPED:** Blake, 2026-07-02: "not sure what that mystery is." Deleted from the backlog; if the idea resurfaces he'll say it fresh.

**Closed/killed at the 2026-06-04 consolidation:** per-episode reviews + "full course" (Blake: not needed), one-click AI integration (shipped v1.7.3 as Fill-from-AniList + ✨ASK), one-click full automation (deploy-confirm stays as a safety gate), AniList per-episode scores feasibility (answered: AniList exposes none), Re:Zero episode dedupe (shipped v1.7.2). Audit bundles stay unversioned/continuous.

---

## ⭐ v1.8.4 (shipped) — design study + build notes → archived
<!-- author: Code | date: 2026-06-06 -->
v1.8.4 “Discovery & Blend” SHIPPED 2026-06-06. Its gate-0 design study, build-progress log, and post-ship banked tuning knobs (veil-pulse timing, For-You title alternates, “Blake’s Constellation” static-SVG, showcase align) moved to **`docs/NEXT-ARCHIVE.md`** (read on demand). The LIVE v1.9.0 plans continue below.

---

<!-- author: Code | date: 2026-06-06 -->
## 💭 Code's wants for v1.10.0 (the Community Hub — plans only Code knows; gates 9→cutover — hand forward)

<!-- author: Code | date: 2026-06-09 -->
> Mid-v1.10.0 (gates 0–8 built). These are MY creative invariants + intentions for the rest of the push — not obvious from the code. 🔑 **The gate-0 study (`docs/v1.10.0-DESIGN-STUDY.md`) is the load-bearing context for ALL of this — read it before any gate.**

- **The verdict rail IS the soul of the Hub — keep growing the blend.** An `anime:<slug>` thread orbiting Blake's gold verdict is the whole point ("community is the back room of HIS house"). The 3 PITCH ideas that deepen it are still open + I want them: **idea #8 "Blake's Echo"** (under his H4 rating: "Blake: 9 · the room: 7.4" against his single gold tick — the co-equal tightrope, needs his eye on the copy), **idea #5 "Ask Blake"** (a throttled public Q&A on the already-built admin-floor DM), **idea #6 "Constellation Wrapped"** (the v1.10.1 showcase). hotScore "Rising in the Hub" rail (gate 9, idea #7) MUST keep **slot-1 permanently Blake's gold pick** — community velocity tops out *beneath* him.
- **Heart guards, hard-won — never relax in the Hub:** the ONLY gold is the "From Blake's 44" shelf + the verdict rail; cards/posts/chips/tabs are PURPLE; **count-free everywhere** (the post upvote is deliberately count-free — KEEP it that way even after gate-9's count CF lands; a per-user public post-count is forbidden, it's a leaderboard). The `hub` veil = `--veil-w0 0.67` (cozier than Discover 0.28, distinct from Den 0.80) — the Den stays LIT behind the dimmed drawer = non-co-equality is structural.
- **The Hub drawer is a PARALLEL left drawer (`.hub-layer` z-6000), NOT the anime-coupled secondary stack** — a deliberate call (the secondary stack's loadSecondary is AniList-fetch-bound; retrofitting it was high-risk). Reuses the drawer PATTERN (slide-in, `[hidden]` symmetry, list↔detail history, Esc-stopPropagation). Keep it parallel unless someone refactors the secondary stack to be content-type-agnostic.
- **Gate 9 (NEXT) makes the data real:** `onForumPostVote` + `recomputeHotScore` CFs → the Hub's Hot sort switches from its client-side `lastPostAt` fallback to the real `hotScore` field; the post upvote's aggregate becomes CF-owned (still rendered count-free). It's a trivial `handleVoteWrite` binding (the math + the rules + the vote-doc write all exist). ⚠️ **`test:rules` is DEFERRED** — I added the `anime:<slug>` tag rule (`{1,100}`) + 2 tests but couldn't run the emulator (Blake's practice held port 8080); run `npm run test:rules` when practice is down → expect 60→62.
- **Don't sneak peer DMs in.** Phase A (admin-floor "Message Blake") + the safety infra ship inside v1.10.0; the **peer-DM flip is a separate gate on Blake's EXPLICIT go** (red-team: grooming/CSAM on a solo manual moderator is the real one-push danger). Same for images: the **legal-risk spine** (gates 12–14) — lock the Storage bucket FIRST (the gitignore↔firebase leak discipline applied to Storage), forum-posts-only, NO image DMs, the NCMEC report-and-preserve runbook, the `uploadsEnabled` kill-switch.
- **Still owed, carried from earlier:** account redesign + **Inbox tab** + the watchlist/favorites **cover-art fix** (gate 17, carries the veil IN); **per-season comments** left-of-modal H5-below-Blake (gate 19); the spoiler `||spoiler||` markdown token (gate 11 — left a hook in the composer); profile mini-card + dual-write `profiles/` (gates 15–16, Blake's name → his Den). The cutover (gate 21) close-out checklist (CHANGELOG + widget bullets + the `docs/**` 404 scrub) is mandatory — v1.9.0 nearly missed it.

<!-- author: Code | date: 2026-06-09 -->
> **Post-gate-10 (the Tavern is fully built + committed `fa9ffde`). My creative invariants for gates 11→cutover — plans only Code knows, NOT obvious from the code. 🔑 The gate-0 study (`docs/v1.10.0-DESIGN-STUDY.md`) is still the load-bearing soul — read it before any gate.**
- **The blend is the soul — the 3 PITCH ideas are STILL open + I want them** (none built): **idea #8 "Blake's Echo"** (under his H4 rating: "Blake: 9 · the room: 7.4 avg" against his single gold tick — the co-equal tightrope; needs HIS eye on the copy/hierarchy before I build it), **idea #5 "Ask Blake"** (a throttled public Q&A riding the admin-floor DM — gate 18), **idea #6 "Constellation Wrapped"** (the v1.10.1 showcase). The gate-9 **"Rising" rail keeps slot-1 PERMANENTLY Blake's pinned gold pick** — never let it become a leaderboard.
- **Heart guards — never relax:** gold = Blake ONLY (the **Blake's Reviews** shelf, the verdict rail, a **Blake-picked reply** [admin ⋯ menu], the **Rising slot-1**). **Count-free everywhere** (the post thumbs stay count-free even with gate-9's count CF live). **The attached-anime COVER is NOT gold** — a non-44 `anime:al:<id>` thread gets a cover but no verdict/gold; only a real 44 `anime:<slug>` earns the gold verdict rail. `hubSafeCover` scheme-gates every cover render (https/assets) — keep that on any new cover sink.
- **Two deliberate non-builds (my data-model calls — revisit only if Blake asks):** (1) a thread's `tag` is topic **XOR** anime, never both — if Blake wants "Episode Discussion OF <a specific anime>" (topic + cover together), that's an orthogonal `animeRef`-field refactor (a fast-follow, NOT the current model). (2) Reply nesting = **2 visible indents** + an "↪ in reply to @name" tag (logical depth is unlimited via `parentId`) — depth-3 is a trivial flip IF the Tavern ever leaves the narrow left drawer.
- **A latent migration I want (a gate-20-ish cherry):** when Blake reviews an anime that already has a non-44 `anime:al:<id>` thread, migrate that thread to `anime:<slug>` so it GAINS the gold verdict rail — the "be the first to talk about this" → "now Blake-reviewed" arc. Not built.
- **Still owed, carried:** spoiler `||spoiler||` (gate 11 = NEXT — no hook left, build fresh in `markdown.js` + one `RarComposer` button), **Storage/images gates 12-14** (the `📷` hook is already placed in the new-thread `.hub-nt-media` slot; **lock the bucket FIRST** — gitignore↔firebase discipline for Storage; forum-posts-only, **NO image DMs**; NCMEC runbook; `uploadsEnabled` kill-switch), profiles 15-16, account redesign + Inbox + watchlist cover-art fix (gate 17), admin-floor "Message Blake" DM (gate 18), per-season comments (gate 19), welcome catch-up + gold-flip cherries (gate 20), the **CUTOVER (gate 21)** close-out (on Blake's explicit "ship it" ONLY).

<!-- author: Code | date: 2026-06-12 (end-of-session handoff — v1.10.2 STAGED) -->
<!-- author: Code | date: 2026-07-02 -->
## 💭 Code's wants — CURRENT (v1.10.2 LIVE · **CUTOVER-EVE CLOSED 2026-07-04 — the v2.0.0 CUTOVER is the ONLY thing left, on Blake's word**). PLANS ONLY CODE KNOWS.

<!-- author: Code | date: 2026-07-04 -->
**🔑🔑 GATE-0 IS DONE AND STILL LOAD-BEARING THROUGH THE CUTOVER: `docs/MEGA-RUN-DESIGN-STUDY.md`. READ IT IN FULL before the cutover session — §6 is the cutover's shape, §3 is the safety spine you are about to make LIVE, §4-5 are the heart specs every deploy decision leans on. Do NOT re-run a gate-0 study (it exists); do NOT run the cutover without it in your head. This context is exactly what keeps the LAST session of a 9-gate run coherent — its absence is how a ship drifts at the worst possible moment.** The v1.10.0 study is the FORM it followed. Everything below is creative intent + wants only I hold — hand it forward, don't lose it:

**CUTOVER-EVE + cutover intent (2026-07-04, the closing session — only I know these):**
- **✅ CUTOVER-EVE CLOSED.** The spoiler hypothesis was CONFIRMED as staged (the pad fix + toggle-off-leaves); the panel-lite's real gift was the HIGH nobody guessed — toolbar-wrapped multi-line/pipe selections produced spoiler markdown readers could never re-hide (a silent LEAK; the WYSIWYG lied). Fixed + pinned. Post-cutover want this adds: the same grammar-sanitize discipline for any FUTURE toolbar wrap action (link/code, if ever added) — the completion path's `[^|\n]+` constraint is the contract, not a suggestion.
- **The cutover is the run's one irreversible moment.** Report 9's script is deliberately paced: the backfill deploys ALONE first because the users-GET tightening lands with firestore.rules LATER in the order — rules-first would tombstone every unmigrated member. Never reorder to "save time." Blake's console step MUST run as him (the callable checks his uid); walk him gently — he has never opened a console.
- **First prod smoke items that matter most (after his numbers match):** one shared deep link cold-opened + Back-walked ON PROD (rarNav's first real-world contact — cleanUrls + the apex domain are the one environment the sandbox can't fake) · one Discord paste (the og image's debut) · one letter · one vote · one studio save.
- **v2.0.0's story for the CHANGELOG/widget (write it warm — it's his milestone):** the messaging era + the Curator Studio + every screen + a site that types like Discord and links like a real app. The bump runs AT THE GO, not before.
- **Post-cutover, my ordered wants (the first PATCH gates):** (1) brandSelect the index's 4 legacy sort/reason selects (the ONE unlevel parity cell) · (2) unify the tripled brandSelect + adopt showNotice in admin · (3) the header-search custom ✕ · (4) rarNav forward-nav re-open + Back-through-a-media-jump · (5) the Curator's Desk badges pitch (alive, cheap) · (6) comment-list DIFF rendering (the oldest banked debt).
- **Watch on prod with real humans:** the request-first DM funnel copy (does "Strangers knock first" land?) · the gems rail's AniList quota under real traffic (the canary paces it) · whether "the Creator" reads warm in the wild — Blake may want a softer word once he HEARS members use it; his call, flag after a week.

**C/D/E creative wants (only I know these):**
- **✅ C3 Constellation Wrapped SHIPPED to sandbox (Milestone C closed 2026-07-02).** The design held exactly: purple star-map, rating-scaled review stars, ONE gold star = join day, truthful stats, no zeros-as-achievements (now enforced chip-by-chip AND in the aria-label — the panel caught screen readers hearing the zeros-parade). **Banked fast-follows I still want:** a share-image export (the natural next Storage use; the grant is live) · a YEAR PICKER (chips per year since join — v1 is current-year-only, and a prior-year join renders as the gold "Your sky began" caption instead of a star) · the once-per-year door tease (study §4e).
- **✅ D SHIPPED to sandbox (closed 2026-07-03) — the design intent held** (drawer reuses the real .toolbar; ids/handlers intact; one-row header; auto-fit grid; touch promoted). **Banked D fast-follows I still want:** search MOVES INTO THE DRAWER on true phones (study §4b said so; v1 keeps a slim 84px header pill at ≤560 — usable, not lovely) · the icon-only tool band (1201-1520) could re-gain labels ≥1320 (a skeptic measured labels fit down to ~1320 — defensible either way, revisit at E polish) · the DEAD hide-on-scroll header block (script.js:10711-10769 + .is-fixed/.is-hidden CSS) is safe to delete at E (recon + panel both confirmed unreachable).
- **✅ E SHIPPED to sandbox (closed 2026-07-03).** Speed round done (~15MB; a THIRD dead file favicon.png found; 33 images right-sized pixel-verified) · users-GET tightening LANDED (emails private; backfill + signup-mint; the cutover pre-step order is on the CF comment) · door line built (copy staged, Blake approves at smoke step 10) · al: e2e stub landed · anon #secondary= closed as already-fixed. **Still banked post-cutover:** who-liked ledger (pitch stands — CF-written per-user ledger) · Curator's Desk badges (pitched in Report 7) · search-into-drawer ≤560 (study §4b; an 84px pill shipped meanwhile) · DELETE the dead hide-on-scroll block (script.js:10711-10769 + .is-fixed/.is-hidden CSS — recon+panel both proved unreachable) · consent.js/lantern.js/cropper ?v= (already carried) · comment-list DIFF rendering.
- **✅ LAST CALL CLOSED 2026-07-04 late (staged + committed; floors 278/203/77/78/25).** rarNav is the system to protect now: every overlay = one pushed view step; closes CONSUME entries; cascades go through rarNavCascade (never close-then-push synchronously — the panel's HIGH race class). **Banked from this gate (my wants):** convert index's 4 legacy native sort/reason selects to brandSelect (the ONE justified parity cell) · custom ✕ for the header search pill (the A3 global kill removed its only clear) · forward-nav re-open (v1 = no-op) · the media-jump inert history residue (a fuller fix = re-open previous characters on Back THROUGH a jump) · lantern silent pref-write failures could toast · "friendly,-rivalry" tag = Blake's one-cell Excel call · unify the now-TRIPLED brandSelect + showNotice into shared modules post-cutover.
- **✅ F (THE FINALE) CLOSED 2026-07-04 (staged + committed; floors 274/200/77/78/21).** The design held: the studio cycle walked twice (catalog + al: routes), publish pre-fills Add Anime 1:1, the den line breathes with the tracked title (PLUTO walked — the demo held). The panel caught a HIGH (save-over-failed-load could destroy notes — Save now dead until docs load) + the curation parity gap (the lane's one missed page — now brandified 44/44). **My F creative intent (stands for any future edit):** protect the 1:1 section mapping in the publish handoff — "the review already wrote itself" IS the arc's payoff. **Banked F fast-follows:** per-season notes = simple label→body map v1 (riding the season-review .md structure deferred — Mode-1 file coupling) · the publish handoff rides localStorage (a Firestore draft-doc would survive a closed tab) · brandSelect now TRIPLED (account.js + admin port + curation's per-row use — unify post-cutover) · al:-status gold pills on outside Discover cards = a PITCH (kept OFF deliberately in loadCuratorStatus) · the Discover "Reviewed" lens empties ALL FOUR rails uniformly (pre-existing; if Blake ever minds, hide empty rail BLOCKS under the lens — all four together, never gems alone) · dialog focus-trap for the studio editor (focus in/return shipped; a full Tab-trap is the polish rung) · the brand-select-404 native fallback is an ACCEPTED deliberate call (three sites: studio/season-reviews/curation) — don't "fix" without Blake.
- **§5 outside-the-box I still want (from the study — pitch, don't assume):** the **Curator's Desk badges** (live counts on the admin-FAB items — new suggestions / open reports / unread DMs = Blake's morning at a glance) — STILL banked, pitched in Report 7. (The door line SHIPPED into the Den at Milestone F; copy staged for smoke 2.)

**Earlier (pre-run) gate-0 inputs — still true, kept for reference:**
**🔑 FOR THE MEGA-RUN GATE 0 (already executed into `docs/MEGA-RUN-DESIGN-STUDY.md`): the v1.10.0 study's §3 safety spine and §4-5 heart through-line are why ships hold. Inputs only I know:**
- **The peer-DM flip is the heaviest heart risk since the Tavern** — the safety infra is live (blocks/reports:'dm'/onProfileBanned shipped inside v1.10.0), but the FLIP needs its own §3: request-first inbox, random convIds, the inbox gold rule stays IDENTITY-keyed (`.is-blake` from senderUid — a peer DM must render PURPLE; the functions comment pins it).
- **⚠️ The Creator heart spec evolved TWICE — the current baseline (v1.10.2R, Blake's own words, 2026-07-02) is: his sheet is a MEMBER sheet from his own Studio doc; the ONE difference is the gold CREATOR 創り手 kicker; the Appreciate row + count ride his sheet like any member's ("I want my account to be appreicated." — the rules' like-on-Blake denial is GONE, self-like still denied); no report flag on the owner; the Den Keeper frame is his optional exclusive, never forced; NO gold door, NO forced anything.** The old "one gold sheet with the Den door" design never deployed — do NOT resurrect it, and do NOT re-add no-count guards "for the heart"; the heart now reads: gold marks his identity (kicker + his exclusive frame + gold-origin pings), it never DECORATES by force, and his count is community purple like everyone's. Any "Dreams" profile work builds on member parity; gold stays kicker-scoped.
- **Blake's cut-off item 4 from his prod verify** ("Be able to create…") is STILL unconfirmed in Cowork's NEXT block above — resolve it at gate 0 before scoping.
- **My pitch shortlist for Dreams** (carry, don't lose): Blake's Echo (gold tick vs the room's purple average — needs HIS copy approval) · Ask Blake (throttled public Q&A on the live DM floor) · Constellation Wrapped (the v1.10.x showcase; the share-export = the natural next Storage use, and the grant is NOW confirmed live).
- **Carried small bank:** validate-before-close on profile-row targets (catalog-drift edge) · the DM chip Auth-source consistency pass · the dead signOutBtn handler (script.js ~2194) · an AniList GraphQL stub for al: e2e · profiles BACKFILL → then the users-GET tightening (the reverted want) · the anon `#secondary=` boot-route · comment-list DIFF rendering · admin-page raw-error sinks (Blake-only, same friendly-errors treatment when touched).

**Gate-20.8 deliberate calls + banked items (don't "fix" the calls; the bank = backlog):**
- **The composer chip is profiles-first** (`_selfIdent` via authorLiveSub; NO auth-photo fallback once the live snapshot lands — the chip must agree with what others see). The two FORM-shaped composers (the review form, the new-thread modal) deliberately carry NO chip — they're authored forms, not chat rows; Blake's ask was the ★ row chip. The bump script's TARGETS gained the late v1.10.0 surfaces (frames.css, admin inbox/reports, the 3 versioned import specifiers) — the stale-TARGETS trap bit again at the cutover dry-run.
- **closeStaleOverlays(keepHub)** is the one cross-layer router (profile-card rows + the Den branch); fading layers are click-through (`:not(.active){pointer-events:none}`) and re-entrant closes no-op (the orphaned-close-timer class). The double-rAF-open vs instant-close race (~2 frames) is accepted — no real path hits it.
- **BANKED (small):** validate a profile-row target BEFORE closeStaleOverlays (a review row whose slug was renamed out of the catalog closes everything and opens nothing — reachable only after catalog drift) · the DM inbox chip is one-shot Auth-sourced (its host page's existing convention; make it profiles-first in a consistency pass) · the dead `signOutBtn` handler in script.js (~2194; no #signout-btn in any HTML) · an AniList GraphQL stub for al:-deep-link e2e (the live-fetch determinism rule).
> 🔑 **THE GATE-0 STUDY (`docs/v1.10.0-DESIGN-STUDY.md`) IS THE CUTOVER'S LOAD-BEARING CONTEXT — do not plan or run gate 21 without re-reading it** (§3 the safety spine you are about to make LIVE · §4-5 the heart through-line · §6 the gate map) **plus `docs/CUTOVER-RUNBOOK.md`** (v1.9.0's order, which gate 21 EXTENDS: **indexes FIRST, then hosting → firestore → storage → functions** — storage is NEW; rules+bucket land together). The POST-MEGA block below holds the cutover additions only Code knows (the `avatars/` storage match is LOAD-BEARING · `sharp` is a functions prod dep · practice accounts are emailVerified BY DESIGN · `commentsMeta/uploads` needs NO seeding).

**CUTOVER-EVE notes (2026-06-11, the session that finished the build):**
- **Close-out checklist is MANDATORY** (v1.9.0 nearly missed it): CHANGELOG entry + the runtime widget bullets + the **1.9.1 → 1.10.0 bump across every string in CLAUDE.md's checklist** + ROADMAP "Current state" + the `docs/**` 404 scrub + a live signed-in smoke on prod.
- **Cache-busting:** frames.css/frames.js already ride `?v=` (bump with the version). `consent.js` / `lantern.js` / `cropper.js` are BARE imports — fresh-on-first-load at THIS cutover, but any later change to them won't bust caches; wire ?v= at the cutover if cheap, else first post-cutover fix.
- **First smokes with REAL prod data:** the catch-up door/sheet signals (only ever seeded data so far; the seenSlugs diff self-seeds silently on first visit, by design) · the 👁 chip + gold-flip on a real request · a real vote→count→notif (the v1.9.0 want that never ran).
- **Post-cutover queue, my order:** (1) profiles BACKFILL → then the users-GET tightening (the reverted want); (2) the anon `#secondary=` boot-route bug (pre-existing — diagnose at the hash-route's openSecondaryModal guard); (3) the **peer-DM flip** (Blake's explicit-go gate; the safety infra is all live); (4) the PITCH trio I still want: **Blake's Echo** (his gold tick vs the room's purple average — needs HIS eye on the copy) · **Ask Blake** (throttled public Q&A on the DM floor) · **Constellation Wrapped** (the v1.10.1 showcase); (5) comment-list DIFF rendering (the rebuild-eats-drafts class, deferred since 4b).
- **The heart held through 26 gates — keep holding:** gold = Blake only (THE DEN KEEPER frame is now its strongest expression — RULES-locked, never just UI-hidden) · community purple + count-free (Appreciate the one carve-out) · his 44 + his voice the permanent center · every cover sink scheme-gated · every component ships its `[hidden]` twin · every visual claim gets SAMPLED PIXELS.

**Gate-20.7 deliberate calls (don't "fix" these):**
- **The avatar-origin loosening is EXACTLY one origin** (`http://127.0.0.1:9199`, full-match anchored in rules; localhost-scoped in the three client gates). ⚠️ Prod inertness rests on the CLIENT hostname gates + the viewer-loopback-no-beacon target — NOT on mixed-content blocking (browsers EXEMPT loopback; the panel corrected the comment). **Every new photoURL render sink must go through safeAvatar** — the gate class has needed two retrofits already.
- **The account preview's look is UNCHANGED by Blake's explicit word** ("I don't want to change the look of the preview in the account") — the crop note is the whole item-1 fix. Don't "unify the geometry" later without his word.
- **The ACCOUNT page's personal activity tabs keep all four** (Comments/Replies) — only the PUBLIC card went shallow (his "public display page"). Don't sweep them for symmetry.
- **The sign-in strip tears down on ANY auth change** (data-uid keyed) and its '+' is keyed to unread.length≥10 (a 10-doc inbox with 3 unread provably has no more unread — the unread set is a prefix of the createdAt-desc fetch). Same '+' fix applied to the door strip.
- **The 👁 chip's count read starts at the TOP of loadSecondary** (parallel with the detail fetch) so it docks in the same paint — don't move it back behind the render.
- **Hygiene noted, deliberately untouched pre-cutover:** script.js's `signOutBtn` handler (~line 2194) is DEAD CODE — no `#signout-btn` exists in any HTML; the real sign-out lives in account.js (which already clears `rar:welcomed`). Remove the dead listener post-cutover.

**Gate-20.6 deliberate calls (don't "fix" these):**
- **The background pipeline is FULL-BRIGHTNESS by spec** (Blake: "the brightness should be the same"): no img opacity, no full-card scrim — two localized caps + text shadows + **per-element PLATES on has-bg sheets** (rgba(16,5,32,.6–.74) on items/chips/tags/featured/cols/like/report; is-active tab keeps its purple plate). Never reintroduce a whole-card dim; new mid-card elements on the sheet need their own has-bg plate (g25 pins alpha ≥ .5).
- **The cropper captures 4:3** (the card's real window; 16:9 chopped sides) and its preview PAINTS the render caps — keep cropper.js's cap stops in sync with style.css's two ::after blocks (three places, all commented).
- **Gradient ring stops are corner-anchored** (0/22/78/100%) and the pairs are deliberately dark→bright; g25 enforces pair Δ ≥ 120 — don't slide a pair back toward analogous hues.
- **The save handler's order is deliberate:** profile-write outcome (incl. the consent RE-OFFER + one retry + the bg-upload retry) resolves BEFORE the bg-error early-return — reordering it back recreates the "Saved —" lie the panel caught.
- **practice-serve emulates ONLY the cleanUrls resolution half** (extensionless → .html). The prod 301 (`/suggest.html`→`/suggest`) is NOT emulated on purpose — e2e specs navigate .html paths.
- **"Major Update" = the purple big tier; "Milestone Update" = the gold semver-MAJOR tier** (Blake renamed the former; the latter renamed to kill the collision — lives in widget-update-skill.md, a Cowork-excluded file, working-tree only until Cowork picks it up).
- **Accepted LOW (banked polish):** light frame motifs (moonlit's moon, storm's fork tips) wash out where they cross the top-cap fade over a BRIGHT background — aesthetic only (ring/glow/washes always read); fix later with per-motif plates or a stronger top cap if Blake notices.
- **Walk hygiene:** pixel walks that SAVE a profile mutate the seed the dream-profile e2e asserts — restart practice after such walks, before e2e or Blake's smoke.

**Gate-20.5 deliberate calls (don't "fix" these):**
- **The users/{uid} GET tightening was BUILT then REVERTED inside the gate** — the panel proved owner-only get tombstones ACTIVE pre-Studio members ("a former member") because profiles docs are minted ONLY by the account Studio save. The flip is BANKED behind a profiles BACKFILL: either (a) dual-write minimal profiles {displayName, photoURL} from every identity-writing path (note: profiles writes are consent-gated — unconsented members still stranded), or better (b) a one-shot admin CF that mints profiles docs for every users/-only account, THEN flip the rule. The spec pins the restored PUBLIC contract so the flip can't sneak back un-backfilled.
- **Error-state season rooms are WATCHED-ONLY on purpose** (success-state rooms are everywhere per Blake's item 10): a successful AniList fetch is the existence proof for an arbitrary id — without it, garbage ids would mint live composers on undiscoverable graffiti keys.
- **The 'blake' frame is RULES-locked** (profileCustomFieldsOk: frame=='blake' requires the admin uid) — the picker hiding the tile for members is courtesy; the rule is the security. No user theme may ever carry gold (g24 greps frames.css; the Den's gold lives in --rarf-den-* assets only).
- **The catch-up sheet MARKS PINGS READ on open** (viewing = reading, the lantern-close contract) and **releases the scroll lock conditionally** (a deep-link's freshly-opened modal owns the lock — the unconditional release was a HIGH).
- **frames.js-missing saves don't touch the frame key** (a blanket deleteField would wipe a saved frame when the manifest fails to load).
- **cf-cascade waitFor default is 45s** (was 20s): the suite runs 34 functions now and the heaviest cascade pass sat at 20.3s — the bump is margin, not a slowdown mask (trigger executions verified ~465/run).
- **Known pre-existing, banked for cutover polish:** the ANONYMOUS `#secondary=` boot-route has never opened the modal (signed-in works; every real consumer — lantern letters, account rows — is signed in). Diagnose at the hash-route's openSecondaryModal guard when picked up.
- **Migration stragglers:** `anime:al:<id>` threads created AFTER a migration need the admin to re-run it (re-offer rides any later Mark-reviewed; acceptable for now).

## 💭 Code's wants — POST-GATE-20 (superseded by 20.5 above; kept for history). PLANS ONLY CODE KNOWS.

**GATE 20.5 — the deliberately-deferred shortlist (NOT silently dropped; the gate-20 prompt asked for this list):**
1. **The gold-flip's MIGRATION half:** the ping half SHIPPED (onSuggestionReviewed: admin flips a suggestion to 'reviewed' → the requester gets a gold Blake-origin `request_done` lantern letter landing on `secondary/<anilistId>`; the suggestionCounts rollup mirrors status so the 👁 chip retires). The thread migration (`anime:al:<id>` → `anime:<slug>`, GAINING the gold verdict rail) is BANKED: the slug truth lives only in animeData.js (client), and AniList titles ≠ Blake's Excel titles, so a CF can't derive it safely. DESIGN: extend the admin suggestions page's "Mark reviewed" action to also pass the catalog slug (admin picks from a dropdown of the 44 — the admin page can load animeData.js) into a callable that retags matching forum threads. ~half a gate.
2. **Season-room AniList decouple:** on a detail-fetch error the room band never mounts though `communityKey` only needs the id (isWatchedAniListId works WITHOUT detail); render the band in the error state too + re-arm the deep-link halo on `.secondary-retry`.
3. **The secondary-modal hardening trio** (carried): Tab containment; a loadSecondary GENERATION TOKEN (slow fetch A overwrites fast fetch B); the 280ms close window can wire comments into a closed sheet (guard on `.active`).
4. **users/{uid} email tightening** (carried): profiles-first reads are in — flip users `get` to owner-only.

**Gate-20 deliberate calls (don't "fix" these):**
- **`.acct-dd-menu[hidden]{display:none}` is THE brandSelect collapse fix** (display:flex beat the UA [hidden] — the same trap class as the consent banner; g23 pins it). Every new display:flex/grid component needs its [hidden] twin — the standing rule with three scalps now.
- **Sign-out clears `rar:welcomed`** (script.js + account.js doSignOut) — Blake's "welcome message doesn't appear signed-out" was the once-per-session door not re-greeting after an in-tab sign-out. A sign-out is a fresh arrival BY DESIGN now.
- **suggestionCounts GET is public, LIST admin-only** — the 👁 chip's contract; the LIST guard preserves the original anti-brigading intent (no queue enumeration/ranking). An OLD rules test asserting non-admin-GET-denied was updated to the new contract (rules.spec.js ~L275) — that flip was deliberate, not a regression.
- **The catch-up door buttons** read: unread lanterns (limit-10 + lastSeenAt), watchlist∩airing (the Discover airing fetch), new-44-since-last-visit (localStorage `rar:seenSlugs` diff — first visit seeds silently). All labels textContent; ids validated before deep-linking. Signals are PROGRESSIVE (buttons appear as data lands; quiet visits show nothing).
- **`secondary/<digits>` is a notif-target kind now** (script.js + lantern.js parseNotifTarget, anchored regex) — request_done lands on the deep-dive.
- **The room band** (`.secondary-room-band`, AFTER `.secondary-body`, BEFORE recs) is Blake's circled spot — round 4's left rail misread him; do not move it again without his words.

## 💭 Code's wants — POST-ROUND-4 (rounds 1-4 + Personal Collections built; 20→21 remain). PLANS ONLY CODE KNOWS — hand forward, do not lose.
> 🔑 **The gate-0 study (`docs/v1.10.0-DESIGN-STUDY.md`) is STILL the load-bearing spec + soul for gates 20-21** — welcome catch-up + cherries and THE CUTOVER RUNBOOK live there and NOWHERE else. Do not build gate 20 or run gate 21 without re-reading it (§6 gate map, §3 the safety spine, §4-5 the heart through-line: gold = Blake ONLY, community purple + count-free except the ONE profile-like carve-out).

**Deliberate ROUND-4 calls (don't "fix" these):**
- **The bare-`header` rules are scoped to `body > header` ON PURPOSE** — bare `header` was the 3×-recurring dim-headings root cause (the site-header ::before scrim painted OVER nested panel-head/hub-header text; computed-style specs passed while pixels were dim). g22-round4.spec.js pins BOTH sides (nested headers clean / site header keeps its backdrop). Never reintroduce a bare `header` selector; new semantic `<header>`s inside surfaces are safe BECAUSE of this scoping.
- **profilebg is dedupe-EXEMPT in `processUploadedImage`** (functions/index.js) — a background is one-per-user; dedupe deleted an identical re-upload while bgRef pointed at it (dangling 404). Don't "simplify" the exemption away.
- **loadFeaturedChoices NEVER writes the dropdown value back into profState on init** — only an explicit onChange pick does. The items-CG fetch is limit-60 across reviews+comments; an evicted/failed load coerced the saved pin to '' and the next unrelated Save silently unpinned (3 adversarial lenses caught it independently).
- **The season-room 3-track grid is ≥1150px only** — at 900-1149px the room spans BELOW the columns; Blake's review column must never be the cramped one (H5 is also a width statement).
- **Collections: private shelves work WITHOUT consent (watchlist parity); PUBLISHING is the community act** (consent modal + rules `public != true || gateOk()`). items[] entries are untyped maps by design (200-cap + 1MiB doc bound; render is escape-first + colSafeCover/safeCov scheme-gated — keep that gate on any new shelf render sink). The collections onSnapshot DEFERS re-render while a rename input is open (the comment-list rebuild class).
- **cropper.js is a bare import like consent.js/lantern.js** — the post-cutover ?v= cache-bust item now covers THREE files.

**Deliberate round-1/2/3 calls the code can't explain itself (don't "fix" these):**
- **consent.js is THE one consent implementation** (both pages import it) — round 1's HIGH was a fix landing in one of two lantern copies; never re-duplicate the modal. `ensureConsent` ALWAYS re-reads the gate doc (no positive cache on the WRITE gate — a mid-session ban must surface); only `peekConsent` caches.
- **subscribeComments' unsub arrays are PER-INSTANCE on purpose** (round-3 HIGH: gate 19 runs two rooms concurrently; statics cross-killed listeners). Never re-staticize.
- **The season room is WATCHED-gated** (`isWatched` in renderSecondaryModal) — rooms only where Blake's presence precedes (H5 + moderation reach). If Blake ever wants rooms-everywhere, it needs an explicit non-watched lead-in block, not just ungating.
- **Inbox gold is IDENTITY-keyed** (`.is-blake` from senderUid) — never perspective (`is-mine`); a future peer DM must NOT gild strangers.
- **The focus floor** (`:focus`/`:focus-visible` at style.css's end) deliberately exempts `.rar-live` + `.filter-narrow` (own glow; floor double-ringed them). The secondary/tertiary dialogs take programmatic focus with suppressed container rings (welcome-door precedent).
- The saved toast + 1.8s reload (consistency reload is intentional); the lantern ::before uses the `content:"🏮" / ""` alt-text form (a11y).

**Pre-cutover work I want (fold into gate 20 or a 20.5):**
1. **The imageRefs edit-strip sweeper** — STILL the one gap: `onProfileWritten` covers profile backgrounds; the 5 imageRefs surfaces (forum thread/post, comment, review, reply) still orphan objects on EDIT. Mirror `bgSweepDecision`'s shape.
2. **Decouple the season room from the AniList fetch** — on detail-error the room never mounts though the key needs only the id (isWatchedAniListId works WITHOUT detail); also re-arm the deep-link halo on `.secondary-retry`. Cold/429 = late halo today (16s runway is the mitigation).
3. **Secondary-modal hardening trio:** Tab containment (focus lands but isn't trapped); a loadSecondary GENERATION TOKEN (slow fetch A overwrites fast fetch B — state corruption on throttled networks); the 280ms close window can wire comments into a closed sheet (guard on `.active`, not `hidden`).
4. **The reviews/al: tripwire:** account activity rows already emit reviews/al: paths + the featured picker anticipates them — when per-season REVIEWS ship, the secondary must wire subscribeReviews + consume pendingReviewDeepLink, or those rows half-land.
5. **A recurring e2e for the al: deep-link landing** needs an AniList GraphQL stub (page.route) — verified by real-pixel walk + an in-page flash-log this round; the flash (2.4s) EXPIRES before `load` resolves on slow asset loads, so naive waitForSelector probes race it.
6. Orphan reaper + admin text-report image preview + users/{uid} email tightening (carried from the post-mega list, still open). consent.js + lantern.js need ?v= cache-busting post-cutover. An admin-pages keyboard walk (they DO load style.css — the focus floor reaches them, spot-checked benign).

**Ops traps refreshed this session:** e2e needs practice WARM (first-run ERR_CONNECTION_REFUSED right after boot = the documented race — re-run before debugging); walks must wait for "PRACTICE MODE READY" in practice.log (the static server answers BEFORE seeding — an early gate-doc read sees a half-seeded sandbox and shows the consent banner for seeded users).
## 💭 Code's wants — POST-MEGA-BATCH (gates 0-18 built; 19→21 remain). PLANS ONLY CODE KNOWS — hand forward, do not lose.
> 🔑 **The gate-0 study (`docs/v1.10.0-DESIGN-STUDY.md`) is STILL the load-bearing spec + soul for gates 19-21** — per-season comments (Blake's left-of-modal idea, his Q7a answer), welcome catch-up + cherries, and the cutover runbook live there and NOWHERE else. Read it before any remaining gate. The heart through-line (gold = Blake ONLY; community purple + count-free; his 44 + his voice the permanent center) is §4-5.

**Deliberate calls the code can't explain itself (don't "fix" these):**
- **Image dedupe is PER-USER exact-bytes on purpose** — two different fans sharing the same panel is legitimate in an anime forum; global dedupe would ban it. pHash (perceptual) is the upgrade path if spam evolves; per-thread/global were weighed and rejected.
- **A dm Lantern ping from Blake renders GOLD by design** (notifIsBlake — a message from Blake IS a Blake surface). A future PEER dm must render PURPLE; the functions/index.js comment states this. Not a heart bug.
- **The review-discussion composer is image-less BY BLESSING** (Blake, 2026-06-09) — live-formatted, no 📷. The one deliberate gap; don't wire it without his word.
- **`[img:N]` is reserved syntax** in prose (hand-typing it places a matching image). Accepted footgun; a composer hint is the cheap mitigation. **Spoiler content can't contain `|`** (cosmetic mis-pair only) — matches markdown.js's limit.
- **The conversation summary is CF-owned** (the participant summary-update rules branch was deliberately REMOVED after review — forgeable lastMessageAt/lastMessageText). The only client conversation update is the admin Lock. Don't re-add a client branch.

**Pre-cutover work I want (small, load-bearing — fold into gate 20 or a 20.5):**
1. **Edit-strip sweeper CF** — an owner who edits an image OUT of a post leaves the object world-readable (no CF diffs imageRefs on update; gate-13-inherited). **PARTIALLY CLOSED (dream-profile batch):** `onProfileWritten` now diff-sweeps the profile BACKGROUND (`bgRef`) on every profiles write — the pattern to mirror. STILL OPEN on the 5 imageRefs surfaces (forum thread/post, comment, review, reply): an `onDocumentUpdated` diff-sweep over `imageRefs` mirroring `sweepImageRefs` (and `onProfileWritten`). The ONE gap left to close before prod.
2. **Orphan reaper** (scheduled CF) — uploads whose doc-create never happened (tab closed mid-flow) live forever world-readable, quota-bounded. Daily sweep of `uploads/` objects >24h with no referencing doc.
3. **Admin TEXT-report preview should show the doc's attached images** (today only image-reports preview the object — Blake can't see a reported comment's images unless the image itself was reported).
4. **users/{uid} carries `email` and is world-GETtable** (pre-existing). Once profiles prove out, tighten users `get` to owner-only — author reads are profiles-first now, the cross-read dependency is dying.

**PITCH backlog (IN/OUT with Blake when energy allows):**
- **`[compare:1,2]` side-by-side token** — the power-scaler killer feature; the token machinery makes it ~a day.
- **Per-image ALT text** — a11y + care; needs a picker design pass (the thumb strip is too cramped for an input today).
- **Reaction-image quick palette** — a tiny Blake-curated set, one-tap insert (purple chips, never gold).
- **The latent migration** (carried): when Blake reviews an anime that has a non-44 `anime:al:<id>` thread, migrate it to `anime:<slug>` so it GAINS the gold verdict rail — the "be first to talk about it" → "now Blake-reviewed" arc.
- **Live-composer caret polish** — completion transforms park the caret via a ZWS pad; a later pass could handle wrapping EXISTING text by typing markers around it. Toolbar covers that flow today.

**Cutover-runbook additions only I know (merge into the gate-21 checklist):**
- Deploy order is now hosting→firestore→**storage**→functions (`storage.rules` is NEW; bucket provisioning + rules land TOGETHER — the gitignore↔firebase leak-class applied to Storage).
- `firestore.indexes.json` gained forum `authorUid+createdAt` (profile pages) — indexes deploy BEFORE clients query.
- `commentsMeta/uploads` needs NO seeding (the kill-switch fails OPEN by design; Blake writes it only to kill uploads).
- The `avatars/` storage match is LOAD-BEARING — without it the staged default-deny breaks the live avatar-upload feature at cutover. Anyone "simplifying" storage.rules must keep it.
- Practice accounts are `emailVerified:true` so uploads smoke; PROD users must verify email before images — designed friction, not a bug.
- `sharp` is now a functions PROD dependency (the pipeline re-encode) — the cutover functions deploy ships it; cold-start cost was accepted (512MiB on the one image CF only).

## 💭 Code's wants for v1.9.0 (Community/Account — the NEXT ship; plans only Code knows — hand forward)

> v1.8.4 shipped 2026-06-06. v1.9.0 (Community/Account/User overhaul) is next and is its OWN gate-0 design study (Blake: "asap"). These are MY creative wants for it — not obvious from the code, so they live here so they aren't lost.

- **PROTECT THE HEART in community too.** When community reviews + comments land, Blake's gold **BLAKE'S REVIEW** stays primary — above/before community reviews, same gold-vs-neutral hierarchy as the cards. His voice leads; the community is supporting cast. NEVER let community reviews out-rank or visually dilute his own (the blend philosophy, extended to the review surfaces). This is the spine of the whole site — keep it.
- **The identity carries IN.** The account redesign + every new community surface should wear the **constellation veil** + the **purple-cosmos admin-floor vocabulary** (kicker + JP label + the gold/purple system), NOT a generic account page. `account.html` already carries `data-surface="foryou"` + the lit veil — build ON it. (The veil pulse is index.html-only today; extending it to account/suggest is a banked trivial add — see the pulse knob above.)
- **Reuse, don't reinvent.** The 3-layer modal (secondary z6000 / tertiary z7000), the shared `admin/chat-drawer.js`, `markdown.js`, and the non-catalog `al:<aniListId>` save schema are the substrate for comments / DMs / notifications. The **"N people requested" chip** (banked above) lands HERE once the Cloud Function count aggregate exists.
- **Account-page cover art** — some watchlist/favorites rows render without art (deferred from v1.8.4 G6 smoke). Fix it in the account redesign.
- **The disciplines DO NOT relax.** Gecko / Blake's headed Firefox Profiler is still the perf arbiter (headless can't measure paint); NO provider names anywhere; Blake's voice in every empty state + notification copy (no-provider-names is a gift that forces it); reduced-motion on every new animation; `[hidden]` symmetry; per-commit `--author=` Blake + ZERO trailers + the 7 Cowork excludes restore-staged out.
- **Scale honestly.** Blake's own words: community is "probably the biggest update yet… the amount of ideas I have and work it will take." Expect MULTIPLE ships / a real design-study phase — don't one-shot it. Cloud Functions (notification pruning, cascade deletes, the count aggregate) are the big NEW infra + a privacy-notice call (first time the site processes this much user data).
- **Welcome-door "catch-up buttons"** (already logged to v1.9.0 in the ROADMAP): intro-screen buttons that catch a returning user up (watchlist updates, a new season on a favorite), each expanding into sub-buttons that deep-link into the site. Needs the per-user follow/notification data v1.9.0 builds. Code to ideate the signal set.

---

<!-- author: Cowork | date: 2026-06-06 -->
## ⭐ v1.9.0 — BLAKE'S BRIEFING (2026-06-06, new Cowork chat — DURABLE; feeds the gate-0 design study)

> Blake's info-dump on top of the banked seeds (HANDOFF + the ROADMAP v1.9.0 row + the v1.8.5 scope entry below). His words quoted — Code treats them as spec.

- **Community Hub (NEW — the headline add):** *"All I can propose now is a community hub of some kind. Top anime conversations, users can make threads, discussion of their own accord (maybe a new header)."* A standalone community surface — visitor-created threads, top/trending conversations, free-form discussion. Possibly a new header/nav place alongside Den / For You / Discover.
- **Complete notification rework:** *"complete notifcation rework. UI rework. bell gets replaced. THE WORKS."* Not a polish — the bell itself is replaced with something new.
- **Scale + framing:** *"this is a COMMUNITY overhaul"* … *"it will be our LARGEST update by far"* … *"this is for the PEOPLE."*
- **Ship-count call:** *"I do hope to do the community overhaul in one ship but with a LOT of gates and prep work. We have done big updates in a single ship before. … if code really thinks its safer to do 2 ships ill allow it."* → One ship is the default; Code may argue for a split at gate 0 only on real risk.
- **Creative mandate to Code:** *"I want code going all out with ideas and thoughts that we didn't consider for users."* Full latitude — pitch beyond the literal scope.
- Everything previously banked rides along: comments overhaul (incl. per-season), community reviews overhaul, DM inbox, account redesign + cover art, Cloud Functions (pruning + cascade deletes + suggestionCounts), privacy notice, welcome-door catch-up buttons, "N people requested this" chip.
- **🚀 THE COMMUNITY HUB MEGA-PUSH — locked decisions (Blake, 2026-06-08; back to sandbox-then-one-cutover model):** Blake: *"I want to stop pushing these updates out till we have a large push."* v1.9.0 (foundation) + v1.9.1 (composer/fixes) are LIVE; everything else in the v1.9 vision now builds as ONE big push, smoked in practice mode, one cutover. **Locked structural picks:**
  - **Scope = EVERYTHING LEFT in one push:** Community Hub (4th nav place, threads, Hot/New/Top, free + anime-tagged) · DM inbox · account redesign (premium + Inbox tab + cover-art fix) · full public profiles · per-season comments (secondary modal, Blake's left-side idea) · welcome-door catch-up buttons · cherries ("N requested" chips, gold-flip on review, Surprise Me, footer glow-up, desktop launcher) · "anime information request" atop the secondary modal.
  - **Public profiles = FULL clickable pages** (his "would be awesome"): author name → their threads/comments/reviews/avatar/member-since.
  - **🔓 PEER DMs UN-BANKED — Blake wants to TRY them:** *"I would like to try peer DMs if possible."* Model: **anyone can DM anyone, with block + report** (a blocked user can't message you). The full safety apparatus (block/report/rate-limit) is LOAD-BEARING, not optional; Code's gate-0 must size it honestly and flag if it endangers the push. (Supersedes the v1.9.0 "bank peer DMs" decision.)
  - **Moderation = solid core incl. BAN:** report + delete + lock + pin + **account ban** (a step up from comments' delete-only), built into the Hub from day one.
  - **Hub post content = text + formatting + anime chips + IMAGE UPLOADS** — Blake: *"1 and 2 but we need some kind of moderation for images."* ⚠️ Image moderation is a real sub-project (manual admin review queue vs. automated NSFW/vision API vs. report-driven post-hoc removal — cost + complexity tradeoffs); gate-0 must lay out the options + recommend. NSFW/storage/cost all in scope.
  - **Profile privacy = Code proposes** (public profile vs. private saves vs. full opt-out — design alongside the privacy notice).
  - **🖼️ Image moderation = NONE automated (Blake, 2026-06-08):** *"none for now. But I will be looking at posts for anything inappropriate."* → images allowed, no NSFW/vision API, no upload review queue. Blake self-moderates manually + report-driven removal + the ban tool. The enforcement footing comes from the rules gate ↓.
  - **📜 COMMUNITY RULES GATE (Blake, 2026-06-08, REQUIRED before posting):** *"We need a rules set that people have to read before posting. click 'I agree' and I understand my account can be banned. stuff like that."* → a community-guidelines/rules screen a user must read + explicitly accept ("I agree", acknowledge ban risk) before their FIRST post/thread/DM. Stored as consent (per-user flag + version, so a rules update can re-prompt). This is the social contract that makes the ban tool legitimate. Blake's voice for the rules copy (Code drafts, Blake approves).
  - **🎨 Code has FULL creative latitude (Blake, 2026-06-08):** *"I want code to propose any cool ideas that will add to the community experience… code has full permission to think outside the box."* The gate-0 study should include an ambitious "ideas we didn't ask for" section — community features beyond the literal scope, marked IN / PITCH / BANKED. Protect-the-heart still governs (gold = Blake only; community is supporting cast).
  - **Version:** TBD at gate 0 (candidate v1.10.0 — bigger than v1.9.0; mobile stays v2.0).
  - **✅ GATE-0 STUDY DONE + Blake's 8 answers (2026-06-08):** Code's study found v1.9.0 already deployed the BACKEND (forum/posts, conversations/DMs, profiles, reports, suggestionCounts — rules+CFs+indexes, zero UI), so v1.10.0 is mostly "give the paid-for backend a face." Blake's picks: **Q1 Peer DMs = PHASED** (safety infra + "Message Blake" in v1.10.0; the user↔user flip is a separate later gate on Blake's explicit go — red-team flagged peer DMs as the #1 one-push risk: grooming/harassment on a solo manual mod). **Q2 Images = forum POSTS ONLY + admin kill-switch** (no image DMs). **Q3 Legal = email-verified-only upload gate** (Blake chose lighter than the +account-age rec) **BUT Cowork keeps the NCMEC report-and-preserve runbook regardless** (it's a worst-case response plan protecting Blake, not user friction — CSAM is a real legal duty for him as US operator). **Q4 Ban = doc-read `moderationGate/{uid}` gate + custom-claim backup** (Code's default; ban is a NO-OP today — no rule reads isBanned — this is the #1 functional gap). **Q5 Profiles = FULL ROUTED PROFILE PAGE NOW** (Blake upgraded from the mini-card-first rec). **Q6 Hub = "Lantern Room"** (Den lit behind the dimmed hub veil = structural non-co-equality). **Q7 Per-season comments = LEFT side of the secondary modal, H5 below Blake's review** (his original idea). **Q8 Extras = ALL FOUR greenlit:** anime-tagged threads + Blake's-verdict rail · spoiler tags (`||spoiler||`) · "Ask Blake" public Q&A · Constellation Wrapped (per-user year-in-anime on the veil). Scope now ~24-26 gates. Full study banked in SHIP-OUTPUT history; gate map = §6 of that study.
- **Post-briefing adds (Blake, 2026-06-06 — Discover-side, can ride v1.9.0 or any nearby ship):**
  - **Random-anime behavior + mini filter:** *"Random anime should be somewhat random. Most of the time its an anime I haven't reviewed from anilist. Sometimes it is one I reviewed. Maybe provide a mini filter so users can truly choose something random or something iv reviewed."* → Random pulls mostly from the wider AniList pool, occasionally from his 44; a small filter lets the user pick the pool (truly random / Blake-reviewed). Verify the current random feature's real behavior at gate time before asserting.
  - **"Hidden gems" Discover category:** *"On the discover tab add a new possible category called 'Hidden gems' It pulls anime that have high ratings but low reviews. Truly hidden gems that aren't seen by many people."* → New Discover rail: high `averageScore`, low popularity/review-count (AniList exposes `popularity` — feasible as a flat query on the existing L2-cache pattern; canary 101922 like the others). No provider name in the visitor copy.
- **Fresh-start wipe latitude (Blake, 2026-06-06 — offered, then settled):** *"Im allowing code to detete ALL comments, users, and reviews if it whats to start fresh and build a better system."* Cowork recommended NO wipe (his 44 are Excel-side and safe regardless; new features are new collections; legacy migration already planned; real visitors' data + his hardcoded ADMIN_UID at stake — same-email re-registration produces a NEW UID). Blake accepted: *"alright alright I hear you. I think the prompt is fine as is."* → **No wipe. The latitude stays on record:** if legacy data genuinely blocks a better system at any gate, Code may come back and ask, naming exactly what it wants to delete and why. Admin Auth account is NEVER deleted.
- **Code self-requests modes (Blake, 2026-06-06, mid-ship):** *"I am reading your output most of the time. if you want ultracode for a specific task after you read a prompt say it. Or you can just request the mode you want at the start after reading each prompt."* → Code may request High/Max/ULTRAMAX at the top of any response after reading a prompt; Cowork keeps recommending a mode per paste line (§13); Blake dials it. Code to bank this in its own docs (CODE-HANDOFF).
- **Commit strategy for the v1.9.0 mega-ship (Blake approved 2026-06-06, post-P1 report):** **checkpoint commits** at milestones (after the CF-infra gates P1–3, again at the gate-6 milestone, then the final sweep) instead of one giant commit after gate 15. Code's recommendation, Blake's pick. Checkpoints commit only — nothing deploys at a checkpoint; the deploy ladder + go-signal discipline are unchanged.
- **Gate-4 smoke PASS + UI polish flags (Blake, 2026-06-06):** all 10 smoke items passed. Two look-and-feel items deferred by Blake, ride gate 5's UI pass or a 4e polish: (1) **reply affordance** — *"Replies was a little weird. I didn't see the 2 reply bubble and I instead clicked on the reply as if I was going to reply instead of seeing a comment section… Just looks ugly but that will be fixed later"* → make the Replies toggle read clearly as "open the conversation", distinct from the reply-composer action; (2) **Report modal** — *"can report. UI needs touch up but that will be done."*
- **🔧 QUICK FIX — "FALL 1999" season label bug (Blake, 2026-06-08, LIVE on prod):** the AIRING NOW strip (Blake's Den) and the Discover surface show **"FALL 1999 · 秋"** instead of the current **"SPRING 2026 · 春"**. *"It should auto update after spring ends as well."* → the season/year label is mis-derived (likely a fallback default or a bad date-math/parse path), AND it must roll forward automatically as seasons change (winter/spring/summer/fall by month). Fix the derivation + make it self-updating. Bundle with the review-deep-link quick fix.
- **"Filter to my own review" (Blake, 2026-06-08 — feature, riding v1.9.1b):** *"One feature that should be available to users is the ability to filter to the review they posted."* → a filter/jump in the community reviews surface that isolates the signed-in user's own review. Cheap (the list is already client-side).
- **🔴 REOPENED + REAL — review-deep-link HALO genuinely fails WITH the server running (2026-06-08, v1.9.1c).** Blake re-smoked WITH `?emu=1` live (My-review chip + comments worked, so emu confirmed running): *"The only thing that didn't work was the auto scroll and halo on the review on both normal and account… the halo should definitely appear and im not seeing it."* Comment/reply deep-links DO halo; the REVIEW row does not. **This contradicts the e2e, which reports it GREEN — so the e2e (even after the v1.9.1 "honest" rewrite) asserts something that is NOT the visible halo** (class-present ≠ visually-flashing; likely wrong element, or a CSS rule on review rows suppresses the flash). This is the load-bearing test-integrity bug — must fix the VISIBLE halo AND make the e2e assert actual visibility before we trust it for the Hub/DMs ahead. Earlier "false alarm" note was Blake's no-server run; THIS is the genuine one.
- **🔧 QUICK FIX (Blake, 2026-06-08 — shipped-with-known-bug, fix in the very next update):** the **review-like deep-link still fails** — clicking the "found your review helpful" notification opens the anime but does NOT scroll to + halo the review in the Community column (comment-reply + comment-like deep-links both work from both pages). Blake's call: *"push it anyway. log for a quick fix in the following update."* 6g fixed the comment/reply cells; the review-target cell still misses live (the e2e claimed it green — so the e2e's review assertion is ALSO suspect; re-verify the review landing against a REAL click, not just the spec, when fixing). First item for the post-cutover fix gate.
- **Reports queue "view the content" (Blake, 2026-06-09, gate-4 smoke — fold into the next batch):** *"I would like an option to visit the comment, post, review that's being reported."* → each report row gets a branded "View" / jump-to-content action that opens the reported comment/post/review in context (reuses the deep-link scroll+halo machinery from gate 5). Everything else in the reports queue passed smoke.
- **🌟 THE DREAM PROFILE/ACCOUNT PLATFORM (Blake, 2026-06-09 — major scope expansion of gate 17):** Blake's verbatim mandate: *"The my account does not look good… I pretty much wanted a COMPLETE UI/UX/LOOK REDESIGN… I want users to be able to change their pfp, edit their bio, add tags to their bio, have precise controls over their favorites, watchlist, have filters, have functionality I can't even dream of, I want their activity to be shown and separated by comments, threads, reviews, replies ETC. I want a DREAM account page. I want people to be able to upvote their profiles. They should be able to add a BACKground to their profile. HECK even GIFs as their background. It should look AMAZING for user profiles. I want THE WORKS… It should feel like a social media platform for user customization. I want unique profiles and I want to see the ingenuity of the community."* + a new asset `assets/account_background.png` (1200×2400, SIDEWAYS — needs correct orientation). **Heart reconciliation (Cowork call, flag-able by Blake):** profile likes/upvotes are ALLOWED (user-to-user social, never gold; Blake's reviews stay supreme) — the one count carve-out from the no-count rule, for user profiles only. Profile backgrounds (image + GIF) ride the existing image pipeline + moderation (report/remove) + perf-safety (reduced-motion, no page-tanking). Building now as the dream-profile gate.
- **🌟 FRAME THEMES — user-pickable profile-edge designs (Blake, 2026-06-10 — staged into gate 20.5):** *"will users get to choose their profile design line changes? I want them to have options to choose from. Like green rainforrest vines wrapping around the edges, moutain rock, purple comsic sky, and at least 10 options. I want code to go all out."* → a FRAME picker beside Accent in the identity studio; ≥10 edge themes (vines / rock / cosmic sky + Code's inventions); renders on the account preview + the live public profile; no gold theme; compositor-cheap + reduced-motion; rules-enum lock-step. If 20.5 ships it, this line is the receipt.
- **🔮 FUTURE — richer card-state labels (Blake, 2026-06-09):** *"Cards either have 'Not Reviewed' or 'Reviewed by Blake' on them. Lets potentially add more like 'Blake Currently Watching' or 'Blakes Watchlist'."* → expand the card sticker vocabulary beyond the binary reviewed/not-reviewed to reflect Blake's real relationship to a title (currently-watching, watchlist, etc.). Heart-aligned (more of Blake's signal on the cards). Needs a per-anime status field Blake controls → pairs with the admin panel idea below. Post-v1.10.0.
- **🔮 FUTURE — Blake's personal card/watchlist admin panel (Blake, 2026-06-09):** *"New admin panel where I can manage my own watchlist in specific thats not tied to my account. I can add quick notes for an anime im watching. etc etc. Just a place to control cards."* → an admin surface (NOT his visitor account's watchlist) to set per-anime status (feeds the card labels above), jot quick private notes, and control how cards present. A curator's control panel. Likely Excel-column-backed (status/notes) per the canonical-data rule, surfaced in a new admin page. Post-v1.10.0; pairs with the card-label idea.
- **"Anime information request" on the secondary modal (Blake, 2026-06-07 — later v1.9.0 gate or rider):** *"Sometimes a secondary modal still won't have enough information. I want users to request the ability for a 'anime information request' in case episodes, seasons, or where to watch aren't available. This would be at the top of the second modal as well."* → A request affordance at the TOP of the secondary modal when data is sparse; pairs with the existing "Request this anime" + `suggestions`/`suggestionCounts` infra (a `kind:'info'` variant is the cheap path). Slot: gate 13 (reports/queue adjacency) or the post-cutover polish — Code sizes it.
- **Gate-9 design input (Blake, 2026-06-06):** *"When will the secondary modal comments be added in? Did I ever mention they should live on the left side of the modal? after it gets pulled up?"* → He never had; nothing left-side exists. Treat **left-side placement of the secondary modal's per-season comments** as a Blake-floated direction for gate 9 — Code weighs it against the below-review default (mobile-honesty + the H5 Blake-review-first invariant) with full design latitude; propose, don't assume.
- **Gate-5 smoke PASS + Blake's design calls (2026-06-06, his words — fold into gate 6+):** all 14 items passed. (1) **Remove Blake's gold rating block from the Community tab:** *"My rating has already been provided in the middle modal in gold. The community tab should be fore the community. The cool UI behind Blakes rating should be given to the community pill. Love the bar."* (His call overrides the H2 duplicate — his gold lives in the center modal; the histogram's gold tick stays.) (2) **Composer upgrades:** *"Users should be able to do crt. b and crt. I on their keyboard. It should also look more obvious in the review that the style is different. code still needs to go all out on the redesign. Enter should also post the review and users shouldn't have to click the word 'post'"* → Ctrl+B/I in the visitor review/comment composers (admin section-editor precedent), visibly-styled formatting in the composer, Enter-posts (Code resolves the multiline conflict, e.g. Shift+Enter = newline), and a full go-all-out composer/review-surface redesign with latitude. (3) **Discussion sign-in:** clicking into a signed-out Discussion shows the inline "Sign in to comment" instead of triggering the branded sign-in modal → wire the CTA to the popup. (4) General: *"UI is still mostly old but I trust that will get rebuilt… im just worried about functionality, UI and upgrades but I know thats coming."*
- **Post-cutover rider — complete "who liked" history (from gate 6c, 2026-06-07):** Blake's ask: *"I want users to be able to see exactly who liked their comment/review/thread/discussion by the time this update is complete."* 6c shipped the cheap honest version (expandable rollup itemizing the RECENT likers — bounded by the 10-notification prune window). The COMPLETE list (everyone ever) needs a per-item `votes/{uid}` query + uid→name resolution — Code proposed it as a post-cutover polish gate. Must land before v1.9.0 closes.
- **Locked at briefing (Blake's picks, 2026-06-06):** Hub = **4th nav place** ("4th nav. let code propose as well." — Code may refine placement/layout within that). Threads = **signed-in post freely + Report + admin mod tools** (no approval queue). DMs = **"Hopefully 2"** → full **user↔user DMs** wanted, **"with a special inbox if people want to message me"** (a distinct message-Blake channel) — Code weighs the real cost at gate 0; admin↔visitor is the floor if user↔user endangers the ship. Profiles = **"1 is fine. 2 would be awesome"** → name+avatar is the accepted baseline, **full public profile pages are the aspiration** — Code studies feasibility.

---

<!-- author: Code | date: 2026-06-06 -->
## ⭐ v1.9.0 — Community Overhaul: gate-0 design study (PROPOSED, awaiting Blake's picks — DURABLE, survives SHIP-OUTPUT rollover)

> **Why here:** the FULL study went to `docs/SHIP-OUTPUT.md` (rolling doc) — banked durably here so no idea is lost. **Status: proposed, NOTHING applied; awaiting Blake's answers to the 7 Qs.** Produced via an 18-agent recon→design→adversarial-review workflow. The single biggest finding: **today's notifications are a live spoof vuln** — `firestore.rules:20-36` let any signed-in user write a forged "from Blake" notification into any other user's bell (client-written, 2 types, `fromDisplayName` unchecked). v1.9.0 closes it by making cross-user notifications **Cloud-Function-written**.

**Recommendation up front:** ONE ship, ~16 gates; **bank open user↔user DMs and full public-profile pages** (schema is peer-ready so both add later with zero rework). Default cut-point if de-risking: after Gate 6.

**Ground truth (verified from `firestore.rules` + spot-checked code):** existing collections = `users/{uid}`(+favorites/watchlist/notifications), `comments/{slug}/items`(+votes), `reviews/{slug}/items`(+votes + per-review `threads/` reply subcoll), `official/{id}`, `suggestions/{id}` (ADMIN_UID-only read). **No `functions/` dir** = first-ever CF/Blaze/emulator. Prune `cleanupOldNotifications` is in `script.js:1585` (homepage listener `:1720`, NOT account.js). `renderMarkdownInline` exists (`markdown.js:22/:141`). Nav funnel: `setActivePlace`(`script.js:3209`)/`moveMarker`(`:3228`)/`showForYou`(`:3250`); `data-surface` set ONLY at `:3218`.

**Data model (NEW/CHANGED):** `forum/{tid}/posts/{pid}` (Hub threads — the name `forum`/`posts` AVOIDS the existing `threads` collection-group collision); `seasonComments/{aniListId}/items` (per-AniList-id, reuses `al:<id>`, mirrors `comments/`); `conversations/{convId=sorted(a)__b}`+`/messages`+`/reads/{uid}` (`kind:"admin"|"peer"` — admin client-written/safe, peer CF-created later); reworked `users/{uid}/notifications` (expanded type enum, `create:if false` CF-only); public `profiles/{uid}` (CF-only `isAdmin`/`isBanned`; cut `counts`); `suggestionCounts/{aniListId}` (CF-only); `reports/{id}` (admin-only, suggestions-pattern).

**⚠️ SECURITY MUST-FIXES (adversarial pass — the design was exploitable without these):** **H1** DELETE the client `hasOnly(['likesCount','dislikesCount'])` update clause (`rules:68-72/108-113`) — today any user sets `likesCount:999999` directly; counts become CF-only via `increment()`. **H2** pin forum counters/`pinned`/`locked`/`removed`==zero/false on create in RULES not prose. **H3** flip notif `create:if false` in the SAME deploy as the CF (Admin SDK bypasses rules — no breakage window); add `createdAt==request.time`. **H4** never add a `collectionGroup('messages')` rule (global leak); pin message-read to `get(parent).participants`. **H5** soft-delete MUST null the body (else "deleted" abuse is console-readable). **M1/M2** reserved-name denylist (blake/admin/mod) + constrain `photoURL` to own storage (avatar-swap vector). **M4** every mod branch is literally `auth.uid==ADMIN_UID`; recommend migrating that UID to a CF-set custom claim. **M5** no hot unread-counter (compute client-side, write only `lastSeenAt`; add 2s vote-toggle clamp). **M6/L5** CF idempotency on `eventId`, DM-send a callable transaction, `onUserDelete` must also delete *foreign* votes + tombstone conversations. **L2** `createdAt==request.time` on every client create. **L3** report queue re-fetches live content (reporter-supplied `snapshotText` is forgeable).

**Day-1 Cloud Functions (4):** `onVoteWriteNotify` (counts+server-sourced notification), `pruneNotificationsOnCreate`, cascade deletes (`reviews→threads→votes` orphans TODAY), `enforceRateLimit` (callable). **Deferred:** `aggregateSuggestionCounts`, `onDirectMessageCreate` (with DMs), scheduled `detectNewSeasonReviews`. **`onUserDelete` cascade = reclassify day-1 if the privacy notice promises deletion (Q3).** Blaze mandatory → budget alert + per-fn `maxInstances` day one. Tests = a SEPARATE `functions/test/` track (emulator) from Playwright.

**Design picks:** Hub = 4th nav place, **single-column list opening threads in the secondary modal (Direction C)**, time-decayed **Hot-score** `(up−down+0.5·replies)/(ageHours+2)^1.5` (falls back to `lastActivityAt` if CF lags), anime-ref **chips** routing through `#secondary=` back to Blake's gold reviews; veil step `community = .46/.60/.74` (a notch darker than For You). Bell → **"The Lantern"** glow glyph (separate `@keyframes lantern-ember`, NOT `#veil-pulse`) opening a veil-wearing notification center; votes roll up, mute/snooze via `notifPrefs`. DMs = **admin-floor + message-Blake shipped, peer BANKED**; Inbox = **Direction B** (a room in the Community place: From Blake / Suggestions / locked "People" folder). Account = **tabs-evolved + Inbox tab + the cover-art heal fix** (A: `onerror` heal hook; B: write-time `coverImage` validation in `setSaveAnilist` `script.js:327-344`; C: observable backfill). Comments = depth-1 `replies` (lazy), `renderMarkdownInline` (headers OFF); reviews = full `renderMarkdown` + Helpful votes + a histogram **kept subordinate to Blake's gold rating**.

**Protect-the-heart invariants (bake as Playwright assertions):** Den is the only nav place with a resting gold accent; Blake's gold rating stays primary ABOVE the subordinate histogram; cut `profiles.counts` (per-user leaderboard); Blake-origin notifications sort above community pings; `.secondary-review` precedes the community block in DOM order; first catch-up-door row is Blake-origin; profile = a count-free mini-CARD not a page; closeout grep new community CSS for `gold` → zero hits except Blake-rating ticks.

**Pitch list:** IN = "N requested" chip + "**Blake answered this**" gold flip + ~~"Surprise me" (random-of-44)~~ <!-- author: Cowork | date: 2026-06-10 -->**(CUT from gate 20 — already exists as the nav's Random ランダム button, Blake caught it; the enhancement [mostly-unreviewed pool + mini filter, his 2026-06-08 quote] stays banked for the future Discover ship)**. PITCH = continue-rail / thread-of-the-week / spoiler-veil / community quote bubbles. BANKED = streaks, taste-match, quiet-hours, **achievements (lean kill)**, full public profile pages (→ mini-card), peer DMs. Riders cheap: footer premium+scroll-reveal (Blake's copy byte-untouched), Mode-1 launcher.

**Gate map (~16):** P0 docs/schema-freeze → P1 CF+emulator scaffold → 1 rules+indexes → 2 CF notify/counts/prune → 3 CF cascade/rate-limit/counts → 4 comments → 5 reviews → 6 notif cutover+Lantern → **[clean split point]** → 7 Community 4th place shell → 8 forum threads → 9 per-season comments into secondary modal → 10 DM floor+Inbox → 11 account redesign → 12 profiles mini-card → 13 mod queue + `/privacy` → 14 welcome catch-up → 15 pitch riders + heart-color audit. Test floor 84→~120+ across 2 tracks; bump-version 47→~70-80 (biggest: Gate 13 `admin/reports.html`≈7 + `privacy.html`≈4).

**⚠️ THE 7 OPEN QUESTIONS (Blake answers via Cowork; my rec in CAPS):** (1) DM scope — **ADMIN-FLOOR+message-Blake, bank peer** vs full user↔user day-1; (2) capture submitter uid on suggestions — **YES** (enables suggestion-reply, ends anonymity for logged-in) vs no; (3) `onUserDelete` cascade — **BUILD DAY-1** (real account deletion) vs manual-email floor; (4) Hub launch sort — **lastActivityAt now, Hot-score when the vote CF lands** vs wait for the CF; (5) @mentions/usernames — **DEFER mentions, ship name+avatar** vs introduce a unique CF-issued @handle now; (6) per-season granularity — **own `al:{id}` thread** vs one franchise gathering place; (7) ship shape — **ONE SHIP ~16 gates** vs split at Gate 6.

---

<!-- author: Code | date: 2026-06-06 -->
## 🧠 PLANS ONLY CODE KNOWS (banked end-of-gate-4 session — DON'T LOSE THESE)

> The next Code: read this. Decisions/ideas in my head that aren't obvious from the code. **🎉 v1.9.0 "Community Overhaul" SHIPPED to prod 2026-06-08 — the cutover is DONE (all 13 gen-2 CFs live; the `docs/**` leak scrubbed).** Full state = `docs/CODE-HANDOFF.md`; the cutover record = `docs/SHIP-OUTPUT.md`; data contract = `docs/DATA-MODEL.md`.

<!-- author: Code | date: 2026-06-08 -->
**🆕 POST-CUTOVER (v1.9.0 is LIVE) — the queue, DON'T LOSE:**
- **FIRST post-cutover fix — the review deep-link bug (Blake's carried item):** review-targeted notification deep-links open the anime but don't reliably scroll-to-highlight the exact review (comment + reply deep-links work from both pages — verified). My 6g emulator e2e proved the `#notif=reviews/...` LANDING works in isolation, so the gap is likely in Blake's real click path (the who-liked-review rollup row, or the `.review-row` being collapsed / the community column needing a beat to render). Repro: practice mode as Mika → click the "found your review helpful" ping. Likely fix in `openNotifTarget` (script.js ~1806): make sure the review row is rendered/scrolled-visible before the highlight poll (the comment path works; the review path may need the community column scrolled in or a longer poll), and add an emulator e2e cell for the real review-vote rollup click.
- **Cutover-process lesson (Blake asked — prevent recurrence):** the homepage update-log WIDGET bullets got skipped at the cutover. THREE artifacts answer to "the changelog" — the version *chip* (auto-bumped), `CHANGELOG.md` (written), and the hand-curated `index.html` `version-section` bullets per `docs/SKILLS/widget-update-skill.md` (MISSED). Cause: `CUTOVER-RUNBOOK.md`'s close-out said only "update CHANGELOG widget" and never referenced the widget skill, and the bumped chip read as "done." **TODO: add an explicit close-out checklist to `CUTOVER-RUNBOOK.md` — (a) CHANGELOG.md · (b) the index.html widget bullets per the widget skill · (c) ROADMAP · (d) refresh SHIP-OUTPUT.** (The widget itself was fixed live in commit `4a68b3c`; the runbook checklist is still TODO.)
- **`lantern.js` cache-bust (from 6f, still open):** imported bare (`./lantern.js`, no `?v=`) → future v1.9.x deploys that change it could serve stale. Not urgent (shipped fresh). Version-bust it (non-async) when convenient.

<!-- author: Code | date: 2026-06-08 -->
**🆕 Gate 6g (deep-link matrix — fix + VERIFY every landing) — APPLIED uncommitted, DON'T LOSE:**
- **Root cause (data, not code):** the practice SEED (`scripts/practice-serve.js`) wrote vote notifications with NO `targetPath` (the `reply` seed had one → it worked; `comment_vote`×2 + `review_vote` didn't). So the who-liked drill-down rows rendered `data-target=""` → `openNotifTarget('')` → kind `none` → opened the anime but never scrolled. This explains ALL of Blake's 6f fails (likes + reviews, BOTH origins — the gap is origin-independent). **NOT a 6f regression** (same-page calls `openNotifTarget` directly; the `#notif=`/`#all` normalize was never involved). **The real CF was already correct** (`functions/index.js:155` `targetPath = parentRef.path`) → PROD vote notifications already carry the right target; only the emulator seed was incomplete.
- **Fix:** added the matching `targetPath` to the 3 seeded vote notifications (comment_vote → `comments/one-punch-man/items/seed-0` = Mika's comment; review_vote → `reviews/one-punch-man/items/prac-mika` = Mika's review). Zero production-code change this gate.
- **VERIFICATION (the gate's 2nd job — raised bar):** new **emulator-backed e2e** proves the LANDINGS, not just the URL builder: `tests-e2e/deeplink-emu.spec.js` + `playwright.emu.config.js` + `npm run test:e2e`. It drives `openNotifTarget` via the `#notif=` hash against the SEEDED practice site and asserts the exact comment/review gets the `.rar-deeplink-flash` halo. **3/3 green** (comment target · reply→parent · review target). The `#notif=` hash is the cross-page entry; same-page + who-liked rows call the SAME `openNotifTarget`, so a green landing = green for every origin. Run it: `npm run practice` (one shell) → `npm run test:e2e` (another). It is NOT in `npm test` (needs the emulator/Java) — that's why it's a separate config/dir/script.
- **Matrix result (my walk):** comment-reply ✅ · comment-like (who-liked) ✅ · review-like ✅ — each × {index, account}. "review discussion/thread reply" = **N/A** (thread + official votes are counts-only; no notification type generates that target — `NOTIF_TYPE_META` has no thread type).
- **⚠️ Note for the cutover smoke:** because the FIX is in the practice seed, re-seed (restart `npm run practice`) before Blake's re-smoke so the new `targetPath`s are present. PROD needs nothing here — the CF already writes them.
- 6g set: `scripts/practice-serve.js` (seed), `tests-e2e/deeplink-emu.spec.js` (NEW), `playwright.emu.config.js` (NEW), `package.json` (test:e2e). `npm test` still 97.

<!-- author: Code | date: 2026-06-07 -->
**🆕 Gate 6f (cross-page deep-link precision — LAST pre-cutover fix) — APPLIED uncommitted, DON'T LOSE:**
- **Root cause:** account-page notification deep-links navigated `index.html#open=<slug>` — opened the anime but never handed the COMMENT id to index's `scrollHighlightNotif` path, so no scroll/halo (failed Blake's 6e items 3+5). Both failing tracks (regular rows + who-liked drill-down rows) funnel through `lantern.js`'s `openNotifTarget`, so one carry fix covers both.
- **Carry mechanism:** new ADDITIVE hash `#notif=<encoded full target>` (chosen over overloading `#open=` so the `#open=`/`#secondary=` parser the account saved-entry routing relies on is untouched). `lantern.js`: `openNotifTarget` → exported pure `notifHref(targetPath, animeId)` → `index.html#notif=<full target>` for comment/review (forum/conversations keep `#<path>`; bare animeId → `#open=`). `script.js` hash router: new `#notif=` branch → `showAll()` + `openNotifTarget(decoded)` (the SAME same-page scroll+poll+highlight path) + `replaceState` to `#all`. Same-page deep-links untouched/unregressed.
- **`notifHref` is exported + window-exposed** from lantern.js (pure, unit-tested). Playwright floor **96→97** (`tests/g6-lantern.spec.js` imports the module + asserts the carry URLs).
- **⚠️ Found + banked (NOT a cutover blocker):** `lantern.js` is imported bare (`./lantern.js`, no `?v=`) so it's NOT cache-busted like every other JS. Fine for THE cutover (brand-new file, nothing cached) but a POST-CUTOVER item: version-bust it (a dynamic `import(\`./lantern.js?v=${APP_VERSION}\`)` works but made the account page's async init flakier under the parallel suite, so I REVERTED to the stable static import — find a non-async cache-bust, e.g. a built/hashed filename or Firebase Hosting cache headers).
- **Test-harness note:** the account page's auto-init didn't reliably set `window.notifHref` under the parallel suite (a module/cache artifact; `initLantern` proven correct via manual call) — so the spec imports the module directly instead of relying on page globals. Also: SWEEP 8765 before runs (a lingering python http.server gets reused via `reuseExistingServer` and serves stale) — bit me this gate.
- 6f set: `lantern.js`, `script.js`, `account.js` (revert to static import + cache-bust note), `tests/g6-lantern.spec.js`. **THE CUTOVER is next on Blake's explicit "ship it"** (he's said "fix that then ship" — this is the gate). The `firebase.json docs/**` leak fix MUST ride that hosting deploy.

<!-- author: Code | date: 2026-06-07 -->
**🆕 Gate 6e (final pre-cutover fixes) — APPLIED uncommitted, DON'T LOSE:**
- **Item 1 (cursor):** `cursor: pointer` on `.notif-btn` (it lost it when 6d dropped the `.as-link` class). `.account-token` already had it.
- **Item 2 — Lantern now WORKS on the account page (the big one):** NEW ES module **`lantern.js`** = a faithful port of script.js's Lantern (model, center, render, subscribe, gold gating, glow, mute, rollup). `account.js` now `import { initLantern }` + calls it; its dead legacy notif shell (renderNotifShell/renderNotifications/updateNotifDot/markVisibleNotifsRead/notifMenu) was removed; unused `updateDoc` import dropped. Deep-links from the account page NAVIGATE to index (`index.html#open=<slug>` for comment/review, `index.html#<path>` for forum/conversations — confirmed against index's hash router). **⚠️ ARCHITECTURE DEBT (intentional):** the Lantern logic now exists TWICE — `script.js` (index, classic script) + `lantern.js` (account, module). I did NOT refactor index's praised, working lantern pre-cutover (isolation > DRY; breaking it at the irreversible cutover is the worst case). **POST-CUTOVER unify:** make `script.js` consume `lantern.js` (or modularize index) so there's one source. Until then, any Lantern change must touch BOTH.
- **Item 3 (back-to-notifications findable):** the `.lantern-back-chip` moved bottom-LEFT→bottom-CENTER, bigger/brighter, with an attention entrance (`@keyframes lanternBackChipIn`, reduced-motion safe) + clearer label "← Back to Notifications".
- **Item 4 + Blake's note (search ✕):** removed the orphaned `.clear-btn` ✕ from account.html's search (it was the only one; index never had it). The `.clear-btn` CSS (style.css ~2852/3314/3333/3348) is now dead — a cheap post-cutover cleanup.
- **Blake's note (unread GLOW):** rows in the center get `.is-unread` when `notifCreatedMs(n) > notifLastSeenMs` (rollup glows if any vote unread). They glow while open; `closeLanternCenter` now AUTO-marks-read so they go cold on the next open. CSS `.lantern-row.is-unread` (purple) + `.lantern-row--blake.is-unread` (gold) in style.css — shared by both pages. Implemented in BOTH script.js (index) + lantern.js (account).
- Playwright floor **95→96** (new account-lantern-load spec in `tests/g6-lantern.spec.js` — asserts account.html loads lantern.js, `window.lanternModel` set, no page errors, no `.clear-btn`). 6e set: script.js, style.css, account.html, lantern.js (NEW), account.js, the spec. **THE CUTOVER is next on Blake's explicit "ship it"** — and the `firebase.json docs/**` leak fix MUST ride that hosting deploy.

<!-- author: Code | date: 2026-06-07 -->
**🆕 Gate 6d (Lantern round 2 + header redesign) — APPLIED uncommitted, DON'T LOSE:**
- **Header rail (item 1):** `.nav-links` flipped column→row — search LEFT, then `.header-tokens` (Lantern + account head) as two equal 44px circular tokens, centered in the 80px header (fixes the "cut off" clip). Lantern 38→44px, glyph 24→30px. Removed `.nav-topline`/`.as-link` (verified zero dangling refs). index.html + account.html + mobile.css (stacks <900px: search full-width, tokens centered).
- **Account head token (item 2):** `#auth-open` is now `.account-token` — head silhouette signed-out (still opens the sign-in modal), avatar/initial signed-in. Old `.nav-topline .as-link.is-avatar` CSS re-homed to `.account-token`; `onAuthStateChanged` signed-out branch injects the silhouette SVG instead of `textContent='Sign in'`.
- **Account-page nav (item 7):** Den/For You/Discover + View All added to account.html (buttons → `index.html?place=foryou|discover`); index `showHome()` boot reads `?place=` → `showForYou()/showDiscover()`. account.html doesn't load script.js, so these are plain navigations (no live marker on account — acceptable).
- **Smaller items:** (4) deep-link halo scoped to `.bubble`/`.row-toggle`, never the avatar; (6) `.lantern-avatar` overflow hidden→visible + `img{border-radius:999px}` (corner badge no longer clipped); (9) agree-rating ▲/▼ → review thumbs SVG + `.official-votes-bar .vote-ico` CSS; (10) lock-thread → branded purple pill (`.comments-admin-lock .lock-toggle`).
- **DEFERRED (Blake-sanctioned):** item 3 (rollup cosmetic polish — functionally fine) + item 5 (discoverability — the enlarged Lantern IS the fix; a welcome-splash "catch-up" row belongs to **gate 14** + `lastSeenAt`).
- Playwright floor **94→95** (header-rail spec in `tests/g6-lantern.spec.js`). 6d set: index.html, account.html, script.js, style.css, mobile.css, the spec. **⚠️ uncommitted now spans the audit fixes + 6d on top of `8ff8551` — a lot is staged for the eventual checkpoint commit.**

<!-- author: Code | date: 2026-06-07 -->
**🆕 Consistency audit (Blake's "things you & cowork miss" request) — DON'T LOSE:**
- **Full findings: `AUDIT_2026-06-07-consistency.md`** (project root; `AUDIT_*`-ignored so it can't leak). 73 couplings mapped, 41 verified clean. **APPLIED this session (UNCOMMITTED, no deploy):** `firebase.json` → `docs/**` (leak fix) · `chatGPT` → "with coding help from Claude" both bios · social og/twitter/canonical on `suggest.html`+`404.html` · deleted the dead GOLD community-avg pill (`style.css`) · **Lantern gold ember now gates on Blake-origin unread ONLY** (`unreadBlake` added to `lanternModel`; community-only unread → new cool-purple `.lantern-lit-community` ember) · reduced-motion guards for 7 animations. Playwright floor **93→94** (`tests/g6-lantern.spec.js` gold-gating spec).
- **⚠️ CUTOVER-CRITICAL:** the `docs/**` change only SCRUBS the 14 already-live leaked docs when the hosting deploy runs (the cutover). `docs/NEXT.md` is publicly readable on prod NOW — it discloses the unpatched notif spoof vuln. Blake chose: **config now, scrub at cutover.** If he wants it gone sooner = a hosting-only redeploy from a CLEAN tree (stash the uncommitted v1.9.0 client first — do NOT ship it early).
- **Blake's decisions this session:** Lantern gold = Blake-origin only (spine wins over the old "gold = any unread" Lantern spec); the bio names Claude on purpose (his credit/story, an intentional exception to no-provider-names).
- **OPEN audit items (queued, NOT done):** mobile.css has ZERO rules for ~8 new v1.9.0 surfaces (composer stepper + histogram break ~320px) — pairs with 6b / v2.0.0 mobile · header/nav diverges across the 4 pages (**6d owns this**) · review body `maxlength=2000` (`script.js`) vs rule `size<=5000` (`firestore.rules:200`) — pick one · `bump-version.js` help text says "14 places", actually 47 (`scripts/bump-version.js:437`) · staged forum/DM/profiles have composite indexes but no client wiring — when wired, the client query MUST match the staged field order or it throws in prod only (auto-indexes silently in the emulator) · report payload (`script.js`) must exactly match the strict create rule `keys().hasOnly` (`firestore.rules:~409`) — spot-check pre-cutover · notifications empty-state copy is bare + vote labels drift (Agree/Like/Helpful) — one voice (rides 6b).

<!-- author: Code | date: 2026-06-07 -->
**🆕 Banked through gate 6c (DON'T LOSE — newest first):**
- **THE CUTOVER (Blake's "ship it") is the next big thing + the only irreversible one.** Order = **hosting → firestore → functions** (per `docs/CUTOVER-RUNBOOK.md`) — that order makes permanent count-corruption impossible (a count CF must never be live while a count-writing client is). Low-traffic window; per-step verify + rollback; don't let urgency rush the ordering.
- **6b — the "go all out" review-composer redesign** (deferred from gate 6/6c, ULTRAMAX-worthy): reuse the `wrap()` selection-toolbar in `admin/section-editor.js`; **Enter=post** (+ Shift+Enter newline for comments, Ctrl+Enter for reviews); formatting visibly styled while typing. It ALSO owns Blake's gate-6c item-8 note ("the community/sign-in/words look ugly + left-behind overall").
- **Rollup "who liked" — complete version (post-cutover).** 6c ships the CHEAP honest version (already-loaded RECENT likers, ~newest-10 inbox window). The COMPLETE who-liked-ever needs a per-item `votes/{uid}` subcollection query + uid→name resolution (via `profiles`/`users`). A clean post-cutover polish; the data's all there.
- **The list-DIFF (deferred since 4b).** The comment list does `listEl.innerHTML=''` + full rebuild on EVERY snapshot → an open reply panel + a half-typed reply collapse when any change lands. Reconcile rows instead of rebuilding. The proper perf/UX fix; livable now (6c's deep-link re-finds rows post-rebuild).
- **Lantern banked knobs:** snooze DEFERRED (only per-type mute shipped — `notifPrefs/prefs.muted.{type}`). The glyph clip fix (6c) is best-effort `filter:drop-shadow`; if Blake still sees clipping, the lever is the header's `overflow-x:clip` / right padding. Deep-links for `forum/<tid>` + `conversations/<id>` route via `location.hash` (no on-page router yet — gates 7/10 wire it). `account.js` still has a legacy bell block the Lantern GUARDS — deletable in a cleanup. `window.lanternModel`/`parseNotifTarget`/`histogramModel` are the exposed pure helpers (extend their specs).
- **Kept-not-retired (Blake can ask to simplify):** the per-review **Discussion threads** (redundant with the main comments — retiring is a ~5-min non-destructive change) and the **`official` "agree with Blake's rating"** vote (now `onOfficialVote`-backed). Both were a judgment call to KEEP.
- **Gate-6 notification LOCKS (in the CF):** a review value `-1` ("Not helpful") sends NO notification; the review verb is **"found your review helpful"**; comment/reply votes still notify "liked/disliked"; thread + official votes are counts-only.
- **Protect-the-heart is the spine of every remaining gate:** gold = Blake ONLY (his rating + the histogram's single gold tick + the Lantern's Blake-origin pin/ember). The Community 4th nav place (gate 7) needs a Den-first resting accent so the marker never reads co-equal. Carry the constellation veil + purple-cosmos into account redesign / DM inbox / every new surface.

---

**Earlier banked (gates 4-5, still relevant below):**

**Gate-4b (Blake's smoke fixes — expect these):**
- Reply **voting** was deferred (replies post/display only, no vote pills). The rules already have `replies/{rid}/votes` — wire the UI like comment votes (new vote model: client writes only the vote doc).
- ⚠️ Known UX bug: the comment list does a full `listEl.innerHTML` rebuild on every snapshot, so an **open reply panel collapses + loses a half-typed reply** when any other change lands. The real fix is a list-DIFF (reconcile rows, don't rebuild). Flagged; do it in 4b/a perf gate.
- The 2s vote-clamp: I swallow the `permission-denied` silently. If Blake wants feedback, add a friendly "give it a sec" + a client debounce.
- Cooldown UX (the 4s post throttle still `alert()`s) — make it a disabled-button countdown.

**Gate 5 (community reviews) — protect-the-heart is BINDING:**
- Blake's gold rating stays its own LARGE primary element ABOVE the rating-distribution histogram; the histogram is subordinate (purple bars + a single GOLD tick marking his score — gold tick, never a gold canvas). Add a Playwright assertion: `.secondary-review` precedes the community block in DOM order.
- ⚠️ Review votes STILL use the old client count+notification transaction (`script.js` ~5328-5391). Move them to the new vote model (client writes only its vote doc; CF owns counts/notif) so the gate-6 cutover doesn't have to retrofit.

**Gate 6 CUTOVER (the one moment prod changes) — the exact deletes:**
- Delete the client notification-write blocks: `script.js:4429-4447` (comment vote) + `5360-5378` (review vote), and the client count-increment in both vote transactions. Deploy CFs (`npm run deploy:functions`) + rules (`firebase deploy --only firestore`) + the new client AS ONE. Flip notif `create:if false` only AFTER confirming CFs mint in prod. 3-step migration is in DATA-MODEL.md.
- Then the **Lantern** notification UI: replace the bell with a constellation/lantern glow glyph (cold=caught-up, gold ember=unread; separate `@keyframes lantern-ember`, NOT the index-only `#veil-pulse`); opens a veil-wearing notification center (a `.secondary-layer` sheet). Per-type + mute/snooze via `notifPrefs`.

**Pitch list (gate 15 / IN-cheap):** "👁 N people requested this" chip on non-catalog cards (rides `suggestionCounts` — the CF exists) → flips **GOLD "Blake answered this"** when an accepted suggestion lands in the 44 (the single best heart-protector — the curator listens but decides). "Surprise me" random-of-44. PITCH: spoiler-veil toggle (the veil IS the metaphor), thread-of-the-week gold ribbon, community-seeded welcome-door quote bubbles. BANKED-lean-kill: achievement badges (makes community co-equal). Public profile **pages** → BANKED; ship a count-free **mini-card** (tertiary modal, ≤5 recent) instead.

**Welcome-door catch-up (gate 14):** returning-visitor signal rows on `#welcome-splash`, gated on a SEPARATE `users/{uid}/lastSeenAt` (NOT reusing `rar:welcomed` — don't re-break first-visit-only). Order by emotional weight but the FIRST row is always Blake-origin when one exists ("Blake posted a new review"). Needs the CF-written notification types (gate 6).

**Context budget (I just audited it — see SHIP-OUTPUT git history for the full consult):** the `deniedMcpServers` deny-list is written into `.claude/settings.local.json` (PARENT dir) — verify it via `/context` next session (MCP should drop from ~108k; fix the `Minutes` name if it didn't). I PROPOSED but did NOT enact: archiving the historical 75% of CODE-HANDOFF + NEXT into `*-ARCHIVE.md` (saves ~30k/boot). If Blake says "go," do that doc split. Cap your own fan-out bundles (they land in your context in full).

**Near-term riders (Blake wants, not yet scheduled):** About-Me/Credits/Contact footer premium pass + scroll-reveal (restyle the container; do NOT rewrite Blake's copy). Mode-1 desktop launcher (local tooling, ~30 min). v1.8.4 banked knobs: veil-pulse tuning (`veilLinePulse 16s`/`.vp-glow top:40%`/peak opacity 0.82) + extending the pulse to account/suggest (index.html-only today); For-You title alternates; "Blake's Constellation" static-SVG behind the Den Top-10.

---

## Phase B remaining (v1.6.x)

| Version | What | Notes |
|---|---|---|
| v1.6.x | Real one-click AI integration | Replace the current paste-back AI panel with a true one-click. See `docs/ai-integration-design.md` — recommended path is a Firebase Cloud Function. |
| v1.6.x | One-click full automation | Drop the explicit deploy confirmation gate after enough trust is built up. Mode 1 server option. |
| v1.6.x | Clickable live preview opens modal | Make the admin form's live preview card clickable; opens a mini-modal showing what the full review experience looks like (using the same modal code as the homepage). Requires extracting modal-opening logic from `script.js` similar to how `card-render.js` was extracted in v1.6.5. Estimated 1–2 hours. Requested by Blake during v1.6.5 smoke test ("I can't click on the live preview to see what a review would look like"). |
| v1.6.x | More Info panel polish bundle | Consolidates several panel-polish items that need to ride together. From v1.6.10 gate-5 smoke feedback. Specifically: **staff whitelist expansion** (currently 4 fixed roles — add 1-2 more like Sound Director / Series Director for richer common-case display); **Re:Zero source-vs-relation episode-list deduplication** (anomaly A from v1.6.10 gate 5 — addressed in v1.7.1 via aggregation infrastructure, but cosmetic dedupe could land sooner); **cosmetic season-header styling** (`.more-info-relation` reuse vs. dedicated class); <!-- author: Cowork | date: 2026-06-03 --> **title text overflowing the format pill** (e.g. on shorter ALSO LIKED rows where titles like "Howl's Moving Castle" wrap on top of the `MOVIE` pill — flagged by Blake during v1.7.0 gate-4 smoke on Princess Mononoke modal); other ad-hoc panel cleanups Blake spots during use. Indefinite slot — bundle ad-hoc when 2-3 items accumulate. |
<!-- Widget version chips moved out of v1.6.x — scheduled as the first v1.7.x polish ship after v1.7.0 lands. See entry below in Phase B-side. -->

## Phase B-side — data infrastructure + secondary modal

- **v1.7.0 — AniList backfill + community score** ✅ **shipped 2026-06-03** (see Recently shipped). Backfilled `AniListId` / `IdMal` / `AniListScore` / `AniListColor` / `TitleEnglish` / `TitleRomaji` on the legacy 44 (40 matched, 4 skipped) via the new `npm run backfill` CLI; unlocked AniListId-based `Media(id:)` lookups everywhere — the prerequisite for v1.7.1's multi-fetch architecture is now in place.
<!-- author: Code | date: 2026-06-03 -->
- **v1.7.1 — AniList enrichment polish bundle** ✅ **shipped 2026-06-03** (see Recently shipped). Japanese/romaji subtitles (`pickSubtitle` + native fallback), per-anime `AniListColor` badge accent, premium `NO MATCHES` empty-state, widget version chips, the 4 skipped anime backfilled, and the `--add-native` / `TitleNative` pipeline. (Was previously slotted as "v1.7.0-polish.")
<!-- author: Code | date: 2026-06-03 -->
- **v1.7.2 — More Info panel overhaul (data architecture + UX redesign)** (MINOR) ✅ **shipped 2026-06-03** (see Recently shipped). Bundled the multi-fetch data architecture (v1.6.10 debt closure) with the panel UX redesign, partial-fail handling + retry, 24h localStorage cache, episode-counter toggle, undated-entry sort fix, and `UPCOMING` kicker.
- **v1.7.3 — Watched Set + Admin Form Completion** (MINOR) ✅ **shipped 2026-06-03** (see Recently shipped). The `✓ REVIEWED` pill now matches a per-review watched set (`WatchedAniListIds` + `KnownAniListIds` columns → sync arrays → render-layer set-membership) so it covers every watched season/movie/special, not just S1. Bundled the shared `franchise-fetch.js` extraction, the admin watched-set checkbox tree, the `✨ ASK` chatbot drawer (`/api/chat`, Haiku), unofficial-platform removal, update-log infinite-scroll, and a backfill CLI that populated all 44 rows.
<!-- author: Cowork | date: 2026-06-04 -->
- **v1.7.4 — Modal Architecture Overhaul** (MINOR, in flight). Bundles: (1) always-visible 3-column modal layout (More Info | Main | Community), (2) LARGE in-site secondary modal replacing v1.6.8's external AniList tab — slide-in over dimmed/blurred primary, banner header w/ cover poster, synopsis + genres + tags + trailer + character grid + staff + MORE LIKE THIS carousel, replace-content history-back navigation, "Request this anime" button on non-catalog entries, (3) per-season review feature (markdown storage + render in secondary modal + admin form + dedicated admin panel) + adjusted WatchedAniListIds routing so secondary watched entries open the secondary modal with the per-season section, (4) clickable characters + staff for detail (Code's gate 3 design). Estimated ~12-18h.
- **v1.7.5 — Watchlist + favorites + per-episode + character/staff detail polish** (MINOR) ✅ **shipped 2026-06-04** (see CHANGELOG/ROADMAP). Secondary-modal Watchlist/Favorite save pills on any AniList entry via a non-catalog `al:<aniListId>` discriminator schema (no Firestore rules change), account-page rendering of non-catalog saves + green ✓ REVIEWED + `#secondary=` route, per-episode expand (thumbnail + equal-weight multi-platform official watch links) + a WHERE TO WATCH section, signed-out saves → branded sign-in modal (+ auth z-index fix), `__underscore-bold__` in the shared `markdown.js`, and an AniList-sourced platforms backfill (`scripts/backfill-platforms.js`) correcting all 44 reviews' streaming listings (41 changed). Per-episode landed *thin* (AniList exposes only `title/thumbnail/url/site`) — the richer "full course" is the entry below.
- **v1.7.x polish slots** (post-v1.7.4): ~~Romaji subtitle on cards~~ ✅ **shipped in v1.7.1** (romaji + native Japanese subtitle, both cards + modal). **AniList per-episode scores feasibility check** — confirm whether AniList's `Episode` GraphQL type exposes per-episode community scores. If so, layer into the episode list; if not, shelve. Deferred from v1.6.9 planning.
<!-- author: Cowork | date: 2026-06-04 -->
- **v1.7.6 candidate — Account saved-entry routing fix** (flagged by Blake 2026-06-04 during v1.7.5 gate-8 preview smoke; noted for the NEXT update, not a v1.7.5 blocker). Clicking a watchlist/favorites entry on the account page that was saved via the secondary-modal pills (`al:<aniListId>` doc) always routes to the **secondary modal** — even when that AniListId belongs to a reviewed franchise. Blake's expectation: those should land on the **primary modal with his main review** (secondary only for genuinely non-catalog / watched-not-primary entries, matching the site-wide v1.7.4 routing split). Likely fix: at account-row click (or render) time, resolve the saved `aniListId` through `primarySlugForAniListId` / the watched-set — if it maps to a catalog review, route `#open=<slug>` instead of `#secondary=<id>`. Small, contained (~1-2h); gate-0 recon should confirm which saves can even collide (primary ids get saved from… where? — verify the real paths before asserting). (surfaced by Blake 2026-06-04 during v1.7.5 gate 1). Every review (main + per-season) follows a fixed scannable section structure: **Intro / Animation / Story / Characters / Design / Music / Feel / Extra Thoughts / Overall** — so visitors can jump straight to the part they care about. Blake's why: *"That way people can scan over exactly what they want to look for."* Open design questions for a Cowork+Code brainstorm at its gate 0: (1) rendering — styled section headers in the modal (kicker-style? collapsible? jump-pills at the top?), (2) authoring — does the admin form/season-review editor get per-section fields or a markdown heading convention (`## Animation`) parsed by the shared renderer, (3) migration — 44 existing reviews need restructuring (pairs naturally with the **admin edit page** item below — restructuring 44 reviews by hand in Excel is exactly the friction that page removes), (4) whether sections are optional per review or all-required. **When:** after v1.7.5; candidate to ride with or just before the admin edit page. Estimated ~4-8h depending on the authoring + migration calls.
<!-- author: Code | date: 2026-06-04 -->
- **v1.7.x/v1.8.x — Per-episode "full course"** (revisit; builds on v1.7.5 gate 3's thin expand). v1.7.5 shipped a *thin* per-episode expand on the secondary modal — thumbnail + full title + a `↗ {site}` link, sourced entirely from AniList's `streamingEpisodes` (the only per-episode fields AniList exposes: `title/thumbnail/url/site` — **no summary, score, or air date**, confirmed by a live 101922 query at gate 0). The "full course" revisit adds real per-episode *content*: (1) **Blake-authored per-episode notes** reusing the season-review storage pattern (`season-reviews/episodes/<aniListId>-<ep>.md` + a `/api/season-review`-style local CRUD + admin editor), rendered above/alongside the AniList strip via the shared `markdown.js`; and/or (2) a **richer external source** (TVDB API — per-episode summary/score/air-date/thumbnail; needs an API key + cost, so weigh against the manual-notes path) if Blake wants automated coverage across all 44 entries' seasons. (3) **Extend the expand to the More Info panel's episode rows too** — deferred at gate 3 because those rows are *renumbered labels* fed by the load-bearing 30-node franchise BFS query (`MORE_INFO_QUERY_NODE`); enriching that query with `thumbnail/url/site` needs a dedicated complexity-canary test (id 101922) and a rethink of the renumbering/dedup→single-episode mapping. **When:** after v1.7.5; pairs naturally with the structured-review-template + admin-edit-page items (same markdown/admin infrastructure). Estimated ~4-8h (notes path) / +API integration (TVDB path).
<!-- author: Cowork | date: 2026-06-03 -->
- **v1.7.x polish — Admin edit page for existing reviews.** Today's admin form (`/admin/new-anime`) only adds NEW anime; existing reviews can only be edited via direct Excel cell edit + `npm run sync` OR by re-running `backfill-watched.js`. Add `/admin/edit/<slug>` (or similar) that loads the existing review data into a pre-populated form (same shape as new-anime), lets admin toggle the watched-set + edit tags/description/etc., saves back via Mode 1 or a new edit endpoint. **Why it matters:** Blake's watched-set picks on the v1.7.3 backfill weren't 100% accurate (his own words: *"I know I got some wrong but im not too worried about it"*). An edit page makes corrections frictionless. Estimated ~3-5 hours. Surfaced 2026-06-03 during v1.7.3 close-out.
- **v1.8.0 — AniList tab on cards** (MINOR). Each anime card gets a separate tab showing verified-source data (genres, ratings, episode counts, streaming where-to-watch badges) at-a-glance. Distinct from the v1.6.8 More Info panel — that's the deeper view; this is at-a-glance.
<!-- author: Cowork | date: 2026-06-04 -->
- **v1.8.5 — Community + Account UI/UX Overhaul** (MINOR/MAJOR, surfaced 2026-06-04 during v1.7.4 close-out). Dedicated overhaul ship slotted before v1.9.0 mobile work. Scope:
  - **Community tab on the secondary modal** — today the main franchise modal has a Community Tab for comments + reviews; the secondary modal (v1.7.4) doesn't. Per-season community engagement needs its own surface. May share data with the main modal's tab (per-franchise) or be per-season (per-AniListId) — design call at gate 0.
  - **Comments overhaul** — UI refresh, threading/replies, moderation tooling, sort + filter, character limits + rich formatting (likely reusing the v1.7.4 shared markdown renderer).
  - **Community reviews overhaul** — UI refresh on the per-anime visitor review surface, sort/filter, rating distribution, helpful votes.
  - **Notifications system overhaul** — currently client-side pruning at top-10; backend Cloud Function (already in the "tech debt" backlog), notification UI redesign, mute/snooze controls, per-type granularity (replies, mentions, suggestion-accepted, etc.).
  - **My Account UI/UX overhaul** — `account.html` rework. Profile tab, watchlist tab (post-v1.7.5), favorites tab, activity tab all get a premium pass. Likely subsumes the v1.8.x **Suggestion DM Inbox** scope (admin↔visitor messaging — pairs with the notification system).
  - **Pairs with** the v1.7.5 watchlist/favorites schema work — non-catalog AniListId entries from the secondary modal need to render correctly in the account-page tabs.
  - Estimated ~15-20+ hours. Largest ship since the v1.6.x admin form work; potentially MAJOR version if scope grows.
<!-- author: Code | date: 2026-06-02 -->
- **v1.8.x — Suggestion DM Inbox.** Admin can respond directly to whoever submitted a suggestion + tell them if Blake liked it. Pairs with the planned notification/comment overhaul. Visitor side gets an Inbox UI; admin side gets a per-suggestion reply thread. **Auth prerequisite:** visitors need a stable identity (email or anon Firebase Auth UID) captured at suggestion-submission time — requires a schema change on `suggestions` docs. Estimated v1.8.x or later. (Requested by Blake during v1.6.11 smoke: *"inboxes where I can respond directly to whoever sent the request… pretty much DMs lol."*)

## Mobile compatibility (v1.9.0)

- **v1.9.0 — Mobile compatibility overhaul.** Site is currently broken at phone viewports per Blake's direct observation (2026-05-11), despite `mobile.css` being loaded under `@media (max-width: 900px)`. Two-step plan: (1) Tier A read-only audit at 375 / 414 / 768px viewports walking every major flow, output as `docs/MOBILE-AUDIT-{date}.md`; (2) one or more fix bundles grouped by area (layout, modal, form, images, fonts), each with its own preview deploy. Slotted after v1.8.0 so fixes target the final card design, before Phase D Mode 2. See ROADMAP `## v1.9.0` for the full spec.

## Phase D — Mode 2 (autonomous caretaker, future)

Cannot start until Mode 1 is in active use and backfill is done. Build order:

1. **Step 1: Read-only weekly health report** — no changes made, just observation, sent to Blake. Foundation for trust.
2. **Step 2: Autonomous data sync** — PATCH-tier updates only (season counts, episode counts, related anime, streaming links from AniList). Reports everything it changed.
3. **Step 3: Health-fix actions** — broken link repair, missing asset detection.
4. **Step 4: Content quality checks** — stale info, things rendering wrong.
5. **Step 5: Tune risk thresholds** based on observed quality.

Each step earns trust before the next is enabled. Mode 2 is constrained to PATCH-tier per project rule #4.

## Audit polish bundles (slot in between phases)

Step 3.5 audit had 56 findings; ~25 closed in v1.3.7 + v1.3.8. Remaining items group cleanly:

| Bundle | Audit refs | What |
|---|---|---|
| Bundle 1 (v1.4.x slot) | §2.1 + §4.1 + §2.3 | 404 page rebuild + return path + Google Fonts cleanup |
| Bundle 2 (v1.4.x slot) | §1.8 + §2.5 + §3.4 | 11 missing avatar files + filename casing + background image (WebP/AVIF) |
| Bundle 3 (v1.4.x slot) | §1.7 + §1.9 + §2.4 | `document.write` removal + inline `onclick`/`onsubmit` on account.html + Top 10 prev/next a11y |
| Standalone | §3.1 + §3.2 | Minify `script.js` (134KB) and `style.css` (107KB) — set up minify step in deploy flow |
| Standalone | §4.2 | Verify low-contrast secondary text (axe DevTools), fix to WCAG AA |

## Polish + tech debt (no version, do whenever)

- **Favicon + Apple touch icons** — browser tab currently shows generic icon
- **Basic privacy notice** — site stores user-generated content; short statement is overdue
- **Cloud Function for notification pruning** — currently client-side; backend would be guaranteed
- **Cloud Function for cascading deletes** — when community review deleted, threads/ subcollection orphaned in Firestore
- **Search-bar matching tuning** — tighten title-vs-tag/genre weighting
- **Investigate deploy file-count drift** — quick `find . -type f -not -path './.git/*' \| wc -l` diagnostic
- **Excel `Image` column** — currently sync derives image from slug fallback. Adding an explicit Image column would make the contract more obvious.
- **Mode 1 `customBullets` field on form** — let Blake write user-facing bullets per ship instead of always defaulting to "Added: <Title>"
- **Playwright test for Mode 1 server using `?skipPush=1`** — automate the synthetic-anime-via-AniList-ID smoke run that's been done manually around Bug 10 / Bug 9 verifications. Spawn the server, POST to `/api/submit?skipDeploy=1&skipPush=1`, assert all 9 SSE step events come through `done`, then revert+restore. Catches the v1.6.0-class regression class structurally, not just at server startup. Estimated 2-3 hour ship; queued behind v1.6.3 live preview.

## Big-vision ideas (no version, no immediate plan)

- **Recommendation engine** — "if you favorited X and Y, you'd probably like Z" (tag/genre overlap, then smarter signals)
- **AI-suggested tags for new entries** — LLM suggests tags from review text + AniList metadata
- **Community Top 5 Favorites panel** — aggregate-counted top 5 across all users on home page (originally v1.1.0, postponed)
- **Stats dashboard** — site-wide stats (most active threads, most prolific reviewers, vote distributions)
- **Admin mode UI** — moderating comments, deleting abusive content, pinning featured anime (admin UID already in PERSONAL.md)
- **Auto-update for new seasons / episodes** — Mode 2 capability once Mode 2 is mature

## Deferred (still real, just lower priority)

- **Anime font** — site typography currently fine; revisit if visual identity ever feels stale
- **@mentions in comments** — comment system works without it; revisit if community engagement grows

## Explicitly NOT happening

- Major architectural rewrites (vanilla JS → React/Vue) — site is small enough that vanilla works
- Monetization (ads, subscriptions, donations) — pet project
- Multi-language support — would conflict with personal-voice nature of reviews
- AniWave / unofficial-streaming-site integration in Mode 1 — AniList only (legal + data quality)
- AI-curated images — per project rule #9, image curation stays human (with hybrid AniList default)
