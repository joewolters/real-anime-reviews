<!-- author: Cowork | date: 2026-06-11 -->
# v1.10.0 — GATE 20.7: Blake's final smoke fixes + Code's own full confirmation pass. APPLY, verify, NO deploy.

> Blake's 20.6 smoke: gradients work · rooms good · request flow works · consent-on-save pops · Den Keeper still his. What's left is below. His close: *"fix these. confirm they are good. then push"* — **the "push" does NOT happen in this gate.** Fix + CONFIRM with evidence; Cowork collects Blake's explicit "ship it" after this report, and only gate 21 deploys. **Mode ULTRAMAX.** 5-lens adversarial review. Checkpoint + push (git only). STAGED — no deploy.

## PART A — Blake's items (his words)

1. **Background preview vs the PUBLIC card (Blake root-caused it himself):** *"The Preview (not the choosing picture preview) the preview after you submit the background is different from the ACTUAL live public page. Thats why its so confusing. When you switch to public view the background gets much wider. That should be made clear to a user somehow. Maybe a small note after you click 'confirm' when choosing a background."* → the edit-view hero and the public-view/live card render at different widths, so the same crop reads differently. **Blake's call (2026-06-11, overrides Cowork's unify-first framing): do NOT change the account preview's look.** *"I don't want to change the look of the preview in the account. There should just be a small note that says 'check live preview to see actual background' or something so people know."* → add a small branded note (Blake-voiced, e.g. after Apply/near the hero) telling users the public page is the true render — point them at Public view / the live page. That's the whole fix; no geometry changes.
2. 🔴 **The flaky public-profile save + avatar propagation (Blake's items 2 + 5 — likely ONE root):** *"what the hell does but the publix profile changes didn't go through mean"* + *"again, pfp didn't update on the the rest of the site."* → during his smoke a profiles write failed (the degraded-path copy fired) and his new avatar did not propagate site-wide. Diagnose for REAL in the sandbox: why did the write fail (emulator state? a race in the save chain? the retry not retrying?), and why didn't the avatar reach comments/threads/rooms after save (profiles-doc propagation, caching, or the failed write itself)? If it's sandbox-only, PROVE it and say so plainly; if there's any real race, fix it — this class cannot ride to prod. Then verify: change avatar → Save → it shows everywhere (comments, Tavern, rooms, profile card) without a manual refresh dance.
3. **Public profile card — shallower:** *"Take out replies and comments on a public display page. No need to go that deep."* → remove the Replies + Comments tabs/sections from the public profile surfaces (the profile card/modal and the live page). Threads + Reviews stay. Adjust the footer hint line to match.
4. **The 👁 request chip placement:** *"Needs to be moved under the thumbnail so its actually visible. make sure its down neatly ofc."* → move the demand chip out of the crowded top action bar to sit neatly under the cover thumbnail on the deep-dive. Branded, purple, visible.
5. 🔴 **The lantern/catch-up demo — SECOND failure, make it bulletproof:** *"Signed in as Yuki and I STILL don't see a lantern under the update log. Thats the SECOND time now. If it actually works I don't need to test it but its frustrating I can't see anything."* → Blake followed the steps and saw nothing, twice. (a) Diagnose WHY his runs show nothing — most likely: Yuki's unread letter was consumed by an earlier run/seed, or he signed in AFTER the door had already shown that session (the strip only exists at door-time). (b) Re-seed a guaranteed-unread state and write him ONE foolproof recipe (exact clicks, exact account, what he'll see, why each step matters). (c) **Design question, Code's call with latitude:** a user who signs in mid-session never sees their catch-up — consider surfacing the strip (or a compact version) on sign-in, not only at session start. If you build it, keep the door behavior intact. (d) Real-pixel screenshot of the strip showing 🏮 in the report — the claim-without-his-eyes pattern is at strike two.

## PART B — the CONFIRMATION PASS (Blake is not re-smoking everything; you are his eyes)
After the fixes: walk the FULL v1.10.0 surface yourself one last time — door (+catch-up), Tavern, rooms (watched + unwatched), images, profiles + frames + accents + cropper, collections, DM Blake, consent flows, the chip chain, reports queue, ban, deep-links, the launcher — and give a one-line PASS/FAIL per area with evidence where it's cheap. State plainly: **"ready for the cutover" or what's not.** This statement is what Blake's go-signal will rest on.

## Standing rules
Full brand parity · `[hidden]` symmetry · gold = Blake only, community purple + count-free (Appreciate carve-out) · reduced-motion · no provider names visitor-facing · no native dialogs.

## Verify
ALL tracks green + specs updated where pinnable (the geometry unification, the chip placement, the profile-card tab removal, any save-chain fix). 5-lens adversarial — lenses: the save-chain race, the geometry change (both surfaces + mobile widths), the sign-in catch-up surface (if built), the tab removal (no orphaned deep-links), perf. Then Blake's (short) numbered smoke.

## Report (lean)
Design vision (1 para) · Part A item-by-item against BLAKE'S numbering (incl. the item-2 verdict: sandbox-only proven OR real-race fixed; the item-5 foolproof recipe + pixel proof) · the PART-B confirmation table with the explicit ready/not-ready line · adversarial findings · checkpoint hash · test counts · Blake's numbered smoke. **NO deploy — the cutover waits for Blake's explicit "ship it" via Cowork.**
