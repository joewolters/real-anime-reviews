<!-- author: Cowork | date: 2026-06-10 -->
# v1.10.0 — GATE 20.5: gate-20 smoke fixes + Code's pre-cutover wants. APPLY, practice verify, NO deploy.

> **Mode ULTRAMAX, full latitude.** Blake: *"I want it to fix these changes and continue update work."* This is the LAST build gate before the cutover — close everything. 5-lens adversarial review. Checkpoint + push. STAGED — no deploy.

## PART A — Blake's gate-20 smoke list (his words — every item lands)

1. **Kill the door's Surprise me:** *"Get rid of the surprise me in the intro animation. No reason for it."* → remove the 🎲 from the door entirely. The nav's Random stays untouched.
2. **Door semantics — verify + explain, light touch:** *"when I meant the door must greet you when you sign back out I meant. Even if a person who never made an account tried to access the website. that intro would appear. sometimes it didn't. But honestly I think the change is fine overall."* → verify in a real browser that a fresh visitor (no account, new session) ALWAYS gets the door; report the exact show/skip rule in plain language so Blake can sign off on it. No redesign unless verification finds a real gap.
3. **WHILE YOU WERE AWAY — bigger + a real destination:** *"Needs to be a bit bigger so its more noticeable. Its kinda small. And when I clicked on it - it didn't bring me a unique page that has a nice and new design. it literally just brought me to my notifications within the website. Thats not what we are looking for."* → (a) scale the strip up so it reads as a door feature, not a footnote; (b) the lantern-pings button must NOT just open the Lantern — design a real catch-up experience (a dedicated veil-wearing surface/sheet with its own premium design that walks you through what you missed; Code has full design latitude, "my jaw dropped" is the bar). Deep-link sub-buttons stay.
4. **"Blake's 44" → "Blake's reviews":** *"Get rid of 'Blakes 44' and replace it with 'Blakes reviews' since 44 won't always be my number of reviews. Across the enter account page."* → audit the whole account page (filter pills, picker group labels, anywhere else); check other visitor-facing surfaces for hardcoded "44" labels while in there.
5. **Status alignment:** *"Line up status with the textbox below it with display name on the left. looks out of place."* → the STATUS kicker + its input misalign with the DISPLAY NAME column (his screenshot). Align the grid.
6. **My Activity round 2:** *"Can we again clean up the My activity page? I want the text to be lined up nicely. have it create more lines."* → his screenshot shows title+snippet crowded right with dead space left and one-line snippets. Design-read with latitude: align the text block, let snippets wrap to more lines, make rows read like a real feed.
7. **"Message Blake" → "DM Blake":** *"Inbox looks great. Lets change it to 'DM Blake' since we will have actual dms in a later update."* → rename across the Inbox card, the thread view, buttons, empty states.
8. **Hover scoping bug:** *"When hovering over the background. The change avatar also appears. it should just be the change background. And when you hover over the avatar it should just be change avatar."* → each pill shows only on its own target's hover.
9. **Accents: gradients broken + go bigger:** *"The graidants don't seem to work. its just one solid color. Make it more noticable. Maybe it goes over the entire edge of the background instead of the avatar profile. let code deciede and impliemnt."* → (a) FIX: gradient accents render as one solid color on the hero (diagnose for real — likely the border/frame can't take a gradient the way it's applied); (b) make the accent treatment more visible — Code decides (e.g. the full hero/card edge, not just the avatar ring). Never gold.
9b. **🌟 FRAME THEMES — a user-pickable profile-edge design system (Blake, 2026-06-10, expanding his line-designs idea):** *"will users get to choose their profile design line changes? I want them to have options to choose from. Like green rainforrest vines wrapping around the edges, moutain rock, purple comsic sky, and at least 10 options. I want code to go all out."* → a **FRAME picker** in the identity studio alongside Accent: **at least 10 distinct edge/frame themes** (his seeds: rainforest vines wrapping the edges · mountain rock · purple cosmic sky — Code invents the rest; full creative latitude, GO ALL OUT). Each theme decorates the profile hero/card edges (account preview AND the live public profile). Constraints: rules-enum + viewer lists extended in lock-step (the accent-key pattern) · **no gold-family theme** · compositor-cheap (SVG/CSS, no giant images, no per-frame JS) + reduced-motion honored if any theme animates · themes render for VIEWERS of public profiles too, scheme-gated like every render sink. Default = the current plain frame.
10. **Rooms beyond the watched set:** *"The Seasons room and discussion on the seconday modal should appear on animes I haven't reviewed as well so people still get a chance to comment."* → this reverses YOUR deliberate watched-gate (your round-3 note said rooms-everywhere needs an explicit non-watched lead-in, not just ungating — Blake is now explicitly asking). Build it properly: a non-watched room needs its own lead-in/empty-state (no Blake review to lead with — Blake's voice in the copy, community purple, no gold), and moderation reach must hold. Flag any real risk in the report.
11. **Demand chip — Blake is NOT smoking it:** *"Make sure code is confidendent it DOES work."* → re-verify the full chain yourself live (request as a member → chip appears/counts → admin Mark-reviewed → gold letter lands → chip retires) and state your confidence + evidence explicitly in the report.
12. **The launcher crashes:** *"I booted up mode 1 and I saw uh a terminal appear for like half a second and thats it."* → `Start Mode 1.bat` flashes and dies. Diagnose for real (working dir? npm not on PATH in that context? instant error?). It must: stay open, survive errors visibly (no silent vanish), and greet plainly ("Mode 1 is running — leave this window open. The admin form will detect it."). Test by double-clicking it the way Blake does, not just from a shell.

## PART B — your pre-cutover wants (all four greenlit; designs are yours in NEXT.md)
- **The gold-flip migration half:** an `anime:al:<id>` Tavern thread migrates to `anime:<slug>` and GAINS the gold verdict rail when Blake reviews it (the admin-page slug plumbing you designed).
- **Season-room ↔ AniList decouple** — now load-bearing given Part A item 10 (rooms must mount even when the detail fetch fails); re-arm the deep-link halo on `.secondary-retry`.
- **The secondary-modal hardening trio:** Tab containment · loadSecondary generation token · the 280ms close-window race.
- **users/{uid} email tightening** (owner-only get; profiles-first reads are in place).
- Anything you can't finish cleanly: list it, don't silently drop it.
- **Housekeeping:** Cowork created `Start Mode 1.ico` in the deploy root (Blake's desktop shortcut points at it — do NOT move or rename it). Add `*.ico` (or the file) to BOTH ignore mirrors (.gitignore + firebase.json), same treatment as `*.bat`.

## Standing rules
Full brand parity BY DEFAULT on every new/touched element — branded buttons, hover states, purple-not-gold, no plain/dim/small text, no native outlines/selects/dialogs. `[hidden]` symmetry. Gold = Blake only; community purple + count-free (Appreciate the one carve-out). Reduced-motion. No provider names visitor-facing.

## Verify
ALL tracks green + new/updated specs (door without Surprise-me, the catch-up surface, gradient accents render, non-watched rooms + their lead-in, the rename, the launcher). 5-lens adversarial — lenses: the non-watched-rooms expansion (moderation reach + heart), the catch-up surface (deep-link spoof/escape), the thread migration (data integrity), the email tightening (nothing breaks author reads), perf. Walk it yourself. Then Blake's numbered smoke.

## Report (lean)
Design vision (1 para) · Part A item-by-item against BLAKE'S numbering (incl. the item-2 door rule in plain language + the item-11 confidence statement) · Part B per item · adversarial findings · checkpoint hash · test counts · Blake's numbered smoke. NO deploy.
