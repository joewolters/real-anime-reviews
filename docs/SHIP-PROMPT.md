<!-- author: Cowork | date: 2026-07-08 -->
# v2.1.0 — 🔴 THE DECEMBER REVIEWS RECOVERY + the post-launch fix list (absorbs the staged v2.0.1). STAGED — no deploy without Blake's word.

> **Mode ULTRAMAX.** Part 0 is data forensics on the CANONICAL master — highest care of the project's life: verify-before-destructive, backup-before-touch, diff-proof everything. Blake: *"I poured a lot of thought into those updates… I want my old reviews back."*

## PART 0 — 🔴 THE EXCEL FORENSICS + RESTORE (do this FIRST, alone, report before the rest)
**What Blake reports (2026-07-08):** *"Back in December 2025. I went through most of my reviews and updated them extensively… now… Most of those reviews have been reverted to what they were before the big update. Whats weird is that the new reviews haven't been touched. Its just December 2025 reviews that got reverted."* He can't find the original sheet in Master List.
**Leads (hypotheses — VERIFY, don't assume):**
1. The master's metadata says **last saved 5/10/2026** while modified 7/4/2026 (your byte-patch) — the content lineage may be a stale pre-December-quality file saved on/before 5/10. If so, later `npm run sync` runs propagated the nerfed text to the site while Mode 1 appended NEW reviews on top (explains "new untouched, December reverted").
2. **Git is the gold mine:** `animeData.js` (and any predecessors) is committed at every ship. Walk its history — find the era with Blake's December texts intact, and the exact commit where texts REGRESSED (that dates the reversion and likely names the operation). The repo is the authoritative recovery source even if every .xlsx is bad.
3. Secondary sources: the 42 `Anime_Master_Table.bak.*.xlsx` files in Master List (diff them chronologically — note the 29KB→88KB jump around 6/03) · the old `All anime reviews .txt` (6/2025 — pre-December, useful as a BEFORE reference) · OneDrive version history / Windows File History on the folder, if reachable · web.archive.org snapshots of the live site as tertiary.
**The restore, disciplined:**
- Back up the CURRENT master untouched first (timestamped copy).
- Build the recovered dataset into a **CANDIDATE file** (never in-place): December-era texts restored for every affected review; post-December additions/edits preserved (the new reviews, the watched-set, statuses, the friendly-rivalry cell). Where December-vs-later conflicts exist on the SAME review, prefer the December-rich text but LIST every such case.
- **Report a per-anime diff table** (review: reverted-text era → recovered-text era, chars before/after) + anything unrecoverable and why + the root-cause finding with evidence + a prevention (e.g. a sync-time tripwire: refuse to sync if total review-text length SHRINKS materially vs the last committed animeData; plus a backup-retention note).
- **STOP after Part 0 and report.** Blake eyeballs the diff table and gives an explicit word before the candidate replaces the master and syncs to the site. His December words are the treasure — nothing overwrites anything until he says so.

## PART 0 — CONTINUATION: THE ARCHIVE RECOVERY (Blake's word, 2026-07-08 — do this before Part A)
<!-- author: Cowork | date: 2026-07-08 -->
**Blake approved the web.archive.org check AND believes far more than 2 reviews reverted:** *"I still feel like im missing a LOT from my reviews. There[']s just no way I didn't have more I remember updating so many more of these."* He confirmed Rascal/Archdemon are exactly what's missing — *"you are correct. This is what im missing. But I feel like theres even more that was missed."*

**Why he's likely right (the git blind spot):** git history begins **2026-04-29**; his big update was **December 2025** (~4 months earlier). Any review trimmed BETWEEN December and late April is invisible to the git forensics — git's earliest snapshot already holds the trimmed text, so it reads as "unchanged" and never flags. The 2 found reverted in **May** (v1.5.0), *after* git started watching. The git pass therefore CANNOT structurally surface a pre-late-April reversion. web.archive.org is the only remaining source for his true-December text.

**Goal — recover every review's richest true-December text; give Blake the complete picture:**
- Enumerate web.archive.org captures of `realanimereviews.com` across **Dec 2025 → Apr 2026** (CDX API, `collapse=digest` for distinct versions). Target `animeData.js` first — it existed pre-v1.5.0 as the hand-pasted data file — and fall back to the rendered pages / any predecessor data file if animeData.js isn't captured that early.
- Extract each review's text from the richest December-era capture. **Compare ALL 44 reviews** — Wayback-December vs current-live vs the git-v1.3.4 baseline. Flag every review whose December text is materially richer than what's live now (not just the original 5).
- Reject June-5 structured-editor junk on sight (the `## Animation dgsdgsdg…` mash / duplicated-fragment signature) — junk length is never "recovery."
- Rebuild the CANDIDATE (never the master) to the richest VERIFIED text per review; expand the per-anime diff table to every newly-found reversion (current chars → recovered chars; source = `wayback:<ts>` or `git:<sha>`).
- If Wayback has no usable capture in that window, say so plainly + list what timestamps DO exist — no papering over a gap.

**Discipline unchanged:** master already backed up + byte-verified; candidate-only; **STOP and report the full expanded diff table; NO sync, NO deploy until Blake's explicit word.** The restore-set decision (the 5 git-found + whatever Wayback adds) waits on this pass. His December words are the treasure.

## PART A — the fix list (his words)
1. **Door quotes on mobile: GONE.** *"Remove quotes for mobile loading page entirely not enough room."* (Supersedes the top-band fade — kill them at phone widths; desktop unchanged.)
2. **Profile reviews rework (supersedes the staged v2.0.1 item 2):** *"Users can pin one review and other users now have to click a button that brings up a separate sheet of all the reviews a user has made so it doesn't take up the entire profile."* → profile shows the pinned review + a branded "All reviews (N)" button opening a separate sheet/layer with the full list. Deep-link + Back behave.
3. **Catch-up strip vs the update log (his photo):** *"needs to not overlap with the update log. Maybe it only shows the last 2 on the watchlist and a '+ 4 more'"* → cap the strip's expansion (2 titles + "+N more" → the sheet), and fix the layering so it NEVER paints over the update log at any width.
4. 🔴 **Safari + Arc on iPhone are broken:** *"do not show the improved mobile work. It looks bloated and not optimized… Especially in the deeper modal pages. Like account and stuff. I want code to verify every single possibility."* → the responsive work was verified in Chromium/Firefox; **WebKit was the gap.** Run the responsive suite under Playwright's WebKit engine + iOS viewports/UA; audit the usual WebKit traps (dvh/svh, -webkit-fill-available, safe-area insets, position:fixed + keyboard, overscroll, backdrop-filter cost); fix account + all modal layers at phone widths; pin WebKit specs so this can't regress. Evidence: screenshots per surface at iPhone sizes, WebKit engine.
5. 🔴 **Legacy members can't finish their profile:** *"Some users are having trouble conforming their profile if they were a previous member. I straight up can't get one of them to work."* → diagnose on prod-shaped data: pre-profile-era accounts (the backfill minted 7 — something still blocks at least one member). Suspects to check, not assume: reserved/duplicate display-name rules, consent-gate state, users-doc shape drift, the backfilled doc missing a field the Studio save requires. Fix for ALL legacy members + a rules/CF test for the legacy shape + tell Blake what that one user should do (or what got fixed server-side).
6. **NEW — admin member-stats page:** *"track member stats included joined this month, active users, comments, reviews posted etc etc"* → an admin page (branded, no native anything): joined-this-month, actives (define honestly — e.g. lantern/门 reads or writes in last 30d), totals + trends for comments/reviews/threads/DMs volume (counts only, no content), CF-aggregated (no client full-collection scans). Code designs the cheap aggregation.
7. **NEW — self-serve account deletion:** *"New way to delete your account in user settings thats available to all members"* → Settings → a branded, multi-confirm delete flow (type-to-confirm; friendly-errors; recent-login reauth per Auth requirements) → an `onUserDelete`-style cascade (profile, saves, shelves, consent, memberships; AUTHORED CONTENT policy: propose tombstone-vs-delete for comments/reviews/threads with a recommendation — thread integrity vs true erasure — Blake picks at report). Rules + CF + tests. The admin account is undeletable.
8. **The absorbed v2.0.1 leftovers:** the profile-editor panel rename (still titled "YOUR CONSTELLATION" — collision with the star map; propose a name) + the 4 native-open dropdowns → brandSelect. (Item 2 above replaces the old pinned-only spec.)

## Constraints (unchanged)
Gold = Blake only · community purple + count-free (Appreciate carve-out) · brand parity everywhere incl. admin · no provider names visitor-facing · friendly-errors · `[hidden]` symmetry · reduced-motion · escape-first sinks · Excel canonical (Part 0 is exactly why) · commits Blake-authored, zero trailers · bump TARGETS grow with the new admin page · **STAGED — Part 0's restore AND any deploy each wait on Blake's explicit word.**

## Report
**Part 0 first and alone:** root cause w/ evidence · the recovery-source map · the per-anime diff table · unrecoverables · the prevention tripwire · then WAIT. After his word: the rest — item-by-item on his numbering (WebKit evidence per surface, the legacy-member root cause, the deletion-policy question) · panel findings (lenses: the restore integrity, the deletion cascade, the stats-page privacy, WebKit) · test counts · his short smoke · deploy plan awaiting his go.
