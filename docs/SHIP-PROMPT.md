<!-- author: Cowork | date: 2026-06-10 -->
# v1.10.0 — GATE 20: round-4 smoke fixes + welcome-door catch-up + THE CHERRIES. APPLY, practice verify, NO deploy.

> **Mode ULTRAMAX, full latitude.** Blake: *"It can take its time. I want everything to look good."* Re-read `docs/v1.10.0-DESIGN-STUDY.md` §6 before building — the catch-up + cherries spec lives there and nowhere else. 5-lens adversarial review. Checkpoint + push. STAGED — no deploy. After this gate, only gate 21 (THE CUTOVER) remains.

## PART A — Blake's round-4 smoke list (10 items, his words — every one lands)

1. **Welcome message signed-out:** *"When not signed in the welcome message doesn't appear. Needs to appear signed in or not."* Diagnose in a real browser, don't assume the cause.
2. **Redundant pickers:** *"Get rid of the Avatar and Background from the large menu now that you can change both from the top."* (The hover pills on the hero are now the one way.)
3. 🔴 **brandSelect collapse bug (his items 3+4+5 — one root cause class, fix it everywhere):** the pinned-review dropdown *"doesn't ever collapse. It came automatically dropped down"*; same on Recently-added sort (*"I can't collapse it"*); same on Private/Public when creating a collection. Every brandSelect must render CLOSED, open on click, close on pick/Esc/outside-click. Audit ALL of them; spec-pin closed-by-default.
4. **Subtext copy pass:** *"Change the description to something more basic. Sounds creepy right now. this applies to all sections in the account page."* Rewrite every account-section subtext pill plainer (e.g. Watchlist's "no judgment on the backlog" tone → simple and friendly). Blake-voiced, not creepy-intimate.
5. **Collections picker — wider pool + real search (item 6):** *"Right now when adding to a collection users can only add anime iv watched. Add the search function to work with anime synced from mal thats already built into other places in the website. The starting list should also include anime that they've already added to their favorites or watchlist not just my reviewed list."* → wire the existing external-search infra (the Discover/live-search AniList machinery) into the add-picker; default list = their watchlist + favorites + the 44. No provider names in visitor copy.
6. **Collection descriptions (item 7):** *"People should be able to add a description to their collections that appears under their title that people can read."* → optional description field (sane cap, escape-first, rules-validated), shown under the shelf title on the account page AND the public profile.
7. **My Activity cleanup (item 8):** *"My activity needs to be cleaned up. Looks really unofficial right now especially with how images are said."* Raw markdown (`**bold**`) and `[img:1]` tokens render literally in activity rows — strip/render formatting, replace image tokens with something clean (e.g. a small 🖼 chip), and give the whole surface a polish pass.
8. 🔴 **RECURRING — Message Blake UI (4th ask):** *"Once again. The message Blake option UI looks unplolished and unfinished. This is now the 4th time iv asked it to be cleaned up."* The "Message Blake" button renders as a bare native-looking button inside the Inbox card. Full premium pass on the Message-Blake card + the conversation view. **PROVE IT with a real-pixel screenshot in the report** — same standard as the dim headings; a 4th claim-fixed-without-proof is not acceptable.
9. **Season room placement — Code misread item 9 last round:** *"code misunderstood what I was asking from the 'this seasons room'. Lets make it easier by adding it to the bottom of the seconday modal. After the links and before the more like this section. It can be pretty wide to the fit the seconday modal."* (He circled the spot in a screenshot: the empty band under LINKS, above MORE LIKE THIS.) → move the room OUT of the left rail to a full-width section there. H5 stays intact (Blake's review untouched up top). Keep the 900–1149px / stacked behaviors sensible.

## PART B — Gate 20 proper (the study §6 is the spec)

- **Welcome-door catch-up buttons:** returning signed-in users get door buttons that catch them up (watchlist updates, a new season on a favorite, etc. — the signal set is Code's to ideate per the study), each expanding into deep-link sub-buttons. Signed-out door still works (Part A item 1).
- **The cherries:** "👁 N requested" chips (suggestionCounts exists) · the **gold-flip** (when Blake reviews a requested/non-44-thread anime: requesters get the Lantern ping; an `anime:al:<id>` thread migrates to `anime:<slug>` and GAINS the gold verdict rail) · the **About-Me/Credits/Contact footer premium pass** (restyle the containers — kicker vocabulary, scroll-reveal "most likely"; do NOT rewrite Blake's copy without asking) · the **Mode-1 desktop launcher** (double-click `.bat`/shortcut, no terminal).
- ⚠️ **"Surprise me" is CUT from this gate — it already exists.** The nav's **Random ランダム** dice button IS this feature (Blake caught it, 2026-06-10: *"uh. that already exists. Its literally called 'Random' with a dice."*). Do NOT build a duplicate, do NOT remove or rework the existing Random. Its ENHANCEMENT (Blake: *"Random anime should be somewhat random. Most of the time its an anime I haven't reviewed from anilist. Sometimes it is one I reviewed. Maybe provide a mini filter…"*) stays BANKED for the future Discover ship (NEXT.md) — not this gate.
- **The imageRefs edit-strip sweeper** (the one pre-prod gap): `onDocumentUpdated` diff-sweep over `imageRefs` on the 5 surfaces (forum thread/post, comment, review, reply), mirroring `onProfileWritten`/`bgSweepDecision`.
- **Your own pre-cutover wants** (NEXT.md "Pre-cutover work I want"): fold in what fits cleanly (season-room AniList decoupling, the secondary-modal hardening trio, orphan reaper, admin text-report image preview); anything that doesn't fit, list for a 20.5 — don't silently drop.

## Standing rules
Every new element at full brand parity BY DEFAULT — branded buttons, hover states, purple-not-gold, no plain/dim/small text, no native outlines/selects/dialogs. `[hidden]` symmetry. Gold = Blake only; community purple + count-free (Appreciate is the one carve-out). Reduced-motion on new animation. No provider names visitor-facing.

## Verify
ALL tracks green + new specs (brandSelect closed-by-default, the activity render, collection description rules, the room's new placement + H5, catch-up buttons). 5-lens adversarial review — lenses: the catch-up deep-links (spoof/escape), the gold-flip migration (heart + data), collections description XSS, the sweeper (don't delete live refs), perf. **Real-pixel proof for item 8 (Message Blake).** Walk it yourself. Then Blake's numbered smoke.

## Report (lean)
Design vision (1 para) · Part A item-by-item against BLAKE'S numbering (which of his 10 passed → fixed how) · Part B per cherry · the sweeper · adversarial findings · what got deferred to 20.5 (if anything) · checkpoint hash · test counts · Blake's numbered smoke. NO deploy.
