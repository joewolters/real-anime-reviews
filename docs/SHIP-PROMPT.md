<!-- author: Cowork | date: 2026-06-11 -->
# v1.10.0 — GATE 20.6: final polish round before THE CUTOVER. APPLY, practice verify, NO deploy.

> Blake's 20.5 smoke: frames "look amazing" · activity "finally perfect" · Mode 1 works · Den-Keeper lock verified (Mika can't see it — PASS). What's left is this list, then the cutover. **Mode ULTRAMAX, full latitude** — his bar stands: "I want everything to look good." 5-lens adversarial review. Checkpoint + push. STAGED — no deploy.

## PART A — Blake's 20.5 smoke fixes (his words)

1. **Background cropper: preview accuracy + brightness parity:** *"When framing a background with a picture from your pc. The preview is incorrect from what actually appears. Users should be able to pick and choose more accurately. Also the background is dimmer from the picture thats chosen. The brightness should be the same."* → (a) the FRAME-IT preview must match the real rendered hero crop (diagnose the aspect/viewport mismatch for real); (b) the rendered background must keep the source's brightness — if a readability scrim is dimming it, redesign so text stays legible WITHOUT dimming the whole image (e.g. localized gradient under the text only). Code's design call, but "brightness should be the same" is the spec.
2. **Gradients round 3:** *"Gradients kinda work. Only the purple one is actually noticeable as a gradient. the others need more obvious changes."* → every gradient accent must read OBVIOUSLY as two colors at a glance (stronger stop contrast / angle / spread — Code decides). Verify each of the 6 visually, not just Nebula.
3. **Room copy, exact swap:** *"Just rewrite the description as 'I haven't reviewed this one (yet). Feel free to discuss anyway.' Thats it"* → use his words verbatim. (Keep the spoiler reminder only if it fits naturally elsewhere — the lead-in line is his sentence, nothing more.)
4. **Catch-up sheet — diagnose, verify, explain:** *"I don't see the catch up sheet anywhere after I signed out and opened a new sheet."* → Cowork's read: he was signed OUT at the door, and the strip is signed-in-only — but PROVE the signed-in path: fresh session + signed-in returning user reliably shows the strip (the warm-auth race was a 20.5 MED — re-verify it's dead). Report the show/hide rule in one plain sentence Blake can sign off on, and make sure the strip's absence for signed-out visitors is the designed behavior, not a regression.
5. 🔴 **Request-an-anime 404:** *"The moment I went to request an anime a not found appeared. Hoping code thinks it work ."* — his screenshot: `127.0.0.1:8765/suggest?title=…&anilistId=182483` → `not found: /suggest`. → the deep-dive request flow routes to an extensionless `/suggest` URL the practice static server doesn't resolve. Fix it (serve clean URLs in practice AND/OR emit the working URL) and **verify the route works in BOTH practice and the prod hosting config** — if prod's rewrite/cleanUrl behavior differs, this is a cutover-blocker class bug. Then re-run the chip chain end-to-end through the REAL request page flow.
6. **Consent must come to the user:** *"I went to go save my new ifno and I can't unless I go comment somewhere else on the website which is DUMB. An 'I agree with the rules' should appear after I request to save changes."* → wire the shared consent modal (`ensureConsent` — the ONE implementation) directly into the profile/account Save path (and any other gated write that today just shows a banner): attempt to save → consent modal appears in place → agree → the save completes without re-clicking. Audit every consent-gated surface for the same dead-end.
7. **Update-log widget:** *"Change Big Update to 'Major Update'. let code do any other touch ups it sees fit to the update log."* → rename the tier label + Code's latitude on widget polish (per `widget-update-skill.md` visitor-first rules).
8. **The backgrounds fine-print line:** *"Clean up the 'Backgrounds up to 5Mb...' into something easier to read that looks offioal."* → restyle/rewrite the requirements line (branded, scannable — not a comma-run of fine print).
9. **OPTIONAL latitude — frame personality:** *"The frames look amazing. Wish some had more personality but overall its amazing. Most notable shadow garden."* → Code MAY enrich the flatter frames toward Shadow-Garden-level character (compositor-cheap, reduced-motion, no gold in user themes). Don't destabilize the gate for it.

## Standing rules
Full brand parity on every touched element · `[hidden]` symmetry · gold = Blake only, community purple + count-free (Appreciate carve-out) · reduced-motion · no provider names visitor-facing · no native dialogs.

## Verify
ALL tracks green + specs updated (cropper parity, gradient readability where pinnable, the consent-on-save flow, the /suggest route in both environments). 5-lens adversarial — lenses: the consent-on-save wiring (no gate bypass), the /suggest fix (prod parity), the brightness change (text contrast/a11y), the cropper math, perf. Walk it yourself. Then Blake's numbered smoke.

## Report (lean)
Design vision (1 para) · Part A item-by-item against BLAKE'S numbering (incl. the item-4 one-sentence rule + the item-5 both-environments proof) · adversarial findings · checkpoint hash · test counts · Blake's numbered smoke. NO deploy.
