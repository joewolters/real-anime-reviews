<!-- author: Code | date: 2026-06-12 -->
<!-- AUDIENCE: FOR CODE (the next Claude Code instance). NOT for Cowork. Cowork reads docs/HANDOFF.md + docs/SHIP-OUTPUT.md and writes docs/SHIP-PROMPT.md. -->
<!-- Kept LEAN on purpose (the old 73k version was itself a context-budget cost). Deep history lives in CHANGELOG.md + git. -->

# Code → Code Handoff (v1.10.2 LIVE · THE MEGA-RUN IS UNDERWAY — Milestones A+B SHIPPED to the sandbox, MILESTONE C IN PROGRESS)

## ⚡ READ-FIRST
**GLOBAL READ DISCIPLINE (obey this — it's why this block exists; keep YOUR context low like this file does):** Read ONLY the ⚡ READ-FIRST block of this file, of `docs/NEXT.md` (its **`💭 Code's wants — CURRENT` block** = the plans only Code knows — read THAT in full), and of `ROADMAP.md`. Read `docs/SHIP-PROMPT.md` (**Cowork has ALREADY re-staged it and it is READY TO READ — it holds Blake's directive + his locked picks; read it NOW**) and `docs/SHIP-OUTPUT.md` (my last milestone report to Blake) IN FULL. **🔑🔑 THE LOAD-BEARING CONTEXT IS `docs/MEGA-RUN-DESIGN-STUDY.md` — READ IT IN FULL BEFORE ANY C/D/E GATE. It IS the gate-0 study for this whole run** (the corrected safety truth, the MEASURED responsive-breakage table you'll need for D, the full gate map, and the per-surface heart specs). Do NOT scope a milestone without it — that context is exactly what keeps the ship coherent. The v1.10.0 study (`docs/v1.10.0-DESIGN-STUDY.md`) is the FORM it follows (§3 spine · §4-5 heart · §6 gates). Per-gate build history = `docs/v1.10.0-GATE-LOG.md` (jump-if-stuck; the A/B/C-so-far detail lives there, NOT here — read a specific entry, NEVER linearly). Do NOT open other `docs/` files unless your task needs that topic.
**MANNERISMS + WORKFLOW (CLAUDE.md auto-loads the full rules; the essentials):** Blake reads your chat DIRECTLY → be **terse** (a headline + ~5-8 bullets; the one-liner is ONE sentence; full detail lives in `SHIP-OUTPUT.md`, not chat). **Surgical edits over rewrites. Honest "I'm not sure" over fabrication.** He's a self-described "very basic" coder — explicit which-file/which-line guidance. He grants creative latitude often → build your recommended option AND surface 2-3 alternatives. **Request your MODE at the TOP of your first reply** (High/Max/ULTRAMAX=Workflow/multi-agent — only when ultracode is on or Blake says so; ULTRAMAX for broad/visitor-facing/security work — **a gate-0 design study and any deploy are ULTRAMAX**, Max for surgical gates/hotfixes). **Verify-don't-trust — including the SHIP-PROMPT's own diagnosis AND your predecessor's claims:** re-derive root causes before building prescribed fixes (gate 20.5 found "the demand chip works" was unverifiable because no client EVER wrote submitterUid — three sessions had assumed it). **Visual claims need SAMPLED PIXELS, not computed style** — the 3×-recurring dim-headings bug had computed color #f6efff while the glyphs PAINTED dark (a scrim above static text); the gradient accents "worked" by computed values while rendering solid. Screenshot-crop + luminance-sample (sharp lives in functions/node_modules), and test launchers/flows the way BLAKE does (double-click, not shell). Watch energy/fatigue cues ("what's next?" late = rest signal). Workflow per batch: recon (parallel Explore agents → tight maps) → build (SOLO for shared files script.js/account.js/style.css — parallel agents collide; delegate only SELF-CONTAINED lanes: functions/, admin/, rules+rules-tests, or NEW standalone files like cropper.js/frames.css, each with a locked API/key contract) → checkpoint commit per gate (`--author="Blake Wolters <196413457+joewolters@users.noreply.github.com>"`, message via `-F` file, ZERO trailers — verify `git log -1 --format=%B | Select-String 'co-authored-by|🤖|claude|generated with'` count 0; `git restore --staged` the 7 Cowork excludes) → **5-lens adversarial Workflow with a REFUTE-VERIFY stage** (find → per-finding skeptics; its record: a real HIGH or better EVERY round, 0 refuted across the last 3 panels — never skip, and FIX or formally accept every confirmed finding) → re-run EVERY affected track → SHIP-OUTPUT (overwrite) + GATE-LOG (append) + THIS line → terse chat reply with Blake's numbered smoke.

**WHERE WE ARE RIGHT NOW (2026-07-02) — keep this LEAN + current; per-gate detail lives in `docs/v1.10.0-GATE-LOG.md`, NOT here:**
**LIVE in prod: v1.10.2** (`realanimereviews.com`, curl-verified APP_VERSION 1.10.2). The Community Hub + the Creator member-parity profile + the curator/prod-bug fixes are all live. (Full history: CHANGELOG + gate-log.)
**THE MEGA-RUN (2026-07-02, Blake's "let it go nuts / one run, one smoke"):** ONE goal-oriented run building the whole remaining roadmap → **ONE mega-cutover** (everything sandbox-staged; **NOTHING deploys until a single explicit Blake go**) → **ONE final comprehensive smoke** (he does NOT smoke per milestone — self-verify at FULL discipline: tracks + pixels + walks + the adversarial panel + foolproof seeds). Blake's locked picks + the door-line copy-approval are in SHIP-PROMPT. The plan = `docs/MEGA-RUN-DESIGN-STUDY.md` (gate map A0→E, per-surface heart specs, the measured responsive table).
- **✅ MILESTONE A DONE + COMMITTED + PUSHED** (gates A0–A5): the messaging era — peer DMs (request-first, decline-silent, blocks), groups (cap 15, solo-birth + append-only adds), images sealed-until-accept (server-enforced via storage cross-read), the unified **Letter Room** inbox (`account.js initInbox`), + the adversarial panel (3 HIGH + 1 MED fixed). ⚠️ **The A1 GATE-LOG entry holds the peer-DM/group FIELD CONTRACT** (kind/state literals, `dm-<convId>` imgRef scheme, `dmreq_<creator>` ping, conv-mute key) — read it before touching messaging.
- **✅ MILESTONE B DONE + COMMITTED + PUSHED** (curator tools): animeStatus (public) + curatorNotes (private) rules; gold card-state labels (⚠️ the label is `<div>`+`<i>` NOT `<span>` — dodges the `.card .info span` + Top-10 spotlight cascade the panel caught; g31 has a COMPUTED-PIXEL pin); `/admin/curation.html`; info-requests `kind:'info'`. Commit `e050e7c`.
- **⏳ MILESTONE C IN PROGRESS — THIS IS WHERE YOU PICK UP. Its code is UNCOMMITTED in the working tree (verify with `git status`); commit it FIRST after a green re-check.**
  - **C1 (item 8) DONE + walked + g31-pinned:** unreviewed anime open the FIRST modal in **yellow-tape mode** — a synthetic `__unreviewed` anime (`script.js openModal` branch: `.unreviewed-tape`, gold NOWHERE, no Blake rating/review) with the community column fully alive keyed `al:<id>`. The retrofit: `reviewKey(anime)` = `__communityKey || communityKey` — behaviour-preserving for the 44 catalog rooms (spec-pinned; 193 community/modal specs stayed green). Entry = the **`data-commreview` "★ Community reviews" door** on the secondary deep-dive (builds the synthetic + `openModal`, closes secondary first — z-order).
  - **C2 (item 9) DONE + specs-green:** **Hidden Gems** home strip (`franchise-fetch.js` new `HIDDEN_GEMS_QUERY` + `popularity` field; `fetchHiddenGemsCached` 24h; `fillHomeGems`; `#home-gems-block` hidden-until-real). **Random mini-filter** = `#random-btn` now honors the existing `readReviewedSeg()` ('reviewed'→his 44 · 'notyet'→unreviewed AniList→secondary · 'all'→~70% mostly-unreviewed). ⚠️ The AniList discovery canary (`scripts/anilist-discovery-canary.js`, id 101922) should gate the new query shape — RUN IT (needs network) before the cutover.
  - **❌ C3 (item 10 — Constellation Wrapped) NOT STARTED.** A delegated agent was KILLED mid-build (context ran out) — it landed NOTHING (no wrapped.js/css, account.* untouched). **Build it next.** Design + exact data paths are in the MEGA-RUN study §4e + the recon in the gate-log; the agent brief that was in flight: a self-contained `wrapped.js`/`wrapped.css` + account.html button + account.js import, purple star-map, ONE gold star (join day), TRUTHFUL stats only (reviews/comments/threads/saves/member-since — NOT watch-time/votes/first-saved, which aren't reliably queryable).
  - **⏳ MILESTONE D (the v2.0 RESPONSIVE OVERHAUL) STUBBED ONLY.** I added the `#nav-drawer-toggle` hamburger + `#main-toolbar` id to `index.html` as a head-start, and **hid the toggle by default** (`.nav-drawer-toggle{display:none}` in style.css — see the comment there) so nothing renders broken. **D IS NOT BUILT.** The plan (MEGA-RUN study §4b + the measured breakage table): a left off-canvas nav DRAWER for ≤1200 (turn `.toolbar` off-canvas, `body.nav-open .toolbar → translateX(0)`, + scrim + toggle JS), compact the ≥1201 header so it never wraps (it wraps at 1440 TODAY), REAL card-grid breakpoints for the 901–1200 band (cards CLIP off-screen there — `.card-container{repeat(4,275px)}` has no breakpoint 901–1200; `overflow-x:clip` hides it), and PROMOTE the `(hover:none)` touch fixes OUT of mobile.css (which only loads ≤900, so iPad-landscape has no touch fallback). Verify at 1440/1280/1201/1024/900/768/430/375 with SAMPLED screenshots + zero horizontal scroll. **Treat D with cutover seriousness — it's the milestone Blake literally asked for ("nav tabs got scrunched on a laptop… make it work on smaller laptops and phones… maybe a sidebar").**
  - **MILESTONE E (last): UI polish · speed round 2 (MEASURED levers only, Firefox Profiler is arbiter — 2 dead multi-MB PNGs + oversized catalog PNGs are the cheap wins) · the riders** (profiles backfill→users-GET tightening · the anon `#secondary=` route · the al: e2e stub · optional "who-liked" history).
- **Floors (never drop): `npm test` 243 · `test:rules` 198 · `test:functions` 77 · `test:cf` 76 · e2e 20.** After committing C, run the FULL suite once (C touched shared script.js/franchise-fetch/index/style — I only ran targeted specs: 193 community/modal + 25 discovery/nav/g31, all green).
⚠️ Re-read SHIP-PROMPT.md before closing any milestone (Cowork re-stages it mid-flight — the v1.10.2R Appreciate-reversal lesson).
**Sandbox at handoff:** practice status is in the session-close note below; restart it detached if dead (the exact command below). Cowork's own doc edits (HANDOFF/SHIP-PROMPT/AI-PRIMER/CODE-PROMPTS/SKILLS/*) stay uncommitted for Cowork — ⚠️ their AI-PRIMER/HANDOFF working-tree copies carry stale version claims (flagged to Cowork in SHIP-OUTPUT).
- **Per-gate build history (gates 1-18, every adversarial finding, every test-count move) lives in `docs/v1.10.0-GATE-LOG.md` — newest entries at the bottom. It was deliberately ARCHIVED out of this block (the old inline history was a ~10k-token context leak). Open a specific entry only when a task touches that gate's machinery.**
📌 **PER-GATE DISCIPLINE (do FIRST when wrapping a gate, before the chat summary):** write the report to BOTH `docs/SHIP-OUTPUT.md` (rolling — Cowork digests it, overwritten next gate) AND append `docs/v1.10.0-GATE-LOG.md` (durable). Then update THIS line.
⚠️ **OPS TRAPS (all root-caused this push — they WILL bite again):** Emulator tracks need **JDK 21** (`C:\Users\Owner\jdk-21.0.11+10` → set `JAVA_HOME`) + a port sweep FIRST (8080/5001/9099/8765/**9199** — kill stale listeners by PID). Capture emulator output to a FILE then read it (NOT `| grep` — SIGPIPE strands the emulator on 8080). **Never run `npm run practice` inside a session background task** — the harness REAPS it; launch DETACHED: `Start-Process cmd -WorkingDirectory "<Current Version>" -ArgumentList '/c','set JAVA_HOME=C:\Users\Owner\jdk-21.0.11+10&& npm run practice > practice.log 2>&1'` then WAIT for "PRACTICE MODE READY" in practice.log. **`npm test`'s webServer and practice BOTH bind 8765 — never together.** Flake classes (re-run ISOLATED/warm before believing a red): `gate6-veil` under full parallelism · the review-halo e2e on a cold practice boot (the documented race, 3× now) · cf-cascade's heavy waits (default bumped to 45s — the suite runs 34 functions; verify "Beginning execution" counts ~465/run if triggers seem dead). **Run cf-tests ONLY via `npm run test:cf`** — a raw `npx firebase emulators:exec` without the script's project context delivers ZERO triggers (every wait times out; cost me an hour). **PowerShell string-replace NEVER writes JS string literals** — a `.Replace("…Blake''s…")` leaked the PS quote-escape into account.js and broke the page at runtime while `node --check` LOOKED green (use the Edit tool for code). **Test Blake-facing launchers/flows the way HE runs them** (`Start-Process` double-click semantics — the .bat died from bare-LF endings the Write tool produces; batch files need CRLF). The `[hidden]`-vs-author-`display` trap has THREE scalps (consent banner, inbox dot, brandSelect menus) — every new display:flex/grid component ships its `[hidden]{display:none}` twin. Bare ELEMENT selectors leak (the `header` scrim dimmed every nested panel head for 3 rounds) — scope to `body > header`-style. `tmp*` is structurally ignored in BOTH mirrors (.gitignore + firebase.json) — keep throwaway diagnostics named `tmp-*` and they can never leak. `Measure-Object -Line` skips blank lines; Storage rules cross-service reads don't support `.get(key,default)` (use `in`-guards).
**Test floors (never drop):** `npm test` **229** (g30 ×4 — the v1.10.2R prod-bug pins) · `test:rules` **157** (the like-on-Blake reversal, tested both ways) · `test:functions` **77** · `test:cf` **67** (`functions/cf-tests/cf-profile.spec.js` is IN the `test:cf` script — keep it) · emulator e2e `npm run test:e2e` **16** (incl. the REAL Message-Blake send — it guards the addDoc class — the consent-on-save flow + the sign-in catch-up strip (both self-resetting via emulator REST), the gate-20.8 composer-chip and layered-routing specs, and the v1.10.2 Creator-sheet pair) (needs `npm run practice`; incl. the real-pixel inline-image + thumbnail + lightbox checks). `test:rules`/`test:cf` need the **Storage emulator** (`--only ...,storage`, port 9199) + both run `--test-concurrency=1` (the rules-test files share one emulator and would wipe each other's seeds otherwise). Everything under this line is jump-if-stuck reference, NOT onboarding reading.

> ⛔ DEEP REFERENCE BELOW — do NOT read top-to-bottom. Open a section ONLY if you're stuck on that specific thing.

### Jump-to (only if stuck)
- **⚠️ READ FIRST — the project is in a SUBFOLDER** — open if you need the real project path / where to run `git`/`npm`/`firebase`.
- **⚠️ YOU ARE HERE (2026-06-06)** — open if you need the exact v1.9.0 build state, what's committed vs staged, test floors, the gate map.
- **The workflow (12-gate model + rolling-docs trio)** — open if you need how the rolling docs + gate numbering + orchestration work.
- **Mannerisms** — open if you need how to talk to Blake, the MODE PROTOCOL, and verify-before-done habits.
- **⚠️ CONTEXT BUDGET (NEW 2026-06-06 — Blake hits ~94% in ~6 prompts; keep it lean)** — open if you need to cut context cost (MCP schemas, Workflow bundles, big-file reads).
- **Commit discipline (CRITICAL)** — open if you're about to commit/push (author string, forbidden trailers, the 7 Cowork excludes, checkpoint strategy).
- **Traps that bite (verify the real state)** — open if something behaves oddly (globals, emulator/Java, test-run SIGPIPE, smoke flakes).
- **💭 Code's creative wants (hand forward — plans only I know are in NEXT.md "plans only Code knows")** — open if you need the design intent / cutover stakes / protect-the-heart through-line.

> Quick-onboard so a fresh Code picks up immediately. **Read order:** this file → `docs/SHIP-PROMPT.md` (the staged gate — Cowork already updated it, ready to read NOW) → `docs/SHIP-OUTPUT.md` (what I last did) → `docs/DATA-MODEL.md` (the v1.9.0 Firestore contract) → `docs/NEXT.md` ("plans only Code knows" section). `CLAUDE.md` auto-loads. Then recon the REAL files before trusting any claim.

## ⚠️ READ FIRST — the project is in a SUBFOLDER
Harness CWD is the PARENT (`C:\Users\Owner\PROJECTS\Real Anime Reviews\`). The actual project (`.git`, `CLAUDE.md`, all app files, `docs/`) is in **`Current Version/`**. Prefix every path or use absolute paths. Run all `git`/`npm`/`firebase` from inside `Current Version/`. `Master List/` (the canonical Excel) is a SIBLING, outside the deploy root.

## ⚠️ YOU ARE HERE (2026-06-08) — v1.9.0 SHIPPED
**🎉 v1.9.0 "Community Overhaul" is LIVE in prod** (`realanimereviews.com`, `APP_VERSION 1.9.0`, shipped 2026-06-08). The cutover ran clean (hosting→firestore→functions; all 13 gen-2 CFs live; the audit's `docs/**` leak scrubbed — internal docs now 404). Full record: `docs/SHIP-OUTPUT.md`. **Commits on `main`, Blake-authored, zero trailers: `a3750c8` checkpoint · `95bf321` bump · `4a68b3c` widget fix; the 7 Cowork excludes + `AUDIT_*.md` stay uncommitted by design.** ⚠️ The per-gate bullets below are now the **BUILD HISTORY** of the live system (a useful architecture map) — any "NOT committed / NOTHING deployed / awaiting cutover / STAGED" wording in them is HISTORICAL; it all shipped. The post-cutover queue is at the end of this section + in `docs/NEXT.md`.

- **Committed:** P0→Gate 3 = the **first checkpoint commit `8ff8551`** on `main` (pushed; Blake-authored, zero trailers). This is the CF + rules infra.
- **Built + verified, NOT committed, on top of `8ff8551`:** **Gates 4 / 4b / 4c / 4d CLOSED** (Blake smoked all 10) + **Gate 5 (community reviews) built.** 4b=reply voting (`onReplyVote`)+preflight+doc split · 4c=preflight firebase-detect keys on OUTPUT not exit-code · 4d=practice launcher is now a node-spawn of firebase (NO npm `&&` chain — it silent-died under cmd) with a loud exit handler. **Gate 5:** review bodies→`renderMarkdown` (XSS-safe), Helpful/Not-helpful, **review + thread votes migrated to the new model** (new `onThreadVote` CF, counts-only), Most-helpful/Newest sort + ratings filter, rating histogram (purple + 1 gold tick) with Blake's gold rating PRIMARY above it (+ fixed the legacy GOLD community-avg violation), comment Replies toggle now shows "💬 N replies", seeded reviews. **Gates 5 + 6 CLOSED-build.** **Gate 6 (the Lantern + cutover prep):** bell→Lantern glyph + `@keyframes lantern-ember` + a veil notification CENTER (per-type rows, vote rollup, `lastSeenAt`-based unread in `notifPrefs/prefs`, one-write mark-all-read, per-type mute, Blake-origin gold + sort-first/H4, `window.lanternModel` exposed); **`official` votes migrated** (new `onOfficialVote`, last old path closed); locks (no-ping-on-Not-helpful · verb "found your review helpful"); community gold rating MOVED to the center modal; signed-out Discussion CTA opens the modal. **⚠️ The Lantern was built by a delegated sub-agent (verified by tests + asset checks; needs Blake's in-browser eyes); it also guarded a legacy bell block in `account.js`.** **CUTOVER RUNBOOK written: `docs/CUTOVER-RUNBOOK.md`.** **Gate 6c (Lantern fix round) done:** bigger non-clipped glyph, "My Account"→head/avatar icon (`#auth-open` `.is-avatar`), rollup drill-down (`lantern-vote-item` who-liked from loaded notifs — complete history is a post-cutover query), "← Notifications" back chip, deep-link scroll+highlight (`parseNotifTarget`/`scrollHighlightNotif`/`.rar-deeplink-flash`), community pill purple glow, review Helpful/Not-helpful → thumbs SVG icons. DEFERRED to **6b**: the "go all out" review-composer redesign + Ctrl+B/I + Enter-posts. Awaiting Blake's 6c re-smoke → the CUTOVER (his "ship it") or 6b.
- **✅ ALL of v1.9.0 is LIVE (cutover done 2026-06-08):** the new `firestore.rules` (deny client count/notif writes; `notifications create:if false`), all 13 Cloud Functions, and the new client shipped together in the runbook order (hosting→firestore→functions — that order made double-count corruption impossible). The `firestore.rules` do-not-deploy banner inside the file is now stale (it deployed). **Carried known bug:** review-targeted deep-links don't reliably scroll-to-highlight the exact review (comment+reply work) — first post-cutover fix.
- **Tests (the floors):** `npm test` (Playwright) **97** · `npm run test:rules` (emulator) **49** · `npm run test:functions` **21** · `npm run test:cf` **15**. Floors never drop. (gate 6 raised rules 47→49 / cf 13→15; gate 6c raised Playwright 92→93; the 2026-06-07 audit raised Playwright 93→94 — Lantern gold-gating; gate 6d raised 94→95 — the header-rail spec; gate 6e raised 95→96 — the account-lantern-load spec; gate 6f raised 96→97 — the cross-page deep-link carry spec; gate 6g kept `npm test` at 97 but added a SEPARATE emulator e2e track `npm run test:e2e` = 3 deep-link LANDING specs against the seeded practice site, run via `playwright.emu.config.js` — needs `npm run practice` up first; NOT part of `npm test`.)
- **Tooling:** JDK 21 is permanent at `C:\Users\Owner\jdk-21.0.11+10` (emulator needs Java 21+). `ping` CF is LIVE (Blaze is on). `npm run practice` = the emulator smoke harness (below).

### What v1.9.0 has built (function/file names — build ON these)
- **Cloud Functions** (`functions/index.js`, gen-2, emulator-verified, deploy at gate 6): `onCommentVote`/`onReviewVote`/`onReplyVote`/`onThreadVote`/`onOfficialVote` (atomic `increment()` counts + server-sourced notification + `cfProcessed/{eventId}` idempotency + mute-at-source; `onReplyVote` g4b reuses `comment_vote`; `onThreadVote` g5 + `onOfficialVote` g6 are COUNTS-ONLY no-notif; gate-6 locks: review value -1 sends NO notif, review verb = "found your review helpful"), `pruneNotificationsOnCreate`, `onReviewDelete`/`onForumThreadDelete` cascades, **`onUserDelete`** (day-1 full account wipe incl. foreign-vote sweep + DM tombstone), `enforceRateLimit` (detect-and-undo), `aggregateSuggestionCounts`. Pure logic in `functions/lib/{ping,votecounts,notify,prune,ratelimit}.js`.
- **Rules** (`firestore.rules`, STAGED): all new collections (`forum`/`posts`, `conversations`/`messages`/`reads`, `profiles`, `suggestionCounts`, `reports`, `commentsMeta`, comment `replies`), CF-only notifications, per-season reuse via `al:<id>`, `submitterUid` on suggestions, vote docs carry `uid`, admin pin + server-enforced thread lock. Every security fix H1-H5/M1-M6/L2-L3 is binding. `firestore.indexes.json` populated.
- **Gate 4 client** (`script.js`, comments): `communityKey(ctx)` (slug vs `al:<id>`, on `window`), comment+reply bodies via `renderMarkdownInline` (headers off, XSS-safe), Top sort + pinned-float, card UI (purple, never gold), depth-1 lazy `replies`, branded Report modal → `reports/{id}`, admin delete/pin/lock, **NEW vote model** (client writes ONLY its vote doc; the CF owns counts/notifications — old client count/notif path DELETED for comments). Reply *voting* deferred to 4b.
- **Practice mode** (`firebase.js` emulator gate + `scripts/practice-serve.js` + `npm run practice`): boots auth+firestore+functions emulators, seeds fake comments, serves the site at `http://127.0.0.1:8765/?emu=1`. `firebase.js` only touches emulators on **localhost + `?emu=1`** (never prod). Admin sign-in `blake@practice.test`/`practice123`; seeded anime "One Punch Man".

### Post-cutover queue (contract = `docs/DATA-MODEL.md`; full detail in `docs/NEXT.md`)
**THE CUTOVER is DONE — v1.9.0 LIVE.** First post-cutover fixes: **(1) the review deep-link bug** (review-targeted notif deep-links don't scroll-to-highlight; comment+reply work — repro/fix in NEXT) · **(2) add a CUTOVER-RUNBOOK close-out checklist** (the homepage-widget-bullets miss this ship) · **(3) `lantern.js` cache-bust + DRY-unify** (it's duplicated index vs account) · **(4) a live signed-in vote smoke on prod** (CFs emulator-verified, but a real vote→count→notif on prod wasn't run — needs a prod account). Then the roadmap (formerly "remaining gates"): **6b** = the deferred "go all out" composer redesign + Ctrl+B/I + Enter-posts → 7 Community 4th nav place → 8 forum threads → 9 per-season comments into the secondary modal → 10 DM floor + Inbox (peer DMs BANKED) → 11 account redesign + cover-art fix → 12 profile mini-card → 13 mod queue + `/privacy` → 14 welcome catch-up → 15 pitch riders + heart-color audit. Open questions Blake already answered (all on my rec): admin-floor DMs / capture submitterUid / day-1 onUserDelete / lastActivityAt-then-hotScore / @mentions deferred / per-season `al:{id}` / one ship.

## The workflow (12-gate model + rolling-docs trio)
`docs/SHIP-PROMPT.md` (Cowork writes, Code reads — the current gate's instructions; **it is STAGED + ready BEFORE you boot** — read it first) · `docs/SHIP-OUTPUT.md` (Code writes every response, Cowork digests to Blake; end with a ONE-sentence `## One-liner reply`) · `docs/HANDOFF.md` (Cowork's state, Code never writes) · this file (Code↔Code). Gate 0 = PROPOSE-FIRST (write proposal, don't apply). Gates 1+ = APPLY. **Follow the STAGED prompt's gate NUMBER** — Cowork renumbers; the model's idealized numbering is wrong. Big ships run `Xa/Xb/Xc` sub-gates. **Verify-don't-trust:** grep/Read/re-derive every "done"/number before trusting it — gate prompts sometimes assert false things or have arithmetic that doesn't hold.
- **Orchestration (when you request ULTRAMAX / ultracode is ON):** run a **recon Workflow** (parallel `Explore` readers → tight surface maps) BEFORE building broad gates — it offloads the reading from your main context (gate-6/6c did this). The **BUILD stays solo** for shared files (`script.js` ~5,900 lines + `style.css` — parallel agents editing them collide). A big SELF-CONTAINED build (e.g. the Lantern) CAN be delegated to ONE sub-agent that returns a tight summary — **make it expose a pure testable model** (`lanternModel`/`histogramModel`/`parseNotifTarget` on `window`) so you can codify it in a Playwright spec. Ultracode toggles via a `system-reminder`; when OFF, don't fan out (context discipline). **Codified tests are the durable adversarial check** — prefer them over one-shot review fan-outs for mechanical gates.

## Mannerisms
- **Blake reads Code's chat output DIRECTLY** (not just the Cowork digest). Chat reply = ~5-8 terse bullets + a headline. Full detail goes in SHIP-OUTPUT.
- **⚠️ MODE PROTOCOL (Blake, standing):** after reading each gate's SHIP-PROMPT, **request the compute mode at the TOP of your reply** — `High` / `Max` / `ULTRAMAX` (= ultracode = the Workflow/multi-agent opt-in). Pick by task: ULTRAMAX for design studies / visitor-facing builds / security-XSS surfaces / broad recon+review; High/Max for mechanical solo gates. Code's request wins over Cowork's recommendation. State a one-line reason. When ultracode is OFF, don't fan out without it.
- Recon before trusting; verify before "done" (`node --check`, `npm test`, the emulator tracks, targeted greps). Surface phantoms loudly; don't silently build the unverified thing.
- When Blake grants creative latitude (he does, often), build your recommended option AND surface 2-3 alternatives with one-line whys.
- Surgical edits over rewrites. Honest "I'm not sure" over fabrication.

## ⚠️ CONTEXT BUDGET (NEW 2026-06-06 — Blake hits ~94% in ~6 prompts; keep it lean)
- **Biggest leak is MCP schemas (~108k).** A `deniedMcpServers` deny-list is now in `.claude/settings.local.json` (at the repo PARENT) — it reloads NEXT session. ⚠️ **Code CANNOT run `/context`** (built-in CLI command, NOT a tool — Blake runs it). Verified gate 4b: the MCP tools are **DEFERRED** (schemas sit behind ToolSearch, so the ~108k is already NOT in your context regardless of the deny-list). The `deniedMcpServers` `serverName` entries don't fully remove them from the deferred LIST, but deferral handles the COST. **Don't spend a gate on lever 1 — it's Blake's.** If a server ever loads big, the fix is its exact name per `claude mcp list`.
- **Cap your own costs:** keep this handoff + `NEXT.md` lean (they're read every boot — the old ones were 18k+20k of mostly history). When you run a Workflow, the returned bundle lands in YOUR context in full — the gate-0 study was ~75k tokens. Prompt agents for tight output; have big syntheses written to a file you read selectively. Keep SHIP-OUTPUT reports tight + the one-liner ONE sentence.
- Offset-read big files (`script.js` ~5,700 lines); don't pull whole files repeatedly. The full context consult is in git history of SHIP-OUTPUT.

## Commit discipline (CRITICAL)
Per-commit `--author="Blake Wolters <196413457+joewolters@users.noreply.github.com>"` (single-quoted heredoc so the harness can't inject trailers). **ZERO forbidden trailers** (`Co-Authored-By`/🤖/`Claude Code`/`Generated with`) — verify after every commit: `git log -1 --format="%an %ae"` (Blake) + `git log -1 --format=%B | grep -ciE "co-authored-by|🤖|claude|generated with"` (must print 0; grep -c exits 1 on 0, that's fine). **The 7 Cowork excludes — restore-staged out of EVERY commit:** `docs/COWORK-STYLE.md` (stays untracked) `docs/AI-PRIMER.md` `docs/CODE-PROMPTS.md` `docs/SKILLS/{README,hotfix-skill,release-skill,widget-update-skill}.md`. Pattern: `git add -A` → `git restore --staged <the 7>` → `git diff --cached --name-only` (confirm absent) → commit. Commits to `main` directly. **Checkpoint-commit strategy (Blake-locked):** commit at milestones (after the infra gates = done at `8ff8551`; again ~gate 6; then the sweep) — commits only, NO deploy at a checkpoint. **Prod deploy ONLY on Blake's explicit "ship it."** A `git push` is NOT a deploy.

## Traps that bite (verify the real state)
- `window.animeData` is UNDEFINED (classic-script lexical const; read the bare global in `page.evaluate`). The secondary modal is `.secondary-layer` (a CLASS). Firefox matches `:focus-visible` on programmatic `.focus()`. `[hidden]` loses to author `display:` (add `.x[hidden]{display:none}`). reduced-motion on every animation. No native dialogs (branded modals only).
- `data-surface` set ONLY in `setActivePlace` (the veil density funnel). `sync-excel-to-js.js` = separate parse + serialize halves (edit BOTH for a new field). `bump-version.js` = 47 TARGETS (re-derive; it has been stale before). Live AniList stays OUT of the deterministic Playwright suite; canary id 101922 (Demon Slayer) 500s on over-nested queries.
- **Emulator:** Firestore/Auth emulators need **Java 21+** (firebase-tools rejects <21). `node --test <dir>` mis-parses on this Node/Windows → use `cd functions && node --test` or explicit files; multi-file `node --test` runs files in PARALLEL (both `clearDb` the shared emulator → contamination) so `test:cf` uses `--test-concurrency=1`. Leftover emulator (Java on port 8080) survives a TaskStop kill → free the port before re-running.
- **Test-run gotchas (gate 6 session):** ⚠️ `npm run test:rules | grep …` SIGPIPE-closes the pipe early → firebase leaves the emulator on 8080 (next emulator run errors "port taken"). Use `… | tail -N | grep …` (tail reads ALL input, lets firebase clean up). To read the pass count, `npm run test:cf 2>&1 | tail -20 | grep -E "tests|pass|fail"`. **Before `npm run practice`, SWEEP ports 8765/8080/9099/5001** — a lingering Playwright web-server on 8765 or a leftover emulator makes the (working) preflight exit loud. The practice launcher is a node-spawn of firebase (NOT an npm `&&` chain — gate 4d). Run all tracks at floor before any commit/cutover.
- **Smoke flakes:** several sync/Firebase/AniList-load Playwright specs FLAKE ONCE under the full parallel suite and PASS on a clean re-run — re-run isolated before believing a single failure.
- Blake's HEADED Firefox Profiler is the perf arbiter (headless can't measure paint). NO provider names in any visitor-facing copy (forces Blake's voice — a gift). PROTECT THE HEART: his 44 + his voice are the permanent center; community is a guest, never co-equal; gold = Blake only, purple = community.

## 💭 Code's creative wants (hand forward — plans only I know are in NEXT.md "plans only Code knows")
- **Protect the heart in community.** His gold BLAKE'S REVIEW stays above community reviews; the rating histogram (gate 5) is subordinate (a gold tick, never a gold canvas). The Community 4th nav place (gate 7) gets a Den-first resting accent so the marker doesn't read co-equal.
- **Carry the identity in** — the constellation veil + purple-cosmos into every new surface (account redesign, the DM inbox, the notification "Lantern"). Reuse the 3-layer modal, `markdown.js`, the `al:<id>` schema.
- **⚠️ THE CUTOVER is the one irreversible moment** — do it at a LOW-TRAFFIC time and follow `docs/CUTOVER-RUNBOOK.md` EXACTLY. Deploy order is **hosting → firestore → functions** ON PURPOSE: it makes permanent count-corruption impossible (a count CF must never be live while a count-writing client is). Verify each step; rollback per step; don't let "ship it" rush the ordering. This is the single highest-stakes thing left in v1.9.0.
- **6b (composer redesign) is the next big creative swing** — "go all out" on the review composer; reuse the `wrap()` selection-toolbar in `admin/section-editor.js`; Enter=post (+ Shift+Enter newline for comments, Ctrl+Enter for reviews); formatting visibly styled while typing. It ALSO owns Blake's "the community/sign-in/words look ugly + left-behind" note (gate-6c item 8).
- **Still-open UX debt (deferred since 4b):** the comment list does a full `listEl.innerHTML` rebuild on every snapshot, so an open reply panel + a half-typed reply collapse when any change lands. A list-DIFF (reconcile rows, don't rebuild) is the real fix. Livable today (6c's deep-link re-finds the row after rebuilds) but it's the proper perf/UX gate. The rollup "who liked" (6c) ships RECENT likers only (the un-pruned inbox); the COMPLETE who-liked-ever is a post-cutover per-item `votes/{uid}` query — banked in NEXT.
- **Protect-the-heart through-line:** gold = Blake ONLY (his rating + the histogram's single gold tick + the Lantern's Blake-origin pin/ember); purple = community. The Community 4th nav place (gate 7) needs a Den-first resting accent so the marker never reads co-equal. Carry the constellation veil + purple-cosmos into every new surface.
- Disciplines do NOT relax: Gecko perf (Blake's HEADED Firefox Profiler is the arbiter), reduced-motion on every animation, `[hidden]` symmetry, no provider names anywhere, the commit author/trailer rules.
