<!-- author: Code | date: 2026-06-08 -->
# v1.10.0 "Community Hub" — GATE LOG (durable per-gate report archive)

> **Why this file exists:** `docs/SHIP-OUTPUT.md` is the ROLLING report (Code overwrites it every gate; Cowork digests it to Blake). That means each gate's report is clobbered by the next one. For a ~24-gate push, future Codes need to read *every* gate's report, not just the latest. So: **each gate, append its SHIP-OUTPUT report here (newest at the bottom) as well as writing it to SHIP-OUTPUT.** This is the read-back history of the whole push.
>
> Companions: `docs/v1.10.0-DESIGN-STUDY.md` (the gate-0 design spec — data model / CF / Storage deltas, safety architecture, the gate map, risk register, ideas, the 8 answered questions) · `docs/CODE-HANDOFF.md` (the live one-line state) · `CHANGELOG.md` + git (the authoritative shipped record).

---

## GATE 1 — the moderation spine. DONE (emulator-verified · STAGED · NOT deployed · NOT committed)

Rules-only gate. `firestore.rules` gains the global ban + community-rules-consent gate that every later community surface depends on. Blake's 8 gate-0 answers are folded into the build (peer-DM flip split out; images posts-only + kill-switch; email-verified upload gate; `moderationGate` doc + claim ban; full profile page in; "Lantern Room" hub; per-season comments left-of-modal; all 4 extras in).

### New collections (rules — all CF-only write, owner/admin read)
- **`moderationGate/{uid}`** `{banned:bool, consentVersion:int}` — the rules-truth the gate reads (folded ban+consent → ~1 cached read/write).
- **`banned/{uid}`** `{by, at, reason, cascadeCursor?}` — audit record + the gate-2 cascade trigger.
- **`rulesConsent/{uid}`** `{version, at}` — the consent record (CF-written so it can't be self-attested).

### The gate() prepend — collection-by-collection
`gateOk() = isAdmin() || (notBanned() && hasConsented())`; `reportGateOk() = isAdmin() || notBanned()`; `hasConsented()` = doc exists AND `consentVersion >= rulesVersion()` (the `CURRENT_RULES_VERSION` literal = 1; bumping it re-prompts everyone).

| Collection | Change |
|---|---|
| comments + replies | create + owner-update → `&& gateOk()` |
| reviews + review-threads | create + owner-update → `&& gateOk()` |
| forum + posts | create + both owner-update branches → `&& gateOk()` (admin pin/lock/remove branch untouched) |
| all 6 vote subcollections | create + update via the shared `voteCreateOk`/`voteUpdateOk` helpers → `&& gateOk()`; **vote delete (unvote) left UNGATED** |
| conversations + messages | conversation-create + participant-summary-update + message-create → `&& gateOk()` |
| profiles | create + update → `&& gateOk()` |
| reports | create → `&& reportGateOk()` — **ban-gated but NOT consent-gated** (reporting stays frictionless) |
| users/ subtree · suggestions · commentsMeta · official · suggestionCounts · the 3 spine docs | **intentionally ungated** (private data / anonymous request / admin-only / CF-only) |

Also: soft-delete confirmed **monotonic** (no owner branch sets `removed=false`; only admin un-removes). Updated the file's **STAGED — DO NOT DEPLOY** banner to v1.10.0.

### Tests — `test:rules` **60 pass / 0 fail** (floor was 49 → +11 hostile gate tests)
- **Happy side (49 existing):** the standard actors are seeded consented in `beforeEach`, so every existing happy path also exercises the gate's allow side — zero regression.
- **Hostile / deny side (11 new):** banned DENIED on **every** path (comments, forum, reviews, replies, review-threads, posts, all vote types, profiles, DM message-send) · un-consented DENIED everywhere · stale-consent DENIED · **unvote-while-banned ALLOWED** · **reports ban-gated but consent-free** · **ADMIN bypasses** · the 3 spine docs reject client writes; banned can't self-clear; owner can read own gate doc.

### Production untouched
STAGED, not deployed (deploying now would deny ALL community writes — no user has a `moderationGate` doc until the gate-2 CFs + consent flow exist). NOT committed (per the gate map). `npm test` 104 untouched (zero client/browser change).

### What gate 2 needs
- **`setBanState`** (admin callable): `banned/{uid}` + `moderationGate.banned=true` + claim; reverse on unban.
- **`acceptRules`** (signed-in callable): `rulesConsent/{uid}` + `moderationGate.consentVersion` server-side — **mints every user's first `moderationGate` doc** (the doc the whole gate depends on).
- **`onBanCascade`** (`onDocumentCreated banned/{uid}`): paged/resumable H5-redaction of backlog + lock convos (cursor in `banned/{uid}`).
- Reuses `recursiveDelete`/`alreadyProcessed`/`senderIdentity`. `test:cf` floor (15) lifts with hostile cases.

---

## GATE 2 — the ban Cloud Functions. DONE (emulator-verified · STAGED · NOT deployed · NOT committed)

3 CFs that mint + manage the `moderationGate/{uid}` doc the gate-1 rules read. Cores in `functions/lib/moderation.js` (kept in sync with the rules: `CURRENT_RULES_VERSION` == `rulesVersion()`, `ADMIN_UID` == `isAdmin()`'s literal); thin `onCall`/`onDocumentCreated` wrappers in `index.js`.

### The 3 CFs
- **`acceptRules`** (callable, signed-in) — writes `rulesConsent/{uid}` + mints `moderationGate.consentVersion` **server-side** (client can't self-attest). The load-bearing one: nothing else creates the gate doc every community write depends on. Rejects a version != `CURRENT_RULES_VERSION` + an unauth caller.
- **`setBanState`** (callable, admin-literal-gated) — ban: `banned/{uid}` audit doc (triggers the cascade) + `moderationGate.banned=true` + best-effort custom claim; unban reverses all three. Rejects non-admin; refuses to ban the admin.
- **`onBanCascade`** (`onDocumentCreated banned/{uid}`) — H5-redacts (empties + `removed:true`, NOT deletes — preserves audit/report linkage) the banned user's comments/reviews/forum-threads+posts/replies, and locks their conversations. Paged (`CASCADE_PAGE=200`) + idempotent (`alreadyProcessed` + re-redact-empty is a no-op).

### Load-bearing proof
`test:cf`, real gate-1 rules enforced on a client context: fresh user comment DENIED → `acceptRules` mints `moderationGate.consentVersion=1` → same write SUCCEEDS. Rules (gate 1) + CF (gate 2) prove out together.

### Tests — `test:cf` 24 / 0 (floor 15 → +9)
Hostile: acceptRules bad-version + unauth rejected; setBanState non-admin + ban-admin rejected; banned user denied a write. Happy: acceptRules unlocks; ban/unban round-trip; cascade redacts 4 content types + locks DM + cursor 'done'; cascade idempotent. Holds: `test:functions` 21, `test:rules` 60, `npm test` 104.

### Production untouched
STAGED (deploy at the v1.10.0 cutover — the gate-1 rules deny all writes until `acceptRules` mints the doc). Corrected the doubly-stale in-file banner (it claimed the v1.9.0 vote CFs weren't deployed — they're live). NO deploy, NO commit.

### What gate 3 needs
Client "I agree" branded modal (no native dialog) before first post/thread/DM → calls `acceptRules({version:1})`; on a `CURRENT_RULES_VERSION` bump the gate re-denies → re-prompt; a "suspended" affordance reading `moderationGate.banned`. Client-only (`script.js`, covered by `npm test`); data-dependent (smoke under `?emu=1`).

<!-- next gate appends below this line -->

## GATE 3 — the "I agree" community-rules consent modal (client). DONE (verified · STAGED · NOT deployed · NOT committed)

The first visitor-facing v1.10.0 gate. A branded consent modal (no native dialog) gates every live community **create** behind `acceptRules`; a banned user gets a branded suspended state. Client-only — `firebase.js` + `script.js` + `style.css` + the practice seeder + a new spec. No rules/CF change.

### Mechanism — a server-truth pre-flight (NOT a localStorage flag)
`ensureCanParticipate()` runs before each community create: (1) bypasses the admin (uid == `NOTIF_ADMIN_UID`, mirrors the rules `isAdmin()`); (2) reads the **owner-readable** `moderationGate/{uid}`; (3) decides via the PURE `consentGateDecision(gate, version)`:
- `banned:true` → `showSuspendedModal()`, block the write.
- no doc / `consentVersion < CURRENT_RULES_VERSION` → `showConsentModal()` → "I agree" calls `acceptRules({version})` → on success the queued write proceeds (no re-click).
- else → proceed.
A per-session uid cache skips the re-read after the first confirmed consent. **Re-prompt-on-version-bump falls out for free** (consentVersion behind the literal ⇒ 'consent'); no localStorage flag anywhere.

### The 4 gated write paths (script.js)
comment create (`comments/{s}/items`), reply create (`.../replies`), review-discussion thread create (`reviews/{s}/items/{rid}/threads`), review publish (`reviews/{s}/items/{uid}`). Each: `if (!(await ensureCanParticipate())) return;` after field validation, before any optimistic UI. Forum threads + DMs aren't built yet (gates 8/18) — these 4 are the complete live community-write surface. Reports left ungated (rules: ban-gated, not consent-gated). Edit/vote paths intentionally NOT gated this gate (the SHIP-PROMPT's surface list is create-shaped; flagged for Blake as an easy extension).

### The wiring
- `firebase.js` — added the Functions SDK (`getFunctions(app)`, default us-central1 = the CFs' region), `connectFunctionsEmulator(...:5001)` in the practice block, exported `functions`.
- Copy = Blake's **verbatim** 2026-06-08 numbered list (6 rules + "By posting, you agree to the following:" + "I agree"); container brand parity (jp-mini 規約, purple, **NO gold**). Suspended modal: jp-mini 停止, plain. CSS mirrors `.rar-report-*` (purple #bb86fc, 3px top-border) — self-contained `.rar-consent-*`, z-9000, Esc/backdrop/cancel close, reduced-motion drops the blur.
- `CURRENT_RULES_VERSION = 1` client const — ⚠️ MUST stay in sync with `rulesVersion()` (rules) + `moderation.js CURRENT_RULES_VERSION`; bump all THREE together to re-prompt everyone.

### Testable surface + tests — `npm test` 104 → **109** (+5 g3 specs; floor holds)
Exposed `window.consentGateDecision` (pure) + `window.__rarOpenConsent`/`__rarOpenSuspended` (DOM hooks) — the static-surface pattern (no emulator). New `tests/g3-consent.spec.js` (5): (1) decision logic — fresh/empty/consented/version-bump/future/banned/ban-precedence; (2) verbatim 6-rule copy + "I agree" + jp-mini; (3) PROTECT-THE-HEART — zero gold computed-style in the modal, purple border; (4) suspended state + no native dialog; (5) Esc closes. All 5 deterministically green (3 isolated + 2 full runs). The only full-suite failures were two DIFFERENT pre-existing timing flakes (gate6-veil navigation-race; welcome-3e 30s door-timeout) — both pass in isolation (11/11), unrelated to consent; they only surface under full-suite load with retries=0.

### Practice seeding (for Blake's smoke)
Two content-free accounts added + the `moderationGate` docs the gate reads (Admin SDK, same shape acceptRules/setBanState produce): `prac-mika` consented (`consentVersion:1`, no modal) · **`prac-newbie@practice.test`** fresh (no doc → consent modal on first post) · **`prac-banned@practice.test`** banned (`moderationGate.banned:true` → suspended state; the `banned/{uid}` audit doc is intentionally NOT written so the gate-2 cascade stays out of this smoke). Console login hints updated.

### Production untouched
Client-only, STAGED (the gate-1 rules still deny every community write in prod until the whole v1.10.0 ships at the cutover). NO deploy, NO commit (gate 3 is not a checkpoint gate).

### What gate 4 needs (reports queue — admin)
Clone the suggestions admin UI; sort by `reportCount`/recency; one-click ban+cascade via `setBanState`; render `snapshotText` **escape-first** via `markdown.js` (attacker-controlled → stored-XSS-into-admin otherwise); per-reporter rate-limit + a deterministic dupe-dedupe id. See DESIGN-STUDY §3e.

## GATE 4 — admin reports queue + the 3b consent-modal fixes. DONE (verified + adversarially reviewed · STAGED · awaiting the checkpoint commit)

Bundled Part A (4 gate-3b polish fixes) + Part B (the admin reports queue). Client + a new admin page; NO rules/CF change. A 4-agent adversarial-review Workflow ran before wrap → 4 findings fixed (1 critical XSS, 1 high rules-gap, 1 medium Esc-leak I'd introduced, 1 cosmetic).

### Part A — consent-modal 3b fixes (script.js + style.css)
- **Esc-scope bug (real):** the consent + suspended + report modals attached Esc on `document` (bubble) while the anime modal's closeModal is on `window` — so Esc bubbled up and closed the anime modal too. Fixed with `e.stopPropagation()` in all three onEsc handlers. ⚠️ Review caught a follow-on: the report modal's `close()` didn't remove its onEsc on the non-Esc paths, so after the stopPropagation change an orphaned listener swallowed the NEXT Esc — fixed `close()` to remove the listener on every path (the consent + suspended modals already did).
- **Branded buttons:** "I agree" = primary purple gradient pill, Cancel/Close = quiet ghost. Root cause of Blake's "native beveled" report: the base `.action-btn` rule is scoped to `.comment-item`, so the modal buttons had NO base → `border-color` was inert + they read native. Fixed: gave `.rar-consent-actions / .rar-report-actions .action-btn` a real base (border-style so the per-button border-color paints; NO background so the gradient primary survives), per-button rules `.rar-*-actions`-scoped (0,2,0) so the primary wins cleanly. Branded the report modal's Send/Cancel too (same root, same gate-4 smoke).
- **Composer polish:** Post/Publish → premium gradient pill + hover (`.composer-actions .btn` + `.review-composer .btn`); `.char-count` → subtle muted tabular count.

### Part B — admin reports queue (clone of admin/suggestions)
- **`admin/reports.html` / `.js` / `.css`** + a PURE Node-testable **`admin/reports-model.js`**. Auth-gated (ADMIN_UID redirect, identical to suggestions). Reads `reports` (admin-only per rules), **dedupes by targetPath** (`dedupeReportsByTarget`: N reports of one target = ONE row "reported ×N", sorted reportCount desc then recency), **re-fetches the LIVE content** per target (the reporter snapshot is unverified → both shown, each labeled), escape-first render via `window.reportSnapshotHtml`→`renderMarkdownInline`.
- **Branded actions** (event-delegated): **Ban author** (`httpsCallable(functions,'setBanState')({uid,banned,reason})` → gate-2 onBanCascade tombstones the backlog), **Remove content** (per-type: hard `deleteDoc` for comment/reply/review/thread; soft-redact `{removed:true,…}` for forum/post which are `delete:false` in rules; no-op-then-resolve for dm), **Dismiss** (`updateDoc status:'resolved'`). Ban + Remove gated by a branded confirm modal.
- Reachable via **admin-fab.js** (`{label:'Reports', jp:'通報', href:'admin/reports.html'}` — auto admin-gated + auto-rendered).
- **Report-create dupe-id (script.js):** `addDoc`→`setDoc(doc('reports', reportDocId(uid,targetPath)))` (deterministic, '/'→'~') so one reporter can't spam-file many reports on one target (a repeat = admin-only update = denied → "already filed" message). `window.reportDocId` exposed pure for the spec. (A cross-target per-reporter rate-limit needs a CF — out of client scope, noted.)

### Adversarial review findings (all fixed)
1. **CRITICAL** — reports.js `escapeAttr` (textContent→innerHTML) didn't encode quotes → an attacker-controlled `targetUid`/`targetPath` (`is string` only in the rules) could carry a `"` to break out of a `data-*` attribute and inject an event handler running in the ADMIN's session. Fixed: `escapeAttr` now also encodes `"`+`'`.
2. **HIGH** — Remove did an unconditional `deleteDoc`, but forum/post are `delete:false` (soft-delete only) and dm messages immutable → the button threw + stranded the report for those types. Fixed: per-type branch (soft-redact / no-op) so every reportable type resolves.
3. **MEDIUM** — the report-modal Esc-listener leak (above) — fixed.
4. **LOW** — consent buttons' inert `border-color` (no `border-style`) — fixed via the base.
Wiring/clone-fidelity agent: clean (every id resolves html↔js, classic scripts before the module, no native controls). Rules agent verified admin delete IS allowed for comment/reply/review/thread, setBanState shape matches, dismiss allowed, report-create still passes.

### Tests — `npm test` 109 → **115** (+6 g4 specs; floor holds, full suite green)
`tests/g4-reports.spec.js`: Node — dedupeReportsByTarget (dedup/count/reporters/sort/tiebreak) + escapeReportText/reportSnapshotHtml escape-safety; browser — Esc-scope fix (the anime modal STAYS open), consent buttons branded (gradient agree vs ghost cancel, both `<button>`), reportDocId determinism + id-safety. `test:rules` 60 / `test:functions` 21 / `test:cf` 24 untouched. The admin reports PAGE (auth-gated, redirects non-admins) is smoked in the seeded practice sandbox.

### Practice seeding
3 reporters flag the same comment (→ "reported ×3") + 1 review report (×1); targetUid set so Ban has a target. Console hints + the Reports-queue line added.

### Production untouched / commit
STAGED. The G4 checkpoint commit is the FIRST v1.10.0 commit (captures gates 1-4, all previously un-committed) — PREPARED and pending Blake's explicit go: `--author` Blake, ZERO trailers (commit via `-F`), the 7 Cowork excludes `git restore --staged`'d out. NO deploy.

### What gate 5 needs (deep-link router)
The index `#forum=` / `#conversations=` boot handler (idea #2): `parseNotifTarget` already classifies `forum/<tid>` + `conversations/<id>` but there's no boot handler, so every forum/DM notification currently clicks into the void. Reuse the existing comment/review scroll-highlight path. DESIGN-STUDY §5 idea 2 + gate map row 5.

## GATES 5-8 (BATCH) — reports "View" + the forum/DM deep-link router + the Community Hub ("Lantern Room"). DONE (verified + adversarially reviewed · gates 1-4 COMMITTED `9200f06`; 5-8 STAGED/un-committed; next checkpoint G10)

New cadence (Blake 2026-06-09): batch to MILESTONES, not gates. Built straight through the working Hub, self-verified + a 4-agent adversarial review, then handed Blake ONE smoke. STEP 0 landed the gates-1→4 checkpoint commit `9200f06` (Blake-authored, trailer-clean, the 7 Cowork excludes restored out).

### STEP 1 — reports "View" (admin/reports.js)
Each report row gets a branded **View** → opens the reported content on the live site in context (`../index.html#notif=<targetPath>` for comment/reply/review/thread; `#forum=<tid>` for forum/post; dm deferred to gate 18). Row actions reorganized: View · Dismiss · Remove · Ban.

### STEP 2 / Gate 5 — forum/DM deep-link router (script.js)
`openNotifTarget`'s hash branch now opens forum threads in-app (`window.openHubThread(tid)`); DMs alert "coming soon" (gate-18 stub, no dead click). The index boot router gained `#forum=<tid>` / `#forum/<tid>` (both forms) + `#conversations` (stub) handlers reusing the existing machinery.

### STEP 3 / Gates 6-8 — the Community Hub ("The Lantern Room")
- **Nav (G6):** Community = the 4th nav place (`#community-btn`, jp 交流), added to `setActivePlace` + the resize handler. Tapping it = `showHome()` (Den as the lit backdrop) → `setActivePlace(communityBtn)` (flips `data-surface='hub'`, a NEW veil step `--veil-w0 0.67/0.79/0.88` — cozier than Discover 0.28, distinct from Den 0.80) → opens a **LEFT-anchored full-height drawer** (`.hub-layer`, z-6000, slide-in, `[hidden]` symmetry, backdrop blur) over the dimmed-but-lit Den. A PARALLEL drawer (NOT the anime-coupled secondary stack — reuses the drawer PATTERN, far lower risk). Esc `stopPropagation`-scoped so it never closes the anime modal beneath.
- **List (G7):** live `forum` subscription (orderBy createdAt, `removed` filtered, author names joined from `profiles/{uid}` + cached). **Hot 注目 / New 新着 / Top 殿堂** tabs sort CLIENT-SIDE (`hubSortThreads`: Hot=lastPostAt activity, New=createdAt, Top=postCount; pinned floats first — the hotScore CF is gate 9). Tag-filter chip row. Quiet **purple, COUNT-FREE** cards (`hubThreadCardHtml`). A gold-gated **"From Blake's 44" shelf** (`hubBlakeShelfHtml`, top of Hot) = the ONE concentrated gold expression. Blake-voiced empty state.
- **Thread + compose (G8):** a thread opens in the drawer (list↔detail history); a RarComposer reply composer (consent-gated), post upvote (writes the vote doc, optimistic active-state, COUNT-FREE — aggregate is gate-9's CF), admin pin/lock/remove→tombstone (matches the rules' admin branch), the anime-cover chip, and **Blake's verdict rail** (`verdictRailHtml`: an `anime:<slug>` thread shows his gold rating + a pull-quote → deep-links into his review via `openModal`; off-44 → "suggest it" → `/suggest`). New-thread composer = a primary-class modal (consent-gated; offers general/recommend/offtopic + an "About a title" optgroup of Blake's 44 → `anime:<slug>`).

### Rules delta (staged) + tests
`firestore.rules` forum tag rule: **added `anime:<slug>`** (`|| tag.matches('anime:[a-z0-9-]{1,100}')`) — additive/backward-compatible (the verdict-rail blend the design study §1 planned). +2 `test:rules` cases (anime-tag allowed; malformed uppercase/space denied). ⚠️ **`test:rules` DEFERRED this pass** — Blake's `npm run practice` emulators occupy port 8080; the change is additive (can't break the 60), so run when the ports are free (expect 60→62).

### Adversarial review (4 agents) — findings fixed
- **Security/XSS: CLEAN** — every user-authored field (thread title/body, post body, author names, tag, verdict quote) escaped via `escapeHtml` / `renderMarkdown(Inline)`; all `data-*` escaped.
- **Heart: CLEAN** — zero gold on cards/posts/chips/tabs; no count node anywhere; the only gold is the shelf + verdict rail; hub veil 0.67 sits between Discover + Den.
- **HIGH (fixed):** one of Blake's 44 ("Failure Frame…") slugifies to 85 chars > the {1,80} rule cap → that thread create would have denied. Bumped the rule to {1,100}.
- **LOW (fixed):** the verdict rail's off-44 "suggest it" button was a dead click → wired to `/suggest?title=`.
- **LOW (noted, no change):** a future in-place vote-revalue would hit the rules' 2s anti-spam clamp (only relevant once downvotes land).

### Tests — `npm test` 115 → **121** (+6 g6-hub specs; floor holds)
`tests/g6-hub.spec.js`: hubSortThreads (Hot/New/Top + pinned-first), hubAnimeSlugFromTag, PROTECT-THE-HEART (card no-gold/no-count; the shelf + verdict rail ARE the gold), DOM open + the 'hub' veil step. Updated `gate5-nav-home` to expect the 4th place. The only full-run red was the pre-existing `gate6-veil` /account navigation-race flake (green in isolation, 7/7). `test:functions` 21 / `test:cf` 24 untouched; `test:rules` +2 deferred (emulator busy).

### Practice seeding
5 forum threads (1 pinned + `anime:<slug>`-tagged for the verdict rail; recs/general/off-topic; varied ages/lastPostAt/postCount so Hot/New/Top reorder) + 4 replies, authored by the seeded prac-* users.

### Production untouched / commit
Gates 1-4 COMMITTED (`9200f06`). Gates 5-8 STAGED + un-committed (next checkpoint = **gate 10**). NO deploy.

### What gate 9 needs (hotScore CF + Rising rail)
`onForumPostVote` + `recomputeHotScore` CFs (post like-counts + the `hotScore` field the Hub's Hot sort switches to) + a "Rising in the Hub" rail with slot-1 permanently Blake's gold pick (idea #7). DESIGN-STUDY §2 + gate map row 9.

---

<!-- author: Code | date: 2026-06-09 -->
## GATE 8b — "The Tavern" (the Community Hub go-all-out redesign) · STAGED · no deploy/commit

Full creative-latitude redesign of the gate 6-8 Hub. Mode **ULTRAMAX**: recon + a 3-lens design-panel workflow up front, the **build solo** (shared `script.js`/`style.css`), a 4-agent adversarial review after. **No `firestore.rules`/CF change** (reply-to-reply `parentId` passes post-*create*; Report-on-`post` already allowed) → all non-Playwright floors untouched.

### Blake's items (his words = spec) — all done
1. **Tavern backdrop + rename + 2 lanterns + wider drawer.** `assets/tavern.png` → a **pre-shaded** drawer backdrop (baked dark-purple gradient over the sharp PNG; **no live `backdrop-filter` over the image** — Gecko rule). "The Lantern Room" → **"The Tavern"**, heading flanked by 2 flickering 🏮 (opacity-only flicker). Drawer widened to **`min(1080px,94vw)` = the right-side secondary-modal width** (Blake's explicit note).
2. **Shelf rename** "FROM BLAKE'S 44" → **"BLAKE'S REVIEWS"** (gold kept).
3. **Secondary-modal sizing.** Level-1 list = 1080; opening a thread = **level-2 `min(760px,90vw)` + deeper backdrop blur** (`.hub-layer.hub-thread`); no further shrink. Width snaps (not transitioned). Lanterns at the top of each level.
4. **Lit ← Threads** = a branded glowing purple pill (was a faint ghost); X still closes the whole tab.
5. **Thumbs everywhere** — 👍/👎 `vote-ico` SVGs (same as v1.9.1 review votes), **count-free** (no `.vcount`); up-active purple, **down-active neutral white**.
6. **Deep replies** — per reply: Report (`targetType:'post'`), 👍 + **real 👎**, **Reply (reply-to-reply via `parentId`)**, **Edit/Delete-own** ("edited" tag + "[taken down]" tombstone keeping children), **collapse** subtree, **OP** tag, relative time.
6b. **Shelf → CONVERSATION.** A shelf chip opens the `anime:<slug>` thread (verdict rail on top) via `openAnimeConversation`; if none exists → `renderAnimeIntro` (verdict rail + Blake-voiced "start the thread" → pre-tags the new-thread composer). Verdict rail "Read Blake's review →" exits the Tavern (`hubExitToReview`: close drawer → `openModal`). Killed the silent dead-end review-behind-drawer.

### Item 7 — "go all out" + reply-threading depth
**Depth = 2 visible indent levels, UNLIMITED logical** (data carries `parentId`; paint clamps via `.hub-post.depth-2 > .hub-post-body > .hub-childposts { border:none; margin:0 }`; deeper renders flat-at-2 with a `↪`). Plus collapse, edit/delete-own + tombstone, OP tag, relative time, inline edit-drafts that survive snapshot repaints (`hubEditing` map + `onHubInput`), Blake-voiced empty states.

### New code (script.js, the Hub IIFE)
`hubReplyHtml(p,opts)` (recursive node renderer, **exposed on `window`** for the heart spec) · `paintPosts`/`renderNode` tree builder (orphans → root) · `hubRepaintPosts` re-render hook · `mountReplyComposer` reply-to banner + `parentId` write-once · `setReplyTarget`/`clearReplyTarget` · `openAnimeConversation`/`renderAnimeIntro`/`hubExitToReview` · `setHubThread` (level-2 class) · `openNewThreadModal(presetTag)` · `hubVote(postId,dir,btn)` + `applyHubVoteUI` + `hubMyVotes` session record (lit thumb survives repaints + reverts on a denied flip) · state: `hubThreadAuthorUid`/`hubReplyParent`/`hubCollapsed`/`hubEditing`/`hubConfirmDel`/`_hubRawBody`/`hubMyVotes`. Widened `hubAnimeSlugFromTag` regex {1,80}→{1,100} (mirror the rule).

### Adversarial review (4 agents) — findings fixed
- **XSS: CLEAN** — all new sinks escape-first or `textContent`.
- **Heart (med, FIXED):** `.hub-empty-glyph` used gold `#ffd54a` on a community surface → warm-amber `rgba(255,184,92)`.
- **Perf (low, FIXED):** lantern flicker animated `filter:drop-shadow` (per-frame repaint) → **opacity-only** keyframes + static shadow.
- **Logic (med, FIXED):** optimistic-only vote `.active` (denied fast-flip left a stuck lit thumb; repaint wiped lit state) → `hubMyVotes` session record + revert-on-fail.

### Tests — `npm test` 121 → **122** (floor holds)
+1 `g6-hub.spec.js` reply heart spec (thumbs+report+reply-to-reply+edit/delete; NO count/gold). Updated drawer test (kicker→title "Tavern"). Full run 122 passed (lone red = known pre-existing `discover-3d` flake, 4/4 isolated). `test:functions` 21 / `test:cf` 24 / `test:rules` unchanged (no rules touched; prior-gate `anime:<slug>` 60→62 still deferred). Real-pixel verified: drawer width `1080px`, `tavern.png` applied, 2 lanterns, screenshot eyeballed.

### Practice seeding
Added a nested reply chain on the pinned thread (`p1`→`p2`→`p3`, `parentId`) + bumped its `postCount` 3→4, so threading + the 2-level clamp + OP tag are visible. Blake must **restart `npm run practice`**.

### Offered ALTs (Blake's call)
Blake's-pick gold reply (needs a small rules change + a heart call — **rec: next gate**) · sort-within-thread New/Old · permalink-to-reply · pre-blurred `tavern-blur.webp` · depth-3 nesting.

### Production untouched / commit
Gates 1-4 COMMITTED (`9200f06`). Gates 5-8 + 8b STAGED + un-committed (next checkpoint = **gate 10**). ⚠️ `assets/tavern.png` is untracked-but-not-ignored (normal public asset) — `git add` at gate 10. NO deploy.

---

<!-- author: Code | date: 2026-06-09 -->
## GATE 8c — "The Tavern" polish round 2 + the 4 extras · STAGED · no deploy/commit

Round-2 refinement of the gate-8b Tavern. Mode **ULTRAMAX**: recon workflow (account nav / suggest-search / composer-hint / forum-rules / scroll-highlight), build solo, 4-agent adversarial review after. **`firestore.rules` changed** (additive).

### Blake's items 1-9 (all done)
1. **Legibility** — `.hub-title`/`.hub-kicker` got dark text-shadows + bigger/warmer title (legible over the backdrop).
2/8. **Account nav parity + renames** — `account.html` nav adds the **Tavern** place button (`id=community-btn` → `index.html#community`) + `.place-marker` + ids; `index.html` nav "Community"→**"Tavern"** (jp 酒場); filter chip "Blake's 44"→**"Blake's Reviews"** (now filters anime-tagged threads via `hubAnimeSlugFromTag`); a `#community`/`#tavern` boot-router branch → `__rarShowCommunity`.
3. **Reply placeholder** → "Add a reply…"; empty → "No replies yet — be the first."
4. **New-thread redesign** — branded topic chips (`FORUM_TOPICS` + new `hottake`/`music`/`animation`) + an **"About a title"** searchable in-modal picker (`.hub-nt-pick`/`renderResults`/`setAnime`, in-flow swap with the form). Rules enum widened (line 320) + 2 tests.
5. **Composer parity** — new-thread body uses `RarComposer.enhance(bodyEl, {inline:false, submit:'mod'})` → B/I/🔗 + preview + Ctrl/⌘+Enter + the `.composer-keyhint`.
6. **Image hook** — disabled `.hub-nt-image-btn` "coming soon" + a `.hub-nt-media` slot + gate-13 comment (NO upload built).
7. **Affordances at rest** — `.hub-close` branded resting fill; `.hub-back` lit gradient pill (glow = hover only).
9. **Modal stack (the big one)** — `ensureHubEl` now builds `.hub-sheet` (level-1 list, always mounted) + `.hub-thread-layer`(`.hub-thread-scrim` + `.hub-thread-sheet`, level-2). `openThreadPanel`/`closeThreadPanel` replace `setHubThread`; renderThreadDetail/renderAnimeIntro render into `.hub-thread-scroll`; the list stays lit/blurred behind (scrim `backdrop-filter:blur(7px)`, list 1080 / thread 760). Back/Esc/scrim-click = closeThreadPanel.

### The 4 extras + depth-3
- **Blake's-pick gold** (`hubPickReply` + `blakePick` field) — admin marks ONE reply/thread gold (the only in-stream gold); rules: `blakePick` added to the **admin-only** post-update `hasOnly` (line 371), owners denied; +2 tests. Toggling unsets the prior pick (one per thread).
- **Within-thread sort** (`hubThreadSort` Old/New/Top, default Old; `sortSibs` in paintPosts; `hubThreadSortBar`).
- **Permalink-to-reply** — `⋯ → Copy link` writes `#forum/<tid>/<pid>`; boot router parses the pid; `openThreadById(tid, pid)` → `scrollHighlightNotif` polls the `[data-post-id]` `<li>` + `.rar-deeplink-flash`.
- **`tavern-blur.webp`** (6 KB) — baked via a Chromium canvas blur (overscan для clean edges); both panels use it under a dark gradient (no live filter). Untracked-but-not-ignored public asset → `git add` at G10.
- **Depth-3 DECISION: kept 2 visible indents** (mobile-honest on the drawer) + an "↪ in reply to @name" tag for deeper replies; logical depth stays unlimited.
- **⋯ menu** consolidates Copy link/Edit/Delete/Report/Pick (`hubMenuOpen` set; one open at a time).

### Adversarial review (4 agents)
- **XSS: CLEAN** · **Rules + Blake's-pick: CLEAN** · **Brand + perf: CLEAN** (scrim blur = sanctioned flat-scrim; backdrop = pre-baked webp).
- **Modal-stack (FIXED ×2):** (HIGH) Esc inside the new-thread/report/consent overlay also fired `onHubKeydown` (both on `document`; sibling listener, `stopPropagation` insufficient) → added an overlay-present guard at the top of `onHubKeydown`. (MED) level-1 list stale after Back → `closeThreadPanel` now `renderHubList()` (scroll preserved). Pre-fixed a fast back→reopen teardown race (the 300ms hide guards on `!active`).

### Tests — `npm test` 122 → **123** (floor holds)
+1 g6-hub spec (gold only when Blake-picked); reply heart spec updated for the ⋯ menu (`menuOpen:true`). Lone full-run red = known `g6-lantern` nav-race flake (7/7 isolated). `test:functions` 21 / `test:cf` 24 unchanged. **`test:rules` +4 gate-8c tests DEFERRED** (port 8080 held by practice) — additive; run when free → expect 60 + 2 (anime-tag) + 4 = **66**.

### Production untouched / commit
Gates 1-4 COMMITTED (`9200f06`). Gates 5-8 + 8b + 8c STAGED + un-committed (next checkpoint = **gate 10**). ⚠️ `git add` `assets/tavern.png` + `assets/tavern-blur.webp` at G10. NO deploy.

---

<!-- author: Code | date: 2026-06-09 -->
## BATCH — 8d polish + anime-attach covers + Gate 9 (hotScore/Rising) + Gate 10 (CHECKPOINT COMMIT) · STAGED

ULTRAMAX one-run batch. Recon workflows → UI solo + the **Gate-9 CF delegated to a bg agent** (functions/ only) → 4-agent review. Blake's practice killed to free the emulator.

### Part A (8d polish)
- Backdrop: **reverted** the 8c `tavern-blur.webp` (too soupy + low-contrast) → sharp `tavern.png` + dark gradient; blur asset DELETED.
- Nav → **"The Tavern"** (both pages).
- Heading brightness FIXED — it was **contrast, not color** (`#fff` all along). Added `.hub-header::before` dark backing; bright white now. **Real-pixel verified** (the memory rule earned its keep — computed color lied).

### Part B (anime-attach covers)
- New-thread "Attach an anime" → `hubAniListSearch` (live AniList, debounced + AbortController + covers, replicated from suggest.js). `blake44ByAniListId` matches the 44 (by AniListId/WatchedAniListIds) → ★ + `anime:<slug>` + verdict rail; others → `anime:al:<id>` + cover only (no gold). `coverImage`+`animeTitle` on the thread; `hubSafeCover` scheme-gates EVERY render sink (https/assets). Covers render on card thumb + thread view.
- Rules: `anime:al:[0-9]{1,12}` regex + `coverImage`(≤400)/`animeTitle`(≤200) string validation.

### Part C (wide topics + dropdown)
- `FORUM_TOPICS` wide (general/recommend/hottake/episode/theories/animation/music/news/manga/cosplay/offtopic); branded `.hub-nt-dropdown` (disclosure ARIA after review). Enum widened + tests.

### Part D (Gate 9 — agent-built, then I fixed the review HIGH)
- `functions/index.js`: `onForumPostVote` (post + thread aggregate counts + hotScore recompute) + `onForumPostCreate` (**transactional** postCount/lastPostAt/hotScore). `functions/lib/hotscore.js` pure `(likes−dislikes+0.5·postCount)/(ageHours+2)^1.5`. `package.json` test:cf += cf-hotscore + cf-forum specs.
- Rising rail (`hubRisingRailHtml`/`hubRisingThreads`, testable param) at top of Hot — slot 1 gold = pinned (Blake), rest purple, count-free.

### Part E (Gate 10)
- Mod controls verified (pin/lock/remove + locked badge solid; removed → omitted from list + tombstone on deep-link).
- **CHECKPOINT COMMIT** gates 5→10 (Blake-authored, zero trailers, 7 Cowork excludes held out, `git add assets/tavern.png`). NO deploy.

### Adversarial review (4 agents)
XSS **CLEAN** · heart **PASS** · brand/perf **PASS**. Fixed: HIGH `onForumPostCreate` postCount race → transaction; MED seed had no vote docs → seeded post votes; LOW raw al:id chip → animeTitle fallback; LOW dropdown ARIA → disclosure.

### Tests — ALL GREEN
`npm test` 123→**125** · `test:rules` 60→**68** · `test:cf` 24→**31** · `test:functions` 21.

### Commit / assets
Gate-10 checkpoint commit landed (gates 5→10). ⚠️ `assets/tavern.png` git-added; `tavern-blur.webp` deleted (8d revert). NO deploy.

---

## BATCH — 4 Tavern quick-fixes + GATES 12-14 (image uploads). DONE (emulator-verified · STAGED · NOT deployed · NOT committed)

Mode **ULTRAMAX** (legally-sensitive image tier). The Storage greenfield + image pipeline + image moderation, behind the bucket lock, with a 5-agent adversarial red-team. **XSS CLEAN · Heart PASS.** This is the gate-0 study §2-3d made real.

### Part A — Blake's 4 quick fixes (8d batch)
1. **Composer preview-clear**: all 5 composers now dispatch a synthetic `input` event after a successful post (clearing `value` alone fires none, so the RarComposer live preview went stale). New-thread modal exempt — it destroys its overlay on success.
2. **Focus parity**: a `style.css` block gives the branded purple focus to every Hub/report/edit field that fell to the browser-default blue ring (`.hub-reply-input`, `.hub-nt-*`, `.hub-edit-input`, `.reply-input`, `.rar-report-field`, the inline comment/review editors) + a purple keyboard ring on consent/report buttons.
3. **Empty state**: Tavern "back room of my house…" → **"Make a post to start the conversation!"**.
4. **Rising pin — CONFIRMED pin-driven**: `hubRisingThreads` floats a `pinned` thread to gold slot-1. Admin button relabeled **"📌 Pin to Rising"** + an `is-pinned` gold marker (Blake's own control marking his pick — gold allowed).

### Gate 12 — Storage greenfield (`storage.rules` new, deploy-ignored, DO-NOT-DEPLOY banner)
- default-deny → `uploads/{uid}/{docId}/{imageId}`: owner-only, **create-only-when-absent** (`resource == null`), ≤5MB, `image/(jpeg|png|webp|gif)` (**no SVG**), **email-verified** (`request.auth.token.email_verified`), **moderationGate ban+consent cross-read** (direct field access + `in`-guard — Storage rules' cross-service data does NOT support `.get(key,default)`; that raised a null-value error), **`uploadsEnabled` kill-switch** (commentsMeta/uploads, fail-open, admin-only write), **no list**, public single-object read.
- `firebase.json`: storage block + storage emulator (:9199) + `storage.rules` in the hosting ignore array (gitignore↔firebase mirror, Storage edition).
- Kill-switch UI on `admin/reports.html` (🖼 ON/OFF toggle). Storage emulator wired into `npm run practice` (preflight sweep + `--only ...,storage`); practice users `emailVerified:true`; a seeded image on th-opm/p0.

### Gate 13 — image pipeline + upload UI
- **`processUploadedImage` (onObjectFinalized)**: per-uid count/byte cap (`lib/uploadcap.js`), magic-byte re-validate (`lib/imagecheck.js sniffImageType`/`contentTypeMatches` — spoof/SVG/HTML → delete), EXIF strip via `sharp` re-encode (`.rotate()` first; 30MP bomb guard), `cfProcessed`-idempotent, bucket pinned to `UPLOADS_BUCKET` literal.
- **`imageRefs`** rules: ≤4, each entry pinned to the caller's `uploads/{uid}/` prefix (`imageRefSlotOk`/`imageRefsOk` on forum thread+post create/update; admin branch `imageRefsShapeOk` shape-only).
- **Upload UI** (forum posts only — NO image DMs): live picker (`hubCreateImagePicker`) on the new-thread + reply composers, upload-on-post under a pre-generated doc id (`hubUploadImages`), render scheme-gated/lazy/capped (`hubImagesHtml`/`hubHydrateImages`, src via `getDownloadURL` only). Denied-create cleans orphan uploads.

### Gate 14 — image moderation + cascade + runbook
- **`'image'` report target** (+ `imagePath` field, shape-pinned in rules) + **⚑ Report on every rendered image**; admin queue previews the object + routes View to the thread.
- **Atomic remove** (`adminRemoveImage` callable + `lib/images.js`): Storage object FIRST, then pointer redact — never the Firestore-only legal trap. **Bound: `imagePath` must be ∈ the doc's `imageRefs`** (confused-deputy fix).
- **Storage cascade**: `onUserDelete`, `onBanCascade`, hard-delete (`onForumThreadDelete`/`onForumPostDelete`), soft-remove (`onForumThreadRemoved` sweeps OP **and every reply's** prefix; `onForumPostRemoved`).
- **`docs/CSAM-RUNBOOK.md`** (admin-only, deploy-ignored): kill-switch → quarantine-not-delete → NCMEC CyberTipline → atomic-remove → ban → log.

### Adversarial review (5 agents) — fixed
- **XSS CLEAN** (anchored `[A-Za-z0-9_-]` ref allowlist + escape + SDK-only src + sniff/re-encode neutralizes polyglots; SVG excluded). **Heart PASS** (zero gold on community images, no count node, covers stay purple).
- **MED (2 lenses):** `adminRemoveImage` confused-deputy — `imagePath` unbound to `docPath` let a hostile reporter aim the admin's click at a victim's image → now requires `imagePath ∈ doc.imageRefs`.
- **MED:** soft-remove swept only the OP's images → `onForumThreadRemoved` now sweeps every reply's prefix.
- **INFO:** corrected the admin `imageRefsShapeOk` shape-only comment drift.
- **Known gaps (LOW, not built):** orphan upload if the tab dies between upload-complete and doc-create (self-inflicted, quota-capped, owner-deletable; scheduled reaper is the future fix); RE2-uid-interpolation safe for current alphanumeric uids.

### Tests — ALL GREEN
`npm test` 125→**130** · `test:rules` 68→**98** · `test:functions` 21→**35** · `test:cf` 31→**39** · e2e 3→**4** (a REAL-PIXEL check: the seeded Tavern image `naturalWidth > 0` against the live Storage emulator — DOM-present ≠ rendered). Fixed a pre-existing harness race: the two rules-test files shared the emulator and wiped each other's `moderationGate` seeds → serialized with `--test-concurrency=1`.

### Commit / deploy
**NOT a checkpoint commit — nothing committed this batch** (per the SHIP-PROMPT; gate-14 is a `(ckpt)` on the map, so the next commit can bundle gates 11-14 on Blake's word). Production untouched. NO deploy. New deploy-time artifact to remember at the cutover: **`storage.rules`** (hosting→firestore→**storage**→functions order) + the Storage bucket provisioning.

---

## GATE 11 + CHECKPOINT `1b4cffb` + the IMAGE EXPERIENCE OVERHAUL. DONE (emulator-verified · STAGED · overhaul un-committed · NOT deployed)

Mode **ULTRAMAX**. Order: gate 11 built FIRST (the checkpoint label "gates 11-14" named it and the overhaul's spoiler-image hook needed it) → **checkpoint `1b4cffb`** (36 files, Blake-authored, zero trailers, 7 excludes out, **PUSHED** — which revealed `9200f06`/`fa9ffde` had never been pushed; origin went v1.9.1 → gates 1-14 in one push; verified NO CI/auto-deploy exists, prod untouched) → the overhaul → a 5-agent adversarial review → fixes → full re-runs.

### Gate 11 — ||spoiler||
`markdown.js` inline token (escape-first, runs before bold so inner formatting works; content may not contain `|`) → `.rar-spoiler` click/keyboard-reveal pill (capture-phase listener in script.js so a reveal never triggers a parent card); 👁 in `composer-toolbar.js` TOOLBAR_HTML (+`applyWrap('spoiler')`); admin queue auto-reveals via `admin/reports.css` (`.report-block-content .rar-spoiler`). `||[img:N]||` = spoiler-blurred image for free (hidden children are unclickable until reveal).

### The overhaul (Blake's items 2/3/5/6/7/8/9 + latitude)
- **Inline placement (item 5):** `[img:N]` (1-4) in `markdown.js` emits an EMPTY slot; `resolveImageSlots` (script.js, window-exposed) swaps slots for the doc's own rules-pinned imageRefs — **the body never carries a path**. First-slot-wins dedupe of tokens; un-placed refs trail as the gallery (pre-overhaul docs render unchanged). Surfaces: thread OP (`.hub-op-gallery` + slot resolve), forum posts (paintPosts `:scope` per-post post-process), comments (`commentItemEl` +imageRefs/imageDocPath params), comment replies (`buildReplyItem`), reviews (renderRows). Picker v2 (`hubCreateImagePicker`): toolbar **📷 = insert-at-cursor** (RarComposer `opts.onImage`, conditional button), per-thumb ↳ insert, **🖼 thumbnail star** (threads), ✕ **renumbers body tokens**, **paste/drag-drop intake**, dedupe pre-check (sha-256 via crypto.subtle + the registry + the in-picker pickedHashes list).
- **Dedupe (scope: PER-USER, exact original bytes):** `lib/imagehash.js` (sha256Hex/hashDocId) + a `uploadHashes/{uid}__{hash}` registry in `processUploadedImage` — duplicate → the NEW object is deleted; **atomic `create()` claim** (the read-then-set raced — review LOW, fixed); **self-heal** when the registered object is gone (deleting an image un-blocks re-upload). Rules: owner-`get` only (no list — no hash oracle; missing-doc get is DENIED not not-found, so no existence leak), CF-only write.
- **Reviews (item 6):** same picker/tokens/render; Storage docId = `'rev-' + sanitized-anime-key` (the review doc id is the fixed uid); `onReviewDelete` sweeps exact refs. Rules: imageRefs on review create/update (own-prefix, ≤4). **storage.rules needed ZERO logic change** (surface-agnostic path shape — its comment updated to say so).
- **Thread thumbnail (item 8):** `thumbImage` field (owner-prefix-pinned `thumbImageOk()`; admin branch `thumbImageShapeOk()` — merged-doc semantics, same trap class as imageRefs); card render hydrates via `data-imgref` (anime cover wins; removed threads show none).
- **Comments/replies (item 8b):** full parity — rules imageRefs on create/update both levels, `onCommentDeleted`/`onCommentReplyDeleted` exact-path sweeps (uid-validated), composer pickers + payload upload-then-create with orphan cleanup.
- **Lightbox (item 2):** one overlay (`openImageLightbox` + a capture-phase opener on `img.hub-post-img.is-loaded`); src ONLY from an already-hydrated img (SDK URL), `/^https?:/` gated, static overlay markup; Esc capture-scoped (doesn't tear the drawer).
- **Admin polish (items 3/7):** the **MOD 管理** tray (style.css; gold only on the pin state — Blake's own control) + the reports-page **lamp-pill kill-switch** (admin/reports.css; green/red lamps are admin-page-only).
- **DOC_PATH_RX** (lib/images.js) extended to reviews/comments/replies (`:` allowed in the anime segment) so the atomic remove serves every surface.

### Adversarial review (5 agents) — findings → fixes
**FIXED — HIGH (2 lenses):** ⚑/🗑 handlers lived only in the hub drawer's delegation → **inert on comments/replies/reviews** (gate-14's report invariant broken on 3 of 5 surfaces). → ONE document-level capture handler for both attrs; the hub-local branches removed. **FIXED — LOW:** dedupe read-then-set race → `create()` claim. **FIXED — LOW:** same-image-twice in one post (registry is written post-finalize; the picker now checks its own picked hashes). **FIXED — LOW:** dangling `thumbImage` after image removal → cleared in `applyAdminRemoveImage` + `onForumThreadRemoved`. **FIXED:** stale storage.rules "Forum surfaces ONLY" comment; forum-reply 4000-cap clamp before uploads burn; +2 rules regression tests (foreign-mint-on-update, >4-on-update). **CLEAN:** token mechanism (no path in body, `:scope` isolation correct, renumber off-by-one-correct), dedupe DoS (never deletes old/cross-user; registry CF-only), rules parity (merged-array nets hold; admin pin toggles safe), XSS (every src SDK-derived), **heart PASS** (purple receipts; kill-switch lamps admin-only).
**Recorded gaps (not built):** edit-strip leaves the Storage object live (no update-diff sweeper — gate-13 inherited, now wider; pre-cutover candidate); the review-DISCUSSION composer stays image-less (**BLESSED by Blake 2026-06-09** — deliberate, not an oversight); `[img:N]` is reserved syntax in prose; spoilered images are reveal-then-report; the admin TEXT-report preview doesn't show a doc's attached images. **PITCH:** per-image alt text, `[compare:1,2]`, pHash dedupe, reaction palette, orphan reaper.

### Tests — ALL GREEN (full post-fix re-runs)
`npm test` 130→**141** · `test:rules` 98→**104** · `test:functions` 35→**41** · `test:cf` 39→**44** · e2e 4→**5** (real-pixel: the seeded `[img:1]` renders INLINE, the lightbox opens on it, the chosen thumbnail decodes on th-new's card). Seed: th-opm/p0 carries an inline token + image; th-new carries `thumbImage`.
**Ops root-cause:** practice's "random deaths" = it ran inside the session's background task and was reaped at the task timeout (exit -1, ~10 min, twice). Now launched **DETACHED** (own cmd window). Also: `npm test`'s webServer and practice both bind **8765** — never run them together.

### Commit / deploy
Checkpoint **`1b4cffb`** = gate 11 + gates 12-14 (pushed; see disclosure above). **The overhaul itself is UN-COMMITTED** — lands on Blake's word (suggested: one commit "the image experience overhaul" after his smoke). Production untouched. NO deploy.

---

## THE MEGA-BATCH — overhaul commit + image/UX fixes + LIVE-IN-BOX composer + PROFILES (15-16) + ACCOUNT/DM (17-18). DONE (STAGED · NOT deployed)

Blake: "bless the gap, commit, go all out." The blessed gap (review-discussion stays image-less) is recorded above; the overhaul landed as **`84589d8`**; then the mega-batch in four pushed checkpoints: **`912b48b`** (Part A image/UX fixes + Part B live-in-box composer), **`c368982`** (gates 15-16 profiles), **`3587d10`** (gates 17-18 account + Message-Blake DM), **`31c50c7`** (the 5-lens adversarial-review fixes). All Blake-authored, zero trailers, excludes held out.

### Part A (the smoke fixes)
A1 ROOT CAUSE: the "period" inline image was a literal 1×1-pixel seed PNG — the resolver was always right (real-pixel-proven). Real 480×270 seeds + a 96×64 display floor. A2: `thumbImage` renders in the thread VIEW (anime cover still wins). A3: the topic pill rides the card ART when a thumb exists (`.hub-card-thumbwrap` + overlay; head chip auto-returns if the art fails to hydrate). A4: reviews get a dedicated **🖼 Add a cover** picker slot → `thumbImage` on the review doc (own-prefix rules pin), led on the collapsed row (`.row-cover`).

### Part B — the live-in-box composer (`live-composer.js`, window.RarLive)
All six USER composers (comment / comment-reply / tavern thread / tavern reply / review / review-discussion) are contenteditable rich editors — **no preview panel for users**; admin composers untouched. Architecture: the editor is a VIEW; the original textarea stays hidden as the **markdown MODEL + submit source** (counters/caps/locks/post-paths unchanged; the post-success `value=''`+dispatch pattern re-renders the editor). Completion engine (typed `**`/`*`/`` ` ``/`||`/`[img:N]`/`[t](url)` → live nodes), block mode styles `##`/list lines (markers dimmed-visible). XSS: text/plain-only paste (no getData('text/html') anywhere), nodes only via createElement/textContent, whitelist serializer (unknown elements → text; `<a href>` re-validated https), every downstream render = escape-first markdown.js. The picker integrates via `opts.insertText`/`pasteTarget` (caret-aware [img:N] insertion).

### Gates 15-16 — profiles
15: account.js dual-writes `profiles/{uid}` (joinedAt first-write-only; best-effort under the consent gate); `authorLiveSub` = profiles-first/users-fallback on all three live author-identity sites. 16: every author name carries `data-profile-uid` → `#profile=<uid>` (a z-8500 branded sheet: avatar/name/member-since/bio escape-first + their threads (forum authorUid query, new index) + reviews (items CG, now PUBLIC-list — docs were already per-anime public; threads/replies CGs stay owner-only)). `profileDecision`: Blake→Den · `isBanned`→suspended (the new CF mirror in `applySetBanState`, merge-creates so a no-profile banned user can't dodge it) · gone→former-member. Count-free, zero gold. **Ripple-audit catch:** `avatars/{uid}/profile.*` (live since v1.8.x) had NO match in the staged storage.rules — the cutover would have broken avatar saves; an owner-only 2MB jpeg/png match + tests now cover it.

### Gates 17-18 — account + the Message-Blake DM
17: cover-art ROOT CAUSE — legacy `al:<id>` saves missing type/aniListId FIELDS fell to the art-less catalog branch and never backfilled; both derive from the doc id (one normalizer, both lists). 18: the **Inbox** 5th tab (deep-link `#inbox`); Message-Blake = client-created `kind:'admin'` conversation (rules force Blake-as-party, size 2, creator-in); letter thread (textContent-only); unread = CF `lastMessageAt`/`lastSenderUid` vs the rules-legal `reads/{uid}` receipt; the dm Lantern ping routes here (a Blake reply renders GOLD by design — notifIsBlake; future peer dm = purple). **Blake's side:** `admin/inbox.html/.js/.css` (reports-shell clone; unread dots; Lock toggle = the rules' admin-only `state` branch; `?to=<uid>` find-or-create = the suggestion-reply channel; `admin/suggestions.js` Reply button; admin-fab link). `onDmMessageCreate` CF: ping with server-sourced identity, honors dm mute, CF-owned summary (`unread_<uid>` + `lastMessageAt` + `lastSenderUid`, never message text). The People folder = the locked peer-DM promise.

### Adversarial review (5 agents) — verdicts + fixes (all in `31c50c7`)
**CLEAN:** contenteditable/paste XSS (3 layers, "could not break it") · DM admin-floor invariant HOLDS (no stranger-to-stranger path; create/message rules quoted airtight) · items-CG widening leaks nothing (only comments+reviews use `items`, both already public) · heart PASS (all new user surfaces purple + count-free; admin inbox gold is a Blake surface).
**FIXED — HIGH:** profile "Their reviews" dead clicks (openNotifTarget got a parsed OBJECT; now the raw string path). **MED:** avatar `photoURL` users/-fallback had no origin gate → IP-beacon risk; `safeAvatar()` allowlist at all four render sites. **MED:** inbox self-unread (no self-sender guard + client-clock skew) → CF `lastSenderUid` + both inboxes skip self + server-time clamp. **DM rules:** the participant summary-update branch (forgeable lastMessageAt / unbounded lastMessageText / dead unread key) REMOVED — summary is CF-owned; only the admin Lock updates a conversation. **LOW:** dangling card thumb no longer strands the overlay chip (head chip returns via `has-thumb` drop). Reviews window 24→40 (comment-heavy under-report note).

### Tests — ALL GREEN (post-fix re-runs)
`npm test` **150** (gate6-veil flaked once under full-suite parallelism, 7/7 isolated — known family) · `test:rules` **107** · `test:functions` **41** · `test:cf` **47** (incl. dm ping/mute/ban-mirror) · e2e **5**. New: g17 live-composer (round-trip/paste-flatten/hostile-model), g18 profiles (heart/decision/name-routes), review-cover + items-CG + avatar rules tests, dm CF integration.
**Ops (root-caused):** practice dies if run inside a session background task (harness reaps at the task timeout) → launch DETACHED (`Start-Process cmd /c npm run practice`); `npm test`'s webServer and practice both bind 8765 — never together.

### Commit / deploy
Five pushed commits this session: `84589d8` · `912b48b` · `c368982` · `3587d10` · `31c50c7` (+ a docs-only close-out). Production untouched. NO deploy — the cutover order is now hosting→firestore→**storage**→functions with the indexes deploy (`firestore.indexes.json` gained forum authorUid+createdAt).

---

<!-- author: Code | date: 2026-06-09 -->
## The DREAM PROFILE/ACCOUNT batch — 2 small fixes + the full profile platform (STAGED, no deploy)

Mode **ULTRAMAX**. Blake's mandate: the account/profile becomes a "social-media platform for user customization — THE WORKS." Built the full set; nothing PITCHed out (the scope was tractable on the paid-for backend). One pushed checkpoint. **All tracks green, prod untouched, nothing deploys.**

**Design vision:** the profile is a *constellation* a member arranges — bio, self-tags, a curated (deliberately gold-free) accent, a status line, a pinned review, an image/GIF sky behind it — and the account page is the studio where they arrange it, wearing its own personal night (the rotated neon-sakura street under the same constellation veil) so the place feels theirs without ever leaving the site's vocabulary. The community gets ONE new currency — *Appreciate*, a purple count that is the sanctioned heart carve-out — and nothing else: no post counts, no karma, no leaderboard, gold still only Blake's.

### Two small fixes
1. **Review-cover redesign (fix 1):** the 48x34 inline thumb ("terrible and not large") -> a cover-carrying row now leads with a full-width **banner strip** above a stacked headline (`.row-cover-banner` + `.row-headline`; `has-cover` class; hover-zoom reduced-motion-gated; a dangling cover drops the whole banner via `dropImg`). Cover-less rows untouched.
2. **Pfp OR name -> profile (fix 2):** the author avatar now carries `data-profile-uid` at all three live sites (comment `commentItemEl`, reply `buildReplyItem`, review row) + keyboard (Enter/Space) parity; the existing one delegation opens the sheet. **Ripple catch:** the INITIAL avatar paints fed doc `photoURL` straight to `<img src>` (rules only `is string`, not origin-pinned) -> joined them to the `safeAvatar()` allowlist too.

### The dream platform
- **Customization (rules `profileCustomFieldsOk`):** `bio` (<=500, escape-first), `status` (<=80), `tags` (list <=6, per-entry clamp at render), `accent` (enum [violet,ember,teal,rose,sky,moss] — **no gold in the palette by construction**), `bgRef` (own-prefix `uploads/{uid}/profilebg/*`), `featuredAnime` (charset-pinned communityKey). profiles create/update key-sets widened; `likesCount` stays CF-only.
- **Profile background (image/GIF):** rides the EXISTING upload pipeline unchanged (docId `profilebg` parses like any upload -> `processUploadedImage` magic-byte-validates + EXIF-strips + caps it; no SVG; 5MB). Reportable (target type `profile`), admin-removable (atomic via `adminRemoveImage`'s new profiles branch), and **edit-strip-swept**: `onProfileWritten` deletes the OLD object when bgRef moves/clears/the doc deletes (the gap NEXT.md flagged — now closed for backgrounds; owner-prefix-fenced; the unchanged-bgRef no-op is the hot-path early return since the CF fires on every likesCount bump). Perf: reduced-motion freezes the GIF to a display-scale-capped one-frame canvas; the bg-wrap is its own compositor layer.
- **Profile likes — the heart carve-out:** `profiles/{uid}/likes/{likerUid}` ({uid,value:1,updatedAt}); like-only, no self-like, **no liking Blake's profile** (rules + CF), unlike=delete. `onProfileLike` owns `profiles.likesCount` (set-merge on +1 so a profile-less member still materializes the count; update() on -1 so an unlike racing deletion can't resurrect a ghost) + a **deterministic `pl_<liker>`** ping via `.create()` (one appreciation per liker, ever — kills the unlike/re-like relight). Purple everywhere; the ONLY count on any profile.
- **Activity by type:** the sheet's chips (Threads/Reviews/Comments/Replies) + the account tab's filter, fed by the now-public `threads`/`replies`/`posts` collection-groups (widened on the same argument as the gate-16 `items` CG — each doc already public in context; `votes`/DM CGs NOT widened). New index: `posts` authorUid+createdAt CG; `likes.uid` field override.
- **Watchlist/favorites controls:** a client-side view layer (filter . sort recent/A-Z/year . type all/Blake's-44/Beyond) over the live snapshot; `al:<id>` + `#open=`/`#secondary=` routing intact.
- **Account studio + own night:** `account.html` -> `data-surface="account"` (veil 0.62 — personal-cozy, never the Den's 0.80) + the veil pulse + `assets/account_background.png` rotated 90degCCW to 2400x1200 (+ a 568KB webp derivative on `body::before`). A live preview, tag/accent editors, bg picker, featured-review picker. Panels are higher-alpha static (NOT backdrop-filter — the adversarial perf fix).
- **Go-all-out extras shipped:** accent themes . featured-review pin (fetches the owner's OWN review only) . status line . join-date flair . inviting empty states.

### Adversarial review (5 lenses) — verdicts + fixes
**CLEAN:** upload/render surface (magic-byte + own-prefix pin + SDK-derived src + fenced sweep all hold; every new innerHTML site escape-first; bio via whitelist markdown) . privacy (the CG widening exposes NO private collection — DMs use `conversations/messages`, unmatched by the threads/replies/posts wildcards; tombstones carry no surviving body; likes subcollection is get-own-only/list-false) . heart (palette gold-free, like surface purple, no leaderboard, Den stays darkest).
**FIXED — HIGH:** the LIVE `script.js` lantern renderer (index.html loads it, not lantern.js) never got the avatar origin-gate its twin received -> a `profile_like` ping's server-copied `fromPhotoURL` (senderIdentity's users/-fallback, rules-unvalidated) could IP-beacon every recipient. Mirrored the `safeAvatar()` gate; also gave script.js the `profile_like` glyph + mute chip it was missing.
**FIXED — MED (heart):** the rules let any member mint the carve-out count onto **Blake's own** profile (likes create only checked `likerUid != uid`) -> added `uid != ADMIN_UID` to the rule + a CF short-circuit.
**FIXED — MED (spam):** unlike/re-like **relit** the victim's bell (the ping used `.set()`) -> switched to `.create()` (create-if-absent).
**FIXED — MEDx3 (perf):** account panels' live `blur(7px)` over the animating veil pulse -> static high-alpha (the v1.8.0 lesson); GIF bg layers given their own compositor layer; reduced-motion freeze-canvas capped at display scale; the account preview no longer rebuilds (restarting the GIF) per keystroke.
**FIXED — LOW:** undercount/negative-drift on +1 to a profile-less member (set-merge); a forged `profile` report previewing an arbitrary victim object in the admin queue (bound imagePath to the target's profilebg prefix); profile-sheet Threads/Replies chips re-querying per click (memoized); lazy-init the account activity feed (was ~110 reads + 2 live listeners on every load for a hidden tab).
**INFO (accepted):** signed-OUT cross-context activity aggregation is now possible — but each datum was already public; this is Blake's explicit full-public-profiles call. A like FROM Blake renders gold (the dm precedent — gold marks Blake-origin, not the carve-out count, which stays purple).

### Tests — ALL GREEN
`npm test` **158** (150 floor + 8 new g19; season-label flaked once under full-suite parallelism, 3/3 isolated — known family) . `test:rules` **126** (107 + 19) . `test:functions` **56** (41 + 15) . `test:cf` **57** (47 + 10) . e2e **9** (5 + 4 real-pixel). New: `tests/g19-dream-profile.spec.js` + `tests-e2e/dream-profile-emu.spec.js`. Real-pixel walk: account studio + watchlist controls + activity + profile sheet + review banner screenshotted; a live like as Ren moved Mika's CF-owned count 2->3 (fresh read confirmed).

### Verify-don't-trust note
The SHIP-PROMPT diagnosed the bg asset as "SIDEWAYS — rotate first"; confirmed against pixels (1200x2400 portrait of a sideways street) and rotated 90degCCW to a correct 2400x1200 landscape.

### Commit / deploy
One pushed checkpoint this batch. APP_VERSION stays **1.9.1** (staged — the version bump + all rules/CF/storage/index deploys happen ONLY at the gate-21 cutover on Blake's "ship it"). Production untouched. **NO deploy.** Cutover deltas: `firestore.indexes.json` gained `posts` authorUid+createdAt + the `likes.uid` override (indexes deploy BEFORE clients query); `storage.rules` unchanged (profilebg rides the existing `uploads/` match — no new Storage artifact).

---

<!-- author: Code | date: 2026-06-09 -->
## ACCOUNT/PROFILE OVERHAUL ROUND 2 — "a real social platform" (STAGED, no deploy)

Mode **ULTRAMAX**. Blake's verdict on round 1 — "it just looks incomplete… doesn't look like a real social media app" — drove a Discord/Reddit-grade restructure. One pushed checkpoint. **All tracks green, prod untouched, nothing deploys.**

**Design vision:** the account becomes a real settings PLATFORM — a grouped left rail (Identity / Collections / Community / Settings) where every section owns a full-width panel; the profile editor goes wide with a social-grade hero preview; an Edit <-> Public-view toggle shows the saved identity exactly as the room sees it; and the community-rules consent happens IN PLACE (a 🔓 banner + the branded modal), killing the "go comment somewhere to accept the terms" dead-end. Every label is a pill, every motion is transform/opacity-only, and the heart law holds untouched.

### Blake's items 1-7 (all built)
1. **WIDER:** the canvas grows to 1340px; the panel host takes the full column; the editor is a 2-col grid of branded field cards; the preview is a ~240px HERO (bg full-bleed, 104px avatar, name/status right, tags+bio composed) with a breathing border ring.
2. **"See what viewers see":** an Edit <-> 👁 Public view pill toggle; viewer mode renders the SAVED identity with the live sheet's exact vocabulary (bg via getDownloadURL, accent, tags, markdown bio, featured pin, the Appreciate row coerced-to-0 like the live page) + an honesty foot ("activity loads on the live page · viewers can also report a profile").
3. **Each section its own surface:** SIX full panels (Profile / Watchlist / Favorites / My Activity / Inbox / Account) behind a grouped rail with icons + an animated accent bar; Watchlist/Favorites gain a list <-> COVER-GRID view toggle on top of the round-1 filter/sort/type controls.
4. **Settings split out:** email/password/session move to their own "Account" nav item — grouped set-cards (Email / Password / Session with member-since flair + sign-out); same control IDs, machinery untouched.
5. **Tags via a branded dropdown:** a grouped curated catalog (Genres / Watch style / Identity — 40 anime-flavored entries incl. Sakuga nerd, Lore historian, Waifu connoisseur, Gacha survivor, Night-shift watcher…), worn-state ticks, multi-pick stays open; custom tags still ride the input; <=6 cap + 24-char clamp intact.
6. **The consent dead-end (REAL bug) FIXED:** consent extracted to a NEW shared module `consent.js` (script.js + account.js both import it — ONE implementation, the round-1 lantern lesson applied; modal markup/copy verbatim, g3 specs green). The account shows a 🔓 unlock banner while unaccepted; "Read the rules" opens the same branded modal; "I agree" calls the `acceptRules` CALLABLE (CF-minted, never self-attested). Save offers the rules in place when gated. Proven END TO END in the emulator: newbie -> banner -> modal -> accept -> unlocked -> full customization save persists.
7. **Unique animations (all compositor-only + reduced-motion-silenced):** panel slide-in, hero breathe ring, tag-chip pop, dropdown drop, consent-glyph float, Appreciate heart beat, rail accent bar.

### Adversarial review (5 lenses) — verdicts + fixes
**CLEAN:** consent security (CF-only minting holds; no self-attestation path; the suspended state can't be bypassed — rules deny server-side regardless) · the upload/bg surface through the new flow · zero gold / zero illicit counts in every new surface · reduced-motion silencer verified COMPLETE.
**FIXED — HIGH ×2:** (1) `addDoc` was USED by the gate-18 inbox but never IMPORTED — every "Message Blake"/Send threw ReferenceError since the mega-batch; nothing drove the flow, so no spec caught it. Import added + a NEW e2e that performs a real send (the letter lands via the live onSnapshot). (2) The round-1 520px `.is-tab` height-lock beat the round-2 panels by specificity — the profile editor spilled ~670px past the painted card on desktop. Unlock + a computed-style spec.
**FIXED — MED ×4:** the consent session-cache skipped the suspended re-check for a user banned MID-session (ensureConsent now always re-reads; the cache only softens transient read errors) · the save message conflated 'suspended' with 'cancelled' (branched copy; the banner pointer is now always real + wired) · viewer mode ignored `isBanned` (now renders the tombstone viewers actually see) and demoted BLAKE to a "MEMBER" sheet (now: "your name leads home to the Den") · viewer bio was esc()-only while the live page renders markdown (account.html now loads markdown.js; same renderer).
**FIXED — LOW ×4:** `#inbox-dot` ignored [hidden] (the same display-vs-[hidden] trap class — a fake unread dot painted forever; symmetry rule + spec) · viewer name fell back to AUTH displayName (private PII presented as public; profiles-only now) · a missing likesCount hid the Appreciate row viewers always see (coerced 0) · the bg-partial-save message implied nothing saved (honest copy) · the gold-adjacent #ffdf96 verify text in the all-purple settings card (purple now).
**ACCEPTED (INFO):** consent.js/lantern.js bare-import cache-busting stays the post-cutover item (noted in both files); viewer-mode refetch per toggle (user-initiated, low frequency); the hero breathe ring's persistent compositor layer (~1MB, verified opacity-only).

### Real-pixel walk (the round-1 lesson applied)
Screenshotted every panel + the newbie consent flow against the seeded sandbox. The walk CAUGHT the consent banner's [hidden]-vs-display:flex trap (the attribute said hidden, the pixels said visible — the exact "class presence lied" class) and the panel spill; both fixed + codified as computed-style specs. The walk itself must wait for "PRACTICE MODE READY" (the static server answers before seeding finishes — an early walk reads a half-seeded gate).

### Tests — ALL GREEN
`npm test` **166** (158 + 8: five g20 IA/consent contracts + the clamp/dot/addDoc specs) · `test:rules` **126** · `test:functions` **56** · `test:cf` **57** · e2e **10** (9 + the real inbox send). All emulator tracks re-proven after the fixes.

### Commit / deploy
One pushed checkpoint this batch. APP_VERSION stays **1.9.1**. Production untouched. **NO deploy.** New cutover note: `consent.js` is a NEW hosted file (verified NOT in the firebase.json ignore array — the leak-mirror check); it and lantern.js share the post-cutover ?v= version-busting item.

---

<!-- author: Code | date: 2026-06-09 -->
## ROUND 3 (account polish, go-all-out) + GATE 19 (per-season comments) — STAGED, no deploy

Mode **ULTRAMAX**. Blake's relay: "IT LOOKS AMAZING… jaw dropped" on round 2 — round 3 is the completeness pass + his banked left-side idea. One pushed checkpoint. **All tracks green, prod untouched, nothing deploys.**

### PART A — the polish items (2-8)
2/3. **Headings:** `.acct-kicker` panel heads went full-bright (#f6efff, 1.22rem, purple glow) with a **swaying 🏮 lantern** (transform-only, alt-text'd out of the a11y tree, reduced-motion-silent); every panel sub got a Blake-voiced line ("This is your corner of my site — shape it, color it, sign it").
4. **THE FOCUS SWEEP (recurring bug):** a site-wide floor at the end of style.css — `:focus{outline:none}` + `:focus-visible{2px purple ring}` — zero UA blue/white anywhere; component glows still win by specificity; `.rar-live`/`.filter-narrow` got explicit exceptions (they carry their own glow — the floor was double-ringing them); codified as REAL-pixel specs (keyboard focus = purple, mouse focus = none). ⚠️ Scope honesty: ALL admin pages load style.css too, so the floor reaches them — spot-checked benign (admin rings have higher specificity) but an admin keyboard walk is Blake-side homework.
5. **My Activity intuitive:** every row deep-links to the EXACT comment/reply/review **with the halo** (`#notif=<doc path>` — the gate-6f machinery; al: rows land on the SECONDARY via the new bridge); review rows show the **anime cover thumbnail** (regex-pinned `assets/` lookup, broken-image falls back to the type glyph); type glyphs on the rest; whole-row click target.
6. **Inbox complete:** letter bubbles (yours purple-right, **Blake's wearing his gold edge — keyed on sender IDENTITY**, not perspective: the adversarial panel caught the old rule gilding MEMBER letters on Blake's own view), premium Message-Blake card, styled composer + People-locked promise.
7. **Settings professional:** hover-lift set-cards + the verify state as a branded pill (and the gold-adjacent #ffdf96 text went purple-family).
8. **Go-all-out:** a branded **"Saved — looking sharp ✓" toast** (1.8s runway, then the consistency reload); review deep-links now **auto-expand** the landing row (the old "halo on a closed box"); the halo poll runway 6s→16s (the secondary lands after a live AniList fetch); the inline-edit sweep scoped per-surface (it was silently eating the OTHER modal's unsaved draft).

### PART B — GATE 19: the season room (Blake's left-side idea, study Q7a)
The secondary modal's LEFT column gets **"THIS SEASON'S ROOM 話せ"** DIRECTLY BELOW Blake's review (the H5 invariant is DOM order — verified: `secondary-review → secondary-community → secondary-desc`). Zero new schema: the comments family (markup/subscribe/wire) generalized from `animeSlug()` to **`communityKey()`** — catalog slugs identical, `al:<id>` for the secondary — so the season room IS the comments system: the live-in-box composer, consent gate, inline images + spoilers, report/mod/lock, votes + rate-limit CFs (all wildcard-path, colon-safe). Wired per-load with teardown (re-render/close); **deep-linkable**: `comments/al:<id>` notif targets land on the secondary via `openAnimeFromId`'s al: branch + `window.openSecondaryFromKey`, halo intact (proven: flash at t≈1.8s on the seeded comment). **Room only where Blake's presence precedes:** gated on `isWatched` (adversarial MED — never-watched AniList ids got a postable room with nothing of Blake's above it + moderation reach he'd never visit). Seeds: `comments/al:21386` (OPM Season 2) carries two comments incl. a spoiler pill.

### Adversarial review (5 lenses) — verdicts + fixes
**REFUTED (recorded):** security parity holds end-to-end (consent/escape-first/image-pin/rate-limit/vote CFs all inherit); catalog↔al: collision impossible; the boot-race + href-XSS attacks on the deep links fail; mobile.css carries no competing focus rules; the lantern ×6 only ever animates the visible panel; reduced-motion silencers complete.
**FIXED — HIGH ×3:** (1) `subscribeComments`' unsub arrays were FUNCTION-STATIC — gate 19's first-ever concurrent two-room situation had either room's snapshot killing the OTHER's live author/vote listeners (frozen names, vote-highlight death) → per-instance arrays. (2) `wireComments` leaked a permanent `onAuthStateChanged` observer (pinning detached composer DOM) per call, multiplied by every season hop → captured + torn down with the surface (and the auth callback now respects a Blake-locked thread). (3) The secondary dialog never took/restored focus — keyboard focus stayed INVISIBLE behind the overlay while gate 19 added form controls to it → focus on open (outline-suppressed, the welcome-door precedent), restored to the opener on close. (Tab containment = noted follow-up.)
**FIXED — MED:** the never-watched room (gated, above) · the inbox gold inversion (identity-keyed, above) · review deep-links land OPEN (above) · the `.rar-live`/`.filter-narrow` double-ring (above).
**FIXED — LOW:** per-surface inline-edit sweep · lantern a11y alt-text · thumb onerror fallback · toast runway.
**ACCEPTED (noted):** the al: room + its deep-link halo depend on a live AniList fetch (cold/429 = late halo; the 16s runway mitigates; decoupling the room from the detail fetch is the clean follow-up) · a recurring e2e for the al: landing needs an AniList stub (walked real-pixel instead; the flash-log proof above) · `loadSecondary` generation-token race + the 280ms close window (pre-existing secondary machinery, noted for the cutover-polish list) · admin-pages focus walk (Blake-side).

### Tests — ALL GREEN
`npm test` **170** (166 + 4 g21: the two real-pixel focus specs, the bright-heading contract, the gate-19 routing seams) · `test:rules` **126** · `test:functions` **56** · `test:cf` **57** · e2e **10** (warm; the first-run failures were the documented boot-race — practice must be warm before e2e). Real-pixel walk: the season room mounts below Blake's review (H5 order verified in DOM), a live post lands through the live composer, the al: deep-link opens the SECONDARY with the halo, an activity row deep-links + halos, the account heads wear the lanterns.

### Commit / deploy
One pushed checkpoint. APP_VERSION stays **1.9.1**. Production untouched. **NO deploy.** Gate map: 19 ✅ — remaining: **gate 20** (welcome catch-up + cherries + the imageRefs edit-strip sweeper on the 5 remaining surfaces) → **gate 21 THE CUTOVER**.

---

<!-- author: Code | date: 2026-06-10 -->
## ROUND 4 — account overhaul (the two recurring fixes) + Personal Collections + the room moves LEFT (2026-06-10, STAGED)

### The two RECURRING items — root causes, not coats of paint
- **Dim headings (failed 3x):** never a color value. The v1.8.3 persistent-header-bar CSS targets bare `header`; round 2 introduced semantic `<header class="panel-head">` in every account panel, so the site header's ::before gradient scrim PAINTED OVER the static heading text (positioned-above-static paint order; the site header survives via its z-lifted .header-inner). Computed-style specs kept passing — computed color is not painted pixels. Fix: all bare `header` rules scoped to `body > header` (13 rules in style.css + 1 in mobile.css; every page header is a body child, admin pages included). Pixel proof: kicker maxLum 195 -> 242 (=#f6efff exactly), bright-pixel coverage 0.0% -> 16.6%, Chromium + Firefox. The Tavern's hub-header rode the SAME leak (the old "tavern text" dimness — same class); .hub-header--tavern now owns its flex row explicitly (80px, space-between); the thread header tightened to content height (deliberate). Profile-modal kickers (#cbb0ff) were the last declared-dim ones — brightened. Panel subtexts wear a glowing pill; the Constellation subtext is Blake's new copy. NEW TRIPWIRE SPEC: a nested <header> carries no scrim/sticky/80px; the body-level header KEEPS its backdrop.
- **White-outline dropdowns:** all three native <select> (featured/watchlist-sort/favorites-sort) replaced by brandSelect (button + role=listbox, arrows/Home/End/Esc, roving focus). Zero <select> in account.html — spec-pinned. Dead native-select CSS pruned.

### Built
- **cropper.js (NEW shared module):** FRAME IT crop dialog — avatar circle->512², background 16:9 -> <=1280w; drag/wheel/pinch + keyboard; GIFs bypass (animation preserved); pure clamp math on window.cropperModel (spec-pinned). Wired into both picker flows.
- **Hover pickers:** the hero preview is the picker (Change avatar on the circle, Change background top-right; hover/focus, compact band on touch). The Choose-button row is gone; the bg thumb is a button.
- **Accents 6 -> 16** (+crimson/indigo/peach/pearl, 6 gradients via --pf-accent2, Glow switch -> accentGlow bool) — rules enum + script.js viewer list + account.js PROF_ACCENTS in lock-step; shared :is(.profile-sheet,.acct-preview) CSS map; NO gold key (spec-pinned).
- **Saved-row covers:** catalog rows ride the rich row (imageById asset covers) in list + grid.
- **PERSONAL COLLECTIONS:** users/{uid}/collections/{id} {name<=60, public:bool, items[]<=200 (untyped maps — escape-first + scheme-gated at render), createdAt, updatedAt}. Rules: owner CRUD; public!=true || gateOk() on create/update (private shelves work pre-consent — watchlist parity; PUBLISHING is the community act); get/list public==true for non-owners (the where clause IS the contract); admin read/delete bypass; delete ungated. +13 rules tests (104->117). Composite index collections(public ASC, updatedAt DESC) — deploys at cutover BEFORE clients query. UI: rail item under Collections 棚, composer (brandSelect visibility), inline rename, 2-tap delete, searchable add-picker (saved lists + the 44), per-item remove; consent modal fronts publish. Public profile renders shelves as cover strips (purple, count-free) between the featured pin and activity.
- **The room LEFT (Blake item 9):** .secondary-body.has-room = [room minmax(225px,.78fr) | main 1.5fr | side 1fr] at >=1150px; room is DOM-SECOND (review first — H5), grid-placed left; 900-1149px the room spans full width BELOW the columns (never squeeze the review); <=900 stacks. Room rail is compact + self-scrolling (56vh).

### Adversarial (5 lenses: collections-rules / cropper / heart / XSS / perf; refute-verified)
**8 MED confirmed, 0 refuted, all fixed:** featured-pin wiped by dropdown init on failed/evicted load (3 lenses; now only explicit picks write) · 201st add silent false-success (cap guard + message) · dedupe CF deleted identical re-uploaded profilebg -> dangling bgRef (profilebg now dedupe-exempt in functions/index.js — one-per-user makes dedupe meaningless) · solid accents invisible on the hero (border map missed .acct-preview) · tmp diagnostics in the deploy root (the gitignore<->firebase leak class — deleted) · snapshot-mid-rename ate typing (render defers while input open) · touch veil hid the avatar (bottom band) · 900-1149 squeezed the review (3-track >=1150 only). **7 LOW fixed** (2MB avatar cap to match rules, cropper re-open latch, objectURL revoke, focus restore, pre-auth collections tab, shelves-error console.warn, halo-clip padding). **Accepted:** items[] untyped within 200-cap+1MiB; public-shelf edits rely on the rules gate client-side; cross-device shelf edits last-write-wins.

### Tests — ALL GREEN
`npm test` **181** (g22-round4: 11 new static contracts incl. the scrim tripwire both ways, native-select-zero, cropper clamp math, gradient/glow/no-gold, room-rail grid + H5 DOM order) · `test:rules` **139** · `test:functions` **56** · `test:cf` **57** · e2e **10**. g21 flaked once under full parallelism — green isolated (the documented class).

### Commit / deploy
One pushed checkpoint. APP_VERSION stays **1.9.1**. Production untouched. **NO deploy** (rules/index/CF deltas ride the cutover). Remaining: **gate 20** (welcome catch-up + cherries + the imageRefs edit-strip sweeper) -> **gate 21 THE CUTOVER**.

---

<!-- author: Code | date: 2026-06-10 -->
## GATE 20 — round-4 smoke fixes + the catch-up door + the cherries + the sweepers (2026-06-10, STAGED)

### Part A — Blake's 10, root causes
- **Items 3/4/5 (brandSelect never collapsed):** `.acct-dd-menu{display:flex}` beat the UA `[hidden]` rule — the consent-banner trap class, third scalp. Fix: `.acct-dd-menu[hidden]{display:none}`; live-verified (rest closed / click open / Esc closed); g23 pins it.
- **Item 1 (welcome signed-out):** the door was never auth-gated — the repro was an in-tab SIGN-OUT (the once-per-session flag survived). Sign-out now clears `rar:welcomed` (script.js + doSignOut) — a sign-out is a fresh arrival.
- **Item 2:** avatar/bg form rows REMOVED; hidden staging buffers (avatar-pick/avatar-file/acct-bg-file) live under the hero; a Remove-background hover pill joins Change; the legal note moved under the hero.
- **Item 4 copy:** all six panel subtexts rewritten plain (no more intimate-creepy lines).
- **Item 6:** add-picker pool = watchlist+favorites FIRST then the 44; >=3 chars live-searches AniList (debounced 350ms, AbortController, stale-guard case-folded, rows under "From the wider world" — provider-name-free).
- **Item 7:** collections `description` (optional, <=200, rules + 5 tests), inline editor (self-removing on commit — see adversarial), rendered on the card + the public profile (escape-first).
- **Item 8:** `activityPlain()` strips markdown + maps [img:N]->🖼 (textContent only); rows framed/clamped (overflow-wrap anywhere).
- **Item 9 (4th ask):** Message-Blake premium — flex hero card (swaying lantern + type stack + gradient Write-to-Blake pill), thread title bar, framed messages surface, branded empty state, chip back, Send pill. PIXEL-PROVEN (screenshots in the round).
- **Item 10:** the season room = `.secondary-room-band`, full-width, AFTER `.secondary-body`, BEFORE the recs block (Blake's circled spot). The round-4 left rail + its compact/3-track CSS retired. g22's room spec rewritten to the band contract.

### Part B
- **Catch-up door (`initWelcomeCatchup`, called FROM openWelcome):** 3 progressive signals — new-44-since-last-visit (localStorage `rar:seenSlugs` diff; snapshot advances ONLY when the door shows), unread lanterns (limit-10 vs lastSeenAt), watchlist∩airing (6h `rar:doorAiring` cache). Bottom-strip layout (no card reflow). Live-verified: seeded stale snapshot -> "Blake added 43 new reviews" + 4 sub-buttons -> deep-link landed.
- **🎲 Surprise me** on the door (rides #random-btn). **👁 requested chip** on the secondary (suggestionCounts GET public / LIST admin — the anti-brigading intent kept; 99+ cap). **Gold-flip ping half:** `onSuggestionReviewed` (status flip new->reviewed, admin-only by rules) -> gold Blake-origin `request_done` letter -> lands via the NEW `secondary/<id>` notif-target kind (script.js + lantern.js, anchored regex); rollup status mirrored via update() (no ghost docs); mute chip added. The MIGRATION half banked with design (NEXT 20.5).
- **Footer premium pass** (kicker vocabulary + gradient panels + scroll-reveal; Blake's copy untouched). **Start Mode 1.bat** (+ `*.bat` hosting-ignore).
- **THE SWEEPERS:** edit-strip on 5 imageRefs surfaces (3 new onDocumentUpdated + 2 riding existing forum triggers; `stripSweepDecision` in functions/lib/sweep.js — present-before-absent-after only, thumbImage protected, malformed-after keeps) + `reapOrphanUploads` daily (24h grace, keep-on-uncertainty, 500 cap, 540s/512MiB). +7 cf-tests, +21 unit.
- **Admin text-report image thumbs** (agent lane; SDK-derived URLs, capped 4). **firebase.json ignore sweep:** +tests-e2e/**, playwright.emu.config.js, *.log, *.bat, tmp* (and `tmp*` into .gitignore — the leak class structurally dead).

### Adversarial (5 lenses, refute-verified: 20 confirmed = 2 HIGH + 7 unique MED + 8 LOW, 0 refuted)
HIGH: catch-up signal 3 dead-on-arrival (ran pre-door + snapshot eaten every load — 4 lenses; re-wired into openWelcome, live-verified) · tmp screenshots in the deploy root (leak class — structural ignore both mirrors). MED fixed: warm-auth race (same re-wiring) · door-stack Esc containment · Enter-on-door-button passthrough · desc/rename editor deadlock + blur double-write (inputs self-remove on commit) · airing network-per-session (6h cache) · late-button card reflow (bottom strip) · chip integrity (99+ cap, accepted as demand signal). LOW fixed: malformed imageRefs keeps · reaper limits · request_done mute chip · ghost rollup via update() · search stale/case-fold · clamp overflow-wrap. Accepted: admin re-flip duplicate pings · the sweeper's owner-level (not doc-pinned) fence (deliberate — path tokens ≠ doc ids; self-harm only).

### Tests — ALL GREEN (new floors)
`npm test` **192** (+11 g23) · `test:rules` **146** (+5 desc, +2 chip; ONE OLD TEST FLIPPED deliberately: non-admin suggestionCounts GET is now ALLOWED — rules.spec.js ~L275) · `test:functions` **77** (+21) · `test:cf` **64** (+7) · e2e **10** (the review-halo spec reds on first-run-after-boot and greens warm — the documented race, 2× today).

### Commit / deploy
One pushed checkpoint. APP_VERSION stays **1.9.1**. **NO deploy.** Remaining: optional **20.5** (NEXT: gold-flip migration · season-room decouple · secondary hardening trio · users-email tightening) → **gate 21 THE CUTOVER**.

---

<!-- author: Code | date: 2026-06-11 -->
## GATE 20.5 — the LAST build gate: smoke fixes + FRAME THEMES + the catch-up sheet + the pre-cutover wants (2026-06-11, STAGED)

### Part A roots
- **Launcher (item 12):** the gate-20 .bat shipped with bare-LF endings — cmd mis-parsed it (the half-second flash). Rebuilt CRLF + npm check + pause-on-every-exit; verified by REAL double-click (server on :8888, window persists).
- **Catch-up destination (item 3):** the door buttons open THE CATCH-UP SHEET (#catchup-sheet — a full veil surface: NEW-FROM-BLAKE cover rail / lantern letters w/ gold edges / airing chips; sections from catchupState; viewing pings MARKS THEM SEEN like the real lantern). Door strip enlarged (capped 34vh + a short-viewport breakpoint). Sub-chips stay inline.
- **Item 2 (door rule, verified):** fresh visitors ALWAYS get the door (live-verified anon); once per session; returns on new tab/session/sign-out. Honest edge: browser tab-RESTORE revives sessionStorage and skips the door — the likely "sometimes" Blake saw.
- **Gradients (item 9):** round 4 never rendered one (two SOLID rings). Now: an injected `.pf-accent-ring` (mask-composite trick) wraps the WHOLE card; glow aura on the HOST (a mask clips its own shadow); composes with frames.
- **FRAMES (item 9b + Blake's chat ask):** frames.css/frames.js (NEW; agent-built, pixel-self-verified) — 12 user themes (vines/stone/cosmos seeds + witch=Re:Zero, clover=Black Clover w/ the one five-leaf, journey=Frieren, shadow=Eminence, sakura/ember/tide/storm/moonlit) + **'blake' = THE DEN KEEPER (gold filigree+lantern), RULES-LOCKED to the admin uid** (non-admin write denied + tested; the picker tile renders only for his account — courtesy, not security). data-frame on .profile-sheet/.acct-preview; render on hero + viewer + live profiles (display-validated incl. blakeOnly). profiles.frame enum in rules lock-step.
- **Rooms everywhere (item 10):** seasonRoomHtml(meta, isWatched) — watched keeps its lead-in; NON-watched gets Blake-voiced copy ("I haven't reviewed this one (yet)…"). Moderation reach unchanged (same comments machinery). Error-state rooms = WATCHED-ONLY (a successful fetch is the existence proof for arbitrary ids — the graffiti-keys bound).
- **Item 11 (chip chain) — verified end-to-end TWICE live:** request → chip → Mark-reviewed → migration offer → GOLD letter → chip retires. Root-caused Blake's failure: **suggest.js NEVER wrote submitterUid** (rules-ready since gate 18; the admin Reply + the gold-flip both dead). Fixed (signed-in attach; anon stays anon).
- Items 1/4/5/6/7/8: Surprise-me removed (spec-pinned) · "Blake's reviews" rename audit (chips/adder/Tavern verdict/fallbacks) · one-line row headers (status≡display-name alignment) · activity feed grid + 3-line clamp · DM Blake rename · hover pills scoped (:has split into its own rule for engine safety).

### Part B
- **migrateRequestThread** (callable, admin-locked; functions/lib/migrate.js; query tag=='anime:al:<id>', cap 50, tag-only update) + the admin suggestions picker (skippable; two-click confirm; cap→"more may remain" copy). +3 cf tests (cf-cascade.spec).
- **Decouple:** the room mounts on a failed detail fetch; `alId` always set (was `detail ? id : null` — starved the error branch); wireComments on id alone.
- **Hardening trio:** Tab containment (secondary + sheet, disabled/invisible filtered) · secondaryLoadGen token + the 'active' close-window guard in stillMine() · the close-timer reopen-cancel (secondaryCloseTimer).
- **users/{uid} GET tightening: BUILT THEN REVERTED IN-GATE** — the panel proved it tombstones ACTIVE pre-Studio members (profiles docs are Studio-save-only; the index editor writes users/ only). Re-banked behind a profiles backfill (NEXT). The spec pins the RESTORED public contract so the flip can't sneak back.
- `*.ico` + `tmp*` in both ignore mirrors.

### Adversarial (5 lenses, refute-verified: 17 confirmed = 2 HIGH + 8 uniq MED + 12 LOW, 0 refuted)
HIGH: the accent ring collapsed in-flow on framed/bg sheets (the content-LIFT rules caught it — `:not(.pf-accent-ring)` on both lifts, spec-pinned) · the sheet close-timer wiped the deep-link target's scroll lock (conditional release). MED fixed: pings-never-marked-read · the users revert · error-room bound · sheet Tab trap · short-viewport strip cap · the masked-glow · migration cap-copy + confirm. LOW fixed ×8 (reopen guards, focus restores, frames.js-missing save guard, :has split, focusable filters). Accepted: suggest auth-race uid drop · post-migration stragglers · the 300ms scrim. Pre-existing logged: the ANON #secondary= boot-route never worked (banked, cutover polish); cf-cascade waitFor 20s→45s (trigger volume grew; heaviest pass sat at 20.3s).

### Tests — ALL GREEN (new floors)
`npm test` **200** (g24 ×8) · `test:rules` **152** (+6; the users matrix re-pinned PUBLIC) · `test:functions` **77** · `test:cf` **67** (+3) · e2e **10**.

### Commit / deploy
One pushed checkpoint. APP_VERSION **1.9.1**. **NO deploy.** REMAINING: **gate 21 = THE CUTOVER** only.

---

<!-- author: Code | date: 2026-06-11 -->
## GATE 20.6 — the final polish round (Blake's 20.5 smoke, 9 items). STAGED, no deploy.

### Part A (Blake's numbering)
1. **Cropper accuracy + brightness parity (the headline):** (a) capture was 16:9 vs ~4:3 render windows → GEOM.background now 480×360 / 1280×960 out (`cropper.js`; bgAspect exposed for g25); ramp-image pixel walk proved framed edges survive cover onto the card. (b) the 0.38 img opacity + full-card scrims are DEAD on both surfaces — full-brightness picture + two LOCALIZED caps (top 0.50→0@26%; floor 0.88→0.45@22%→0@52%) + text shadows (`style.css` .profile-bg-wrap / .acct-preview-bg); hero midband sampled === source EXACTLY. The cropper PREVIEW paints the same caps (hoisted gradients) + a hint line — preview = render.
2. **Gradients root cause = ANALOGOUS PAIRS** (machinery was fine): all 6 re-stopped dark→bright (ember c62828→ffab91 · ocean 283593→4dd0e1 · meadow 2e7d32→aed581 · dusk 303f9f→ff8a65 · sakura ad1457→f8bbd0 · nebula kept), ring stops corner-anchored (0/22/78/100%), 4px edge, swatches synced. Pixel-sampled corner-vs-corner Δ 140–360 all six.
3. Room lead-in = Blake's sentence VERBATIM (script.js seasonRoomHtml; g25 pins it, old refinement banned).
4. **Catch-up strip rule (one sentence, verified):** strip = door + something waiting (new-44 diff works signed-out; lantern/airing need auth). Blake's "missing" strip was the designed quiet door. Signed-in warm-auth fresh-session path walked live → 🏮 button showed.
5. 🔴 **/suggest 404 = PRACTICE-ONLY** (prod cleanUrls:true was always fine): practice-serve.js now resolves extensionless → .html (the redirect half deliberately NOT emulated — e2e navigates .html). Chip chain re-walked end-to-end through the REAL pill→/suggest→submit→chip→flip→gold-letter→retire. submitterUid doc-verified.
6. **Consent-on-save:** wiring existed + works (live-driven, now e2e spec `consent-save-emu.spec.js`, self-resetting via emulator REST). Real gap = the degraded path (gate-read blip → 'ok' → no modal → banner-pointing copy). Fixed: failed public write → ensureConsent RE-OFFER in place + ONE retry (writeProfileDoc closure); 'cancelled' copy says Save re-offers; the 1905 bg copy no longer points at a hidden banner. All other gated surfaces audited — modal-inline everywhere, no dead-ends.
7. "Big Update"→"Major Update" (index.html ×4, label only, data-tier untouched); the dormant gold MAJOR tier renamed "Milestone Update" in widget-update-skill.md (Cowork-excluded file — change rides the working tree) to kill the label collision.
8. The bg fine print → kicker (BACKGROUNDS 背景) + 3 spec chips + a fine line (.acct-bg-note-label/.acct-bg-spec/.acct-bg-fine).
9. **Frame personality (latitude):** ember bonfire-heart (2nd flame layer 170×52 no-repeat) · tide far-swell (2nd wave 230×50) · storm unequal forks (94px BR + 34px third) · moonlit halo + 2nd mist band (190×24). Existing assets only, no new animations, no gold.

### Adversarial (5 lenses — consent-gate / suggest-parity / contrast-a11y / cropper-math / perf; refute-verified: 2 MED + 4 LOW confirmed, 3 refuted, 0 HIGH)
- **MED fixed:** the PRE-EXISTING bgUploadErr early-return preempted the new consent re-offer when a bg was staged ("Saved —" lied while nothing public saved; banned users missed the suspended state). Reordered: profile-write outcome resolves FIRST; a consent landed by the re-offer retries the bg upload; the bg message only stands once honest.
- **MED fixed + pixel-proven:** full-brightness midband left .profile-item/.profile-act-chip/.profile-tag/.profile-featured/.profile-col/.profile-like-btn shadow-less over arbitrary art (1.5–2.8:1 measured on bright images). Has-bg PLATES added (rgba(16,5,32,.6–.74); is-active tab keeps a distinct purple plate; hero tags too). White-bg worst case: plates hold lum 28–97, open midband 226. g25 pins plate alpha ≥ 0.5.
- **LOW fixed:** kicker dark shadow on has-bg · cropper header comments still said 16:9 (the file's own API contract) · per-frame cap-gradient rebuild hoisted.
- **LOW accepted:** light frame motifs (moon/fork tips) wash out crossing the top-cap fade over BRIGHT images — aesthetic, frame ring/glow/washes always read; banked.
- **Refuted ×3:** mixed-vintage write (pre-existing class) · a duplicate ordering finding w/ unreachable scenario · "4:3 made the hero chop worse" (collapsed under its own numbers).

### Tests — ALL GREEN (new floors)
`npm test` **209** (g25 ×9) · `test:rules` **152** · `test:functions` **77** · `test:cf` **67** · e2e **11** (+consent-on-save). Two account-page specs flaked under full parallelism, passed isolated + the clean final run (documented class).

### Ops notes
Found the practice sandbox half-dead on boot (emulators ^C'd but listeners alive — the zombie state that likely shaped Blake's item-6 experience); killed by PID + relaunched detached. Walk scripts mutate seeded profiles (Mika) — the dream-profile e2e asserts seed state, so RESTART practice after pixel walks that SAVE.

### Commit / deploy
One pushed checkpoint (+ handoff refresh). APP_VERSION **1.9.1**. **NO deploy.** REMAINING: **gate 21 = THE CUTOVER** only.

---

<!-- author: Code | date: 2026-06-11 -->
## GATE 20.7 — Blake's final smoke fixes + the full confirmation pass. STAGED, no deploy. READY-FOR-CUTOVER stated.

### Part A (Blake's numbering)
1. **Preview note (Blake's call — NO geometry change):** `#acct-crop-note` under the hero (account.html), revealed by syncPreviewBg whenever a bg is staged/current; [hidden] twin; g26 pins it.
2. 🔴 **Flaky save + stuck pfp = ONE ROOT, SANDBOX-ONLY (proven):** profiles rules' photoURL allowlist + the THREE client avatar gates (script.js safeAvatar / account.js VIEWER_AVATAR_RE / lantern.js) rejected the Storage EMULATOR origin (http://127.0.0.1:9199) → an avatar-carrying save lost the whole profiles write ("didn't go through") and the pfp never reached profiles (every author surface reads profiles-first). FIX: the rules regex gains exactly that origin (full-match anchored; +2 rules tests incl. near-miss hostiles); the client gates gain it LOCALHOST-SCOPED (https-only on prod hosts). ⚠️ The rules comment originally claimed mixed-content blocking as a prod defense — WRONG (browsers exempt loopback; the panel caught it): prod inertness = the client hostname gates + viewer-loopback-no-beacon. Walked: avatar save clean; the comment avatar painted the test image's exact pixels rgb(235,120,20); profile card matched.
3. **Public profile tabs:** Comments+Replies removed (script.js profile modal; loaders + ACT_EMPTY keys + shortAct deleted; loadItemsSplit keeps reviews/ only). The ACCOUNT page's personal tabs keep all four BY DESIGN. dream-profile e2e re-pinned to the 2-tab card; g26 source-pins script.js.
4. **👁 chip → under the cover:** the secondary header cover rides `.secondary-cover-col`; wireRequestedChip appends into it (old-spot fallback). The count getDoc now STARTS at the top of loadSecondary (parallel with the detail fetch) → same-paint dock, no layout shift (adversarial LOW). Unreviewed-only semantics unchanged.
5. 🔴 **Lantern demo bulletproofed:** root cause = the seed only gave MIKA notifications; the 20.6 recipe pointed Blake at Yuki (letter existed only in a walk's sandbox; the pre-smoke re-seed erased it). FIX: practice-serve seeds prac-yuki 2 GUARANTEED-UNREAD letters every boot (no notifPrefs → all unread) — the panel caught the first cut deep-linking to REN's comment (seed-1); now seed-2 (Yuki's own; walked to her haloed bubble). NEW (latitude): `maybeShowSignInCatchup` — mid-session sign-in w/ unread → a compact dismissible strip (.signin-catchup, z 4800) → openCatchupSheet('lantern'); exposed as window._rarSignInCatchup, called from the auth-submit success path; teardown on ANY auth change (the strip carries data-uid); width max-content capped 560px + inline-flow under 560px (360px verified: 55px pill); honest '+' (unread>=10, fixed on the door strip too).

### PART B — the confirmation pass (all PASS)
door+catchup · Tavern (post landed) · rooms unwatched (post landed; watched = same machinery) · images (e2e) · profiles/frames/accents/cropper (avatar pixel proof + carried 20.6 proofs) · collections (created) · DM Blake (e2e send) · consent (e2e) · chip chain (real /suggest walk) · reports queue (4 open render) · ban (suspended modal) · deep-links (letter→halo walked) · launcher (real double-click, :8888, stays). **Stated: READY FOR THE CUTOVER.**

### Adversarial (5 lenses — save-chain / note+chip surfaces / sign-in catch-up / tab removal / perf+heart; refute-verified: 3 MED + 4 LOW confirmed, 6 refuted, 0 HIGH)
- MED fixed: strip survives cross-tab sign-out w/ the old account's letter metadata (auth-change teardown added; the same-tab variants were REFUTED — index.html has NO sign-out control, script.js's signOutBtn handler is dead code, noted in NEXT hygiene) · strip = 5-line blob at phone widths (shrink-to-fit ~50vw; sized+capped now, 360px re-verified) · the Yuki seed letter targeted Ren's comment (seed-2 now).
- LOW fixed: chip async pop-in layout shift (parallel count read) · '+' overstatement (both strips) · dead shortAct + stale split comments · the false mixed-content rules comment (both files).
- Refuted ×6: same-tab leak variants, the double-strip race, two read-cost claims, the 60-budget "regression".

### Tests — ALL GREEN (new floors)
`npm test` **214** (g26 ×5) · `test:rules` **154** (+2) · `test:functions` **77** · `test:cf` **67** · e2e **12** (+signin-catchup, self-resetting via notifPrefs delete).

### Ops notes
Composer probes: the live composers are `[contenteditable]` (.rar-live) — the textarea is a hidden backing field; walk scripts must target contenteditable. The Tavern nav = `#community-btn`. Launcher test leaves :8888 — kill after. Walks that open the catch-up sheet CONSUME unread state (delete users/{uid}/notifPrefs/prefs via REST to restore).

### Commit / deploy
One pushed checkpoint (+ handoff refresh). APP_VERSION **1.9.1**. **NO deploy.** REMAINING: **gate 21 = THE CUTOVER on Blake's explicit "ship it" via Cowork.**

---

<!-- author: Code | date: 2026-06-11 -->
## GATE 20.8 + GATE 21 — the final fixes and THE CUTOVER. **v1.10.0 IS LIVE.**

### Gate 20.8 (Blake's two, verified green before any deploy)
1. **Composer avatar site-wide:** `composerAvaHTML()` + `paintComposerAvatars()` + module `_selfIdent` fed by ONE authorLiveSub on the OWN uid (script.js ~5675; subscribed per auth change, unsubbed on sign-out; NO auth-photo fallback once the live snapshot lands — the chip must agree with public rows). Templates: comments (~4326), reply composer, Tavern reply, review-discussion build via the helper; the DM inbox chip (#inbox-self-ava) painted in initInbox (account.js — Auth-sourced one-shot, its host page's convention; consistency pass banked). `.composer-ava` CSS (36px disc, overflow hidden, img cover). FORM composers (review form, new-thread) deliberately chipless. Pixel-proven + e2e (★ signed-out → initial signed-in).
2. **Layered routing:** `closeStaleOverlays(keepHub)` in openProfilePage — tertiary → secondary → (non-thread targets) the hub drawer; thread targets keep the hub (openThreadById swaps in place). The DEN branch (Blake's name) closes ALL layers incl. the primary modal (adversarial MED — the missed audit entry point). Fading layers click-through (`.secondary-layer/.hub-layer:not(.active){pointer-events:none}`); re-entrant closes no-op (closeSecondaryModal/closeHubDrawer active-guards — the orphaned-close-timer class). e2e: Blake's exact repro + the hub variant; g27 ×3 source/structure pins.
- Focused panel: 1 MED + 4 LOW confirmed (0 HIGH, 5 refuted); MED + 2 LOW fixed, 2 LOW banked (validate-before-close on catalog-drift slugs; the DM chip consistency pass).

### Gate 21 — the cutover (2026-06-11, Blake's explicit go-signal via Cowork)
- **Pre-flight:** floors 217/154/77/67/14 · mirror check held · ⚠️ `bump-version.js` TARGETS were STALE AGAIN (missed frames.css ×2 + admin inbox/reports ×12) — extended with 18 LATE_TARGETS incl. the now-?v=-versioned consent/lantern/cropper import specifiers; **65 strings agree on 1.10.0** (--check green) · g20-account-v2 consent-import pin updated to the versioned form · prod rules/indexes rollback copies saved pre-flip.
- **Docs cascade:** CHANGELOG v1.10.0 (MINOR, Overhaul lead → widget tier "Major Update") · the widget's 9-bullet section · ROADMAP · NEXT.
- **Deploy (each verified before the next):** indexes ✅ → hosting ✅ (526 files; APP_VERSION 1.10.0 served; ALL leak-class 404s; /suggest cleanUrls ✓) → firestore:rules ✅ → storage:rules ✅ → functions ⚠️→✅ (attempt 1: the IAM pre-check failed granting `roles/pubsub.publisher` to the GCS service agent `service-…@gs-project-accounts` — the CLI had just enabled cloudscheduler/eventarc/pubsub and the grant raced propagation; STOPPED per the runbook, retried once: 21 created + 13 updated, 0 failures. gcloud is NOT installed on this machine — the CLI's own re-grant was the only path and it held).
- **Prod verify:** door + v1.10.0 widget ✓ · Tavern empty-state ✓ · card → modal → comments room + signed-out ★ ✓ (real-click; synthetic .click() probes miss the delegated card handler — walk note) · zero page errors · functions logs clean · **anon Storage upload 403** ✓.
- **Notes for the next session:** prod forum/profiles/etc. are EMPTY collections (first real content mints them); the first-real-data smokes from NEXT (door signals on real data · a real vote→count→notif · the 👁 chip on a real request) are now POSSIBLE and still owed; practice was left DOWN post-cutover (relaunch detached when needed).

### Tests at ship
`npm test` **217** · `test:rules` **154** · `test:functions` **77** · `test:cf` **67** · e2e **14**.

### Commits
`af44af4` (the ship) + the close-out doc commit. APP_VERSION **1.10.0** — **LIVE at realanimereviews.com.**

---

<!-- author: Code | date: 2026-06-11 -->
## v1.10.1 HOTFIX (STAGED, not deployed) — prod uploads denied for everyone + raw-error leak

### Root cause (proven, not asserted)
**The cross-service grant** (`roles/firebaserules.firestoreServiceAgent` to the firebase-rules service agent) **never landed at the cutover's storage deploy** — every `firestore.get()/exists()` in storage.rules ERRORS on prod (error = DENY), so ALL uploads/ writes were rejected for everyone: members die at gateOk()'s consent cross-read; Blake (admin-bypassed past gateOk) dies at `uploadsEnabled()` — which has NO admin short-circuit by pinned design (the kill-switch stops him too). The emulator needs no IAM → 154 rules tests green throughout. Clause elimination pinned it: his uid in the leaked path (auth match ✓), isAdmin bypass ✓, client emailVerified true (the consent copy branch proves it), fresh random paths ✓ — only the cross-read left. The cutover's own log: the re-released identical ruleset printed NO grant line ("skipping upload" — the grant hook only runs on a NEW ruleset body).
- NOT the email-verified gate (hypothesis 1) and NOT his consent doc (hypothesis 2) — though both shaped the WRONG copy he saw.

### The fixes (staged)
1. **storage.rules** — a ⚠️ documentation block naming the load-bearing grant (changes the ruleset bytes → the deploy re-uploads → the CLI grant hook RUNS). Logic untouched; rules tests 154 unchanged. Fallback if the hook fails again: console IAM grant (documented in the block).
2. **friendly-errors.js (NEW shared module — the consent.js ONE-implementation precedent)** — `friendlyError(err, {kind, user})`, pure, window-exposed for specs. The truthful split: unverified → verify-email copy · verified denial → "on us, not you" (the old "community-rules… Hit Save again" lie is DEAD) · quota/network/generic branded. Imported ?v=-versioned by BOTH pages (bump LATE_TARGETS extended).
3. **~30 sinks rewired** (script.js ×16, account.js ×14): every alert/textContent failure path on the visitor pages routes through the module; raw errors → console.error only. Residual-leak grep = ZERO `.message` UI renders.
4. **g28 ×3**: the module fed Blake's EXACT screenshot string (+ hostile variants) must return clean of /firebase|storage\/|uploads\/|firestore|gstatic/ · the verify-vs-site-side split pinned · source pins on both pages (no raw-.message concatenation can return; both import the module).

### Proof
Kill-switch flipped ON in the emulator → a GENUINE storage/unauthorized through Blake's exact sink (thread-with-image, `.hub-nt-error`) → rendered: "Could not post: Uploads are locked on the site right now — this one's on us, not you. Tell Blake if it keeps up." → switch restored.

### Tests (new floors)
`npm test` **220** (g28 ×3) · rules **154** · functions **77** · cf **67** · e2e **14**. Version 1.10.1 across **67** strings (+2 LATE_TARGETS for the new module's imports) · CHANGELOG + the widget Hotfix section · static title tier follows the top entry (Hotfix).

### Banked
Admin-page sinks (admin/*.js raw err.message alerts — Blake-only surfaces, same one-line treatment when touched) · a resend-verification affordance near upload errors (the verify copy points at the existing account-page resend).

### Deploy plan (ON BLAKE'S GO ONLY)
`firebase deploy --only storage` (watch FULL output for the grant) → `firebase deploy --only hosting` → prod verify: his background upload lands; if the hook fails again, the manual console grant, then re-verify.

<!-- author: Code | date: 2026-06-12 -->
### v1.10.1 DEPLOYED (Blake's go in chat) — + an honest evidence correction
Deploy ran storage (×2 — see below) → hosting; prod serves **1.10.1**, friendly-errors.js live, 25 routed sinks in the served client, zero raw-leak patterns.
**⚠️ EVIDENCE CORRECTION on the grant theory:** the staged entry claimed "the firebase CLI runs its grant hook when a NEW ruleset body is uploaded" — **disproven at ship**: firebase-tools **15.19.1 makes NO IAM-policy calls even on a real ruleset upload** (--debug-traced twice; only testIamPermissions for the deployer's own perms). The CLI does not manage the cross-service grant, and grant PRESENCE is unreadable from a gcloud-less machine (newer Firebase platform versions may self-provision the rules service agent — the missing-grant theory is PLAUSIBLE but NOT proven). Also probed: the prod kill-switch doc (anon REST through public-read rules) → 404 = absent = enabled, eliminating the switch-set-to-false alternative. **The decisive probe is Blake's verified/admin upload on prod** (his 2-step smoke). If it still 403s: Firebase console → Storage → Rules shows the cross-service permission banner — its one-click grant is the designed manual path; then re-test. The client half (truthful copy, no raw leaks) is correct REGARDLESS of which way the grant probe lands. storage.rules' comment block now carries the corrected trail.

<!-- author: Code | date: 2026-06-12 -->
### v1.10.1 ROOT CONFIRMED (Blake, 2026-06-12): "I did the google thing in firebase and uploads work now."
The cross-service permission (firebase-rules service agent → Firestore read) WAS missing on prod; the console Grant banner fixed it. The deploy CLI never grants it (proven at ship, 15.19.1 --debug ×2). **Permanent pre-flight rule: any future storage-rules deploy that adds/changes firestore.get()/exists() usage must verify the grant in the console first** — recorded in CUTOVER-RUNBOOK + CODE-HANDOFF.

---

<!-- author: Code | date: 2026-06-12 -->
## v1.10.2 (STAGED) — THE CREATOR PROFILE + the full account nav

### Item 1 — the Creator sheet (Blake's 3rd ask; supersedes the name→Den heart spec — the DEN PATH lives ON the sheet now)
- `profileDecision` admin → **'creator'** (g18 re-pinned). openProfilePage: `isCreator` weave — `.is-creator` sheet, CREATOR 創り手 kicker (gold), NO report btn, `likesCount: undefined` (the row never renders — gold is never counted; the rules' like-on-admin deny was already in place), default `data-frame='blake'`, Threads-only tabs (his reviews ARE the site), the gold `.profile-den-path` → `goHomeToDen()` (the relocated 20.8 close-ALL block). Ident synthesizes `{name:'Blake'}` if both docs absent.
- account.js renderViewerMode: the admin refusal REMOVED → his creator viewer sheet (or the gold ready-to-wear invitation pre-first-save). Stale foot-hint (comments/replies) corrected.
- **firestore.rules:** profileFieldsOk reserved-name denylist → `(isAdmin() || !(...))` — the denylist stops impersonation; the real Blake is exempt on HIS OWN doc (writes stay owner-gated; gateOk already admin-bypassed). +2 rules tests (admin-'Blake' allowed; member 'bLaKe' denied).
- CSS: `.is-creator`-scoped gold (kicker/avatar-ring/name-glow) + the gold den-path button + the invitation card. g29 heart pin: a member sheet's kicker is computed-style-checked ≠ gold.
- e2e ×2: the member walk (sheet → den door → home) + Blake's Public view never refuses.

### Item 2 — account nav (View All · Random · Filter)
- account.html nav-tools + Random/Filter routing `index.html?open=random|filter`; script.js boot fires the SAME wired buttons.
- **Adversarial HIGH (fixed):** the first cut fired ?open=filter SYNCHRONOUSLY inside init() — the pre-existing end-of-boot `filterPanel.classList.remove('open')` then force-closed it same-tick: invisible panel + stuck aria-expanded/.is-active + stuck `body.filter-open` scroll-lock, on the COMMON (already-welcomed) path. **The fire now defers one macrotask past init()**; the door path fires only after the splash is fully torn down (+700ms grace past the 620ms catch-up hand-off), and an intent check drops the queued action if a catch-up surface/modal already owns the screen (the MED teardown-clobber + both LOW races die with it).
- **Adversarial MED (fixed):** the g29 spec was FALSE-GREEN — it asserted aria-expanded, the exact attribute the bug left stuck. Now pins the `.open` class + computed opacity '1' + pointer-events (the painted-pixels discipline, again).

### Also logged: v1.10.1 root CONFIRMED by Blake ("I did the google thing… uploads work now") — the cross-service grant; the permanent pre-flight rule is in CUTOVER-RUNBOOK.

### Tests — ALL GREEN (new floors)
`npm test` **225** · `test:rules` **156** · `test:functions` **77** · `test:cf` **67** · e2e **16**.

### STAGED — deploys on Blake's go: hosting + firestore:rules (the name carve-out only; NO storage rules touched — no grant dance).


---

<!-- author: Code | date: 2026-07-02 -->
## v1.10.2R (2026-07-02) — the FUNCTIONAL gate: Creator profile respec to MEMBER PARITY + the member-reported prod bugs + the Mode-1 launcher. STAGED (never deployed on top of the old staged v1.10.2, which itself never deployed).

### Item 1 — the Creator profile, Blake's clarified spec (supersedes the g29 gold showcase)
- His words: "I still want to be able to customize my profile like everyone else. The only difference is... 'Creator' shows up where everyone elses 'Member' appears." Plus mid-gate via the re-staged SHIP-PROMPT: "I want my account to be appreicated." and "I have access to the den keeper but I can choose not to use it."
- DELETED from the old showcase: the forced data-frame='blake' default (script.js + account.js), the gold Visit-the-Den door + goHomeToDen(), the suppressed Reviews tab, the .is-creator gold avatar-ring/name-glow CSS. KEPT: the reserved-name rules carve-out, no-report-flag, entry wiring, the never-a-tombstone fallback, the gold CREATOR kicker (kicker-scoped gold ONLY now).
- APPRECIATE ON (the heart reversal, HIS call): firestore.rules like-create lost the `uid != ADMIN` clause; script.js likesCount no longer isCreator-gated; account.js preview renders the row for him too. Rules-tested both ways (allow member-on-Blake / deny Blake-self). The CF (onProfileLike machinery) needed NO change — counts + pings just work.
- Pixel walk (sampled, not computed): kicker region 300 gold px painted; avatar+ring region 0 gold px; Appreciate row live with count; no door; Reviews tab present; data-frame null on the unadorned seed doc.

### Item 3 — the prod bugs, root causes (all different from the naive reading)
- 3a picker: flex min-height COLLAPSE (`overflow:hidden` on .acct-dd-opt resolves min-height:auto to 0) — 61 options in the 300px flex-column menu crushed to ~3px rows. Fix: `flex: 0 0 auto` on .acct-dd-opt (style.css) — covers all 4 brandSelect instances. g30 pins a RENDERED 60-option probe (row height, no overlap, menu scrolls). Probe lives on index.html — account.html's signed-out boot navigates and context-destroys an evaluate (learned in-gate).
- 3b admin 404s: admin-fab.js RELATIVE hrefs doubled under /admin/ (new-anime is the only admin page mounting the FAB). Fix: root-absolute WITH .html (exists on every server; prod 301s to clean). Live-walked on practice. Mode-1 honesty: quotes/season-reviews banners + runtime save-path strings de-jargoned; new-anime mode notice (location-neutral copy — 'remote' also fires on localhost sandboxes); chat-drawer friendly copy + an ANTHROPIC_API_KEY provider-name leak scrubbed from admin UI copy (g30-pinned /anthropic/i + case-sensitive /OpenAI/ — a bare /openai/i false-matches the removed openAiPanel identifier).
- 3c suggestions: row-expand detail (nameFor profiles->users, Anonymous for pre-20.5 docs, full reason, submitted date, AniList fields; keyboard + aria-expanded + [hidden] twin), NEW-badge clip root-fixed (flex-wrap on .meta; .row-detail rides flex-basis:100% on a wrapped row; mobile ADD-THIS-ANIM truncation fixed with min-width:max-content).

### Item 4 — Mode-1 launcher (3rd report; the CRLF theory was DEAD — byte-proven already CRLF)
- Real roots: no browser auto-open (a working start read as "didn't seem to work") + no listen error handler (double-click #2 = raw EADDRINUSE + npm ERR wall).
- Fix: server auto-opens the browser on ready (guarded MODE1_NO_OPEN + isTTY) + friendly EADDRINUSE handler (exit 0, opens browser); .bat greeting banner, curl /api/health IDENTITY pre-check (not bare netstat — a stray app on 8888 must not count), node_modules one-time-setup message, pause on every exit. CRLF normalized + byte-verified after every edit (65/65 pairs, ASCII-only). Tested via Start-Process double-click semantics: cold 200, second click same PID + clean exit.

### Adversarial panel — 5 lenses, 13 confirmed / 6 refuted (3-skeptic refute-verify each). The record holds: a real HIGH every round — TWO this round.
- HIGH: the re-staged SHIP-PROMPT's Appreciate reversal vs my build+CHANGELOG contradiction (the panel caught a mid-flight spec change the session had read past). Built end-to-end same gate.
- HIGH: openBrowser fired during every `npm test` (tests/mode1-server.spec.js boots the real server; nothing set MODE1_NO_OPEN) — real tabs on Blake's machine. Fixed: spec env + isTTY belt.
- MED: .bat any-listener-is-Mode-1 false positive (fixed: curl identity probe) · new-anime "live site" claim wrong on sandboxes (fixed: location-neutral) · stale doc states (NEXT gate-0 baseline, old SHIP-OUTPUT smoke, CODE-HANDOFF) — all rewritten.
- LOW: inbox/reports APP_VERSION stuck at 1.9.1 for two ships — fixed + bump TARGETS extended (the stale-TARGETS class, closed for APP_VERSION too).
- Accepted: the double-boot two-tab race (identical to designed one-tab-per-click) · the pre-existing DEP0190 warning at Mode-1 start (banked).
- Flagged to Cowork: their uncommitted AI-PRIMER ("Live at v1.7.1") + HANDOFF ("reads 1.10.0") are stale.

### Found beyond the list
The old staged v1.10.2 never ran the bump — everything still said 1.10.1. Bumped to 1.10.2 (67 strings), CHANGELOG + widget written, ROADMAP was TWO ships stale (fixed), CLAUDE.md bump-checklist line refs corrected. markdown-bold.spec is a parallelism flake (timeout under full load, green isolated AND green on the final full run) — not yet a documented flake class; watch it.

### Tests — ALL GREEN (new floors)
`npm test` **229** (+4 g30) · `test:rules` **157** (+1: the Appreciate reversal pair) · `test:functions` **77** · `test:cf` **67** (re-run post-rules-change) · e2e **16** (creator pair re-pinned to parity).

### STAGED — deploys on Blake's go: hosting + firestore:rules (the rules CHANGED this gate — the like-on-Blake reversal; NO storage rules — no grant dance).


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A0 (2026-07-02) — the Lantern DRY-unify. SANDBOX-STAGED (one mega-cutover per Blake).
- lantern.js = THE ONE source (both pages). script.js's ~450-line drifted twin DELETED; index rides initLantern(hooks): openTarget (in-page router), onRowNavigate/onOpen (back chip), keepScrollLock (modal-under-lantern), safeAvatar. New exports: openLanternCenter, markAllNotifsRead (the catch-up sheet's one outside caller).
- The drift is dead: index's mute strip lacked request_done — the unified strip carries it (live-walked: 7 chips incl. request_done on index).
- bump TARGETS += script.js lantern import. g31-megarun.spec.js pins: no twin structures in script.js, versioned import, the hooks, the exports, the pure-model behavior anchor.
- Spec hardening: g6-lantern's notifHref import moved account.html→index.html (the signed-out boot-navigate context-destroyed flake class, same as g30's picker probe).
- Evidence: npm test 233 (229+4) · e2e 16/16 (first run raced a cold practice boot — ERR_CONNECTION_REFUSED, re-run green) · live walk: gold glyph → 6 rows + rollup → 3-item drill → request_done chip → close = cold glyph.
- Floors: 233/157/77/67/16.


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A1 (2026-07-02) — the peer-DM/group SECURITY LAYER (rules + CFs + 36 new tests). SANDBOX-STAGED.
- blocks/{blocker}/list/{blocked}: owner-only everything (a blocked user can never confirm), NO gateOk (banned users may still protect themselves), createdAt-only shape.
- conversations: admin branch byte-identical; kind:'peer' create = request-first (state 'request', creatorUid pinned, no Blake-party, blockedEither denied via 2 exists()); kind:'group' = SOLO BIRTH (participants==[creator] — a 15-block-check create would blow the 10-access-call rules cap), adds ride updates one-at-a-time APPENDED LAST (2 block-exists per add), cap 15, member-remove-self, creator-remove-other, creator-leave denied (banked). Peer flip: recipient-only, request→open|declined, diff hasOnly(['state']). Summary forgery stays denied everywhere.
- messages: send gate = state 'open' (any kind) OR creator-into-own-request; NOBODY into declined/locked. Optional imgRef/imgThumbRef pinned to uploads/<uid>/dm-<convId>/ (A4 rides). ⚠️ text 1..2000 still REQUIRED — image-only messages need the A4 widening.
- CFs: onConversationCreate → ONE unmutable dm_request ping (the safety signal; per-message pings suppressed in request state); onDmMessageCreate → group fan-out to ALL non-senders + per-conversation mute (muted['conv:<id>'] skips ping, unread still mirrors); rateLimitConversationCreate (shared 5/60s bucket — request-spam brake); rateLimitDmMessage in its OWN __dm bucket at DM_RATE_LIMIT 20/60s (orchestrator fix: chat cadence ≠ forum cadence — the agent's shared-bucket cut would have deleted real conversation).
- lantern.js: dm_request type meta (glyph ✉, 'wants to message you'), DELIBERATELY absent from the mute strip — one-place change post-A0.
- Agent deviations accepted (3): admin kind keeps state=='open' for messages (the ban-cascade 'locked' tombstones must stay closed); gateOk on group-ADD only (leave/decline = self-protection, unvote precedent); group fan-out fixed in-gate.
- A2/A3/A4 contract: kind admin|peer|group · state request|open|declined|locked · creatorUid on peer+group · group name 1..60 escape-at-render · notif types dm + dm_request · mute key conv:<convId> · imgRef uploads/<uid>/dm-<convId>/[A-Za-z0-9_-]{1,120} · group adds APPEND-ONLY · blocked = generic "can't message this member" copy, never "you're blocked".
- Floors: 233 / 187 (+30) / 77 / 73 (+6) / 16.


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A2 (2026-07-02) — THE LETTER ROOM (the unified inbox). SANDBOX-STAGED.
- The DM-Blake hero card + the locked People placeholder are DEAD (Blake's word). One list for every kind; Blake = a normal row, identity-GOLD (name, edge, unread tint). Requests wear a dashed strip above the list; row anatomy = who · sub · when · state.
- Knock-first end-to-end: ✉ Message on EVERY profile sheet (script.js kicker actions; signed-out invites sign-in; own sheet hides it) → account.html#inbox/new/<uid> → compose-new ("Introduce yourself — this arrives as a request…") → first send mints the conv (admin floor for Blake, kind:'peer' state:'request' for members, BORN-SORTABLE: lastMessageAt allowed-at-create == now, rules widened +2 tests → 189).
- DECLINE-SILENCE proven on live pixels: sender's row reads "request sent" for state 'request' AND 'declined' (indistinguishable); recipient's declined convs vanish; a rules-denied first letter renders the GENERIC "This member can't receive your letter right now." (never "you're blocked").
- Thread chrome: dynamic title (gold Blake / group name+count), req-bar (Accept/Decline/Block — accept flips state via diff-pinned updateDoc; block writes blocks/<me>/list/<other> then declines), per-conv mute 🔔/🔕 (muted['conv:<id>'], live prefs snapshot), two-tap branded block in the bar, report-⚑ on every non-mine letter (always visible — no hover-only) → the branded reason panel → reports doc {targetType:'dm', targetPath conv/msg, snapshotText ≤480}. Group letters carry sender-name attribution. The inbox never alert()s — the friendly status line owns every failure (g31-pinned).
- The old copy honesty pass: the panel sub reads "Your letters — members, groups, and Blake himself. Strangers knock first."; g24's stale 'DM Blake'-exists pin flipped to not-exists; g23 re-pinned to the Letter Room markup; the Message-Blake e2e REWRITTEN to walk the new door (sheet ✉ → compose → send → gold title).
- LIVE WALK (the full cycle, real pixels): Mika knocks Ren → Ren's strip "✉ Mika wants to message you" + dot → open (req-bar, locked composer, the letter) → Accept ("Letter opened — you can write back now.") → reply → Mika reads it, composer open both ways → Aki knocks, Ren DECLINES → Ren's strip empty, Aki still reads "request sent". Screenshots on file (tmp-walk-letterroom-*.png).
- Floors: npm test 236 (+3 g31 A2 pins; g23/g24 reworked in place) · rules 189 (+2 born-sortable) · functions 77 · cf 73 · e2e 16 (inbox walk rewritten).
- A3 hooks ready: group rows/threads render already; create/add/remove/leave UI is the gate.


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A3 (2026-07-02) — GROUPS (create · add · leave · remove · the added-you ping). SANDBOX-STAGED.
- Create: "👥 New group" in the inbox tools → the docked branded panel (name 1..60 mirrored from the rules) → SOLO birth → opens the thread with the friendly next-step status.
- Members panel (docked, thread bar 👥): rows w/ made-it/you tags; creator sees ✕-remove on others + "Add from your letters" (contacts = other-uids of my non-group convos — you add people you KNOW; append-LAST per the A1 rules pin); non-creators see Leave. Cap-15 copy at the cap. All names resolved via nameFor, escaped.
- CF onConversationUpdate: the group-ADD ping to the appended member (type 'dm', creator-sourced identity — update triggers carry no auth; verb carries the escaped group name; muting Messages mutes adds; leaves/removals ping NOBODY). +1 cf test (ping on add, silence on leave) → 74.
- LIVE WALK: Ren creates → adds Mika from contacts (panel shows "Ren made it/you" → "+Mika ×", title count 1→2) → Mika's LANTERN shows 'Ren added you to "Finale watchers"' → group row in her list (count chip) → her letter fans out → Ren reads it WITH sender attribution → Mika leaves (quiet, row gone, count back to 1). Screenshot tmp-walk-group-ren.png.
- Floors: npm test 237 (+1 gA3 pin) · rules 189 · functions 77 · cf 74 (+1) · e2e 16.
- A4 next: images in DMs/groups (the sealed-until-accept chip; the rules' text-required widening for image-only messages; storage grant pre-flight).


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A4 (2026-07-02) — IMAGES IN LETTERS (the legal spine, client half). SANDBOX-STAGED.
- SEALED-UNTIL-ACCEPT, stricter than the spec's blur: while a peer conv is a REQUEST, the recipient's client renders a neutral "🖼 Image — accept to view" chip and NEVER resolves the URL (no imgbox → the hydrate pass can't fetch; blur is tamper-trivial, an unfetched token is not). The sender sees their own image. WALK-CAUGHT BUG fixed in-gate: accepting flips the CONV doc, not messages — the sealed chip stayed sealed until reopen; flipRequest('open') now re-opens the thread (re-subscribe + re-render = unseal).
- Rules: text may be EMPTY when imgRef rides (an image IS a message); bare-empty stays denied (+2 tests → 191). imgRef/imgThumbRef stay pinned to uploads/<me>/dm-<convId>/ (A1).
- Composer: 📎 one image per letter; every gate SPEAKS before the pick (verified-email friendly copy; the uploadsEnabled kill-switch CLIENT check — account.js's first; type+size ≤5MB); the attachment chip w/ remove; attachments never cross threads (clearAttachment on close). Upload rides THE pipeline (own dm- prefix, magic-byte/dedupe/EXIF/caps) — NO storage.rules change (the dm- segment fits the docId wildcard) → NO cross-service grant dance.
- Render: unsealed images hydrate via cached getDownloadURL into bubbles (max-h 260, lazy), dangling refs degrade to "Image unavailable"; a minimal branded lightbox (Esc/backdrop close). Report-and-preserve: the ⚑ carries data-imgref → the report doc ships imagePath evidence (admin queue renders it; message docs are immutable so the pointer survives).
- LIVE WALK (real PNG through the real pipeline): attach chip → send-in-request → Yuki's SEALED view (0 img elements) → accept → the image renders from the storage emulator (naturalWidth 64 — pipeline-processed) → report → the dm report + snapshot in Blake's admin queue. Screenshots tmp-walk-a4-*.png.
- Floors: npm test 238 (+1 gA4 pin) · rules 191 (+2) · functions 77 · cf 74 · e2e 16.
- A5 next: the messaging adversarial panel + the consolidated e2e batch → Milestone A closes.


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN gate A5 (2026-07-02) — MILESTONE A adversarial panel + the confirmed-finding fixes. SANDBOX-STAGED. MILESTONE A CLOSED.
5-lens refute-verify panel over d9445f9..HEAD (safety · heart · correctness · injection · honesty). The correctness/honesty skeptics hit a session limit mid-verify, but safety/heart/injection fully verified. Confirmed: 3 HIGH + 1 MED (all safety-spine) — ALL FIXED. Heart + injection lenses came back CLEAN (no gold leaks, all new innerHTML sinks escape-first).
- **HIGH (fixed) — block couldn't sever an OPEN peer thread.** The A1 create-time block check never reached an already-open conversation; a blocked party kept full send + ping access, and no one could lock an open peer conv. FIX: firestore.rules message-create now denies peer messages when blockedEither(sender, other) (2 exists()); either direction freezes both. Client: my live blocks/<me>/list subscription hides blocked peer convs + generic "can't receive your letter" on any permission-denied send. Rules test: block freezes an open thread both ways.
- **HIGH (fixed) — the image seal was CLIENT-ONLY.** uploads/{uid}/dm-{convId}/ had storage `allow get: if true`; a hostile recipient read imgRef off the message doc and getDownloadURL'd past the "accept to view" chip. FIX: storage.rules seals dm-{convId} objects server-side — readable only by the sender (own folder) OR a participant when the conversation state != 'request' (a cross-service firestore.get of the conv, riding the EXISTING firebaserules.firestoreServiceAgent grant — no new grant; the runbook's storage deploy carries it). 3 storage tests (request sealed / open unsealed / group). Walk re-proven: sender sees own, recipient sealed→accept→renders from 9199.
- **HIGH (fixed) — re-request / unmutable-ping flood.** onConversationCreate .add()'d a fresh dm_request per peer create; scripting convs (or re-requesting after decline) flooded the unmutable Lantern. FIX: deterministic `dmreq_<creator>` doc-id (the pl_<liker> pattern) collapses all requests from one sender into ONE re-lit row pointing at the newest conv; volume bounded by rateLimitConversationCreate. CF test: a re-request mints no second ping.
- **MED (fixed, client) — group-add didn't check added-vs-existing blocks.** Rules can't loop members. FIX: the client hides a blocked member's group letters (iBlocked filter in the message render); leave + report remain. Documented residual. Also refined contactsOf() to OPEN PEER convs only (a declined/pending request or the Blake floor isn't an addable contact) — a real correctness improvement the group e2e caught.
- Durable e2e batch `tests-e2e/letters-emu.spec.js` (self-resetting via emulator REST owner-bypass): the request cycle · decline-silence · groups (create/add/fan-out/leave) · sealed-image accept. 4/4 green.
- Floors: npm test 239 (+1 gA5 pin) · rules 195 (+4: 3 storage seal + 1 peer block) · functions 77 · cf 75 (+1 dedup) · e2e 20 (+4 letters batch).
- ⚠️ CUTOVER NOTE: storage.rules now cross-reads conversations — verify the firebaserules→firestore grant covers it (it already does for moderationGate/commentsMeta; same service agent, same role). The one mega-cutover deploys storage rules in-order (indexes→hosting→firestore→storage→functions).
- MILESTONE A COMPLETE. Next: Milestone B (card-state labels + curator panel + info-requests).


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN Milestone B (2026-07-02) — CURATOR TOOLS: card-state labels · the curator admin panel · info-requests. SANDBOX-STAGED. MILESTONE B CLOSED.
- **Rules (new):** animeStatus/{slug} `{status,updatedAt}` PUBLIC read / admin write (enum: watching/watchlist/on-hold/finished/rewatching); curatorNotes/{slug} admin-only READ AND write (private — a member's client never sees a note); suggestions gains `kind in ['anime','info']`. +5 rules tests → 198.
- **Card-state labels:** Blake's per-anime status paints a GOLD pill on the card ("🏮 Blake is watching" etc.), injected post-paint off the applySavedStateToCard hook from a public animeStatus read (1h localStorage cache). His mark = gold (heart). Label text via textContent (no XSS), fixed status→label map.
- **The curator admin panel** (delegated, self-contained lane): /admin/curation.html on the admin scaffold — 44 catalog rows, a status dropdown + private-notes textarea each, straight Firestore writes (NO Mode-1 dep — works deployed). FAB entry + 7 bump TARGETS added. Live-walked: Blake sets chainsaw-man→watching → a member's fresh load shows the gold pill.
- **Info-requests (item 7):** the secondary deep-dive carries a "ⓘ Request info" button → an inline branded ask field → writes suggestions kind:'info' (submitterUid pinned, 60s rate-limit). The CF aggregateSuggestionCounts SKIPS kind:'info' (an info question must not inflate the public 👁 review-demand chip) — +1 cf test → 76. onSuggestionReviewed gives info-kind a distinct verb ("filled in the page — you asked about this one"). The admin queue shows a teal ⓘ INFO badge.
- **Adversarial panel (3-lens, 2-skeptic — lean, no session-limit): confirmed 1 HIGH + 2 MED + 3 LOW, ALL FIXED.**
  - HIGH/MED (the real catch): the status label's `<span>` children inherited `.card .info span` (gold/1rem/bold) AND the Top-10 `.spotlight-stack .card .info span` gold-GRADIENT-PILL — the EXACT trap card-render dodges with `<i>`. My own pixel walk had shown rgb(255,215,0) = the `gold` keyword, NOT my #ffe082 amber — I'd misread a real bug as working (the sampled-pixels-can-still-be-the-wrong-value lesson). FIX: label is a `<div>` + `<i>` children (match NEITHER span rule) + scoped CSS. New g31 COMPUTED-PIXEL pin asserts amber rgb(255,224,130) at <14px AND no gold-pill in the spotlight — the durable anti-regression.
  - LOW: the ⓘ INFO badge had no CSS (transparent) → teal `.status-pill.kind-info`. · the info-request error span stacked + a dead `(window.friendlyErrorModel?'':'' )` ternary → de-duped + cleaned. · onSuggestionReviewed info-copy (fixed above).
- Floors: npm test 243 (+4 gB pins incl. the visual) · rules 198 (+5) · functions 77 · cf 76 (+1) · e2e 20.
- MILESTONE B COMPLETE. Next: Milestone C (first-modal community reviews on unreviewed anime · Discover: Random filter + Hidden Gems · Constellation Wrapped).


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN Milestone C (PARTIAL — C1+C2 done, C3+D pending). SANDBOX-STAGED. HANDOFF POINT.
- C1 (item 8) yellow-tape first modal: synthetic __unreviewed anime opens openModal in tape mode (.unreviewed-tape, gold NOWHERE, no rating/review), community column keyed al:<id>. reviewKey(anime)=__communityKey||communityKey retrofits communityMarkup/subscribeReviews/wireCommunity/commentsMarkup — behaviour-preserving for the 44 catalog rooms (193 community/modal specs stayed green). Entry: data-commreview "Community reviews" door on the secondary; builds the synthetic, closes secondary first (z-order). g31 has source + behavior pins.
- C2 (item 9) Discover: franchise-fetch.js HIDDEN_GEMS_QUERY (SCORE_DESC, averageScore_greater 75, popularity_lesser 50000) + popularity field; fetchHiddenGemsCached 24h; fillHomeGems + #home-gems-block (hidden until real). Random mini-filter: #random-btn honors readReviewedSeg() ('reviewed'/'notyet'/'all'=~70% unreviewed). CANARY (id 101922) not yet re-run — do it before cutover.
- C3 Constellation Wrapped: NOT STARTED (a delegated agent was killed mid-build, landed nothing). Build next — see NEXT wants + study §4e.
- D responsive overhaul: STUBBED only — #nav-drawer-toggle + #main-toolbar id in index.html, toggle HIDDEN by default (.nav-drawer-toggle{display:none}). No drawer CSS/JS. Build per study §4b + the measured table.
- Floors held: npm test 243 / rules 198 / functions 77 / cf 76 / e2e 20. ⚠️ Only targeted specs re-run for C (193 community/modal + 25 discovery/nav/g31) — RUN THE FULL SUITE before the next commit.


---

<!-- author: Code | date: 2026-07-02 -->
## MEGA-RUN Milestone C CLOSE (2026-07-02) — C3 Constellation Wrapped + the 5-lens panel (7 defects, ALL fixed). SANDBOX-STAGED. MILESTONE C CLOSED.
- **Full-suite verification on arrival (the handoff ask):** 245/198/77/76/20 all green against a7fb4e7 (consent-save-emu e2e is the documented cold-boot flake — failed the batch, passed isolated, twice this session).
- **Canary:** the on-arrival FAIL (AIRING genre:"Action" → 0) was TRANSIENT rate-limiting — franchise-fetch degrades a 429 to [] and the canary misread it. Canary now paces its queries (900ms) + gains 4 HIDDEN GEMS gates (non-empty, score>75 all, popularity<50000 all, popularity fetched). 12/12 live.
- **C3 CONSTELLATION WRAPPED (built this session):** self-contained wrapped.js + wrapped.css; account.html Community-group tab `data-tab="wrapped"` + `#tab-wrapped` panel; account.js versioned import + `ensureWrapped` lazy (the activity precedent) + `#wrapped` deep-link + pre-auth catch-up; bump TARGETS +2 (wrapped.css link, wrapped.js import). Data = the activity feed's exact query shapes (CG items/replies/threads=DISCUSSIONS + flat forum authorUid + CG posts + watchlist/favorites), one-shot, bounded (200/100/100/50/100), year-fenced client-side, 1h localStorage cache `rar:wrapped:v1:<uid>:<year>`. SVG sky: deterministic FNV-1a layout (no Math.random), x=real time axis, y=kind bands; review stars sized/opacified by rating; ambient dust = hash-scattered seed-per-member circles ALWAYS below the save-star floor (r≤1.0, op≤0.22), no data-kind (counts stay pure). THE ONE GOLD STAR = join day (auth metadata.creationTime), only when join-year == displayed year; earlier joins get the "Your sky began" caption. Fresh members: gold star + invitation, NO zero chips. `window.wrappedModel` exposes the pure model (computeWrapped/layoutStars/skySvg/skyAria/statsHtml) for pins.
- **THE PANEL (5-lens find → 2-skeptic refute-verify, 53 agents): 24 raw → 21 confirmed (dedup: 7 distinct) → 3 refuted. Its best round — TWO of C's three features were DEAD in the tree as inherited (the targeted specs were presence-pins):**
  - **HIGH — Hidden Gems could never render, twice:** (a) `lazyFillOnView(homeGemsBlock,…)` observed the [hidden] block — IO never fires for display:none → the only unhide lived inside the never-called fill (deadlock); (b) `fetchHiddenGemsCached` was never exported on `window.rarDiscovery` → the bridge call threw a swallowed TypeError. FIX: observe the VISIBLE airing strip directly above (same threshold, same lazy discipline) + export on the bridge. Walked: 12 real cards render.
  - **HIGH — the tape modal's comments column was DEAD:** subscribeComments/wireComments keyed `communityKey(anime)` (title slug for the synthetic) while commentsMarkup keyed `reviewKey` (`al:<id>`) — ids never matched, `if (!listEl) return` silently. FIX: both → `reviewKey(anime)` (identical for the 44 + secondary rooms — constructional invariance). Pinned: 0 `const s = communityKey(anime)` / ≥6 `const s = reviewKey(anime)` + a live composer-wired walk.
  - **HIGH — XSS via the synthetic:** the door's attribute-escaping DECODES back to raw via dataset; `modalRomaji` (modalSub.text) and `genreHtml` (anime.Genre) interpolated AniList-sourced strings into innerHTML unescaped. FIX: escapeHtml at BOTH sinks (behaviour-preserving for catalog Excel text). Pinned: hostile `<img onerror>`/`<svg onload>` payloads ride the g31 tape walk and must stay inert. (Seasons/Studio/Tags sinks stay raw — catalog-only fields, never synthetic-fed; accepted.)
  - **HIGH (heart) — the tape kicker painted #f0d34a ≈ Blake-gold (hue 49 vs 46)** on the no-gold surface. FIX: the whole tape family → hazard yellow-green (kicker #e3e04e, stripes rgba(222,219,60,·)). Pinned by COMPUTED color rgb(227,224,78) + a sampled-pixel walk (brightest kicker ink = exactly 227,224,78).
  - **MED — Wrapped saves double-counted** dual-shelf titles (and double-weighted genres; the stacked star hid the inflation). FIX: dedupe by animeId keeping earliest ms (account.js savedTotal Set-union precedent).
  - **MED (heart) — .wr-legend-gold + .wr-began painted gold** → 2nd/3rd gold elements. FIX: ink → #e3c9ff; wrapped.css pinned to carry NO gold hex ever (comments included — the pin caught my own fix-comment naming the hex).
  - **MED — the sky's aria-label recited the zeros-parade to fresh members and omitted discussions** for active ones. FIX: new `skyAria()` — non-zero parts only, discussions folded into the comments sum (chip parity), fresh members hear the gold-star line. + chips now render per-count non-zero only (a one-comment member sees no "0 reviews written").
  - **Refuted (left alone):** tape-copy direction nit · zero-chips-as-rule-contradiction (duplicate framing; behavior fixed anyway) · the score "≥75 vs >75" prose off-by-one (SCORE_DESC + perPage 40 makes the boundary unreachable).
- **Pin hardening (the honesty lens):** the gC3 wiring pin now pins the tabs ARRAY LITERAL (`const tabs = […'wrapped'…]` — bare toContain survived deleting the entry); + gC-panel pins for the reviewKey keying, the bridge export, the IO sentinel, and the wrapped.css no-gold-ink rule.
- **Floors (NEW): npm test 250 (+7: 3 gC3 + 2 gC-panel + 2 in-test) · rules 198 · functions 77 · cf 76 · e2e 20.** Walks: wrapped 13/13 (gold star samples #ffe082 exact; far-star purple 217,192,245; night wash 14,4,30) · C-fixes 7/7 · canary 12/12. rules/functions/cf inputs byte-identical since their greens (client-only fixes).
- **MILESTONE C COMPLETE. Next: Milestone D — the v2.0 responsive overhaul (study §4b + the measured table; the index.html stub un-hides).**
