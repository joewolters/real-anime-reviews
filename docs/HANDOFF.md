<!-- author: Cowork | date: 2026-07-08 -->
# Session Handoff — v2.0.0 LIVE + users stress-testing · **v2.1.0 IN FLIGHT: Code is running the DECEMBER-REVIEWS RECOVERY (Part 0) right now — a `report` is pending and it's a STOP-AND-APPROVE gate**

> Written 2026-07-08 at the close of the mega-run Cowork session (v1.10.2 → v2.0.0 cutover → v2.1.0 staged). The ⚡ section is the new chat's startup script. **Read this file end-to-end before replying to Blake.**

---

## Current production
**Live:** `realanimereviews.com` serving **v2.0.0** (cutover 2026-07-04; the profile backfill ran clean — `ok:true, minted:7, existing:5, total:12`). v2.0.0 = THE MEGA-RUN: peer DMs (request-first, block/report/rate-limits) · group chats (cap 15) · sealed-until-accept DM images · unified inbox ("Message the Creator", Blake's row gold) · the **Curator Studio** (track ANY anime, notes in the 9 review sections, ✨ASK, "Publish this one" → pre-filled Add Anime; cloud-first, Excel-at-publish) · card-state labels + the Den watching line · yellow-tape community reviews on unreviewed anime · Hidden Gems + Random filter · Constellation Wrapped + legend · the responsive overhaul (drawer nav ≤1200) · shareable deep links + honest Back · the Discord-grade composer everywhere · the de-Blake copy sweep (~50 strings; name survives only in The Den / CREATOR marks / his bio / the watching line) · OG link previews (door art) · his new bio · ~15MB lighter · member emails owner-only. **Floors at ship:** Playwright 285 · rules 204 · functions 77 · triggers 78 · e2e 25. Users are stress-testing via Blake's Discord announcement.

## ⚡ For the next Cowork chat (startup script)
1. **Read order:** `AI-PRIMER.md` (top warning block only — its §4 is historical) → this file end-to-end → `COWORK-STYLE.md` (tone + ALL session notes, incl. §15 the goal-based era) → `NEXT.md` (the mega-run block, the verdicts, the OPEN LEDGER cross-check below) → `SHIP-OUTPUT.md` (Code's latest) → `SHIP-PROMPT.md` (the v2.1.0 prompt Code is applying NOW).
2. **YOUR FIRST MOVE: a `report` is pending on v2.1.0 — and Part 0 is special.** Code is investigating why most of Blake's **December-2025 review rewrites reverted** to older text (new reviews untouched). The prompt orders: forensics (git history of `animeData.js` = the gold-mine recovery source; the master xlsx says "last saved 5/10/2026" though byte-modified 7/4 — the reversion likely predates 5/10) → build a CANDIDATE file, never touch the master → **STOP and report a per-anime diff table.** When Blake says `report`: digest Part 0's findings + the diff table plainly. **The master is NOT replaced and nothing syncs until Blake explicitly approves the diff.** This is his most emotionally loaded data — his December words. Treat it with cutover-grade care.
3. After Part 0 approval, the rest of v2.1.0 lands (Code continues): mobile quotes removed · profile = pinned review + an "All reviews (N)" sheet (SUPERSEDES the old pinned-only spec) · catch-up strip capped (2 + "+N more", never over the update log) · 🔴 **the WebKit hunt** (Safari/Arc on iPhone missed the responsive work — WebKit was the untested engine; Playwright-WebKit + iOS viewports, per-surface screenshots) · 🔴 legacy members who can't finish their profile (at least one member hard-blocked — diagnose on prod shapes) · the admin member-stats page · self-serve account deletion (Code will bring a tombstone-vs-full-delete policy question — Blake picks) · the absorbed v2.0.1 leftovers (profile-editor panel rename — it still says "YOUR CONSTELLATION", colliding with the star map — + the 4 native-open dropdowns → brandSelect).
4. **Blake's working style (the goal-based era, his words 2026-07-02):** *"give it an end goal and let it work its way to the goal"* · *"Let it work for however long it needs and then one smoke test."* → END-STATE prompts + constraints; method is Code's; long autonomous runs; Cowork digests milestone reports; ONE comprehensive Blake smoke at the end. Small fixes stay surgical Max gates. Recommend a mode with every paste line (High/Max/ULTRAMAX).
5. **Deploy discipline (battle-tested, unchanged):** Cowork NEVER deploys. Blake's explicit words only, quoted + dated into the prompt; conditional approvals carry a STOP clause. Blake sometimes gives the go directly in Code's chat — valid; the file carries the gate. Curl the live site = ground truth.
6. **The generalized workflow doc now exists:** `C:\Users\Owner\PROJECTS\Claude_Workflow\COWORK-CODE-WORKFLOW.md` (project-agnostic, for all his future projects). This project's docs override it where they conflict.

## The report style (Blake NEVER reads SHIP-OUTPUT — you are his reader)
`report` = read SHIP-OUTPUT.md FRESH → a self-contained chat digest (5–15 lines + tables) → **numbered smoke steps that state the obvious** (which sign-in, what to click, what he should see). Split messages rather than bury steps. Map his smoke replies item-by-item — he numbers his bugs and notices if one goes missing. Screenshots he sends = fix items. When HE must do a technical step (console, Firebase console), write it click-by-click assuming zero prior knowledge — the F12 walkthrough pattern worked perfectly at the cutover.

## 🌟 THE OPEN LEDGER — every idea not yet shipped (cross-check NEXT.md; none may be lost)
- **IN FLIGHT (v2.1.0, staged prompt):** everything in §3 above.
- **⚠️ VERIFY-SHIPPED:** the **"anime information request" affordance** on sparse deep-dives was mega-run end-state #7 but no report explicitly confirmed it — have Code confirm it exists live, or fold it into the next gate. (Flagged in NEXT.md too.)
- **Banked, optional:** "who liked" COMPLETE history (Code proposes a clean ledger design if ever; Blake: "let code propose… if it wants to") · admin-menu live-count badges (pitch — the new stats page may satisfy it; check before building twice) · search-fully-into-drawer on phones (usable now, lovelier later) · Discover's "Reviewed" lens empties all rails incl. Hidden Gems (uniform pre-existing behavior — banked polish question) · assorted LOW residues in the gate log.
- **The long arc (order Blake last locked):** post-v2.1 stress-fix rounds as members report → **Cloud Admin** (edit/publish from any device, retires Excel-canonical — BIG, own design study) → **Mode 2** (the autonomous caretaker — Phase D, the original endgame).
- **KILLED — do not re-pitch:** Ask Blake Q&A · Blake's Echo · the June "Be able to create ___" mystery (Blake dropped it) · per-episode reviews · anime-characters-on-pages.
- **DO-NOT-"FIX":** Attack on Titan's **15/10** (his voice, spec-enforced) · the sanctioned "Blake's Den"-era strings in history/changelog · Appreciate-on-Blake (his explicit carve-out) · curator notes are cloud-first BY DESIGN (Excel = reviewed catalog only).

## What this whole arc learned (wish-I-knew list for the next me)
1. **Data integrity is now priority zero.** The December reversion happened SILENTLY, months ago, and no test caught shrinking review text. Part 0's prevention tripwire (refuse to sync if review text materially shrinks) must actually land. Treat the Excel master like production data: byte-surgical edits, diff-verified, backup-first.
2. **Blake's messages sometimes cut off mid-sentence.** Ask IMMEDIATELY ("create what?") — the June mystery survived three weeks and died unanswered. Also: his typos carry meaning ("conforming" = confirming/completing); quote him verbatim anyway.
3. **Verify hypotheses, never assert them** — the practice-only /suggest 404, the emulator-origin avatar failures, the "CRLF fix" that was actually a browser-never-opened UX gap. Say "likely, Code verifies," not "it is."
4. **The adversarial panels caught a HIGH-or-better in EVERY round** (a save that could erase his private notes, a spoiler LEAK, a bio-erasing editor, XSS lanes). Never let Code skip one; solo-panel + independent re-run is the honest fallback when agent budget dies.
5. **Demos he'll verify need PERMANENT seeds** (hand-seeded data dies on sandbox restart — bit twice) and foolproof recipes.
6. **WebKit is a first-class engine now** — Chromium+Firefox green ≠ iPhone green. Safari/Arc = WebKit.
7. He renames for future-proofing ("The Den", "Reviewed", "Message the Creator") — default NEW copy to neutral-but-warm; his name only where identity is the point.
8. Match his energy. "actually flabbergasted" / "BLOWN away" were this arc's highs — let those moments land before moving to the next gate.

## Rolling files
`SHIP-PROMPT.md` = v2.1.0 (Code applying NOW; Part 0 stop-gate) · `SHIP-OUTPUT.md` = will be the Part-0 forensics report when the pending `report` comes · `NEXT.md` = the ledger + verdicts · `CODE-HANDOFF.md` = Code's own state · `MEGA-RUN-DESIGN-STUDY.md` + gate log = the v2.0 architecture record · `../Claude_Workflow/COWORK-CODE-WORKFLOW.md` = the generalized loop.

## Process rules still apply
Excel canonical (reviewed catalog) · author markers on doc edits · Blake-authored commits, ZERO trailers · test floors before prod · gitignore↔firebase mirror · no provider names visitor-facing · no native dialogs/selects (last 4 die in v2.1.0) · `[hidden]` symmetry · friendly-errors · reduced-motion · gold = Blake ONLY (CREATOR kicker, his DM row, verdict rails; Appreciate-on-Blake = his carve-out) · community purple + count-free · the storage cross-service grant pre-flight on any storage-rules deploy · rar-ops artifact at session end only · **Cowork NEVER deploys.**

## Blake
USF student, "very basic" coder, and now the owner of a real community platform with real members on it. Explain at file level; three-copies model (Excel master / practice sandbox / live site) when he asks "how does this work?"; multiple-choice for decisions (leave room for his better custom answers); bank every idea verbatim + dated, show receipts on "is X saved?". He curses casually, jokes, says "lol/lmao" when happy; watch for fatigue. This site went from a hand-edited spreadsheet to v2.0.0 in ~two months of this loop — it's his portfolio piece, his pride, and now his community. **Protect the heart, and protect his December words.**
