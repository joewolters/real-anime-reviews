{
  "name": "real-anime-reviews",
  "version": "1.0.0",
  "private": true,
  "description": "Local tooling for realanimereviews.com (test runner + Excel sync + Mode 1 server — site code is plain HTML/JS).",
  "scripts": {
    "test": "playwright test",
    "sync": "node scripts/sync-excel-to-js.js",
    "sync:check": "node scripts/sync-excel-to-js.js --dry-run",
    "bump": "node scripts/bump-version.js",
    "anilist": "node scripts/anilist-fetch.js",
    "backfill": "node scripts/anilist-backfill.js",
    "mode1": "node scripts/mode1-server.js",
    "test:functions": "cd functions && node --test",
    "emulators": "firebase emulators:start --only functions,firestore,auth",
    "deploy:functions": "firebase deploy --only functions",
    "test:rules": "firebase emulators:exec --only firestore,storage --project demo-rar \"node --test --test-concurrency=1 functions/rules-tests/rules.spec.js functions/rules-tests/storage.spec.js\"",
    "test:cf": "firebase emulators:exec --only functions,firestore,storage --project demo-rar \"node --test --test-concurrency=1 functions/cf-tests/cf-integration.spec.js functions/cf-tests/cf-cascade.spec.js functions/cf-tests/cf-moderation.spec.js functions/cf-tests/cf-hotscore.spec.js functions/cf-tests/cf-forum.spec.js functions/cf-tests/cf-images.spec.js\"",
    "practice": "node scripts/practice-preflight.js",
    "test:e2e": "playwright test --config=playwright.emu.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "xlsx": "^0.18.5",
    "express": "^4.21.0"
  }
}
